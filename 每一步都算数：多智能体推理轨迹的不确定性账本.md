> [!info] 基本信息
> - **论文题目**：Every Response Counts: Quantifying Uncertainty of LLM-based Multi-Agent Systems through Tensor Decomposition
> - **中文题目**：每一步回应都算数：用张量分解衡量 LLM 多智能体系统的不确定性
> - **期刊/会议**：ACL 
> - **年份**：2026
> - **主题**： #多跳推理 #基准测试 #逐步评估 #链式思维 #错误传播 #知识瓶颈 #问答系统 #大语言模型评估

---
```table-of-contents
```
---
# 一、研究背景（前人做到哪一步）

## 1.1 传统ML的UQ（基础）

贝叶斯近似、证据深度学习等方法解决了分类/回归任务的不确定性量化，但不适用于LLM的自由文本生成

---
## 1.2 针对LLM的UQ

1. **需要logit的方法**：语义熵（Kuhn et al. 2023），把token概率聚合到语义等价类，闭源不可用

2. **黑盒语义一致性方法**：多次采样后用NLI构建蕴含矩阵，取图拉普拉斯特征值（Eigv，Lin et al. 2023）。不需要logit，但只处理单轮最终输出，上下文长度限制使其难以处理长推理链

3. **自评估方法**：P(true)（Kadavath et al. 2022），直接问模型自己答案是否正确

**共同局限：** 全部针对**单模型单轮输出**，没有考虑推理过程

---
## 1.3 Agent UQ的初步尝试

1. **SAUP**（Zhao et al. 2024/2025）：第一个专门针对Agent的UQ方法，对每步输出加权熵累加。局限是把每步独立处理，忽略完整轨迹的整体结构，且只针对单Agent单轨迹设计，不能直接用于MAS

2. **Kirchhof et al. 2025**：ICML立场论文，指出当前UQ框架对Agent设置不适配，但只定义问题，没有提出解决方案

---
## 1.4 前人留下的空白

针对MAS的UQ在这篇论文之前几乎是空白——没有方法能同时处理多步推理的级联误差、Agent间通信路径的差异、以及不同拓扑结构之间的泛化性这三个问题。SAUP强行迁移到MAS时需要把不同Agent的步骤拼接，既丢失了Agent间的结构信息，又对拓扑变化没有泛化能力。

---
# 二、拟解决的问题（为什么现有方法不行）

## 2.1 现有方法不行的根本原因

都是为单模型单轮设计的，放到MAS里有三个具体失效点：
### 2.1.1 只看最终输出，丢失了中间过程

语义熵、Eigv这类方法的做法是**多次采样，对比最终答案的语义一致性**。在MAS里这样做有两个问题：

1. 多步推理的不确定性分散在整条轨迹里，只看最终答案会漏掉大量信号

2. 如果强行把整条轨迹拼成一个长文档再送进NLI模型，NLI的上下文长度撑不住，而且NLI无法区分"这个矛盾是某个Agent自己幻觉了"还是"两个Agent之间交接时逻辑对不上"
### 2.1.2 通信路径的多样性被忽略

同一个任务、同一套Agent，不同运行之间的通信顺序可能不一样——Agent A可能先回应再追问，也可能直接给结论。这种路径差异本身就是不确定性的来源，但现有方法只关心语义内容，对路径结构是盲的
### 2.1.3 换个拓扑就失效

Eigv、SAUP都是在固定的单模型或固定结构上开发和验证的。MAS有星形、链式、动态等多种拓扑，每种拓扑产生的轨迹统计特性完全不同。一个在链式结构上调好的方法，放到动态拓扑上没有任何保证

---
## 2.2 SAUP的特殊位置

SAUP是唯一一个专门为Agent设计的方法，但它的问题是把每步当作独立节点处理，逐步累加熵值。这样做有两个缺陷：

1. 步数多的轨迹天然熵值高，跟实际不确定性无关（论文案例里一个100%正确的任务因为某次运行步数多，SAUP给出了0.88的高不确定性分数）

2. 它只处理单条轨迹，多条轨迹之间的关系信息完全丢弃了

---
# 三、创新点与贡献

## 3.1 核心创新：把轨迹一致性问题转化为张量压缩问题

以前的方法问的是这几次输出**语义上**一不一致，MATU问的是"这些轨迹能不能被一个低秩结构解释"。这个视角转换带来了三个具体好处
### 3.1.1 第一个系统定义MAS的UQ问题

论文明确区分了MAS里不确定性的三个来源：$$\begin{cases}\ 多步推理的级联 \\ Agent\ 间通信路径的变化 \\拓扑结构的多样性\end{cases}$$并给出了形式化定义。这个问题定义本身就是贡献，之前没有人把这三个维度拆清楚过
### 3.1.2 ragged tensor + PARAFAC2的方法设计

技术上有两个关键选择：

1. **ragged tensor**：不同运行、不同Agent的步数不一样，强行对齐会**丢信息或引入噪声**。用不规则张量直接表示这种异构结构，保留了完整的轨迹信息

2. **PARAFAC2**：普通CP分解要求维度一致，处理不了ragged tensor。PARAFAC2通过在每个切片上引入正交对齐矩阵，在不强制对齐长度的前提下找到跨运行的共享低秩结构。这个工具选择使得方法天然对拓扑结构无感知——不管星形还是动态，只要能收集轨迹就能跑
### 3.1.3 重建误差作为不确定性度量有理论依据

不是启发式的设计。论文附录里证明了CP-2重建误差在数学上等价于**轨迹集合在潜空间里的方差**——低秩近似找到的是"共识表示"，重建误差衡量的是各轨迹对这个共识的偏离程度，和统计学里方差的定义直接对应。这给方法提供了比"看起来有效"更扎实的理论支撑
### 3.1.4 OOD检测的副产品

实验发现MATU的不确定性分数在分布内数据（0.13）和OOD数据（0.92）之间有清晰的分隔，而SAUP在同样设置下几乎区分不开（0.29 vs 0.46）。这个能力没有专门设计，是张量结构自然带出来的，算是意外但有价值的发现

---
## 3.2 局限（论文自己承认的）

- 需要跑N=10次轨迹，推理成本线性增加
- 依赖通用embedding模型，高度专业化领域可能失效

这两点是真实的工程约束，不是小问题

---
# 四、方法与模型

## 4.1 整体逻辑

三步走：**轨迹embedding → 构建ragged tensor → CP-2分解求重建误差**
### 4.1.1 多步推理的embedding

每个Agent在每一步的输出（不管是自然语言还是工具调用结果）都用一个预训练embedding模型编码成一个向量 $e_t \in \mathbb{R}^d$

把Agent $k$ 在第 $j$ 次运行里所有步骤的向量按时间顺序拼起来，得到一个矩阵：$E^{(j,k)} \in \mathbb{R}^{T_{j,k} \times d}$

其中 $T_{j,k}$ 是这次运行这个Agent走了几步。这一步的关键作用是把异构的输出（长短不一、格式不同）映射到同一个语义空间，使得后续的结构比较有意义
### 4.1.2 构建ragged tensor

把所有运行、所有Agent的矩阵组织成一个三维对象：$\mathcal{X} = \{ E^{(j,k)} \mid j=1,\ldots,N;\ k=1,\ldots,K \}$

这是一个不规则张量——第三个维度（步数）在不同运行、不同Agent之间不一样长，所以不能用普通的三维数组表示。这个结构同时编码了三个维度的信息：
$$\begin{cases}\ runs（运行间的变异）\\ agents（不同角色的贡献） \\steps（推理过程的时序）\end{cases}$$
### 4.1.3 PARAFAC2分解

对 $\mathcal{X}$ 做CP-2分解，求秩为 $R$ 的低秩近似 $\hat{\mathcal{X}}_R$​，计算重建误差：$L_R = \|\mathcal{X} - \hat{\mathcal{X}}_R\|$

对多个秩值从1到 $R_{\max}$​ 分别做一遍，把所有重建误差加起来作为最终不确定性分数：

$U = \sum_{R=1}^{R_{\max}} L_R$

- **为什么用多个秩**：单个秩的重建误差对秩的选择很敏感。用一系列秩的累加，相当于问"在不同压缩程度下，这些轨迹整体有多难被压缩"，比单点估计更鲁棒

- **为什么重建误差等于不确定性**：CP-2找到的低秩结构是多次运行轨迹的"共识表示"，重建误差衡量各轨迹对这个共识的偏离——数学上等价于轨迹集合在潜空间里的方差（论文附录有证明）

---
## 4.2 实现细节

embedding模型用Qwen3-Embedding-0.6B，维度选256（实验表明再大收益边际递减）。每个任务收集N=10条轨迹，采样温度0.9。运行在单张A100-80GB上

---
# 五、实验与结论（数据说明了什么、有什么局限性）

## 5.1 实验设计

三种MAS框架（Camel/AutoGen/AnyMac）× 三种数据集（MATH/MoreHopQA/MMLU）× 三种模型（GPT-4o/Qwen2.5-7B/Llama3.1-8B），外加HumanEval的工具集成实验。评估指标用AUROC和AUARC，衡量不确定性分数预测答案对错的能力

---
## 5.2 数据说明了什么

1. **静态拓扑（Camel/AutoGen）**：MATU在几乎所有组合上优于所有baseline。提升幅度在开源小模型上更显著，比如Camel+MATH+Llama3.1-8B上AUROC从baseline最高的0.5668提到0.7354，涨幅很大。GPT-4o上提升相对保守，可能因为强模型本身输出更一致，各方法的分辨空间都变小了

2. **动态拓扑（AnyMac）**：提升幅度比静态拓扑小，但依然一致领先。说明ragged tensor对动态结构有泛化能力，但动态系统的轨迹变异更复杂，方法的优势有所收窄

3. **工具集成（HumanEval）**：MATU把工具调用结果当字符串处理后embedding，依然有效。说明方法对输出模态不敏感

4. **OOD实验**：用数学专用prompt去跑MMLU通识题，MATU的不确定性分数在分布内（0.13）和OOD（0.92）之间分隔清晰，SAUP同样设置下几乎区分不开（0.29 vs 0.46）。这个结果是意外收获，说明**张量结构捕捉到了比语义一致性更底层的信号**

5. **下游任务（backbone选择）**：用不确定性分数从四个backbone里选最低不确定性的答案，准确率显著高于随机选和其他方法。说明MATU的分数有实际部署价值，不只是评估指标上好看

6. **案例分析**：一个100%正确的数学任务，SAUP给出0.88的高不确定性（因为某些轨迹步数多），Eigv给出0.35（NLI处理长上下文时噪声大），MATU给出0.05。直观验证了方法对轨迹长度变化的鲁棒性

---
## 5.3 局限性

1. **推理成本**：需要跑N=10次轨迹，成本线性增加。在需要实时响应的场景里不现实，更适合离线评估或高风险决策场景

2. **依赖embedding质量**：在高度专业化领域（医学、法律、代码的细粒度语义），通用embedding模型可能捕捉不到关键的语义差异，导致不确定性估计失准

3. **AUROC数值整体偏低**：即使是MATU最好的结果，AUROC也多在0.65-0.75区间，离理想的0.9+还有较大距离。说明用不确定性预测答案对错这个任务本身就很难，方法的提升是相对的，绝对性能仍有限

4. **没有针对单Agent场景的对比**：论文完全聚焦MAS，没有说明在单Agent场景下MATU和SAUP相比怎么样，边界条件不够清晰