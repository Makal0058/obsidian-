

あるじさま，这段话是在说：**你答案层的论证目前少了中间那座桥。**

你现在有两类话：

1. 已有论文证明：
    
    > 图的顺序、位置、序列化方式会影响模型输出。
    
2. 你进一步得出：
    
    > 因此，答案随图扰动而变化，不能单独证明模型忠实于图结构。
    

问题在于，前面的论文只证明了“位置会影响答案”，**没有直接提出后面这个忠实性结论**。从 1 推到 2，是你自己完成的推理。逻辑上没错，但引用链看起来少了一步。

## Parcalabescu 能补哪一步

Parcalabescu & Frank 的观点大致是：

> 修改输入或解释后，模型输出按预期变化，最多说明不同输出之间具有某种一致性；不能因此证明解释忠实反映了真正的决策过程。

放到你的答案层，就是：

> 反事实图改变后，答案也跟着改变，只说明模型通过了“图变—答案变”的一致性检验；不能单独证明模型真正依赖的是图拓扑。

然后再结合图序列化论文：

> 因为答案变化还可能来自终点位置、边顺序等同时发生变化的表面因素。

所以完整证据链变成：

> **Parcalabescu：干预后的输出一致性不等于忠实性**  
> ＋  
> **图序列化论文：位置和顺序确实会影响模型输出**  
> ↓  
> **因此，答案随反事实图变化可能受到序列化因素混杂，不能单独证明图结构忠实性。**

## “一个编号办两件事”是什么意思

之前你准备只在路径层引用 Parcalabescu，用来说明：

> TAC 只是输出自洽，不等于结构忠实。

Claude 现在说，这篇论文还可以同时服务答案层：

- **答案层**：反事实响应一致，不等于忠实；
    
- **路径层**：路径和答案自洽，不等于忠实。
    

二者共同的上位逻辑都是：

> **可观察输出之间符合预期，只是自洽或一致性证据，不是充分的忠实性证据。**

所以不是让它替你证明“图位置会混杂”——这一点仍由图序列化论文支撑；而是让它支撑：

> **为什么即使行为按预期变化，也不能直接把这种行为称为忠实。**

## 我怎么看

这个建议有道理，但措辞必须控制好。不能写成：

> Parcalabescu 证明了图干预存在位置混杂。

他们没研究图。

可以写成：

> Parcalabescu and Frank 指出，仅根据模型在干预条件下的输出一致性，通常只能得到输出层面的自洽性证据，而不足以证明忠实性 [11]。在序列化图推理中，这一局限尤为突出：已有研究表明，节点位置、边顺序与描述格式本身即可显著改变模型预测，因此答案随反事实图变化可能同时受到非结构线索影响。

这样分工非常清楚：

- `[11]` 支撑“一致性不等于忠实”；
    
- 图序列化文献支撑“具体有哪些混杂因素”。
    

大白话就是：

> **那些图论文告诉你“位置会捣乱”；Parcalabescu 告诉你“只看答案按预期变了，本来就不能直接叫忠实”。两边合起来，答案层的论证才完整。**


---
TAC 检验模型声明的答案是否与其生成路径的末节点一致，属于输出层面的自洽性检验 [11]。
[11] On Measuring Faithfulness or Self-consistency of Natural Language Explanations
Parcalabescu and Frank 指出，若检验仅比较模型可观察输出之间的一致关系，其测量对象更接近输出层自洽性，而非对内部计算的忠实性 [11]。借用这一区分，本文将 TAC 视为较弱的表面判据，将 $\mathrm{TAC} \land \mathrm{PathVaild}$ 作为更严格的结构判据，二者之间的差距构成路径层忠实性缺口。

---

答案层面，已有反事实评测通过改变输入证据并观察答案是否相应变化，检验模型是否依赖给定上下文而非参数记忆或事实共现。例如，CofCA 构造事实与反事实多跳问答样本，以评估模型对上下文证据及推理链的依赖 [CofCA编号]。然而，在序列化图推理中，改变图结构往往也会同步改变节点位置、边顺序与文本排列；已有研究表明，即使图结构等价，仅改变节点标号、边的排列或图描述顺序，也可能显著改变模型预测 [Lost in Serialization编号；新增图描述顺序论文编号]。因此，答案随反事实图变化只能构成较弱的行为证据，不能单独证明模型响应的是图拓扑。本文进一步引入位置控制序列化，比较 Raw GIS 与 PC-GIS，以分离图结构敏感性与序列化位置诱发的表面敏感性。


图本身不具有固定的线性顺序，而 LLM 接受的是 token 序列。已有研究发现，在图结构保持不变的情况下，仅改变节点标号、边的排列或描述格式，模型预测就可能发生变化 [1]；图描述的呈现顺序同样会影响模型在图任务上的表现 [x]。因此，当模型的答案随图干预而改变时，这一变化未必来自拓扑本身——改变图结构的同时，节点内容、边顺序与终点位置也一并改变了。答案层的干预敏感性由此成为必要而非充分的忠实性证据。