输出契约与候选结构共同决定序列化检索策略；Chain 的候选唯一性掩盖该策略，Branching 的候选竞争将其暴露为死分支和非法轨迹。

表面忠实性由输出契约和序列化候选结构共同塑造；唯一候选会掩盖捷径，候选竞争会把捷径转化为可观测的非法推理轨迹。

与其将模型描述为稳定执行全局图遍历，不如说其行为更符合**接口条件化的序列化检索**。对，但最好写得更精确：

> 与其将模型描述为稳定执行全局图遍历，不如说其行为更符合**接口条件化的序列化检索**。

具体分成两种：

- `direct_minimal`：从全文候选中进行终点/答案抽取；
- path-output：以当前生成节点为查询，在文本中检索同源候选边，再逐步提交路径。

因此 path-output 看起来“沿图走”，实际可能更接近：

\[ v_i\ \text{作为查询} \rightarrow \text{检索文本中的 }(v_i,\cdot) \rightarrow \text{按位置与显著性选择候选} \]

它缺少的是稳定的全局验证：

\[ \text{当前状态维护} +\text{剩余可达性判断} +\text{完整路径合法性检查} \]

这正好统一数据：

- Chain：同源候选唯一，局部检索与图遍历行为重合；
- Branching：候选竞争出现，检索偏好暴露为死分支；
- strict：死分支后可能停止、拒绝或非法补全；
- oracle state：直接提供当前节点后准确率暴涨，说明长程状态维护是瓶颈。

但不要写成“模型没有推理，只是在检索”。证据还不足以否定一切推理。最稳的 claim 是：

> Models often behave less like globally validating graph traversers and more like interface-conditioned retrievers over serialized edge descriptions.

中文：

> 模型的行为通常不像具有全局验证能力的图遍历器，而更像受输出接口调节、在序列化边描述上执行局部检索的系统。

这句话就是你整篇论文最有辨识度的理论中心。

-----






一个**真在读图结构**的模型,e_f 在边集合里就一条边,你把它写一遍还是散布三遍写,对"这条边存在"这个事实**毫无影响**——理想推理器应该 δ=0。所以如果你测到**分散显著优于集中**(散布提醒降低失败率),那等于直接证明:模型不是在查一个图里"这条边在不在",而是在**沿着序列局部地、易遗忘地处理信息**,需要在"它快走到的地方"附近被提醒。**这是 serialization-anchoring 几乎最强的一种行为指纹**——它说明模型的"记得这条边"是位置局部的、会衰减的,不是图级别的全局事实。




如果模型真的把输入当作图来推理，那么一条边出现一次还是三次，不应该改变“这条边存在于图中”这个事实。



$\[ E_{\mathrm{dedup}}(M=1)=E_{\mathrm{dedup}}(M=3) \]$

因此：

$\[ P(\text{正确选择 } v_{k-1} \mid M=1) = P(\text{正确选择 } v_{k-1} \mid M=3) \]$

理论上 RD 应该接近 0。


重复同一条边改变了模型行为，尽管去重后的图结构不变。这构成对图遍历忠实性的反证：模型的最后分支选择依赖文本出现频率，而不只是图结构中的边存在性。

```
C:\Users\13403\Documents\Obsidian Vault\未命名.md
```

它不改变这个文本指向的底层图。
M=3 不引入新的图语义信息。

有，あるじさま。信息论里可以很清楚地说：**重复同一条已知信息，本身不提供新的 Shannon 信息**。关键是看“第二次出现”在知道第一次出现之后还有没有不确定性。

同源边重复

这有两种解释：

### 解释 A：频次锚定

模型看到这条边出现三次，所以觉得它更显眼，更容易选它。

这是不忠实，因为图里这条边并不因为出现三次就更重要。

### 解释 B：冗余纠错

模型本来可能会这条边。  
重复三次以后，它更不容易漏读，于是更准确。

这有点像信道里的重复码：

```
发送一次：可能看漏发送三次：更容易恢复真实信息
```

这种情况下，M=3 帮上忙不一定是不忠实，也可能是模型读取能力有噪声，重复帮助它恢复了真实边。

所以这句话说：

> 你现在只重复正确终点边，分不开“频次锚定”和“冗余纠错”。

## 5. 怎么把 anchoring 钉死？重复错误竞争边

最关键的实验补丁是：

> 不只重复正确边，还重复一条图中已经存在、但不是正确续接的竞争边。

例如当前节点是 v10：

```
正确边：v10 -> v11竞争边：v10 -> v12
```

v10 -> v12 本来也是图中的合法边，但不是黄金路径该走的边。

然后你做：

```
M=1: v10 -> v12 出现一次M=3: v10 -> v12 出现三次
```

去重后图还是同一个图。

如果模型因为 v10 -> v12 出现三次，就更容易错误选择 v12，那就说明：

> 它不是在“读得更准”，而是在被出现次数带偏。

因为冗余纠错只能帮助恢复真实边，不应该让模型更偏向错误分支。





终端约束效应定义为弱化 / 替换终端窗口后，$v_{k-2}$ 处黄金后继选择概率的提升：$\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}on}}=p_R-p_F$，$\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}off}}=p_{\mathrm{full}}-p_{\mathrm{local}}$。由 $2\times2$ 结构，状态维护效应之差与终端约束效应之差为同一交互项的两种等价分解：$\Delta_{\mathrm{interaction}}=\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}on}}-\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}off}}=\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}on}}-\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}off}}$，即 $\Delta_{\mathrm{interaction}}=(p_R-p_F)-(p_{\mathrm{full}}-p_{\mathrm{local}})$。



状态维护效应定义为 oracle 当前状态与完整前缀状态之间的差异：$\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}on}}=p_{\mathrm{local}}-p_F$，$\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}off}}=p_{\mathrm{full}}-p_R$。终端约束效应定义为弱化 / 替换终端窗口后，$v_{k-2}$ 处黄金后继选择概率的提升：$\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}on}}=p_R-p_F$，$\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}off}}=p_{\mathrm{full}}-p_{\mathrm{local}}$。






## 1. 单条信息的信息量：surprisal

信息论里一个事件 x 的信息量是：

```
I(x) = -log P(x)
```

越意外，信息量越大。

比如第一次看到：

```
["v11", "v12"]
```

如果你原本不知道这条边是否存在，那它会提供信息：

```
I(e_f exists) = -log P(e_f exists)
```

---

## 2. 但第二次重复的信息量要看条件信息量

第二次看到同一句：

```
["v11", "v12"]["v11", "v12"]
```

第二行带来的“新信息”不是再算一次 I(x)，而是算：

```
I(X_2 | X_1) = -log P(X_2 | X_1)
```

如果 X_2 是 X_1 的确定性重复，那么：

```
P(X_2 = x | X_1 = x) = 1
```

所以：

```
I(X_2 | X_1) = -log 1 = 0
```

也就是说：

> 已经看到第一遍后，第二遍逐字重复没有新信息。

三遍同理：

```
I(X_3 | X_1, X_2) = 0
```

---

## 3. 用熵写更清楚

如果第二次 mention 是第一次 mention 的确定性复制，那么条件熵是：

```
H(X_2 | X_1) = 0
```

三次重复的联合信息量也不会变成三倍：

```
H(X_1, X_2, X_3) = H(X_1)
```

因为 X_2 和 X_3 没有新增不确定性，它们完全由 X_1 决定。

对你的 E4-M 来说就是：

```
H(edge facts | M=1) = H(edge facts | M=3)
```

或者更贴近你的图语义：

```
IG_graph(M=3 over M=1) = H(G | T_{M=1}) - H(G | T_{M=3}) = 0
```

意思是：

> 看了一遍 e_f 后，再看两遍相同 e_f，不会进一步减少关于底层图 G 的不确定性。

---

## 4. 逻辑上就是 p 和 p ∧ p ∧ p

如果把这条边事实写成命题：

```
p = “(v11, v12) ∈ E”
```

那么：

```
p
```

和：

```
p ∧ p ∧ p
```

在逻辑语义上等价。

所以：

```
p ∧ p ∧ p ≡ p
```

重复不增加新的 graph-semantic information。

---

## 5. 但为什么模型会变好？

这正是你的实验要抓的点。🌱

信息论 / 图语义上：

```
new graph information = 0
```

但模型行为上：

```
failure rate changes
```

这说明模型不是像理想图遍历器那样只读取：

```
(v11, v12) ∈ E
```

而是受：

```
mention frequencytextual saliencerepeated exposurelocal availability
```

影响。

所以你可以写得很锋利：

```
M=3 adds zero new graph-semantic information, but still changes model behavior.
```

中文：

```
M=3 不增加任何新的图语义信息，却仍然改变模型行为。
```

---

## 6. 最适合论文里的表述

```
From an information-theoretic perspective, the additional mentions are conditionally redundant: once the first serialized edge row is observed, exact duplicate rows have zero conditional surprisal with respect to the graph-denotational content. Thus, M=3 adds no new graph-semantic information over M=1. Any behavioral difference therefore reflects sensitivity to repeated textual exposure rather than additional graph evidence.
```

中文：

```
从信息论角度看，额外 mention 是条件冗余的：一旦第一条序列化边行已经被观察到，完全相同的重复边行相对于图指称内容具有零条件惊讶度。因此，M=3 相比 M=1 不增加新的图语义信息。任何行为差异都反映模型对重复文本暴露的敏感性，而不是额外图证据。
```

一句话：**公式上就是条件信息量为 0：I(X₂ | X₁)=0。**



这个E4，要是加入明确设定会怎么样？额外比较明确指令 与 高频重复
如果明确告诉它“重复边不增加证据”，它能不能恢复图语义不变性

