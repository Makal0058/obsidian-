给定解释 EEE，从解释中推导一个关于模型行为的反事实预期；随后对相关变量进行干预，并检验模型行为是否满足该预期。

这份材料在讲一件事：**CoT 忠实性检验的方法学骨架，以及它的推断限度**。拆开看是四层。

## 1. 共同骨架

所有这类工作（Turpin 的 CoT 忠实性、更早的自然语言解释忠实性）都是同一个动作：

> 从解释 E 里读出一个反事实预期 → 对相应变量做干预 → 看模型行为是否符合该预期。

关键在于"预期"是有方向的，而方向由解释自己声明的内容决定。

## 2. 两个相反的判据

这是全文最实质的一点，也是最容易被人搞混的地方：

|你干预了什么|忠实的解释应该表现为|出问题的信号|
|---|---|---|
|解释**声称使用**的证据|答案随之改变|**不变**|
|解释**没提到**的表面因素（位置、few-shot 偏置、用户暗示）|答案保持稳定|**变化**|

所以"答案没变=不忠实"这个直觉只在第一栏成立。Turpin 的两组实验正好各占一栏：BBQ 是第一类（换掉弱证据看模型是否仍锁定刻板印象选项），BBH 是第二类（注入 Suggested Answer / Answer is Always A，答案迁移了但 CoT 从不承认）。

## 3. 为什么"不变"也不能直接判死刑

因为存在冗余因果路径：解释说"因为 X 和 Y 所以 A"，你只删了 X，Y 单独也足以支撑 A。答案不变不代表 X 没被使用。同理还有：被扰动的不是决定性证据、两条路径殊途同归、扰动强度没跨过决策边界。

所以第一类检验实际测的是**"解释所提因素是否具有足够显著的反事实影响"**，而不是内部因果过程本身。

## 4. 为什么"符合预期地变了"也不能证明忠实

这是全文的落点，也是对你最有用的一句。删边之后答案变了，看似支持忠实性，但删边这个操作同时改变了：文本位置、终点词频次、邻近格式模式。模型可能是对这些混杂变量敏感，而不是对图结构敏感。

因此这类方法在文献里更准确的名字是 explanation–prediction consistency / counterfactual consistency，**必要不充分**。Turpin 自己在 limitations 里承认了这一点。

## 与 GIF 的接口

材料末尾给你的定位是：你不是在否定"扰动—观察"这个范式，而是在指出它的**混杂控制缺口**——

> 答案随图改变，只能说明模型对该次干预敏感；不能排除它利用的是序列化引入的位置、频次或距离捷径。

这正是你 TAC≈99% 而 PathGoldExact 只有 8.4% 想说的事：成功信号本身没有把"对图敏感"和"对图的序列化表象敏感"区分开。你的 E4（频次/距离分解）和 GFI/Δ_illegal 就是在补这个归因层。

一句可以直接进 related work 的压缩版：

> 该变而不变、或不该变却变，都构成不忠实的证据；但该变时确实变了，只说明通过了一次一致性检验，不能单独证明忠实。













