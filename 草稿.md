输出契约与候选结构共同决定序列化检索策略；Chain 的候选唯一性掩盖该策略，Branching 的候选竞争将其暴露为死分支和非法轨迹。

表面忠实性由输出契约和序列化候选结构共同塑造；唯一候选会掩盖捷径，候选竞争会把捷径转化为可观测的非法推理轨迹。

与其将模型描述为稳定执行全局图遍历，不如说其行为更符合**接口条件化的序列化检索**。对，但最好写得更精确：

> 与其将模型描述为稳定执行全局图遍历，不如说其行为更符合**接口条件化的序列化检索**。

具体分成两种：

- `direct_minimal`：从全文候选中进行终点/答案抽取；
- path-output：以当前生成节点为查询，在文本中检索同源候选边，再逐步提交路径。

因此 path-output 看起来“沿图走”，实际可能更接近：

\[ v_i\ \text{作为查询} \rightarrow \text{检索文本中的 }(v_i,\cdot) \rightarrow \text{按位置与显著性选择候选} \]

它缺少的是稳定的全局验证：

\[ \text{当前状态维护} +\text{剩余可达性判断} +\text{完整路径合法性检查} \]

这正好统一数据：

- Chain：同源候选唯一，局部检索与图遍历行为重合；
- Branching：候选竞争出现，检索偏好暴露为死分支；
- strict：死分支后可能停止、拒绝或非法补全；
- oracle state：直接提供当前节点后准确率暴涨，说明长程状态维护是瓶颈。

但不要写成“模型没有推理，只是在检索”。证据还不足以否定一切推理。最稳的 claim 是：

> Models often behave less like globally validating graph traversers and more like interface-conditioned retrievers over serialized edge descriptions.

中文：

> 模型的行为通常不像具有全局验证能力的图遍历器，而更像受输出接口调节、在序列化边描述上执行局部检索的系统。

这句话就是你整篇论文最有辨识度的理论中心。

-----



可以，按你这个版本我会这样改。核心修正是：删掉 \(\Delta p_{\mathrm{cond}}\)，主比较改为 \(\Delta p\)；把 \(p_{\mathrm{dead}}\) 改成任意合法非黄金后继；删掉 \(p_{\mathrm{error}}\)；E2 桥接用 \(R/S/I/F\)。

## B.6：同源候选数量与输出约束

E6 检验 chain 是否可以看作没有同源终止分支候选竞争的退化 branching 情形。通过控制局部出度，E6 考察同源候选竞争是否降低模型选中黄金后继的概率。该结果与 E2 协同解释：E6 诊断同源候选竞争如何影响模型选中 \(e_{\mathrm{dead}}\) 的概率，E2 则解释模型进入 dead branch 状态后，输出接口如何将其转化为停止、拒绝、短路径或非法续写。二者共同刻画从局部候选竞争数量，到模型选中终止分支，再到路径层非法性的传导机制。

令

\[ d\in\{1,2,3,4,5\} \]

表示每个黄金路径节点处的局部出度。\(d=1\) 表示只保留黄金后继，此时 branching 退化为 chain；\(d=3\) 对应主 branching 实验设定，记为 canonical；\(d\in\{2,4,5\}\) 用于数量扫描。为保证不同 \(d\) 条件下输入边列表长度一致，对于 \(d\in\{1,2\}\)，将被移除终止分支的 source 改写为新生成的断开节点；对于 \(d\in\{4,5\}\)，将等量干扰边的 source 改写为当前路径节点。这样只改变局部候选集合，而不改变输入总长度。每个 \(d\) 条件采用相同 shuffle seed。

为排除地板效应与天花板效应，E6 的路径长度 \(k\) 参考 E5 中 \(q_{\mathrm{reach}}(k)\) 与 \(p_F^{\mathrm{cond}}(k)\) 的估计结果选择。每种 \(d\) 与三种输出接口交叉：`direct_minimal` 仅输出答案；`jsoncot_basic` 输出路径与答案，但不逐条强调约束；`jsoncot_strict` 要求正确起点、恰好 \(k+1\) 个节点、逐边合法，并要求答案等于路径末节点。由此形成 \(5\times3\) 因子设计，每格 400 次调用，共 6000 次调用。

|出度 \(d\)|条件名称|direct_minimal|jsoncot_basic|jsoncot_strict|小计|
|---|---|---|---|---|---|
|1|无竞争，chain|400|400|400|1200|
|2|低出度扫描|400|400|400|1200|
|3|标准设定，canonical|400|400|400|1200|
|4|高出度扫描|400|400|400|1200|
|5|更高出度扫描|400|400|400|1200|
|**合计**|—|**2000**|**2000**|**2000**|**6000**|

全样本下按照 `sample_id` 聚类估计 95% bootstrap CI，未到达局部决策点的样本记为 0。主要统计指标为完整成功率：

\[ p(d) = q_{\mathrm{reach}}(d)\times p_F^{\mathrm{cond}}(d), \]

并比较不同出度下完整成功率的变化：

\[ \Delta p = p(d=5)-p(d=1). \]

其中

\[ q_{\mathrm{reach}}(d) = \Pr(R\mid D=d) \]

表示到达率，\(R\) 表示模型正确完成前 \(k-2\) hop 并抵达局部决策节点 \(v_{k-2}\)。该指标用于诊断前缀整体可达难度，包括前缀状态维护与前缀各局部节点的候选竞争。

条件黄金后继选择率定义为：

\[ p_F^{\mathrm{cond}}(d) = \Pr(\hat v_{k-1}=v_{k-1}\mid R,D=d). \]

该指标作为机制分解指标，用于解释完整成功率 \(p(d)\) 的变化究竟来自前缀可达率下降，还是来自抵达后局部黄金后继选择率下降。若 \(q_{\mathrm{reach}}(d)\) 随 \(d\) 大幅下降，则跨 \(d\) 直接比较 \(p_F^{\mathrm{cond}}(d)\) 可能受到幸存者组成变化影响，因此 \(p_F^{\mathrm{cond}}(d)\) 不作为主对比量，而必须与 \(q_{\mathrm{reach}}(d)\) 并列报告。

在 \(R\) 条件下，局部选择结果被划分为三个互斥类别：黄金后继、合法非黄金后继、图外非法后继。三者满足闭合关系：

\[ p_F^{\mathrm{cond}}(d) + p_{\mathrm{dead}}(d) + p_{\mathrm{illegal\_local}}(d) = 1. \]

其中终止分支选择率定义为：

\[ p_{\mathrm{dead}}(d) = \Pr(\hat v_{k-1}\in N^+(v_{k-2})\setminus\{v_{k-1}\}\mid R,D=d), \]

即模型选中任意一条合法非黄金后继，也就是任意同源 dead successor 的概率。局部非法后继选择率定义为：

\[ p_{\mathrm{illegal\_local}}(d) = \Pr(\hat v_{k-1}\notin N^+(v_{k-2})\mid R,D=d), \]

表示模型在局部决策点直接选择图中不存在的后继。E6 同时报告已观察局部选择中的 first/last candidate preference，用于诊断候选顺序偏好是否解释出度效应。

与 E2 的协同分析仅作用于 \(p_{\mathrm{dead}}(d)\) 这一支：当模型选中合法非黄金 dead successor 后，沿用 E2 的终止分支出口分类 \(R/S/I/F\)，其中 \(R\) 表示显式退出，\(S\) 表示非显式停止或短路径，\(I\) 表示非法续写，\(F\) 表示形式完整但非法。E6 到 E2 的传导项可写为：

\[ p_{\mathrm{dead}\rightarrow I\cup F}(d) = p_{\mathrm{dead}}(d) \times \Pr(I\cup F\mid \hat v_{k-1}\in N^+(v_{k-2})\setminus\{v_{k-1}\},R,D=d). \]

其中图外非法后继 \(p_{\mathrm{illegal\_local}}(d)\) 本身已经构成路径层非法，不经过 E2 的 dead-branch 出口分类。这样，E6 将路径层失败分解为两个环节：同源候选竞争是否提高模型选中 dead successor 的概率，以及进入 dead branch 后，输出接口是否将该状态转化为停止、拒绝、短路径或非法续写。