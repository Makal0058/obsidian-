# 4 Experimental Setup

本节说明实验如何保证 GIF 诊断具有可解释性。第 3 节已经定义 GFI、$\Delta_{\mathrm{illegal}}$、TAC、PathValid 等指标，本节不再重复指标含义，而是说明合成图、序列化控制、prompt 条件、模型调用和统计方法如何共同排除替代解释。除特别说明外，所有非重试主实验均采用确定性解码；verifier-retry 仅在首次尝试失败后引入轻微采样扰动，以降低模型反复生成相同结构错误的概率。

## 4.1 实验设计原则：受控识别与诊断有效性

GIF 关注两类假忠实性：答案层的位置捷径，以及路径层的自洽但非法轨迹。为使这两类现象能够被干净识别，实验设计需要满足三项要求。第一，排除答案先验、候选偏好和节点语义造成的混淆；第二，分离图结构变化与文本位置变化对模型答案的影响；第三，将轨迹-答案自洽性与路径在图上的合法性作为两个独立属性进行检验。

为落实这些要求，本文从四个维度施加控制。第一，节点语义与答案先验控制。所有节点均采用随机符号标识，以减少参数记忆、常识联想、事实共现和自然语言语义对模型预测的影响。同时，实验设置 prior-only 和 candidate-only prior controls：前者完全不提供图结构，后者只提供候选节点集合，用于检查模型在无图条件下是否对特定答案或候选节点存在系统性偏好。上述控制结果单独报告，不与图任务结果混合计算。

第二，位置变量控制。对于同一反事实图对，实验构造 endpoint-first、endpoint-middle、endpoint-last 和 decoy-last 四种序列化。不同序列化版本保持节点集合、边集合、起点、跳数和黄金答案不变，仅改变终点相关信息或诱饵节点在文本序列中的位置。前三种序列化用于检验模型是否能够在终点位置变化后仍保持图干预响应；decoy-last 用于检验模型是否被最后出现的诱饵节点吸引。由此，Raw GIS 与 PC-GIS 的差异可用于估计位置控制前后的表面图干预敏感性差距，EAR 则提供末位诱饵吸引的直接证据。

第三，路径合法性与任务正确性控制。答案层正确性由 $\mathrm{Accuracy}^{(j)}=\mathbb{1}[\hat y^{(j)}=y^{(j)}]$ 衡量；路径层合法性由 $\mathrm{PathValid}^{(j)}$ 衡量；路径层任务完成度由 $\mathrm{PathGoldExact}^{(j)}$ 衡量。在本文采用的唯一合法路径设定下，符号生成器验证 $\mathcal W(G,s,k)={p^*}$，因此 $\mathrm{PathValid}^{(j)}=1\iff\mathrm{PathGoldExact}^{(j)}=1$。同时，若 $\mathrm{PathGoldExact}=1$ 且 $\mathrm{TAC}=1$，则模型路径等于黄金路径且声明答案等于路径末节点，因此可推出 $\mathrm{Accuracy}=1$。这些关系保证了路径合法性、路径任务正确性和答案正确性之间的层级连接，也使 $\Delta_{\mathrm{illegal}}$ 能够专门捕捉“轨迹与答案一致但不满足图结构约束”的样本。

第四，拓扑与难度控制。链式图用于隔离答案层位置捷径；分叉图用于暴露状态追踪失败、错误分支选择和非法跳转。chain-4hop 与 branching-4hop 构成相同跳数下的拓扑对照，用于区分位置捷径与分叉结构带来的路径层失败；branching-12hop 作为主要困难设置，用于观察长路径、多状态更新下的非法轨迹；其余跳数用于推理深度扫描，分析不同失败形态随路径长度变化的趋势。

## 4.2 合成图构造

每个基础样本由起点 $s$、跳数 $k$、唯一黄金路径 $p^*$ 和黄金答案 $y$ 构成。对于每个基础样本，生成器首先构造一个反事实图对 $(G_1,G_2)$，两张图共享相同起点和跳数，但具有不同黄金终点 $y_1\neq y_2$。随后，对每张图构造四种位置控制序列化。因此，一个基础样本在单一模型、单一 prompt 条件下展开为 $2\times4=8$ 次图任务调用。默认情况下，每个数据集包含 $N=200$ 个基础样本，即每个模型和 prompt 条件下共有 $200\times8=1600$ 次图任务调用。prior-only、candidate-only prior 和 verifier-retry 属于额外控制或缓解实验，不计入这 8 次基本图任务调用。

所有进入实验的图均通过符号程序验证。验证内容包括：起点 $s$、跳数 $k$、黄金路径 $p^_$、黄金答案 $y$ 的一致性；从 $s$ 出发恰好 $k$ 跳的合法路径集合是否满足 $\mathcal W(G,s,k)={p^_}$；反事实图对是否满足 $y_1\neq y_2$；decoy-last 条件中的诱饵节点是否不是对应图的黄金答案。

### 4.2.1 节点编码：随机符号节点

为实施节点语义控制，每个节点使用固定长度的随机字母-数字组合标识，例如 `J9E9`、`W6S2`。节点标识不携带实体类别、关系语义或自然语言含义，并在同一基础样本的反事实图对和不同序列化版本间保持一致。随机符号本身不能完全排除 token 偏好，因此实验同时使用 prior-only 和 candidate-only prior controls 检查模型是否对特定符号节点或候选集合表现出稳定偏好。若某一数据集在无图条件下已经出现明显答案偏好，则其图任务结果需要结合该控制结果解释。

### 4.2.2 拓扑设计：链式图与分叉图

本文使用链式图和分叉图两类拓扑。二者并非仅表示不同难度，而是服务于不同诊断目的：链式图用于隔离答案层位置捷径，分叉图用于暴露路径层状态追踪失败和非法轨迹。

链式图包含一条从 $s$ 出发、长度恰好为 $k$ 的唯一黄金路径 $p^_=(v_0^_,v_1^_,\ldots,v_k^_)$，其中 $v_0^_=s$，$v_k^_=y$。生成器可以加入背景干扰节点或干扰边，但这些干扰不从黄金路径上的节点引出可继续完成 $k$ 跳任务的替代路径。因此，模型若已经定位到当前黄金路径节点，则下一步基本唯一。链式图的作用是降低局部状态选择难度，使 answer-only 设置下的高 Raw GIS 与低 PC-GIS 更容易被解释为终点位置捷径，而不是分支选择失败。

分叉图在黄金路径节点处加入结构性分叉。对于某些黄金路径节点 $v_i^*$，图中存在多个候选后继节点，其中只有一个后继能够继续通向完整的黄金 $k$-hop 路径，其他后继通向终止分支或无法在剩余步数内完成任务的路径。设分叉点集合为 $\mathcal B$，每个分叉点的候选后继数为 $d_i$，则随机选择后继的理想化成功概率可写为 $\Pr[\text{all branch choices correct}]\approx\prod_{i\in\mathcal B}1/d_i$。这一基线只用于说明分叉结构会显著提高状态追踪与分支选择难度，不作为模型真实决策机制的假设。

分叉图的诊断意义在于：当单一末位位置线索不再稳定指向黄金答案时，模型若要完成任务，就必须持续维护当前节点并在多个候选边中选择正确后继。失败因此可能从答案层的位置捷径转化为路径层的错误分支选择、非法跳转或围绕被锚定的终点进行的路径合理化。本文将 branching-12hop 作为主要困难设置，用于诊断多步状态更新下的非法路径；将 branching-4hop 作为 prompt × topology 交互的关键探针，因为它与 chain-4hop 具有相同跳数，却引入了分叉结构。该设置用于区分两类现象：在 answer-only 条件下，分叉拓扑是否削弱固定位置策略的收益；在 structured-output 条件下，要求输出路径是否会把原本的答案层位置依赖转化为路径层的自洽但非法轨迹。相关实证结果在 §5 和 §6 中报告。

汇聚图不纳入本文主实验。原因是汇聚图允许多条合法路径到达同一黄金终点，在该设定下 $\mathrm{PathValid}$ 不再等价于 $\mathrm{PathGoldExact}$，会引入多路径等价与路径选择歧义。需要强调的是，$\mathrm{PathValid}$ 与 $\Delta_{\mathrm{illegal}}$ 的定义本身不依赖唯一性约束，可以扩展到多黄金路径集合；但 PathGoldExact 需要从单一路径匹配改为成员集合检验。本文将汇聚图和多路径设定下的 GIF 扩展留作未来工作。

## 4.3 序列化与 Prompt 条件

每张图均被转换为文本序列输入模型。为控制位置变量，本文使用四种位置控制序列化：endpoint-first、endpoint-middle、endpoint-last 和 decoy-last。endpoint-first、endpoint-middle 和 endpoint-last 分别将包含黄金终点的相关信息放置在序列靠前、中间或靠后位置；decoy-last 则将一个非黄金诱饵节点放置在序列末尾，用于测量模型是否倾向于输出最后出现的节点。四种序列化保持图结构、节点集合、边集合、起点、跳数和黄金答案不变，只改变文本呈现顺序。

Prompt 条件分为三类。direct_minimal 要求模型直接输出最终答案，不要求输出路径，主要用于诊断答案层位置捷径。jsoncot_basic 要求模型输出答案和路径，但对格式与合法性约束较弱，用于观察结构化输出的基础影响。jsoncot_strict 要求模型以严格 JSON 格式输出路径与答案，并明确路径应从给定起点出发、包含恰好 $k$ 次跳转、末节点与答案一致。jsoncot_strict 提高了输出可解析性和轨迹可见性，但不会向模型提供符号验证器，也不会保证路径中的每一步都是输入图中的合法边。因此，它适合用于区分“可读、可解析、自洽”的轨迹与真正图上合法的轨迹。

## 4.4 模型、解码与失败处理

实验覆盖 DeepSeek、Qwen 和 GPT 三个模型族。DeepSeek 包括 DS-Flash、DS-Pro no-thinking 和 DS-Pro thinking；Qwen 使用 Qwen Max；GPT-5.4-mini 结果来自第三方中转服务，因此本文按 provider-routed model configuration 报告，不将其等同于官方 API 配置。除特别说明外，同一实验单元中的模型使用相同图样本、相同序列化条件和相同 prompt 模板。

所有非重试主实验采用确定性解码，以减少采样随机性对 GFI、PC-GIS、EAR、TAC 和 PathValid 的影响。若模型输出无法解析、路径字段缺失、起点错误、长度错误或答案-路径末节点不一致，则按照预定义错误类型记录。格式错误和解析失败不被静默丢弃，而是作为失败类别进入相应统计；API 超时或服务异常单独报告，并在表格中说明实际有效调用数。

## 4.5 verifier-retry 与成本—收益评估

verifier-retry 用于评估外部符号验证能否以较低成本缓解路径层非法轨迹。对于每个样本，模型首先在确定性设置下生成一次输出；若输出无法通过符号验证，则触发后续重试。每次重试后，符号验证器检查输出是否满足格式、起点、长度、逐边合法性以及任务要求；一旦通过验证即停止。本文报告 pass@$K$，即最多允许 $K$ 次尝试时的累计通过率。由于本文采用唯一合法路径设定，PathValid 与 PathGoldExact 等价，因此 pass@$K$ 可同时反映路径合法性和路径层任务完成度。

为区分 verifier-retry 的真实收益与简单重复采样带来的收益，本文使用独立重试零基线作为参照。若每次尝试独立且成功概率恒定为 $p_1$，则最多 $K$ 次尝试的理论通过率为 $1-(1-p_1)^K$。将观测 pass@$K$ 与该零基线比较，可以判断 retry 是否真正改善了模型的图状态追踪，还是主要来自重复抽样带来的机会增加。本文同时报告累计延迟，并以 pass@$K$ 与延迟共同衡量 verifier-retry 的成本—收益。

## 4.6 统计推断与报告原则

本文对主要比例指标报告均值和 bootstrap 置信区间。非重试主实验采用确定性解码，因此置信区间主要反映有限样本带来的不确定性；verifier-retry 中后续尝试引入轻微采样扰动，因此其置信区间同时受到样本差异与解码随机性的影响。对于同一基础样本在不同序列化条件、prompt 条件或模型设置下的对比，优先采用配对统计，以保持反事实图对和位置控制设计的对应关系。多重比较使用 Holm 校正控制显著性检验的家族错误率。

报告结果时，本文遵循联合解读原则。GFI 不单独解释，而与 Raw GIS、PC-GIS 和 EAR 一起报告；当 Raw GIS 接近零时，低 GFI 不被解释为位置依赖消失，而被解释为模型已无法稳定响应图干预。类似地，$\Delta_{\mathrm{illegal}}$ 不单独解释，而与 TAC、PathValid、Accuracy、FailureHop 和 ErrorType 一起报告；它表示轨迹-答案自洽但图上非法的比例，不直接等同于模型内部的生成因果机制。FailureHop 分布和反向可达性分析用于进一步支持机制层解释，但相关结论均表述为行为层现象，而非关于模型内部推理顺序的直接声明。