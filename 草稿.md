输出契约与候选结构共同决定序列化检索策略；Chain 的候选唯一性掩盖该策略，Branching 的候选竞争将其暴露为死分支和非法轨迹。

表面忠实性由输出契约和序列化候选结构共同塑造；唯一候选会掩盖捷径，候选竞争会把捷径转化为可观测的非法推理轨迹。

与其将模型描述为稳定执行全局图遍历，不如说其行为更符合**接口条件化的序列化检索**。对，但最好写得更精确：

> 与其将模型描述为稳定执行全局图遍历，不如说其行为更符合**接口条件化的序列化检索**。

具体分成两种：

- `direct_minimal`：从全文候选中进行终点/答案抽取；
- path-output：以当前生成节点为查询，在文本中检索同源候选边，再逐步提交路径。

因此 path-output 看起来“沿图走”，实际可能更接近：

\[ v_i\ \text{作为查询} \rightarrow \text{检索文本中的 }(v_i,\cdot) \rightarrow \text{按位置与显著性选择候选} \]

它缺少的是稳定的全局验证：

\[ \text{当前状态维护} +\text{剩余可达性判断} +\text{完整路径合法性检查} \]

这正好统一数据：

- Chain：同源候选唯一，局部检索与图遍历行为重合；
- Branching：候选竞争出现，检索偏好暴露为死分支；
- strict：死分支后可能停止、拒绝或非法补全；
- oracle state：直接提供当前节点后准确率暴涨，说明长程状态维护是瓶颈。

但不要写成“模型没有推理，只是在检索”。证据还不足以否定一切推理。最稳的 claim 是：

> Models often behave less like globally validating graph traversers and more like interface-conditioned retrievers over serialized edge descriptions.

中文：

> 模型的行为通常不像具有全局验证能力的图遍历器，而更像受输出接口调节、在序列化边描述上执行局部检索的系统。

这句话就是你整篇论文最有辨识度的理论中心。










先给三条**写作元建议**,再交完整版。建议是关于"你这份大纲本身能不能直接落笔"的判断,完整版按你的大纲严格执行。

---

### 写作建议

**建议 1:第 3 节("错误归类规则")的 9 类优先级和 TerminalIllegalBridge 这些名词,需要先和正文统一**

你目前 paper 正文里 ErrorType 只大致说了"格式错误、起点错误、长度错误、非法边、轨迹-答案不一致"五类(§3.3 footnote 之前那段)。这里突然冒出 9 类带具体技术名(legal dead stop / dead branch followed by illegal continuation / immediate illegal transition / TerminalIllegalBridge),会有两个问题:

- **§3.3 和 §4.4 名词不一致**:审稿人会查
- **TerminalIllegalBridge 这个词如果在 §6 才定义,§4.4 提前露脸会让审稿人迷惑**:你自己也在大纲里提醒"不要直接命名为'位置锚定诱发',因果解释由 E3/E4 支持"——说明这个词是带因果暗示的,不该在统计章节裸出现。

我在完整版里的处理:**只列 5 类对外名词(和 §3.3 一致),内部 9 类作为"以下优先级的实现细节"用一个 enumerate 列出,但用中性命名**(把 TerminalIllegalBridge 换成 "illegal final transition",把 "dead branch followed by illegal continuation" 改成中性的 "post-deviation illegal edge")。如果你坚持用 TerminalIllegalBridge,请在 §3.3 先定义它。

**建议 2:第 4 节(分母)是你这版大纲里最有价值的一部分,Findings 审稿人会专门看这一节**

绝大多数 LLM 论文写到分母这块都是含糊的("我们对 200 个样本评估"),然后表格里数字对不上。你这里列了 5 类 N,这是 ML 评测里的好做法,但要做就要**做彻底**:每个具体指标用的是哪个 N,要在 §4.4.2 的指标列表里**逐条标注**,不能只在 §4.4.3 笼统说"条件机制指标仅在有效子集计算"。我在完整版里给每个指标加了 N 的标注。

**建议 3:Verifier-retry 那一节(第 6 节)在大纲里和 §5.4 实验内容会重复**

你大纲说"verifier 检查哪些条件 / pass@K 定义 / wall-clock latency / 累计延迟 L^(K) / timeout 如何计入"——但你正文 §5.4 已经有这些。建议:

- **§4.X.5 只讲"怎么算"**:pass@K 公式、L^(K) 公式、timeout 如何并入、repeated_same_hop 如何判定、run-level 波动如何汇报
- **§5.4 只讲"算出来什么"**:具体数字和模型对比
- **不要让两节都写一遍 verifier 验证哪 5 项**:那 5 项验证内容放在 §4.X.1(因为本质上是符号验证器的功能),§4.X.5 只说"在 retry 设置下,§4.X.1 的验证器被反复调用"

完整版按这个切分写。

---

### 完整版

下面是按你大纲交付的完整版,标题用你推荐的 4.X.1–4.X.5 编号(投稿时你按实际章节号替换)。中文为主,英文术语保留;数字、N 名、公式都按你的规范处理。

---

#### 4.X Evaluation and Statistics

本节说明输出如何被解析、指标如何被计算、误差如何被归类、分母如何被界定、统计推断如何被实施,以及 verifier-retry 设置下的成本量化。指标的定义与诊断动机已在 §3 给出,本节不重复推导,只规范计算流程,使报告数字可被独立复现。

##### 4.X.1 Output Parsing and Symbolic Verification

模型在 `direct_minimal` 下返回单一答案 token,在 `jsoncot_basic` 与 `jsoncot_strict` 下返回 JSON 对象,包含 `path`(节点序列)与 `answer`(单个节点)。解析器按以下步骤提取字段:剥离 markdown 围栏与额外文本,定位首个完整 JSON 对象,按 schema 读取 `path` 与 `answer`。任一步骤失败均记为 `parse_failure`,不进入路径层指标计算。

对每个可解析输出,符号验证器独立重算结构合法性,而非采信模型自身声明。验证内容包括:

1. `path[0]` 是否等于任务给定的起点 ss s;
2. `path` 长度是否等于 k+1k+1 k+1;
3. 对每个 i∈{0,…,k−1}i \in \{0,\dots,k-1\} i∈{0,…,k−1},(v^i,v^i+1)(\hat v_i, \hat v_{i+1}) (v^i​,v^i+1​) 是否属于输入图边集合 EE E;
4. `path[-1]` 是否等于 `answer` 字段。

验证器**不访问** gold answer yy y,**不访问** gold path p∗p^* p∗,**不访问**任何关于"哪条边可走"的提示。它仅基于任务给定的 (G,s,k)(G, s, k) (G,s,k) 与模型输出 (p^,y^)(\hat p, \hat y) (p^​,y^​) 计算 PathValid 与 TAC。这保证了:(i) verifier 反馈在 retry 设置下不构成 oracle leakage(§4.X.5);(ii) 即使模型在 chain-of-thought 中声称"该边合法",该声称不影响判定。

解析失败与图非法是两类独立错误,分别计入 `parse_failure` 与 `illegal_edge_at_hop_i`,不混合到同一统计量。

##### 4.X.2 Metrics and Error-State Assignment

**指标清单**(完整定义见 §3,此处仅声明计算范围)。

|层级|指标|计算分母|
|---|---|---|
|答案层|Accuracy|NeffectiveansN_\text{effective}^{\text{ans}} Neffectiveans​|
|答案层|Raw GIS|Npaired clustersN_\text{paired clusters} Npaired clusters​(配对反事实图对)|
|答案层|PC-GIS|Npaired clustersN_\text{paired clusters} Npaired clusters​(同上,要求四种序列化均返回有效输出)|
|答案层|GFI|Npaired clustersN_\text{paired clusters} Npaired clusters​(Raw GIS 与 PC-GIS 共同有效集)|
|答案层|EAR (decoy-last)|仅 `decoy-last` 序列化下的 NeffectiveansN_\text{effective}^{\text{ans}} Neffectiveans​|
|路径层|TAC|NparseableN_\text{parseable} Nparseable​ 中 `path` 非空且 `answer` 非空者|
|路径层|PathValid|NparseableN_\text{parseable} Nparseable​ 中 `path` 字段合规者|
|路径层|PathGoldExact|同 PathValid|
|路径层|Δillegal\Delta_\text{illegal} Δillegal​|NparseableN_\text{parseable} Nparseable​;correct/wrong 分项以 Accuracy 拆分|
|辅助|FailureHop|仅起点正确、长度足以逐边检查的样本|
|辅助|ErrorType|全部 NreturnedN_\text{returned} Nreturned​,按下文优先级唯一归类|

**联合报告规则:**

- GFI 不单独报告,必须与 Raw GIS、PC-GIS、EAR 一同呈现。报告 GFI 时若 Raw GIS 接近 0,需在文字中说明"低 GFI 对应敏感性崩塌而非位置依赖消解"(§3.2)。
- 在唯一合法路径设定下,PathValid 与 PathGoldExact **数值相同**,但**诊断含义不同**:PathValid 用于构造 Δillegal\Delta_\text{illegal} Δillegal​,描述"路径在图上是否合法";PathGoldExact 用于任务正确率叙述,描述"路径是否完成任务"。紧凑主表报告 PathGoldExact,Δillegal\Delta_\text{illegal} Δillegal​ 的构造仍调用 PathValid。
- Δillegal\Delta_\text{illegal} Δillegal​ 始终与 Δillegalcorrect\Delta_\text{illegal}^\text{correct} Δillegalcorrect​ 与 Δillegalwrong\Delta_\text{illegal}^\text{wrong} Δillegalwrong​ 分项一同报告,以区分场景 A(答案对、路径非法)与场景 B(答案错、路径自洽但非法)。

**ErrorType 互斥归类优先级。** 每条返回按以下顺序自上而下唯一归类,匹配即止:

1. `request_failure` — API 异常、网络中断、超时未返回任何内容
2. `parse_failure` — 返回非空但无法提取合法 JSON 或字段
3. `wrong_start` — `path[0] \neq s$
4. `wrong_length` — `path` 长度 ≠k+1\neq k+1 =k+1
5. `illegal_edge_at_hop_i` — 长度与起点均正确,首条非法边发生在 hop ii i
6. `trace_answer_mismatch` — 路径每边合法但 `path[-1] \neq answer`
7. `valid_and_correct` / `valid_but_wrong_answer` — 路径合法,按 `answer == y` 拆分

此优先级保证:(a) 每条返回归入唯一类别;(b) `parse_failure` 不被误算为"非法路径"(关键,避免低质量输出夸大 Δillegal\Delta_\text{illegal} Δillegal​);(c) FailureHop 仅在第 5 类样本上有定义。

**关于非法路径来源的次级分类。** 当本文报告非法路径的内部结构(如 §6 的失败类型表),主分母固定为  

NTAC-illegal=#{j:TAC(j)=1∧PathValid(j)=0}N_\text{TAC-illegal} = \#\{j : \mathrm{TAC}^{(j)} = 1 \land \mathrm{PathValid}^{(j)} = 0\}NTAC-illegal​=#{j:TAC(j)=1∧PathValid(j)=0}

即"路径与答案自洽但路径在图上非法"的样本集合。在此分母下,本文给出 illegal final transition、post-deviation illegal edge、early illegal edge 等中性命名的次级类别比例。**这些命名描述路径在图上失败的位置与形态,不包含关于"为什么失败"的因果断言**;机制解释(如"位置锚定诱发"、"事后合理化")由 §6 的 FailureHop 分布与反向可达性分析(E3 / E4)承担。

##### 4.X.3 Denominators and Effective Subsets

本节统一声明五类样本计数,所有主表表头与脚注必须使用这套命名。

- NallN_\text{all} Nall​:**计划调用总数**。例如 chain-4hop 主条件下为 200×2×4=1600200 \times 2 \times 4 = 1600 200×2×4=1600,branching-12hop + jsoncot_strict + Qwen Max 主条件下为 1600。
- NreturnedN_\text{returned} Nreturned​:**实际返回的调用数**。Nall−NreturnedN_\text{all} - N_\text{returned} Nall​−Nreturned​ 计入 `request_failure`。
- NparseableN_\text{parseable} Nparseable​:**可解析的输出数**。Nreturned−NparseableN_\text{returned} - N_\text{parseable} Nreturned​−Nparseable​ 计入 `parse_failure`。
- Neffective(指标)N_\text{effective}^{\text{(指标)}} Neffective(指标)​:**该指标真正可计算的样本数**。例如 PathValid 只在 NparseableN_\text{parseable} Nparseable​ 上有定义;EAR 只在 `decoy-last` 序列化条件下有定义。
- Npaired clustersN_\text{paired clusters} Npaired clusters​:**条件对比中两侧共有的 sample_id\texttt{sample\_id} sample_id 数**。Raw GIS、PC-GIS、GFI 仅在两图侧均返回有效答案的反事实图对上计算;跨模型比较仅在所有被比较模型均返回有效输出的 cluster 上进行。

**实操规则:**

1. **实际数字不被四舍五入为计划数字。** 若 Nreturned=1598N_\text{returned} = 1598 Nreturned​=1598,正文与表格均写 1598,不写 1600;并在表注说明原因(2 次 timeout / API failure)。
2. **timeout、API 异常、解析失败分别报告**,不合并入"其他"。
3. **解析失败不自动归为非法边**:`parse_failure` 是 ErrorType 的独立类别,不进入 PathValid 分母,也不计入 Δillegal\Delta_\text{illegal} Δillegal​。
4. **条件机制指标仅在有效子集计算**:GFI 不在"两侧之一未返回"的图对上估计;EAR 不在 `decoy-last` 之外计算。
5. **inconclusive 标记**:当 Npaired clusters<30N_\text{paired clusters} < 30 Npaired clusters​<30 或 Neffective<50N_\text{effective} < 50 Neffective​<50,该单元格在主表标 `inconclusive`,bootstrap CI 不报告或加宽报告;典型情形如 Qwen Max 在 chain-4hop + direct_minimal 条件下 NeffectiveN_\text{effective} Neffective​ 过低的子集(详见 §5.2 表注)。

每张主表的表注按以下模板交代分母:

> 该表 Nall=1600N_\text{all} = 1600 Nall​=1600;Nreturned=⋅N_\text{returned} = \cdot Nreturned​=⋅;Nparseable=⋅N_\text{parseable} = \cdot Nparseable​=⋅;GFI 在 Npaired clusters=⋅N_\text{paired clusters} = \cdot Npaired clusters​=⋅ 上计算。

##### 4.X.4 Statistical Inference

**独立单位与 cluster 结构。** 本文以 sample_id\texttt{sample\_id} sample_id(即一个 base sample 对应的反事实图对身份)作为最小独立单位。同一 sample_id\texttt{sample\_id} sample_id 下,G1G_1 G1​ 与 G2G_2 G2​、四种序列化(endpoint-first / middle / last / decoy-last)、三种 prompt(`direct_minimal` / `jsoncot_basic` / `jsoncot_strict`)以及 verifier-retry 的多次 attempt 共同构成一个 cluster。所有重采样在 sample_id\texttt{sample\_id} sample_id 层进行,cluster 内的条件结构整体保留或整体丢弃,以避免低估方差。

**Bootstrap.** 使用 B=2000B = 2000 B=2000 次 cluster bootstrap,采用 percentile 法构造 95% 置信区间(下界为 bootstrap 分布第 2.5 百分位,上界为第 97.5 百分位)。对于 Raw GIS、PC-GIS、GFI 等配对量,bootstrap 在 Npaired clustersN_\text{paired clusters} Npaired clusters​ 上进行,每次重采样后两侧条件均按抽到的 sample_id\texttt{sample\_id} sample_id 重算。

**效应量。** 主要效应量为 **risk difference**(条件 A 与条件 B 下指标的差值,如 GFIstrict−GFIminimal\text{GFI}_\text{strict} - \text{GFI}_\text{minimal} GFIstrict​−GFIminimal​),与其 95% CI。对二元指标(如 PathValid 在两个 prompt 下的对比),补充 **odds ratio** 及其 log-scale CI。

**跨模型比较。** 仅在所有被比较模型的共同有效 cluster 上进行——即对每个 sample_id\texttt{sample\_id} sample_id,要求所有模型均返回可解析输出。这避免某模型 parse_failure 率较高导致样本子集偏移。共同有效 cluster 数在每个跨模型表的表注中报告。

**主要对比 vs 探索性对比。** 在 §5 主文报告的对比(如 chain vs branching、direct_minimal vs jsoncot_strict、no-thinking vs thinking)为预先设定的主要对比,bootstrap CI 不做多重比较调整;在 §6 失败模式分析与附录消融中出现的对比(如逐 hop FailureHop 占比、ErrorType 子类比例)标为探索性,在文字中明示不可作为 confirmatory 证据。

**报告规范。** 本文不单独报告 pp p 值。结论以"效应量 + 95% CI + cluster 数"三元组呈现。CI 跨 0 **不自动解释为"无效应"**,而解释为"在当前样本量下,效应方向不可确定";同时报告点估计与 CI 宽度,使读者可独立判断 effect size 是否具有实践意义。

##### 4.X.5 Verifier-Retry, Latency, and Timeout Handling

**Verifier-retry 流程。** 在 `jsoncot_strict` 设置下,模型首次返回经 §4.X.1 验证器检查。若任一项失败,验证器仅向模型反馈**失败类型与首条失败 hop**(例如 `illegal_edge_at_hop_3`),不反馈 gold path、gold answer、正确下一跳或合法邻居集合。模型基于该反馈重新生成,首次尝试 temperature=0\text{temperature}=0 temperature=0,后续尝试 temperature=0.3\text{temperature}=0.3 temperature=0.3,以降低反复触发同一错误的概率(§4.3.3)。最大尝试次数 Kmax=5K_\text{max} = 5 Kmax​=5。

**pass@K.** 令 Zj,t∈{0,1}Z_{j,t} \in \{0,1\} Zj,t​∈{0,1} 表示样本 jj j 在第 tt t 次尝试是否通过验证。则  

pass@K=1N∑j=1N1[∃t≤K: Zj,t=1]\mathrm{pass@}K = \frac{1}{N}\sum_{j=1}^{N}\mathbb{1}\big[\exists t \leq K:\ Z_{j,t}=1\big]pass@K=N1​j=1∑N​1[∃t≤K: Zj,t​=1]

其中大写 KK K 为重试预算上限,与图任务中的小写跳数 kk k 区分。

**残留失败的结构性诊断。** 对达到 KmaxK_\text{max} Kmax​ 仍未通过的样本,记录:

- 最终 ErrorType 与最终 FailureHop;
- `repeated_same_error`:多次尝试中 ErrorType 相同的样本占比;
- `repeated_same_hop`:多次可解析的 `illegal_edge_at_hop_i` 尝试中,首次失败 hop 重复的样本占比。  
    这两项用于判断剩余失败是否构成稳定结构瓶颈(§6.2-6.3)。

**Run-level 波动。** DeepSeek-V4-Flash 进行 3 次独立 run,Qwen Max 进行 2 次独立 run。本文同时报告:

- 单个 run 内的 cluster bootstrap CI(反映样本选择不确定性);
- 跨 run 的均值与 min–max 范围(反映 temperature=0.3\text{temperature}=0.3 temperature=0.3 引入的解码波动)。  
    两类不确定性来源在表注中分别标记,**不合并为单一 CI**。由于 run 数 ≤ 3,跨 run 范围仅作描述性统计,不解释为总体置信区间。

**延迟与超时。** 所有调用记录端到端 wall-clock latency ℓj,t\ell_{j,t} ℓj,t​。给定预算 KK K,样本 jj j 的累计延迟为  

Lj(K)=∑t=1min⁡(τj,K)ℓj,tL_j^{(K)} = \sum_{t=1}^{\min(\tau_j, K)} \ell_{j,t}Lj(K)​=t=1∑min(τj​,K)​ℓj,t​

其中 τj\tau_j τj​ 为首次通过验证的尝试编号;若 KK K 次内均未通过,则 min⁡(τj,K)=K\min(\tau_j, K) = K min(τj​,K)=K,累计全部 KK K 次耗时。数据集层累计平均延迟为 L(K)=1N∑jLj(K)L^{(K)} = \frac{1}{N}\sum_j L_j^{(K)} L(K)=N1​∑j​Lj(K)​。

**Timeout 处理。** 单次请求超过预设 timeout 阈值记为 `timed_out`,在 retry 中被视为一次失败尝试,其实际等待时间(即 timeout 阈值)计入对应预算下的累计延迟。timeout 输出不计为正确答案、不计为合法路径、不进入 PathValid 分母。

**报告维度。** Latency 同时报告**均值、中位数、p95**,并单独报告 **timeout rate**,以避免少数极端慢请求掩盖整体趋势。pass@K\text{pass@}K pass@K 与 L(K)L^{(K)} L(K) 联合构成 verifier-retry 的成本—收益曲线,用于 §5.4 的 thinking 模式对照分析。

---

### 落笔后还要做的两件事

**1. 全文统一 N 的命名。** NallN_\text{all} Nall​ / NreturnedN_\text{returned} Nreturned​ / NparseableN_\text{parseable} Nparseable​ / NeffectiveN_\text{effective} Neffective​ / Npaired clustersN_\text{paired clusters} Npaired clusters​ 这五个名字一旦在 §4.X.3 定义,**§5 所有主表的表注必须复用相同记号**。当前 paper 的表注里有的写 "200 个 base sample",有的写 "1600 次调用",混用会让审稿人质疑分母透明度。

**2. §3.3 与 §4.X.2 的 ErrorType 对齐。** §3.3 那段"格式错误、起点错误、长度错误、非法边、轨迹—答案不一致"要么扩展为本节这 7 类,要么改成"详细优先级见 §4.X.2"。两节并存会被 reviewer 抓到。

如果你想,我可以把上面这版直接转成 EMNLP/ACL 模板的 LaTeX 段落格式(包括 `\subsection` 编号、`\paragraph{}`、table 的 caption 风格),你回 paper 就能 paste。


`Evaluation and Statistics` 应写六类内容，核心是**怎样计算**，不是重新解释指标理论。

## 1. 输出解析与符号验证

交代：

- 如何从 JSON 提取 `answer` 和 `path`；
- 检查起点、长度、逐边合法性、末节点—答案一致性；
- Prompt 声称合法不算通过，必须由验证器重算；
- 解析失败与图非法分开；
- 验证器是否访问 gold answer/path。

## 2. 使用哪些指标

只引用第 3 章，不重复推导：

- 答案层：Accuracy、Raw GIS、PC-GIS、GFI、EAR；
- 路径层：TAC、PathValid、PathGoldExact、\(\Delta_{\mathrm{illegal}}\)；
- 辅助诊断：FailureHop、ErrorType。

只需额外说明：

- GFI 必须与 Raw GIS、PC-GIS、EAR 联合报告；
- 唯一路径设置下 PathValid 与 PathGoldExact 数值相同，但诊断含义不同；
- \(\Delta_{\mathrm{illegal}}\) 同时报告 correct/wrong 分项。

## 3. 错误归类规则

说明 FailureHop 和互斥 ErrorType 的计算优先级：

1. request/API failure；
2. parse/format failure；
3. wrong start；
4. wrong length；
5. legal dead stop；
6. dead branch followed by illegal continuation；
7. immediate illegal transition；
8. trace–answer mismatch；
9. valid/gold exact。

论文若使用非法路径来源表，还要说明主分母是：

\[ \mathrm{TAC}=1\land\mathrm{PathValid}=0. \]

不要把 `TerminalIllegalBridge` 直接命名为“位置锚定诱发”，因果解释应由 E3/E4 支持。

## 4. 分母、缺失与有效子集

必须写清楚：

- `N_all`：计划或实际调用总数；
- `N_returned`：得到返回的调用；
- `N_parseable`：可解析输出；
- `N_effective`：该指标真正可计算的样本；
- `N_paired_clusters`：条件对比中的共同 `sample_id`。

规则：

- 1598 不写成 1600；
- timeout、API 异常、解析失败分别报告；
- 不把解析失败自动归为非法边；
- 条件机制指标仅在有效子集计算；
- Qwen E1 等有效配对过少的结果标为 inconclusive。

## 5. 统计推断

交代：

- 独立单位是 `sample_id`；
- G1/G2、序列化、prompt 和重复条件保留在同一 cluster；
- 使用 \(B=2000\) 的 `sample_id` cluster bootstrap；
- 采用 percentile 95% CI；
- risk difference 为主要效应量；
- 二元对比补充 odds ratio；
- 跨模型比较只使用共同有效 cluster；
- 主要对比与探索性对比区分；
- 不只报告 \(p\)，也不把 CI 跨零简单解释为无效应。

## 6. Verifier-Retry 与成本

这是第 3 章没有定义的，应完整保留：

- verifier 检查哪些条件；
- 不泄露 gold answer/path；
- pass@\(K\) 定义；
- repeated_same_error / repeated_same_hop；
- run 数量及 run-level 波动；
- wall-clock latency；
- 累计延迟 \(L^{(K)}\)；
- timeout 如何计入 retry；
- 报告均值、中位数、p95 和 timeout rate。

推荐最终结构：

```
4.X Evaluation and Statistics
4.X.1 Output Parsing and Symbolic Verification
4.X.2 Metrics and Error-State Assignment
4.X.3 Denominators and Effective Subsets
4.X.4 Statistical Inference
4.X.5 Verifier-Retry, Latency, and Timeout Handling
```

不应再放入本节的内容：

- GFI 和 \(\Delta_{\mathrm{illegal}}\) 的完整理论推导；
- Chain/Branching 机制解释；
- E1–E6 的实验设计表；
- “模型更像检索器”的结论；
- 实验结果和具体数值。