输出契约与候选结构共同决定序列化检索策略；Chain 的候选唯一性掩盖该策略，Branching 的候选竞争将其暴露为死分支和非法轨迹。

表面忠实性由输出契约和序列化候选结构共同塑造；唯一候选会掩盖捷径，候选竞争会把捷径转化为可观测的非法推理轨迹。

与其将模型描述为稳定执行全局图遍历，不如说其行为更符合**接口条件化的序列化检索**。对，但最好写得更精确：

> 与其将模型描述为稳定执行全局图遍历，不如说其行为更符合**接口条件化的序列化检索**。

具体分成两种：

- `direct_minimal`：从全文候选中进行终点/答案抽取；
- path-output：以当前生成节点为查询，在文本中检索同源候选边，再逐步提交路径。

因此 path-output 看起来“沿图走”，实际可能更接近：

\[ v_i\ \text{作为查询} \rightarrow \text{检索文本中的 }(v_i,\cdot) \rightarrow \text{按位置与显著性选择候选} \]

它缺少的是稳定的全局验证：

\[ \text{当前状态维护} +\text{剩余可达性判断} +\text{完整路径合法性检查} \]

这正好统一数据：

- Chain：同源候选唯一，局部检索与图遍历行为重合；
- Branching：候选竞争出现，检索偏好暴露为死分支；
- strict：死分支后可能停止、拒绝或非法补全；
- oracle state：直接提供当前节点后准确率暴涨，说明长程状态维护是瓶颈。

但不要写成“模型没有推理，只是在检索”。证据还不足以否定一切推理。最稳的 claim 是：

> Models often behave less like globally validating graph traversers and more like interface-conditioned retrievers over serialized edge descriptions.

中文：

> 模型的行为通常不像具有全局验证能力的图遍历器，而更像受输出接口调节、在序列化边描述上执行局部检索的系统。

这句话就是你整篇论文最有辨识度的理论中心。

-----








机制实验采用样本内配对干预，在保持基础图、节点标签及非目标文本不变的条件下，每次只改变输出接口、约束、终点边位置、文本支持、状态可用性或同源候选数量，并以 `sample_id` 为聚类单位估计干预效应。

先说明它与原始主实验的关系：

> 主实验用于建立 GFI 与 \(\Delta_{\mathrm{illegal}}\) 现象；因果干预套件用于区分这些现象的竞争性机制解释。

然后用一张表概括：

|实验|操纵变量|保持固定|主要结果变量|
|---|---|---|---|
|E1|answer-only / path+answer|图、候选及顺序|候选内早/后位选择|
|E2|长度约束 × 合法性约束|图、固定死分支前缀|停止、拒绝、非法续写|
|E3|final edge 位置|其余边及正文|最后分支失败率|
|E4|final edge 次数 × 前驱距离|图结构|最后分支失败率|
|E5|full state / oracle state|最后局部决策|准确率、死分支选择|
|E6|出度 1/2/3/5 × 输出约束|生成框架与路径深度|正确率、死分支及非法续写|

正文必须交代：

- 每个实验的研究问题和操纵；
- 哪些内容严格固定；
- 样本数、模型和条件；
- 主要终点及有效子集；
- `sample_id` cluster bootstrap；
- 这些实验是预设验证还是探索性追加实验。

不需要在 Setup 展开结果，也不必塞入完整 prompt。完整模板、任务示例、全部 factorial cells 和符号验证规则放 Appendix。

推荐结构：

```
4 Experimental Setup
4.1 Models and Inference
4.2 Graph Construction
  4.2.1 Symbolic Generation and Validation
  4.2.2 Chain and Branching Topologies
  4.2.3 Causal Intervention Suite
4.3 Prompt and Serialization Conditions
4.4 Metrics
4.5 Statistical Analysis
```

六个实验不应各写一大页；一段总述加一张设计表最合适。结果和“支持了什么”留在第 5/6 节。