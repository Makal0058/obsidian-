大语言模型能否忠实地遵循给定图结构进行多步推理，是评估其结构化推理能力的一个核心问题。现有研究常采用两类观测接口：一类只要求模型输出最终答案，通过答案是否正确或是否随图结构变化来判断模型是否使用了图；另一类进一步要求模型输出完整路径，并将路径合法性或答案—路径一致性作为更直接的过程证据。

然而，这两类接口都存在尚未被充分讨论的测量问题。

首先，**端点读数存在识别不足**。模型给出正确答案，甚至在图输入改变后相应改变答案，都不一定意味着它执行了目标图推理。图的文本序列化会同时引入位置、邻近性和末位显著性等非结构因素，而这些因素可能产生与真实图推理相同的端点变化。因此，仅依据最终答案，难以区分模型是在遵循图拓扑，还是利用了边在上下文中的排列位置。

其次，**轨迹接口并不是对既有推理过程的被动读取**。要求模型显式生成一条路径，会增加精确长度、逐边合法、格式合规以及答案—路径一致性等新的输出义务。这些要求不仅改变输出形式，也可能改变模型本身的选择行为。因此，路径并不必然是模型内部推理轨迹的透明外显，而是在特定输出契约下重新产生的行为结果。

这形成了忠实性测量中的一个基本张力：较弱的端点接口对模型干预较少，却容易受到位置等因素混杂；较强的轨迹接口提供了更多过程信息，却可能通过新增输出义务改变被测行为。换言之，**更丰富的观测不一定意味着更中性的观测**。

本文通过一组受控图干预实验系统研究这一张力。我们分别操纵图边的序列化位置、输出契约、分支竞争、路径长度、节点出度以及死端后的可用动作，同时保持与目标比较无关的图拓扑、候选身份或局部状态不变。该设计使我们能够区分三类因素：图结构本身允许哪些失败状态出现，接口允许模型采取哪些动作，以及模型在约束冲突中最终牺牲哪一项要求。

实验首先表明，端点表现具有显著的位置敏感性。仅移动决定最终答案的边，而不改变图的结构关系，就足以大幅改变模型准确率。这说明，部分看似由图干预引起的答案迁移，实际上可以由序列化位置捷径解释。

更重要的是，改变输出接口会改变模型行为，而不只是改变其报告格式。在保持图拓扑和候选集合不变时，从答案接口切换到路径—答案接口，会使三个模型的准确率大幅变化，并反转模型之间的相对排序。局部选择诊断进一步显示，同一模型在两种接口下甚至会偏向不同的错误分支。这表明，显式轨迹应被理解为**接口干预后的行为**，而不是对固定内部轨迹的无扰动观测。

我们的分析还揭示了一种由生成历史产生的失败状态。模型可能首先选择一条在局部上真实存在、但无法完成整个任务的边。一旦该前缀已经输出且不可撤销，精确路径长度、逐边合法性和保留既有前缀可能无法同时满足。我们将这一状态称为**前缀诱发的不可满足性**。在这种状态下，模型必须在提前停止、显式报告失败和生成不存在的后续边之间作出选择，而接口是否提供退出或回退动作，会直接改变这些行为的分配。

这些结果说明，端点指标和轨迹指标不应被视为同一忠实性变量的粗粒度与细粒度版本。端点评估需要控制序列化位置等混杂因素；轨迹评估则需要明确输出契约，并分别报告路径长度、逐边合法性、答案一致性、停止、退出和非法续写等行为。更一般地，忠实性评估不仅取决于模型是否对图输入作出反应，也取决于**测量接口如何塑造这种反应**。

本文的主要贡献如下：

1. 我们指出图推理忠实性测量中的两类互补威胁：端点接口的混杂问题与轨迹接口的反应性问题，并说明更强的过程接口并不天然提供更忠实的读数。
2. 我们提出一套受控图干预框架，通过位置置换、输出契约交叉操纵和局部状态控制，区分拓扑、接口与模型策略对观测结果的影响；同时引入位置校正后的图敏感性指标，以估计去除位置捷径后剩余的结构响应。
3. 我们识别并形式化前缀诱发的不可满足性，表明路径失败通常并非始于生成非法边，而是始于局部合法但全局不可完成的分支选择；模型后续是否停止、退出或非法续写，则由接口所提供的动作空间与模型策略共同决定。