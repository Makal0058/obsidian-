结论先说：**这个方向够资格成为一条新贡献，但 CC 给出的版本还不能原样写进论文。**

它真正有价值的地方，不是“模型会失败”，而是：

> **路径失败不是随着 hop 均匀变差，而是集中出现在特定结构位置，并且至少受到两类可区分压力的影响：长前缀状态负担，以及局部同源候选竞争。**

这比“自洽不等于忠实”强得多，也和贡献三不重复：

- 新贡献回答：**模型是怎么进入 dead state 的？**
- 贡献三回答：**进入 dead state 后，模型选择停止，还是编一条非法边？**
- 前者是失败的形成，后者是失败后的处置。

所以，**作为上游失败机制贡献，成立；作为已经完成因果归因的贡献，不成立。**

## 一、§1–§4 到底用了哪些数据

|部分|已经用到的数据|没有用到的数据|
|---|---|---|
|§1 引言与贡献|E1：DeepSeek 从约 2.4% 提升到85.6%；E2：退出动作使非法续写下降80pp以上|E5倒数第二跳、E6 hop×D、E7同源竞争都没有进入贡献列表|
|§2 相关工作|基本没有使用本文实验数据|A/B/C/D都没有|
|§3 概念框架|定义表面正确、结构忠实、缺口等概念|没有实证结果|
|§4 方法与实验设计|列出了E1–E7实验设计和指标|没有报告A/B/C的实际发现|

因此，CC 所说“E5/E6/E7没有占贡献位置”是对的；但“这些数据没有被论文使用”是不对的：

- A 已在§6.3和结论中使用；
- B 已在§6.1和结论中使用；
- C 已在§6.2、§6.4和结论中使用；
- D 已经是§5.4的主要发现。

准确说法应当是：

> **这些结果已经写进分析部分，但尚未被提升为论文的核心贡献。**

## 二、A：倒数第二跳瓶颈，支持到什么程度

A里面最扎实的是路径位置曲线：

- DeepSeek倒数第二跳下降12.61pp；
- GPT下降19.49pp；
- Qwen下降26.62pp；
- 一旦到达最后一个中间节点，最后一步成功率仍为99.2%–100%。

这确实说明：

> 失败不是每一步均匀积累，也不是最后一条边特别难；风险集中在到达终点前的局部选择阶段。

这一部分完全可以写成发现。

但“oracle卸载前缀状态证明瓶颈来自状态维护”说得太满。目前E5只能作为**诊断性证据**，不能叫干净的因果归因，原因有四个：

1. O-full与R不只是“是否携带前缀”不同，输入内容和有效搜索空间也变了。
2. GPT和Qwen到达该位置的比例偏低，条件成功率存在明显的幸存者选择。
3. 当前依赖的是带`provisional`标记的E5产物。
4. 论文声称的3.4/10.4/15.5pp，是直接比较两组条件成功率，不是分析脚本里正式定义的配对contrast。

我重新按配对样本检查，在R条件确实到达该节点的样本中，O-full相对R的提升约为：

- DeepSeek：+4.0pp；
- GPT：+12.0pp；
- Qwen：+16.2pp。

所以方向是稳的，但只能说：

> 状态重置边界对比与“前缀负担”解释一致。

不能说：

> oracle实验已经因果证明失败就是状态维护导致的。

此外，A还漏了一项更大的结果：**终端窗口效应**。O-full相对O-local：

- GPT约提高40pp；
- Qwen约提高38pp；
- DeepSeek基本不敏感。

这意味着局部失败不只有“长前缀负担”，还包括**是否看得见终点附近结构**，而且模型差异很大。CC把机制归纳成A+B两个因素，有点过早封口。

## 三、B：同源竞争“阈值”，基本支持，但要降调

D=1→2时，原本依赖末位候选的样本确实大量瓦解：

- DeepSeek：37.5%；
- GPT：51.7%；
- Qwen：90.9%。

这是非常强且有意思的结果。它表明：

> **仅仅加入第一个同源竞争候选，就足以打破大量原本看似稳定的选择。**

D=3→4的新增影响只有约：

- DeepSeek：0.22pp；
- GPT：1.33pp；
- Qwen：1.40pp。

所以“第一个竞争者最重要，后续边际影响明显减小”是成立的。

但不宜直接叫严格的“竞争阈值”，因为：

- GPT在D=2→3时仍额外下降约7.4pp，并非完全平台；
- Qwen的有效基线样本只有143个，置信区间较宽；
- 12,000是任务设计规模，不是该条件统计的有效分母；
- 增加dead候选时同时替换了背景边，纯竞争效应与背景变化尚未完全拆开；
- 计划中的背景扰动对照目前没有有效结果。

因此最稳的说法是：

> **最大的性能断点出现在第一个同源竞争候选加入时，继续增加候选的边际影响通常明显减小，呈现平台化趋势。**

不要写成：

> 存在一个经过因果识别的D=2硬阈值。

还有一点：“chain(D=1)下dead后终止和非法续写全为0”并不是很强的证据，因为那个条件下本来就没有同源dead候选，基本是结构定义的结果。

## 四、C：hop和D方向相反，能用，但适合辅助A+B

C的数据确实支持一种有意思的分离：

- hop从4增加到12，进入dead后非法续写概率上升约49.45pp；
- D从2增加到5，进入dead后非法续写概率反而下降约7.77pp。

大白话就是：

> 路径变长时，模型更容易硬着头皮编下去；竞争出口变多时，模型反而更容易提前停下来或选中dead候选。两种“变难”没有产生同一种失败。

这能证明不能把所有难度都压成一个标量，也能把新贡献和贡献三连接起来。但它不是严格的同一实验内全因子因果比较，而且存在模型例外，所以适合作为**补强证据**，不适合单独扛住新贡献。

## 五、旧贡献一不该直接扔掉

CC说旧贡献一“只是限制，不是贡献”，我只同意一半。

旧贡献一现在的写法确实很弱：

> 两类缺口不能跨接口比较。

单独拿出来像一个方法限制。

但它背后的E1其实是目前最干净的因果证据之一：只改变输出契约，行为就大幅改变。DeepSeek从约2.4%变到85.6%，另外两个模型也有很大提升，配对contrast非常稳定。

所以问题不是旧贡献没有价值，而是**写成了消极的测量警告**。它更应该被改写为：

> **输出接口不是被动的测量窗口，而是会因果改变模型策略的行为干预。**

然后与位置控制、符号验证方法合并成一条“框架与方法贡献”。直接删掉，会丢掉论文里比E5更干净的证据。

## 六、我建议最终组织成三条贡献

1. **框架与方法**  
    输出接口会改变策略，因此不能把不同接口下的表面—结构缺口当作同一标尺；本文通过位置控制和逐边符号验证分别识别答案层与路径层失真。
    
2. **上游失败定位——这就是新贡献**  
    路径失败不是均匀逐hop累积，而表现出至少两类可区分的行为压力：长前缀负担对应末端局部选择瓶颈，第一个同源竞争候选造成最大性能断点；终端窗口还产生显著且模型异质的调节作用。
    
3. **下游失败处置——现有贡献三**  
    模型进入不可满足状态后，会优先维持长度、格式和答案—终点一致性，而牺牲图合法性；提供合法退出动作可显著改变这种处置策略。
    

这样就形成一条完整链条：

> **接口如何改变策略 → 路径失败在哪里形成 → 失败后模型牺牲哪项约束。**

## 七、可以直接写进论文的安全版本

> **第二，本文在行为层面定位了路径失败的结构来源。失败风险并非沿hop均匀累积，而集中出现在末端局部选择阶段：倒数第二跳的条件成功率下降12.6–26.6个百分点，而到达最后一个中间节点后的终步成功率仍高于99%。边界状态重置可部分恢复该局部选择，表明长前缀负担是一个重要压力来源；配对出度干预进一步显示，最大的性能断点出现在首个同源竞争候选加入时，后续候选的边际影响明显减小。这些结果揭示了前缀负担与局部候选竞争两种可区分的行为特征，但不将其解释为对模型内部机制的直接识别。**

最终判断：

> **可以升级为新贡献，而且比“表面自洽不代表结构忠实”更像真正的发现；但应叫“路径失败的行为定位与结构分解”，不能叫已经完成的oracle机制归因。**

在真正改稿前，还要先修正§4中的几处口径：E6/E7样本规模与hop设置和当前产物不一致、bootstrap写成10,000但分析实际是2,000、E5表中部分`q_reach`数字实际是联合成功率，以及背景扰动对照当前为空。否则审稿人沿着新贡献查实验设置时，很容易发现正文与产物对不上。

相关位置：[第四版.md (line 32)](C:/Users/13403/Documents/Obsidian Vault/图忠实性论文/第四版.md:32)、[E1报告 (line 19)](/D:/Graph-Intervention Faithfulness/gif_eval/causal_e123/report.md:19)、[E5结果 (line 1)](/D:/Graph-Intervention Faithfulness/gif_eval/e5_state_terminal_2x2_k8_l6_provisional_20260714/cell_metrics.csv:1)、[E7结果 (line 1)](/D:/Graph-Intervention Faithfulness/gif_eval/e7_same_source_position_collapse/ss_ear_metrics.csv:1)、[E6背景对照 (line 1)](/D:/Graph-Intervention Faithfulness/gif_eval/e6_background_perturbation_control/analysis_summary.json:1)。





---






我觉得这个总体结构是对的，而且主实验不能删。三条贡献和实验的对应关系应该这样理解：

- 贡献一：主要由 E1 支撑。  
    回答“输出接口是不是被动的测量窗口？”结论是：不是，接口本身会因果改变行为。
    
- 贡献二：不是单独某一个实验。  
    它先由主实验发现“最后一次竞争选择特别脆弱”，再由 E5 和 E7 分别做受控定位：
    
    - E5：完整长路径任务与 oracle 局部任务的对照；
    - E7：同源竞争从无到有以及继续增加候选的配对干预。  
        所以它是“主实验发现现象，E5/E7拆解现象”。
- 贡献三：核心是 E2。  
    回答模型进入给定的不可满足状态以后如何处置：继续满足长度、格式、答案—终点一致性，却牺牲图合法性；增加合法退出动作后，这种处置显著改变。主实验中的非法续写和 TAC 主要提供现实背景，E2才是干净的因果检验。
    

整篇论文可以形成这个逻辑：

> 主实验发现异常现象 → 受控实验提出并检验解释 → 三条贡献分别回答接口、失败形成位置和失败处置方式。

主实验承担的不是直接“证明三条贡献”，而是三个不可替代的作用：

1. 证明这些问题确实会在常规长路径任务中出现，而不是只存在于人为构造的最小实验里。
2. 决定后续受控实验要解释什么，避免论文看起来像随意挑选几个漂亮的干预结果。
3. 给出效应的实际规模和随 hop、模型变化的总体图景；最小实验负责因果识别，但不能代替这个总体图景。

不过“防止先射箭后画靶”不要直接写进论文。仅仅把主实验放在前面，并不能证明后续假设不是看完数据才形成的。更准确的表述是：

> 主实验用于发现并界定稳定的经验异常，随后通过独立构造的最小受控实验检验针对这些异常提出的解释。

如果这些后续实验确实用了独立任务、独立样本或新生成的数据，可以进一步写成“发现—验证分离”。如果没有预注册，就不要叫“预先设定的验证”，但这种探索后再用独立干预验证的结构本身完全合理。

最清楚的章节分工是：

- 主实验：发生了什么、规模多大、在哪些模型和长度下出现；
- E1：接口为什么不能只当测量工具；
- E5＋E7：失败为什么集中暴露在最后一次竞争选择；
- E2：模型走不通以后选择牺牲什么；
- 缓解实验：这些解释能否预测什么干预有效。

所以不是“三个贡献和主实验没关系”，而是：

> **主实验负责提出并证明问题值得研究；最小实验负责把问题变成可识别的贡献。**

删除主实验以后，论文可能更短、更像一组漂亮的因果实验，但审稿人很容易反问：这些极小构造中的效应，在正常图推理任务中到底是否真实存在、是否重要？保留主实验，恰好补上这一层。



---


|章节|核心任务|对应实验|
|---|---|---|
|1 引言|提出问题和三条发现|只放结论|
|2 相关工作|说明现有研究缺什么|文献|
|3 问题定义与诊断框架|定义两层指标及测量边界|不放结果|
|4 实验设计|区分主实验与受控验证|全部实验概览|
|5 主实验：三种经验异常|证明问题真实、稳定、值得解释|主实验|
|6 受控实验：三条发现|分别检验三条贡献|E1；E5/E7；E2|
|7 讨论与干预启示|串成完整行为链，放缓解实验|thinking、retry 等|
|8 局限性|限制结论范围|—|
|9 结论|三句话收束三个发现|—|
现在这版不适合继续局部修补，应该先重建正文骨架。当前[《第四版》 (line 1)](/C:/Users/13403/Documents/Obsidian Vault/图忠实性论文/第四版.md:1)在第4章后直接跳到附录，缺少实验设置、主实验结果、受控实验、讨论和结论；附录还在引用不存在的 §4.4、§5.4、§6。

最重要的结构调整是：

> **主实验负责发现问题并说明问题有多普遍；E1、E5/E7、E2负责用最小干预验证解释。**

这样主实验不再和三条贡献争位置，也不会沦为无关的大数据表。

## 建议的正文结构

|章节|核心任务|对应实验|
|---|---|---|
|1 引言|提出问题和三条发现|只放结论|
|2 相关工作|说明现有研究缺什么|文献|
|3 问题定义与诊断框架|定义两层指标及测量边界|不放结果|
|4 实验设计|区分主实验与受控验证|全部实验概览|
|5 主实验：三种经验异常|证明问题真实、稳定、值得解释|主实验|
|6 受控实验：三条发现|分别检验三条贡献|E1；E5/E7；E2|
|7 讨论与干预启示|串成完整行为链，放缓解实验|thinking、retry 等|
|8 局限性|限制结论范围|—|
|9 结论|三句话收束三个发现|—|

---

## 摘要

当前摘要是空的，最后再写。建议固定为五部分：

1. 图推理忠实性的核心困难。
2. 本文如何区分答案层与路径层。
3. 贡献一：接口是行为干预，2.4%→85.6%。
4. 贡献二、三：末次竞争瓶颈；无退出动作时牺牲图合法性。
5. 实践含义：固定并报告输出契约、逐边验证、提供合法失败动作。

摘要不要罗列 E1/E2/E5/E7，也不要介绍所有指标。

---

## 第1章 引言

### 保留

现在第6—9行对“答案随图变化”和“路径—答案一致”都可能高估忠实性的铺垫可以保留。

### 必须修改

第10行现在写：

> 答案层的位置锚定需在仅要求最终答案的接口下诊断。

这不准确。答案层在任何包含答案字段的固定契约里都能测。应改成：

> 答案层与路径层必须在各自定义成立的固定契约内诊断；显式路径契约下，两层可以并列观察，但不能压缩为同一分数。

### 三条贡献

目前三条贡献方向已经正确，但每条应压成“发现—关键证据—意义”，不要在引言里写完整实验报告。

建议保持这个对应：

- 贡献一：E1——输出契约是因果变量。
- 贡献二：主实验＋E5/E7——末次竞争瓶颈。
- 贡献三：E2——不可满足状态下的约束取舍与退出动作。

贡献三最后的“拓扑、接口、模型三个层次协同影响”暂时删掉。“协同”暗示联合因子设计和交互检验，当前证据更适合在讨论中作为综合解释。

### 加一段路线图

引言末尾明确告诉读者：

> 本文首先用大规模主实验识别三种稳定异常，再通过独立的最小受控实验分别检验输出契约、末次竞争和失败动作空间的作用。

如果受控实验使用独立任务和样本，可以写“发现集—验证集分离”；没有预注册则不要写“预先验证”。

---

## 第2章 相关工作

现在三个方向基本正确，但需要更直接地对应三条贡献。

### 2.1 忠实性的参照对象与测量

保留 plausibility、self-consistency、intervention-based faithfulness。

结尾落到：

> 现有研究说明自洽不等于忠实，但尚未解决图任务中“忠实于哪个外部结构属性”以及“输出契约是否改变被测行为”的问题。

### 2.2 图序列化与路径证据

把两条线分清：

- 序列化顺序会改变答案；
- 检索生成的合法路径不能替代对模型自生成路径的验证。

不要在这里提前讲本文结果。`[GCR]` 这种占位引用必须补成正式文献或删除。

### 2.3 输出契约、约束执行与外部验证

这一节同时服务贡献一和贡献三：

- 格式/输出要求能够改变行为；
- 多约束任务中模型不一定遵循语义优先级；
- verifier反馈不等于模型拥有可执行的合法动作。

如果保留 verifier-retry，相关工作里补外部反馈、自纠错、验证器引导生成；否则第7章突然出现 retry 会显得离题。

---

## 第3章 问题定义与双层诊断

把当前第3章和第4章的指标部分合并。现在两章重复讲“表面检验不足”，而正式任务定义被放在“框架”章里，边界不清。

建议改为：

### 3.1 任务与结构参照

定义：

- 图、起点、规定 hop；
- 唯一合法路径；
- 黄金答案；
- 输出契约。

只讲数学对象，不讲具体生成多少边、随机种子等。

### 3.2 答案层诊断

定义位置控制、RawGIS、PC-GIS、GFI、EAR。

要明确：

> GFI是在某一个固定契约内定义的答案层估计量，不是跨契约通用分数。

### 3.3 路径层诊断

定义 TAC、PathValid、\(\Delta_{\mathrm{illegal}}\)。

把“PathValid在唯一合法路径下等价于PathGoldExact”留在这里。

### 3.4 两种不同的测量边界

专门用一小节把此前反复混淆的两个问题拆开：

1. **跨契约不可直接比较**：因为E1证明契约会改变行为。
2. **跨层不合并**：因为两层检验不同结构属性；answer-only下路径层还无定义。

同时说明：

> 在路径—答案联合契约下，答案正确性与路径合法性可以并列报告，但并列观察不意味着可以相减为单一总分。

当前第49行已经接近正确，但还应该用这两个编号明确拆开。

---

## 第4章 实验设计

这一章回答“为什么这些数字具有可解释性”，不要再定义指标。

### 4.1 总体识别策略

开门见山说明两阶段设计：

- 主实验：现象发现与范围检验；
- 最小受控实验：对解释进行针对性检验。

建议放一张实验映射表：

|主实验发现的问题|受控检验|对应贡献|
|---|---|---|
|输出路径后准确率和错误形态大变|E1只改变输出字段|贡献一|
|错误集中于最后一次竞争选择|E5边界任务＋E7配对出度|贡献二|
|进入dead后补足长度但生成非法边|E2长度×合法性×退出动作|贡献三|

### 4.2 主实验

压缩介绍：

- chain和branching；
- hop扫描；
- 四种序列化；
- 三种输出契约；
- 三个模型；
- 主指标和统计单位。

主实验的定位必须写成：

> 它估计这些失败在一般任务矩阵中的规模、位置和跨模型稳定性，不单独承担因果归因。

### 4.3 受控实验

正文只保留四个核心设计：

- E1：`answer_only` vs `path_answer`；
- E5：完整长路径任务 vs oracle局部边界任务；
- E7：末次竞争位置的出度配对干预；
- E2：给定不可满足前缀后的长度、合法性和退出动作。

E3、E4、E6不再与核心实验平行陈列：

- E3已经标“删除”，彻底删；
- E4作为终点文本支持的稳健性分析放附录；
- E6作为hop与出度趋势的辅助分析放附录，必要时在正文引用一句。

### 4.4 模型、统计和分母

集中说明：

- 模型版本和推理模式；
- 解码设置；
- 配对单位；
- bootstrap层级；
- 解析失败如何处理；
- 哪些指标按task、哪些按sample pair计算。

附录现有的大分母表可以移到这里简述，完整表仍留附录。

---

## 第5章 主实验：三种需要解释的异常

这是整篇论文目前最缺的一章。它不是贡献证明章，而是问题发现章。

### 5.1 输出契约改变表现，而不是单纯暴露更多信息

展示主实验中：

- answer-only、path-basic、path-strict表现差异；
- 答案层锚定如何随接口变化；
- 但明确写：主实验同时改变了路径字段和约束，不能单独归因于输出契约。

结尾自然提出：

> 这种变化究竟来自图更容易了，还是提交路径本身改变了行为？E1对此进行最小检验。

### 5.2 长路径错误集中在最后一次竞争选择

展示：

- 在同样出度的决策点中，条件失败风险并不相同；
- 三个模型都在最后一个存在同源竞争的位置最脆弱；
- 不把终步99%当作排除位置退化的因果证据。

结尾提出两个问题：

- 完整长路径任务与从该处直接开始的局部任务有何差别？
- 关键是候选不断增多，还是竞争从无到有？

分别引向E5和E7。

### 5.3 模型进入dead后仍维持形式要求

展示：

- PathValid随hop下降；
- TAC、格式和长度保持较高；
- 非法续写随深度增加。

这里只描述现象，不说“模型主动牺牲合法性”，因为主实验里的dead状态是模型自己造成的，前缀来源、能力和后续行为混在一起。

结尾引向E2：

> 需要把模型如何进入dead与进入dead后如何处置分开。

### 5.4 主实验小结：三个待检验问题

只列三个问题，不再总结几十个数字：

1. 接口是否因果改变行为？
2. 最后一次竞争选择为何特殊？
3. dead状态下，模型为何继续生成非法路径？

这一小节就是主实验与第6章的桥。

---

## 第6章 受控实验：三条发现

这才是贡献章。每节都按统一结构写：

> 一句可复述的发现 → 最小干预 → 核心数字 → 证据边界 → 对主实验的解释。

### 6.1 输出契约是行为干预：E1

核心句就是贡献一。

只需要证明：

- 图、候选、边序和问题不变；
- 仅从答案字段改成路径＋答案；
- DeepSeek 2.4%→85.6%，其他模型作跨模型验证。

不要把GFI、PathValid的完整定义在这里重讲。

### 6.2 最后一次竞争选择具有特殊脆弱性：主实验＋E5＋E7

分三步：

1. 主实验：等出度位置比较，风险集中在最后一次竞争。
2. E5：换成oracle局部任务后，同一位置改善；只解释为完整任务与局部任务间的可恢复差距。
3. E7：竞争从无到有变化最大，继续增加候选的边际效应较小。

这一节不要写：

- “证明了纯前缀机制”；
- “竞争数量完全不重要”；
- “阈值效应”；
- “两个因素扮演不同角色”。

可以把贡献二压成一句标题式结论：

> 相同的局部选择在长路径中并不同等困难，模型特别脆弱于最后一次竞争选择，并对竞争的出现比其后续增量更敏感。

### 6.3 模型缺的不是更强警告，而是合法失败动作：E2

这节可以比当前贡献三更锋利：

- 给定相同dead前缀；
- 没有退出动作且要求补足长度时，几乎必然非法续写；
- 加强合法性措辞最多下降1.4pp；
- 提供合法退出动作下降80pp以上。

主发现不是“自洽不代表合法”，而是：

> 当任务状态已经不可满足时，输出契约提供哪些合法动作，会决定模型牺牲哪项约束。

需要分清：

- external prefix：干净地研究“进入dead以后”；
- self prefix：验证这种行为也会出现在模型自己走入dead的情况下，但不能作为同样干净的因果证据。

---
## 第8章 局限性

旧版局限性大部分可以恢复，但要增加两点：

1. 主实验到最小实验属于“现象发现—独立干预检验”，不是预注册确认性研究；不能据此声称完全排除了事后假设形成。
2. E5同时改变起始位置、任务上下文和有效搜索范围，因此只能支持完整任务与局部任务之间存在可恢复差距。

删除旧结论里基于9—25个样本的“答案层错误迁移成非法路径”强结论，除非正文明确报告样本量并降成探索性观察。

---

## 第9章 结论

结论只说三件事：

1. 输出契约是行为变量，不是透明窗口。
2. 长路径失败集中暴露于最后一次竞争选择。
3. 不可满足状态下，合法动作空间决定模型继续违法还是退出。

最后一句落在实践含义：

> 可靠的图推理评测不仅要检查答案和解释是否自洽，还必须固定输出契约、逐边验证路径，并允许系统合法表达无法继续。

不要重新罗列所有指标，也不要在结论中首次引入retry或模型异质性。

---

## 附录怎么整理

当前附录最乱，建议重新编号：

- Appendix A：图生成、唯一性验证、prompt全文；
- Appendix B：指标分母、解析规则、统计方法；
- Appendix C：E1、E2、E5、E7完整设计；
- Appendix D：完整主实验表与稳健性分析；
- Appendix E：E4、E6、thinking、retry等辅助实验。

必须清理：

- 两个“A.2”重复编号；
- `B.3 固定正文与终点边位置（删除）`；
- 空白结果表；
- 不存在的§4.4、§5.4、§6引用；
- B.6末尾未写完的公式和残句；
- 尚未完成的背景扰动对照，不能以已完成实验口吻保留。

最终整篇文章的叙事应该是：

> **主实验发现三种异常，但不能给出归因；E1隔离接口效应，E5/E7定位末次竞争瓶颈，E2隔离dead状态后的动作选择；讨论再把三者串成一条行为链。**

这既保住了主实验的价值，也让三条贡献都有明确、独立的证据来源。



------

你的感觉是对的：**当前写法仍像“我们测到三个数发生变化”，还没有把发现提炼成一条新的行为认识。**但数据本身已经不只是数字结论了。现在够不够ACL，关键不在继续跑更多数字，而在于把现有结果组织成一个更高层的发现。

我的判断是：

> **现有证据足以支撑一篇ACL Findings规模的论文，但目前的贡献表述还没有达到这个强度。对ACL主会，我不会说已经稳。**

真正的发现不是“最后一跳错误率高”或“退出动作降低非法率”，而是：

> **长路径失败具有“进入—处置”的两阶段结构：任务边界与局部竞争影响模型何时进入无合法后继状态；进入之后，输出契约提供的可执行动作决定模型停止、退出还是编造图外边。**

这句话才是论文可以被别人复述和引用的东西。

## 贡献二应该是什么发现

现在贡献二写成“某位置风险最高＋若干干预数字”，像实验报告。它真正揭示的是：

> **结构相同的局部选择在长路径中并不具有位置可交换性，模型会在最后一次同源终止竞争处形成稳定的决策边界。**

这个发现有三层证据：

- 候选数相同，但风险集中在特定位置，因此不是简单的局部候选数量差异；
- 改变完整任务与局部任务的边界会改变通过该位置的表现；
- 仅加入一个同源终止竞争者，就在18个模型—跳数—契约组合中稳定造成5.75–25.25个百分点的下降。

它不是“某个数高”，而是：

> **局部选择的困难由它在完整推理过程中的位置和竞争结构共同塑造，不能把长路径看成若干同质局部决策的简单串联。**

不过不要再写“继续增加候选影响递减”。现有 \(D=2,3,4\) 数据并不稳定支持这一点。新补的 \(D=1\) 至 \(D=2\) 才是最干净的核心发现。

## 贡献三应该是什么发现

贡献三其实是全篇最有ACL味道的一条。它真正说明的是：

> **在约束不可同时满足时，重申规则不能替代一个可执行的合规动作。**

这不是普通的提示词效应。你的设计固定了无合法后继状态，并区分：

- 是否要求规定长度；
- 是否强调逐边合法；
- 是否提供显式退出动作。

结果表明，单纯加强合法性声明几乎不能改变处置；而加入退出动作后，非法续写与其他不合规响应的合计比例下降86.3–100个百分点，行为主要重新分配为退出或停止。

因此可以提炼成：

> **模型的约束遵循不仅取决于规则写了什么，还取决于输出契约允许它做什么。**

这有直接的领域外意义：验证器、结构化生成、工具调用和智能体设计不能只强调“不得违规”，还必须为不可满足状态设计合法动作。

## GIF能不能算贡献

能，但它是评测方法贡献，不是经验发现。它的价值在于为上面两条发现提供了可复现的观测手段：

- 配对反事实图；
- 序列化控制；
- 逐边符号验证；
- 契约条件化报告；
- 明确的分母与失败分类。

如果代码、数据生成器和验证器能够公开，GIF够做第一条方法贡献；如果只是给几个指标起了总名，就会被认为是包装。

## 建议把三条贡献改成这个层次

> 第一，本文提出GIF，一套面向序列化图推理的双层忠实性评测框架。GIF通过配对反事实图与序列化控制识别答案层的非结构依赖，并通过逐边符号验证区分路径自洽与图上合法性，从而为图推理失败的形成位置和处置方式提供可程序验证的诊断。
> 
> 第二，本文发现，长路径失败具有明确的决策边界：在局部候选数相同的情况下，三个模型的条件失败风险均在最后一次同源终止竞争处达到最高。任务边界干预改变了模型通过该位置的表现，而严格配对的局部干预进一步表明，仅加入一个同源终止竞争者即可稳定降低选择成功率。该结果说明，长路径并非若干位置可交换的局部选择，完整任务条件与局部竞争共同塑造了末次竞争处的特殊脆弱性。
> 
> 第三，本文发现，不可满足状态下的约束处置受可执行动作支配：单纯强化逐边合法性声明几乎不能改变模型的失败行为，而提供显式退出动作会使非法续写与其他不合规响应下降86.3–100个百分点，并将行为重新分配为退出或停止。该结果表明，规则声明本身不足以保证结构遵循；输出契约是否提供合规的失败动作，是决定模型如何解决约束冲突的关键条件。

## 投稿前必须修的两个口径

第一，当前贡献三的“98.6%–100%降至0%–7.1%”来自条件分母，不能作为主要因果数字。正文主结果应使用固定400次调用分母上的86.3–100个百分点变化，条件结果只能作为补充。

第二，\(\Delta_{\mathrm{illegal}}\) 不能写成“TAC与PathValid之差”，也不能直接解释为“长度正确但包含图外边”。它表示TAC通过而PathValid失败，失败还可能来自长度不足、错误起点等。图外续写必须依靠轨迹分解单独证明。

所以结论不是“数据不够形成发现”，而是：

> **你已经有两条真正的行为发现，但现在仍用指标变化的语言描述它们。论文需要从“哪些数字变了”提升为“图路径失败如何形成、进入后如何被处置”。**

做到这一点，加上统计口径清理和完整开源资产，我认为它够一篇扎实的ACL Findings论文。




诊断实验  和 受控实验  行吗











注释 1 对，B真正想表达的是“约束类型的选择性遵循”，而不是单纯的“长度战胜合法性”。

但“模型只靠自己能验证”略带内部机制推断。更稳妥的操作化分类是：

- 响应局部约束：只需结合一个简单任务参数检查当前输出，例如根据规定跳数计算节点数量。
- 实例关系约束：必须把输出中的每次转移与当前输入图的边集合逐项比对，例如逐边合法性。

E2制造了一个两者无法同时满足的状态。DeepSeek和Qwen通过补一个图外节点满足规定长度，说明它们在行为上维持了响应局部约束，而违反了需要回查实例图的关系约束。合法性声明的零效应和长度关闭后的停止行为，使这个取舍更加明确。

因此，B可以概括为：

> 在约束无法同时满足时，DeepSeek与Qwen表现出对不同核验类型约束的选择性遵循：模型倾向于维持可由当前输出与简单任务参数直接核验的长度要求，而违反必须回查实例图才能判定的逐边合法性。












