既有工作表明输出形式或显式推理干预会改变模型表现；**本文进一步指出，这种接口依赖在忠实性评测中造成了特殊的测量两难：增强接口既扩展可观测维度，又改变被测行为，因此不同接口下的诊断量不能自动视为同一潜在忠实性量的可比读数。**

同一个 sample_id，在 minimal 下输出类别 A，在 strict 下输出类别 B。


拓扑决定哪些失败机制有机会被触发；接口决定哪些维度可观察，同时还作为干预改变模型行为。因而忠实性指标必须被理解为“拓扑 × 接口”条件下的诊断结果，不能由一个不注明条件的单一数值完整报告。

想尽量少干预模型地测答案层，就只能接受路径过程不可观测；想把路径忠实性变得可测，就必须接受强接口可能改变模型原来的作答行为。

在缺少跨接口测量等价性假设的情况下，不能从这些接口条件化指标中识别出一个统一、接口无关的忠实性标量

- 弱接口产生的是“只报答案”条件下的行为；
- 强接口产生的是“必须生成完整路径”条件下的行为；
- 强接口不是把弱接口下已经发生的路径显示出来，而是让模型重新执行了另一套生成任务；
- 因此，两边不是同一行为的两个观察角度。

在缺少跨接口测量等价性的情况下，不能从 GFI 与 $\Delta_{\mathrm{illegal}}$ 中识别出一个单一、接口无关的忠实性度量。本文的固定拓扑对照进一步表明，该等价性不能被直接假定：增强输出契约不仅使路径合法性变得可检验，也改变了模型的选择行为与失败空间。因此，两层指标应被理解为不同接口条件下的诊断结果，而非同一潜在忠实性标量的可比读数。

---
### Compact Constraint Encoding：不用引，但它提的那个反驳你得挡

那篇发现：模型有自己的"默认写法"，你的要求跟它默认的一致就照办，不一致就大概率不听。

**为什么和你有关：** 有人会拿这个来反驳你——

> 模型走进死路还继续编节点，是不是就是因为"接着往下写"是它的本能？跟你说的"查图费劲"没关系啊。

这个反驳很自然，而且要是成立，你 §6.5 整章就塌了。

**但你已经有反驳的材料了：给个退出口，非法续写掉 80 多个点。**

如果真是"本能就想接着写"，那你多给一个"我走不下去了"的选项，本能又没变，效果不该这么大。效果这么大，说明问题不在本能，在**它被逼着必须二选一**。

所以：文献不用引，但在 §6.5 花两三句把这个替代解释点出来、然后用退出口实验挡掉。审稿人自己想到这一层时，发现你已经处理过了。




我明白了，不能问弱接口下的 anchor 到强接口下去了哪里？迁移不成立是因为接口本身就是行为干预

|贡献|概念/方法落点|实证落点|
|---|---|---|
|C1 测量两难与接口干预|§3|§5|
|C2 双层诊断的操作化|§4|§5|
|C3 不可满足状态下的约束取舍|§6 的实验设计|§6 的结果|








---
TAC 检验模型声明的答案是否与其生成路径的末节点一致，属于输出层面的自洽性检验 [11]。
[11] On Measuring Faithfulness or Self-consistency of Natural Language Explanations
Parcalabescu and Frank 指出，若检验仅比较模型可观察输出之间的一致关系，其测量对象更接近输出层自洽性，而非对内部计算的忠实性 [11]。借用这一区分，本文将 TAC 视为较弱的表面判据，将 $\mathrm{TAC} \land \mathrm{PathVaild}$ 作为更严格的结构判据，二者之间的差距构成路径层忠实性缺口。

---

答案层面，已有反事实评测通过改变输入证据并观察答案是否相应变化，检验模型是否依赖给定上下文而非参数记忆或事实共现。例如，CofCA 构造事实与反事实多跳问答样本，以评估模型对上下文证据及推理链的依赖 [CofCA编号]。然而，在序列化图推理中，改变图结构往往也会同步改变节点位置、边顺序与文本排列；已有研究表明，即使图结构等价，仅改变节点标号、边的排列或图描述顺序，也可能显著改变模型预测 [Lost in Serialization编号；新增图描述顺序论文编号]。因此，答案随反事实图变化只能构成较弱的行为证据，不能单独证明模型响应的是图拓扑。本文进一步引入位置控制序列化，比较 Raw GIS 与 PC-GIS，以分离图结构敏感性与序列化位置诱发的表面敏感性。


图本身不具有固定的线性顺序，而 LLM 接受的是 token 序列。已有研究发现，在图结构保持不变的情况下，仅改变节点标号、边的排列或描述格式，模型预测就可能发生变化 [1]；图描述的呈现顺序同样会影响模型在图任务上的表现 [x]。因此，当模型的答案随图干预而改变时，这一变化未必来自拓扑本身——改变图结构的同时，节点内容、边顺序与终点位置也一并改变了。答案层的干预敏感性由此成为必要而非充分的忠实性证据。