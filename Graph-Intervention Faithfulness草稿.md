# Introduction

## 大背景（说明这个方向为什么重要）

图结构广泛存在于知识图谱、推荐系统、任务规划等场景中，随着大语言模型（LLM）在数学、代码、自然语言推理等任务上的能力不断提升，LLM正在逐渐成为图神经网络（GNN）之外的另一种图推理方案。研究者把图的节点、边、路径或邻接关系等序列化成文本表示，塞进prompt中[3]，让LLM利用其在大规模文本语料训练上获得的推理能力，对图问题进行推理，已成为知识图谱问答（KGQA)[21]、图增强检索生成（graph-RAG)[22]等智能体规划的重要方向


%% 大模型正越来越多地被当作"图推理器"使用：研究者把图结构序列化成文本塞进 prompt，让模型沿路径游走、选择关系、或回答受图约束的问题。这一范式已成为知识图谱问答（KGQA）、图增强检索生成（graph-RAG）乃至智能体规划的核心环节。在这些系统里，一个普遍的隐含做法是：**只要模型在这些任务上答得准、或答案会随图的改动而改变，就认为模型"用上了图结构"**——图被当成了可信赖的推理底座。 %%

## 现有方法的问题（不要只说“previous work has limitations”，要具体）

 GNN天然具有置换不变性，而基于图生成的文本编码进行推理的LLM，并不能保证类似的一致输出。
 
 
 
 
 
 
 
 
**第 1 段 · 大背景**

大模型正越来越多地被当作"图推理器"使用：研究者把图结构序列化成文本塞进 prompt，让模型沿路径游走、选择关系、或回答受图约束的问题。这一范式已经成为知识图谱问答（KGQA）、图增强检索生成（graph-RAG）乃至智能体任务规划的核心环节——从让模型"沿关系走"而非"找文本"，到把检索到的子图序列化进上下文当作推理骨架，再到用图来验证或约束模型的多步决策。在所有这些系统里都潜伏着一个共同且很少被言明的假设：**只要模型在这些任务上答得准、或者答案会随着图的改动而改变，就说明它"用上了"图结构**。图，被默认成了一个可信赖的推理底座。

**第 2 段 · 现有方法的问题**

但"答得准 = 用了图"这个假设从未被真正验证，而它很可能是错的。较高的答案正确率、甚至"答案随图干预而变"这一现象，**完全可以由模型利用序列化文本里的位置线索产生，尤其是终点节点的出现位置（endpoint anchoring），而非真的沿图结构推理**。现有研究的三条路线恰好都够不到这一层。其一，忠实性评测的_口头承认_路线只问"模型有没有在 CoT 里说出承重因素"，并依赖一致性极低（κ≈0.06）的主观判定，测的是 verbalization 而非模型是否因果地依赖结构；其_行为/末端任务_路线（如按上下文长度做消融）只能说明"删掉某成分会掉分"，区分不了这一成分是被忠实遵循、还是只是顺带进了上下文。其二，graph-RAG 一侧普遍用检索消融自证"图有用"，却**只把这件事验证到检索层，把最该检验忠实性的生成层完全留空**；更有甚者默认"工具调用轨迹 = 真实推理"，在显式图约束下却出现灾难性失败。其三，近期主流的提升路线（把图推理解耦为"设计算法 + 代码执行"）通过**把推理外包给符号执行**来提升准确率——这恰恰是回避、而非验证"模型是否真在图上推理"。结果是：学界缺少一个能在生成层、把**真实图跟随**从**序列化位置捷径**中分离出来的因果诊断工具。

**第 3 段 · 核心洞察（灵魂）**## 第 3 段：忠实性背景，引用 2–3 篇

用来说明：

> CoT 看起来合理，不等于真实反映模型使用了什么信息。

我们的核心洞察是：**"答案随图干预而变"并不是忠实图推理的充分证据——表面上的图跟随，可能只是序列化位置的产物。** 要把真实的图跟随从位置捷径里剥离出来，关键不在看准确率，而在一次精心控制的对照：**在保持图语义、答案和 gold path 全部不变的前提下，只移动终点节点在序列化文本中的位置**。如果模型行为随之改变，说明它消费的是位置而非结构。由此，"原始图干预敏感度"与"位置控制后的敏感度"之间的落差，就量化了一个我们称之为**图推理虚胖（graph-reasoning inflation）**的现象：

GFI=Raw GIS−PC-GIS.\mathrm{GFI} = \mathrm{Raw\ GIS} - \mathrm{PC\text{-}GIS}.GFI=Raw GIS−PC-GIS.

这一刀切下去，"模型在图上推理"这件被普遍默认的事，第一次成为一个可被证伪的因果命题。

**第 4 段 · 你怎么验证**## 第 4 段：你的缺口和方法，引用 1–2 篇即可

这里主要写你自己的贡献，不要堆引用。

基于这一洞察，我们提出 **Graph-Intervention Faithfulness（GIF）**，一个黑盒、任务无关的诊断型评测框架。GIF 通过**成对反事实图干预**（构造 G1/G2，并用 BFS/DFS 符号程序验证 gold path 在每张图上唯一）与**四种位置控制序列化版本**（endpoint-first、endpoint-middle、endpoint-last、decoy-last）的对照，报告 Raw GIS、PC-GIS、核心指标 GFI、终点锚定率（EAR）、路径合法性、轨迹-答案一致性与失败跳数。我们在多跳符号图上评测，并系统对比两种提示接口——_仅要求输出答案_ 与 _要求显式输出结构化路径 + 答案_——下的图忠实性表现。整个诊断**不要求模型"说出"推理过程**：所有指标都从干预前后的输出行为算出，因而绕开了口头承认路线对主观判定的依赖。

**第 5 段 · 贡献（3 条）**

1. **问题（缺口）**：我们指出，"高准确率 / 图干预敏感度"这一被广泛采用的证据**无法区分忠实图跟随与序列化位置捷径**；既有忠实性工作停在口头承认层或末端行为层，graph-RAG 只验到检索层——**生成层的结构忠实性至今无人量化**。
2. **方法**：我们提出 GIF——成对反事实干预 + 四种位置控制 + 符号验证 gold path 的黑盒诊断框架，并给出核心指标 GFI=Raw GIS−PC-GIS\mathrm{GFI}=\mathrm{Raw\ GIS}-\mathrm{PC\text{-}GIS} GFI=Raw GIS−PC-GIS 量化图推理虚胖、以 EAR 单独隔离终点锚定。
3. **实验**：我们在多跳符号图上发现，仅要求输出答案时表面图跟随很高、**一旦控制终点位置即近乎塌陷，暴露出严重的图推理虚胖**；要求显式输出结构化路径能大幅压低终点锚定。基于此，我们论证**忠实的图推理是接口依赖的**——可由提示设计与外部可验证机制改善，而非仅靠扩大模型规模。









### 🎯 第一梯队 · 定义 GIF 自身 / 直接动机（4 篇，最核心）

   ** 未命名.md** — 你的中文摘要草稿，全文骨架来源。
1. **图没变，字变了：LLM 图推理的序列化不变性危机**（Lost in Serialization）— GIF 最近邻，序列化不变性危机 = 引言第 2–3 段的直接动机。
2. **图帮文本去噪，文本帮图复活**（TGS-RAG）— GFI = Raw GIS − PC-GIS 精确表述、pilot 数字、"生成层留空"缺口、真实 RAG testbed。
3. **别让大模型手算图：图推理应从复现算法转向设计算法**（Simple-RTC）— "工程绕过 vs 诊断显微镜"的对照，metrics 语境。

### 🔬 第二梯队 · 忠实性谱系（Related Work 主战场，9 篇）

4. **思维链会招供，最终答案会闭嘴**（Lie to Me, Young 2026）— verbalization vs structural faithfulness 的关键区分；跨家族 roster 借用源。
5. **思维链不忠实：模型如何把偏见包装成推理**（Turpin 2023）— 行为干预测忠实性的思想祖先。
6. **给 CoT 推理拍 X 光：用计算图验证**（CRV）— 把 GIF 定位成黑盒/任务无关诊断框架；pilot 规模小→定位为 diagnostic benchmark 的依据。
7. **忠实性不是"看起来合理"：NLP 解释该如何证明自己没撒谎** — faithfulness 的严格定义来源，写 intro/定义节必引。
8. **会解释，不代表真会想：大模型忠实思维链为何困难**（Hardness of Faithful CoT）— "忠实本身就难"的理论背书。
9. **大模型的"推理小作文"可信吗？**（Measuring Faithfulness in CoT）— 忠实性测量方法谱系。
10. **先有答案，再编理由：真实场景中的 CoT 不忠实**（CoT in the Wild）— 真实场景不忠实的证据。
11. **偏见进了答案，却没进思维链**（Bias & CoT Faithfulness, VLLM）— 偏置注入法的旁证。
12. **模型嘴里的推理，未必是脑中的理由：CoT 忠实性失真** — 忠实性失真证据。⚠️ 这篇 frontmatter 的"论文题目"写成了 FiDeLiS，疑似复制错，建议核对。

### 📍 第三梯队 · 位置偏置/序列化（支撑 endpoint anchoring，5 篇）

13. **排名别看位置：InvariRank** — "注意力泄漏 ≫ 位置编码"的机制分解；endpoint anchoring 升级到机制归因的钩子。
14. **排在前面就更香：RISE** — 位置偏置量化框架；给你第二条反锚定干预臂（逐跳单步选择）。
15. **把图塞进 KV：Graph-KV** — 强制串行化→位置偏置/Lost-in-the-Middle，序列化弊端的总论来源。
16. **选项换个座位，模型换个答案**（MCQ 选项顺序敏感）— 位置敏感的跨任务旁证。
17. **顺序一换，模型就乱：LLM 输入排列敏感性** — 排列敏感跨 benchmark 证据。⚠️ frontmatter"论文题目"写成 Graph-constrained Reasoning，明显是复制错，建议修。

### 🧭 第四梯队 · 图推理方法（被诊断的"增强路线" + 数据/testbed，11 篇）

18. **图结构不是文本：LLM 需要导航**（GraphWalk）— 底座（合成图生成器）+ 反例 + 弹药三合一。
19. **边看边走：SoG**（Search-on-Graph）— 最佳真实世界 testbed。
20. **把计划画成图：用 GNN 抓任务规划错接漏步**（GNNVerifier）— 真实图数据源（TaskBench/UltraTool）。
[21]. **路径别全塞：KGQA 需要看几跳历史？**（BPC）— 行为冗余的层级互补论据。
[22]. **让大模型沿着图走：知识图谱把推理从"表演"拉回"路径"**（RoG）— 忠实 KG 推理代表方法。
21. **别让大模型硬算图：拆成三步调用图工具**（GCR, Graph-constrained Reasoning）— 标题自带"faithful reasoning"，正面对照。
22. **别让 CoT 瞎想：让大模型顺着知识图谱走**（Follow the Path）— KG 路径推理。
23. **让大模型沿着图走：Graph-CoT** — RAG 从找文本升级到走关系。
24. **给大模型一张地图：FiDeLiS** — 忠实 KGQA 约束路径。
25. **边想边查图：用知识图谱逐步补脑**（Graph-Augmented Reasoning）— 迭代式 KG 检索。
26. **KG 当奖励模型：路径对齐训练组合推理** — 下游意义/安全攸关迁移的研究故事（"忠实诊断是前置必要条件"）。
27. **LLM 真的会看懂图吗？**（Revisiting Graph Reasoning Ability）— 图推理失败的实证弹药。

### 📐 第五梯队 · 多跳评测/反事实基准（3 篇）

30. **别让模型背答案：CofCA** — 反事实多跳、抗记忆虚胖，与你"反事实图构造"是姊妹方法。
31. **Omanic：一步步检查多跳推理** — step-wise 多跳评测。
32. **答案对了，不代表会推理**（多维度推理步评估）— 推理质量评测维度。

### 🛰 第六梯队 · 验证/Agent（弱关联，按需，3 篇）

33. **把推理拆成图：GoV 用 DAG 验算** — DAG 拓扑序验证骨架。
34. **把多条思路织成一张图：GraphReason** — 图结构验证法。
35. **从"会调用"到"会纠错"：工具增强大模型的元验证与反思学习** — 工具增强忠实性，关联 verify-retry 那条实验线。
36. **Thinking While Listening**（FSRM）— 最弱，仅"递归架构能自发学结构但是否忠实仍需诊断"一句。