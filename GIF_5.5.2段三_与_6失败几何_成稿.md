# §5.5.2 段三与 §6 "失败几何" 段成稿(中英)

> 数值均取自最终版分析(canonical absorbed 口径)。方括号 [核] 处为动笔前最后核对项,见文末清单。

---

## §5.5.2 段三:retry 收益的边界由实例级异质性刻画(中文)

verifier-retry 的收益为何在预算内到顶?逐轮经验危险率给出第一条线索:各模型的单轮通过概率随轮次单调塌陷(如 DeepSeek 12-hop 从首轮 0.48 依次降至 0.21、0.12、0.12、0.07),时齐性诊断在 18 个设置中仅 GPT-12-hop 通过 δ=0.10(其余 max TV 置信上界 0.10–0.30,附录 X)。这一模式与实例级成功概率异质性(instance-level success-probability heterogeneity)一致:可修实例在早期轮次被吸收带走,风险集逐轮向低成功概率实例浓缩。三个 run 的首轮结果直接证实该异质性是实例稳定属性而非 run 间波动:各设置 run 间首轮通过率无显著差异(Cochran Q,最小 p=0.34),而对 DeepSeek,92.9%(8-hop)/91.3%(12-hop)的实例在三次独立首轮尝试中结局完全一致——同质基线下的预期一致率仅为 33.1%/25.1%;Beta-Binomial 过散度检验拒绝同质性 [核:LRT 统计量],拟合形状参数 α̂,β̂ 均远小于 1(8-hop:0.079/0.040),对应近两点的 U 形分布:首轮成败近乎是(模型 × 实例)对的确定性函数。

据此可将同质 iid 基线的缺口定量分解。以三个 run 的首轮结果拟合 Beta-Binomial,得到异质性校正的独立重试基线 J(K);观测曲线 A(K) 在全部六格中位于 H(K) 与 J(K) 之间(如 DeepSeek 12-hop:H(5)=96.4%,A(5)=70.6%,J(5)=54.7% [95% 带见附录])。H−J 在各格均不小于 H−A,即实例级异质性足以解释同质基线的全部虚高;而 A(5) 高于 J(5)(GPT-12-hop 处为边际:24.5% 对带上界 23.1%)表明真实 retry 优于"固定成功率的独立重抽",与 verifier 反馈的净正效应一致。该正效应有两条不依赖 J 估计的独立证据:其一,Qwen-12-hop 出现符号反转——首轮失败后的第二轮通过率(9.6%)反而高于总体首轮通过率(8.1%;差 +1.5 个百分点,bootstrap 95% CI [+0.1, +2.9],单侧 p=0.018)——纯选择效应下该差值不可能为正,反转幅度不大但方向确定;其二,即使在三个 run 首轮全部失败的稳定失败池内,单条 retry 轨迹仍在 K≤5 内吸收其中相当比例(DeepSeek:8-hop 53.8%、12-hop 40.8%;GPT-12-hop 仅 16.9%)。综合而言:retry 的收益边界不由预算决定,而由底座模型在实例上的稳定成功概率分布决定;反馈能够撬动稳定失败池的一部分,其撬动比例本身随模型与深度衰减。本节诊断不区分"实例固定属性"与"轮间上下文依赖"对轮间正相关的相对贡献,fresh-context 独立重采样对照留作 future work。

## §5.5.2 Paragraph 3 (English)

Why do verifier-retry gains saturate within budget? Per-round empirical hazard rates provide the first clue: single-round pass probability collapses monotonically across rounds (e.g., DeepSeek at 12-hop: 0.48, 0.21, 0.12, 0.12, 0.07), and the time-homogeneity diagnostic passes in only one of eighteen settings (GPT at 12-hop; elsewhere max TV upper bounds range 0.10–0.30; Appendix X). This pattern is consistent with instance-level success-probability heterogeneity: repairable instances are absorbed early, and the risk set concentrates round by round toward low-probability instances. Three independent first attempts per instance confirm that this heterogeneity is an instance-stable property rather than run-level instability: first-attempt pass rates do not differ across runs (Cochran's Q, min p=0.34), whereas for DeepSeek 92.9% (8-hop) and 91.3% (12-hop) of instances have identical outcomes across all three first attempts—against homogeneous-baseline expectations of only 33.1% and 25.1%. A Beta-Binomial overdispersion test rejects homogeneity [LRT statistic], with fitted shape parameters far below one (8-hop: 0.079/0.040), corresponding to a nearly two-point, U-shaped distribution: first-attempt outcome is close to a deterministic function of the (model, instance) pair.

This admits a quantitative decomposition of the homogeneous-iid deficit. Fitting a Beta-Binomial to first-attempt successes yields a heterogeneity-adjusted independent-retry baseline J(K); the observed curve A(K) lies between H(K) and J(K) in all six cells (e.g., DeepSeek 12-hop: H(5)=96.4%, A(5)=70.6%, J(5)=54.7%; 95% bands in Appendix). H−J is at least as large as H−A in every cell—instance-level heterogeneity alone suffices to account for the inflation of the homogeneous baseline—while A(5)>J(5) (marginal for GPT at 12-hop: 24.5% versus a band upper bound of 23.1%) indicates that actual retry outperforms independent resampling at fixed per-instance rates, consistent with a net positive effect of verifier feedback. Two lines of evidence for this positive effect do not depend on the estimate of J. First, Qwen at 12-hop exhibits a sign reversal: the second-attempt pass rate conditional on first-attempt failure (9.6%) exceeds the marginal first-attempt rate (8.1%; +1.5pp, bootstrap 95% CI [+0.1, +2.9], one-sided p=0.018)—under pure selection this difference cannot be positive; the reversal is modest but directionally unambiguous. Second, even within the stable-failure pool of instances failing all three first attempts, a single retry trajectory still absorbs a substantial fraction within K≤5 (DeepSeek: 53.8% at 8-hop, 40.8% at 12-hop; GPT at 12-hop only 16.9%). Taken together, the ceiling on retry gains is set not by budget but by the model's stable per-instance success distribution; feedback can pry loose part of the stable-failure pool, and that fraction itself decays with model and depth. Our diagnostics do not separate fixed instance properties from within-episode contextual dependence as sources of the across-round correlation; a fresh-context resampling control is left to future work.

---

## §6 "失败几何的模型指纹" 段(中文)

四组相互独立的测量收敛于同一结论:深分叉图上失败的组织方式是模型特异的,失败身份主要属于(模型 × 实例)配对,而非图实例的内禀属性。其一,FailureHop 分布:DeepSeek 呈双峰集中(熵 0.803 [核:以定稿数据为准])而 Qwen 散射(熵 0.927)。其二,失败终点集中度:同一实例多轮失败的答案终点 HHI 在全部六格显著高于图结构基线(在该实例的非金标终端节点上均匀抽样的期望 HHI),比值 1.46–1.72×,其中 Qwen 在两个深度上均最低——与其"illegal 自环最黏(0.91)而失败终点最散"的画像一致:Qwen 每轮都失败,但失败的落点在换;DeepSeek 与 GPT 的失败更集中于同一终点吸引子。其三,失败集的时间稳定性:同一模型跨 run 的首轮失败集 Jaccard 大幅超出按集合规模计算的独立基线(DeepSeek 8-hop:0.868 对基线 0.20);对失败率超过 90% 的格(GPT/Qwen 12-hop)该指标饱和、不具区分力,其稳定性证据由过散度检验承担。其四,也是裁决性的一组:跨模型失败集的重叠仅微弱超出独立基线(超出量 +0.003 至 +0.051),与模型内跨 run 的超出量(+0.55 至 +0.67)相差一个数量级——若失败由实例结构驱动,两个超出量应同量级;实测的悬殊比值表明,对一个模型稳定不可通过的实例集合,对另一个模型基本是普通实例。两点注记:GPT 的失败答案约 30% 不在图节点集内,其 HHI 部分反映无效节点吸引子而非图内终点吸引子 [核:图内子集敏感性分析];终点吸引子与设计诱饵 z 的重合率见 [核:decoy 占比],该比率联结本段与 §5.4 的锚定证据。

## §6 Failure-Geometry Fingerprint (English)

Four mutually independent measurements converge on the same conclusion: on deep branching graphs, the organization of failure is model-specific—failure identity belongs primarily to the (model, instance) pair rather than to the graph instance itself. First, FailureHop distributions: DeepSeek fails bimodally (entropy 0.803 [verify against final data]) while Qwen scatters (0.927). Second, endpoint concentration: the HHI of failed-attempt answer endpoints within an instance exceeds a graph-structural baseline (expected HHI under uniform sampling over that instance's non-gold terminal nodes) in all six cells, by factors of 1.46–1.72×, with Qwen lowest at both depths—consistent with its portrait of highest illegal self-loop stickiness (0.91) yet most dispersed failure endpoints: Qwen fails every round, but at changing endpoints, whereas DeepSeek and GPT concentrate on a stable endpoint attractor. Third, temporal stability of failure sets: within-model, cross-run Jaccard of first-attempt failure sets far exceeds the size-matched independence baseline (DeepSeek 8-hop: 0.868 versus 0.20); for cells with failure rates above 90% (GPT/Qwen at 12-hop) this index saturates and carries no discriminative power, and stability there rests on the overdispersion test instead. Fourth, and decisively: cross-model failure-set overlap exceeds its independence baseline only weakly (+0.003 to +0.051), an order of magnitude below the within-model excess (+0.55 to +0.67). Were failure driven by instance structure, the two excesses should be comparable; their disparity indicates that instances stably unsolvable for one model are, for another, largely ordinary. Two caveats: roughly 30% of GPT's failed answers fall outside the graph's node set, so part of its HHI reflects an invalid-node attractor rather than an in-graph endpoint attractor [verify: in-graph-restricted HHI sensitivity]; and the overlap between endpoint attractors and the designed decoy z is reported at [verify: decoy share], which links this paragraph to the anchoring evidence of §5.4.

---

## 动笔前最后核对清单

1. **过散度 LRT 统计量与 p 值**(六格,Beta-Binomial vs Binomial)——段三占位符。
2. **FailureHop 熵数值**以定稿数据复核(0.803/0.927 来自较早分析,反向可达性删除后确认口径未变)。
3. **GPT 图内子集 HHI 敏感性**:仅保留图内答案重算 HHI 与基线比值;若比值仍 >1.4,正文注记改为"结论对图内限制稳健"。
4. **终点吸引子 = 诱饵 z 的占比**:每实例 top 失败终点等于 z 的比例——高则与 §5.4 锚定线互证(retry 层的锚定显形),低则吸引子为特异死支终点;两个结果都可写,占位符按结果填。
5. **跨模型 Jaccard 超出量的 bootstrap CI**(+0.02~+0.05 是否出零)——决定 §6 里 "weakly" 是写"微弱且显著"还是"不显著"。
6. 措辞纪律复查:全稿无 "hard instances / 难样本 / 永不可修";正效应处 consistent with / suggests,不用 demonstrates;Qwen 反转为 "modest but directionally unambiguous"。
