
```table-of-contents
```

----
# 摘要（abstract）

大语言模型（LLM）被越来越多地用于知识图谱问答（Knowledge Graph Question Answering，KGQA）、图上搜索（Graph Search，GS）和工具增强推理（Tool-Augmented Reasoning，TAR）。这类任务通常以最终答案准确率或路径-答案的一致性作为成功标志。然而，LLM 不直接处理图，而是接受图的文本序列化表示。文本序列在表达节点和边的同时，也引入了图本身不具有的终点位置、边顺序和格式等非结构线索。因此，答案随图干预变化，或生成路径能够导向最终答案，都只是必要而非充分的行为信号，均不足以单独证明模型忠实地遵循了输入图结构。

本文提出 Graph-Intervention Faithfulness（GIF），用于诊断序列化图推理的两类假忠实性。第一类是答案层假忠实性：模型的答案看似随反事实图干预而变化，但这种敏感性在控制终点位置后可能消失；第二类是路径层假忠实性：模型给出的推理路径虽然能够导向最终答案，却可能包含输入图中不存在的非法跳转。为此，本文设计了一个双层诊断框架，结合反事实图对、位置控制序列化和符号路径验证，基于合成链式图、分叉图等设置，在 DeepSeek、Qwen、GPT 三个模型族上开展实验，分别量化答案层与路径层的结构忠实性。得到四条主要发现。第一，弱提示下链式图容易诱发答案层位置捷径：模型可以通过直接输出显著位置节点获得表面正确的反事实响应，且能力提升并不自动消解这种依赖。在 chain + answer-only 设置下，更强的 DS-Pro 反而比 DS-Flash 更稳定地利用末位与诱饵位置线索；第二，结构化输出可以在简单链式图上显著降低位置依赖，但在分叉图上揭示新的失败模式：模型生成的路径形式完整，末节点与答案一致，却包含输入图中不存在的非法跳转；第三，对锚定样本的逐样本流向追踪表明，minimal 接口下的答案层锚定错误在更强的路径输出约束下并非简单消失：在浅层分叉图上，多数锚定错误被纠正；而在更深分叉图上，相当比例转化为形式自洽但图上非法的路径输出。该转化比例在更深结构中整体更高，且转折深度因模型而异，说明更强模型可以在更长范围内维持状态追踪，但能力提升本身并不自动消除非结构依赖。链式图在本文测试深度下非法出口接近于零，提示这种迁移主要需要分叉结构中的同源候选竞争。失败来源分解进一步显示，这些非法路径包含异质的构造成分，其中早期偏离死支后继续构造占有相当比例，并非单一的末端失败模式。因此，本文将该现象解释为答案层错误向路径层的迁移，而不是终点驱动反向合理化机制的证明；第四，高推理预算下的思考模式几乎可以消除这两类失败，但延迟较高；verifier-retry 成本更低，却受底座模型状态追踪能力和样本难度异质性的限制。整体结果表明，答案正确、答案随图变化和轨迹-答案一致都不能单独证明图忠实性；序列化图推理需要同时诊断答案层位置捷径与路径层结构合法性。本文的证据进一步提示，分叉图上的路径层失败在行为上更接近答案层位置依赖在结构化输出下的延续形态，而非一种完全独立的新失败机制。

英文版：Large language models (LLMs) are increasingly used in Knowledge Graph Question Answering (KGQA), Graph Search (GS), and Tool-Augmented Reasoning (TAR). These tasks typically regard final-answer accuracy or path–answer consistency as indicators of success. However, LLMs do not process graphs directly; instead, they consume textual serializations of graphs. While textual sequences represent nodes and edges, they also introduce non-structural cues that are absent from the graph itself, such as endpoint position, edge order, and formatting. Therefore, an answer changing under graph intervention, or a generated path leading to the final answer, is only a necessary but not sufficient behavioral signal; neither is sufficient on its own to prove that the model faithfully follows the input graph structure.

This paper proposes Graph-Intervention Faithfulness (GIF), a framework for diagnosing two forms of false faithfulness in serialized graph reasoning. The first is answer-level false faithfulness: a model’s answer may appear to change in response to counterfactual graph interventions, but this sensitivity may disappear once endpoint position is controlled. The second is path-level false faithfulness: the reasoning path generated by the model may lead to the final answer while still containing illegal transitions that do not exist in the input graph. To this end, we design a two-level diagnostic framework that combines counterfactual graph pairs, position-controlled serialization, and symbolic path verification. Based on synthetic chain and branching graph settings, we conduct experiments on three model families—DeepSeek, Qwen, and GPT—to quantify structural faithfulness at both the answer and path levels. We obtain five main findings. First, under weak prompting, chain graphs readily induce answer-level positional shortcuts. Models can obtain superficially correct counterfactual responses by directly outputting the node occupying a salient textual position. Second, stronger model capability does not automatically eliminate positional dependence. In the chain + answer-only setting, the stronger DS-Pro exploits last-position and decoy-position cues more reliably than DS-Flash. Third, structured output can eliminate positional dependence on simple chain graphs, but reveals a new failure mode on branching graphs: paths that are formally complete and whose final node agrees with the answer, yet are invalid on the graph. Fourth, the FailureHop distribution and reverse-reachability enrichment jointly indicate that many illegal paths are not random deviations, but instead more closely resemble a behavioral pattern of path rationalization around an anchored target node. Fifth, high-budget thinking mode can almost eliminate both failure modes, but incurs high latency; verifier-retry is cheaper, yet its effectiveness is limited by the base model’s state-tracking ability and the heterogeneity of instance difficulty. Overall, the results show that answer correctness, responsiveness to graph changes, and trace–answer consistency cannot independently establish graph faithfulness. Serialized graph reasoning requires diagnosing both answer-level positional shortcuts and path-level structural validity. Moreover, this paper further shows that path-level failures may themselves be a disguised form of answer-level positional dependence under structured output.

关键词：忠实性；图推理；知识图谱问答；序列化偏差；位置偏差；结构化推理；验证

Key Word：faithfulness, graph reasoning, knowledge graph question answering, serialization bias, position bias, structured reasoning, verification

---
# 1 引言

大语言模型（Large Language Models，LLMs）正被广泛用于知识图谱问答、图上搜索 [19] 和工具增强推理 [23] 等任务。由于 LLM 无法直接处理抽象图，图结构通常需要被转换为边列表、邻接表或 JSON 等文本序列。然而，文本序列除了表达图的节点和边，还引入了顺序、位置和格式等图本身不具有的属性。因此，一个本应由图拓扑决定的任务，也可能被序列化过程中产生的非结构线索驱动。现有图推理研究通常将答案正确、答案随图变化 [30]，或推理轨迹导向最终答案 [22] 视为模型使用图结构的证据。本文指出，单一现象均不足以证明模型忠实地遵循了图结构，即不足以建立图忠实性（graph faithfulness）。首先，答案正确并不意味着模型真正忠实于图。模型可能依赖节点名称、候选偏好、参数记忆或答案在文本中的位置得到正确结果；其次，答案随图干预而变化也不是充分证据。改变图结构时，文本中的节点内容、边顺序或终点位置往往同时变化。因此模型可能只是响应序列化文本的变化而非图的拓扑变换。换言之，答案随图变化是图忠实性的必要条件之一，但不是充分条件；最后，模型可能生成一条推理轨迹导向最终答案，但轨迹中包含输入图中不存在的边。此时，轨迹与答案虽然相互自洽，模型却不忠实于输入图结构。

为诊断这类问题，本文提出 Graph-Intervention Faithfulness（GIF）。GIF 在答案层结合反事实图对与位置控制序列化，检验模型的答案变化究竟来自图结构，还是来自终点位置等非结构线索；在路径层使用经符号验证的路径区分“轨迹-答案一致”与“路径在图上合法”。使用随机符号节点减少常识联想、参数记忆或节点语义的影响，并通过无图控制排除答案先验和候选偏好。实验覆盖链式图与分叉图，考察 prompt 与拓扑的交互以及两种缓解机制的效果。结果显示，弱提示链式图更容易诱发答案层位置捷径，能力提升并不自动消解这种依赖，在 chain + answer-only 设置下，能力更强的模型反而可能更稳定地利用 endpoint-last / decoy-last 位置线索；结构化输出可以在简单链式图上消除位置依赖，却在更困难的分叉图上暴露新的失败形态：模型能够生成形式完整、末节点与答案一致的轨迹，但其中大量跳转并不对应输入图中的真实边。在 branching-12hop 上，Qwen Max 的结构化输出能产生[ TAC 99.2% 但 $\Delta_{\text{illegal}}$ 90.8% 的轨迹]，即形式完整、末节点与模型答案一致，却有大量样本无法通过图上合法性验证。进一步分析表明，这些非法路径并非随机走偏：FailureHop 分布与反向可达性富集共同显示，许多非法轨迹在行为上更接近围绕某个被锚定的终点进行路径合理化，而非单纯的前向状态追踪失败。高推理预算下的思考模式几乎消除这两类失败，但带来较高延迟；verifier-retry 能以较低成本缓解部分非法路径，但其收益受底座模型状态追踪能力和样本难度异质性的限制。

本文贡献如下。第一，在概念层面，本文指出答案层位置捷径与路径层非法轨迹不应被孤立理解。二者都体现了模型通过序列化文本中的非结构线索生成表面自洽输出，而非稳定追踪输入图结构：在弱输出接口下，这种失败表现为直接输出显著位置节点；在强结构化接口下，它可能转化为围绕被锚定终点合理化出的图上非法路径。因此，答案正确、答案随图变化与轨迹-答案一致这三种常用证据都只是图忠实性的局部行为信号，任何单一信号都不足以建立模型真实遵循图结构。

第二，在方法层面，本文提出图干预忠实性诊断框架 GIF，将输出接口约束、推理深度 k 与同源竞争强度 d 纳入统一的受控实验设计。链式图刻画无同源竞争的退化边界，分叉图刻画存在同源竞争时的结构追踪压力；答案层指标 GFI 用于度量弱接口下由位置线索造成的图跟随虚胖，路径层指标 Δillegal 用于度量强接口下自洽但图上非法的轨迹。通过位置控制、符号路径验证与跨深度比较，GIF 使答案层和路径层失败能够在同一实验框架内被比较、定位和追踪。

第三，在发现层面，本文揭示了错误形态随接口约束和任务深度发生迁移：在浅层结构中，结构化约束能够显著压制答案层位置捷径；但在更深、更具同源竞争压力的分叉结构中，约束并不必然消除非结构依赖，而可能将其转化为路径层非法轨迹。随着深度增加，模型从表面位置锚定转向自洽路径合理化的趋势更加明显。不同模型的转折深度不同，说明更强模型可以在更长范围内维持图上状态追踪，但能力提升本身并不自动消除位置依赖。链式图中非法出口接近于零，进一步表明这种迁移主要需要分叉结构中的同源竞争压力，而不是由深度单独导致。

英文版：
# 1 Introduction

Large Language Models (LLMs) are increasingly used in tasks such as knowledge graph question answering, graph search [19], and tool-augmented reasoning [23]. Because LLMs cannot directly operate on abstract graphs, graph structures typically need to be converted into textual sequences, such as edge lists, adjacency lists, or JSON representations. However, beyond representing nodes and edges, textual sequences also introduce properties that are absent from the graph itself, including order, position, and formatting. As a result, a task whose answer should be determined by graph topology may instead be driven by non-structural cues introduced during serialization. Existing graph-reasoning studies often regard answer correctness, responsiveness to graph changes [30], or reasoning traces that lead to the final answer [22] as evidence that a model uses the graph structure. This paper argues that no single phenomenon is sufficient to prove that a model faithfully follows the graph structure, that is, to establish graph faithfulness. First, answer correctness does not necessarily imply that the model is truly faithful to the graph. A model may obtain the correct answer by relying on node names, candidate preferences, parametric memory, or the position of the answer in the text. Second, an answer changing under graph intervention is also not sufficient evidence. When the graph structure is changed, node content, edge order, or endpoint position in the text often changes simultaneously. The model may therefore be responding to changes in the serialized text rather than to topological changes in the graph. In other words, responsiveness to graph changes is one necessary condition for graph faithfulness, but it is not sufficient. Finally, a model may generate a reasoning trace that leads to the final answer while the trace contains edges that do not exist in the input graph. In this case, although the trace and answer are internally consistent, the model is not faithful to the input graph structure.

To diagnose these problems, this paper proposes Graph-Intervention Faithfulness (GIF). At the answer level, GIF combines counterfactual graph pairs with position-controlled serialization to test whether changes in model answers are driven by the graph structure or by non-structural cues such as endpoint position. At the path level, GIF uses symbolically verified paths to distinguish “trace–answer consistency” from “validity on the graph.” We use random symbolic node identifiers to reduce the influence of commonsense associations, parametric memory, and node semantics, and introduce graph-free controls to rule out answer priors and candidate preferences. Our experiments cover both chain and branching graphs, examining the interaction between prompts and topology and comparing two mitigation mechanisms. The results show that weakly prompted chain graphs are more likely to induce answer-level positional shortcuts. Stronger models do not automatically overcome this dependence; in the chain + answer-only setting, more capable models may instead exploit endpoint-last and decoy-last positional cues more consistently. Structured output can eliminate positional dependence on simple chain graphs, but exposes a new failure mode on more challenging branching graphs: models can generate formally complete traces whose final nodes agree with their answers, while many transitions in these traces do not correspond to real edges in the input graph. On branching-12hop, Qwen Max under structured output achieves a TAC of 99.2% but a $\Delta_{\text{illegal}}$ of 90.8%, meaning that the traces are formally complete and their final nodes agree with the model’s answers, yet a large fraction of samples fail graph-validity verification. Further analysis shows that these illegal paths are not random deviations: the FailureHop distribution and reverse-reachability enrichment jointly indicate that many illegal traces behaviorally resemble path rationalization around an anchored endpoint, rather than simple failures of forward state tracking. High-budget thinking mode almost eliminates both failure modes, but incurs high latency. Verifier-retry can mitigate some illegal paths at lower cost, but its gains are limited by the base model’s state-tracking ability and the heterogeneity of instance difficulty.

The contributions of this paper are as follows. First, at the problem level, we formally distinguish three common surrogate signals of graph faithfulness—answer correctness, responsiveness to graph changes, and trace–answer consistency—from graph faithfulness itself, and show that each is necessary but not sufficient. Second, at the methodological level, we propose GIF, a graph-intervention faithfulness diagnostic framework for serialized graph reasoning. GIF combines counterfactual graph pairs, position-controlled serialization, and symbolic path verification to diagnose answer-level positional shortcuts and path-level illegal traces, and defines core metrics such as GFI and $\Delta_{\text{illegal}}$. Third, at the empirical level, we reveal that the failure modes of LLM graph reasoning transform through the interaction between prompts and topology: (1) weakly prompted chain graphs mainly expose answer-level positional shortcuts, and stronger models do not automatically overcome positional dependence; (2) structured output removes GFI on simple chain graphs, but exposes formally self-consistent yet graph-invalid paths on branching graphs; and (3) the FailureHop distribution and reverse-reachability enrichment further suggest that path-level failures on branching graphs more closely resemble a disguised form of positional dependence under structured output, rather than an independent new failure mechanism.

---
# 2. 相关工作
## 2.1 思维链忠实性

Chain-of-Thought（CoT）提示能够提升大语言模型在数学、逻辑与多步推理任务中的表现，但模型生成的推理过程未必真实反映其预测依据。早期忠实性研究区分了合理性（plausibility）与忠实性（faithfulness）：一段解释可以看起来完整可信，但不忠实于模型实际使用的证据 [7]；行为干预式研究进一步通过改变输入中的偏置信号、修改或移除模型生成的 CoT，观察最终预测是否随之变化；Turpin 等人指出，模型解释可能“看似合理，但不真正反映导致预测的因素” [5]；Lanham 等人通过修改、截断或移除 CoT 检验最终答案是否依赖所生成的推理过程 [9]。近期工作还考察模型是否会在语言化推理中报告影响其答案的提示信号 [4]；或借助内部激活与计算图开展白盒验证 [6]，这类白盒方法与本文采用的黑箱诊断视角形成互补：白盒可直接观察内部计算，但通常需要模型权重访问权限，本文则在仅有 API 访问的条件下，通过结构干预和输出验证检验图忠实性。这些研究共同表明，答案正确、解释连贯和推理忠实是不同属性。GIF 将这一观察从语言化推理迁移到序列化图推理：答案正确、答案随图变化、轨迹-答案一致都是必要但不充分的图忠实性指标。

## 2.2 图序列化及其位置敏感性

图本身通常不具有固定的线性顺序，但 LLM 接受的是 token 序列。将图转换为边列表、邻接表、JSON 或自然语言描述时，节点与边会获得图结构本身不包含的位置、排列和格式等信息。已有研究发现，LLM 图推理器缺乏序列化不变性：在图结构保持不变的情况下，仅改变节点标号、边的排列顺序或描述格式，就可能显著改变模型预测 [1]；长上下文中的中间信息更容易被忽略 [37]，多项选择题的选项顺序会影响答案 [16]，列表排序与推荐任务中的项目位置也会改变模型判断 [13,14]。反事实测评为识别此类捷径提供了重要手段。CofCA 通过构造反事实多跳问答样本，降低参数记忆和事实共现对结果的影响，从而检验模型能否依据给定上下文完成推理 [30]。与其不同，CofCA 主要在输入证据层面控制，即通过改变反事实内容，检验模型是否依赖给定上下文而非参数记忆或事实共现；本文则关注输入表示层面控制，在图任务关系通过控制位置、顺序、终点摆放，模型是否依赖图拓扑而非文本位置线索。二者构成对 LLM 推理忠实性的两类互补干预维度：前者削弱参数记忆和事实共现带来的表面正确性，后者隔离文本序列化带来的位置和顺序捷径。即使模型的答案能够随反事实图变化，这种变化也可能来自改变图结构时同步变化的节点内容、边顺序或终点位置等信息，而非图拓扑本身，模型可能只是在响应序列化文本的变化，而非图本身的拓扑变换。因此，仅观察图干预前后的答案变化仍不足以建立图忠实性。本文通过位置控制序列化，进一步区分真正的图结构敏感性与由终点位置诱发的表面敏感性。

## 2.3 图推理与工具增强推理

已有大量工作通过显式图结构增强 LLM 推理。一类方法生成或检索知识图谱关系路径 [22]，沿给定路径完成多跳推理 [24]，构造显式图推理过程 [25]，或利用路径约束引导答案生成 [26]；另一类是将推理建模为图上搜索 [19]、受约束解码或工具调用 [23]，使模型在推理过程中逐步获取邻居、关系或其他结构化信息。还有工作利用知识图谱路径作为训练或奖励信号，以增强组合推理与路径对齐能力 [28]。然而，图结构被提供、检索或生成，并不意味着模型一定忠实地沿图推理，模型可能依赖节点位置、边顺序或候选偏好等非结构线索，也可能生成一条导向答案，但包含非法跳转的路径。已有工作开始超越纯答案正确率。与本文最为接近的是这样一类测评：只有在答案正确且预测路径可达时，才将一次预测视为正确 [29]。它加强了答案与路径间的联系，但仍未充分隔离序列化位置线索，也没有充分区分”路径能够导向答案“与”模型生成的每一步均为输入图中的真实边“。

综上，CoT 忠实性研究说明，看起来合理的推理不等于真正驱动预测的推理，必须通过干预检验模型是否真的受某类证据约束，本文继承其干预式方法论，但将研究对象从自然语言解释本身转向外部图结构；图序列化研究则表明，图一旦被转为 token 序列，节点标号、边顺序和格式变化都可能显著改变模型输出；图推理与工具增强推理研究则表明，显式图结构或工具接口能够提升推理性能。将这三条研究线索结合起来，便形成了 GIF 所关注的核心缺口：在序列化图推理中，答案随图改变和路径与答案自洽都只是必要行为信号，均不足以单独证明模型忠实地遵循了输入图结构。更重要的是，两类失败信号在不同 prompt 与拓扑条件下并非彼此独立——本文进一步发现，结构化输出下分叉图上的路径层失败可能本身就是答案层位置依赖的伪装形态，二者共享同一根本机制。

英文版：
# 2 Related Work

## 2.1 Chain-of-Thought Faithfulness

Chain-of-Thought (CoT) prompting can improve the performance of large language models on mathematical, logical, and multi-step reasoning tasks, but the reasoning processes generated by models do not necessarily reflect the actual basis of their predictions. Early work on faithfulness distinguished plausibility from faithfulness: an explanation may appear complete and convincing while still being unfaithful to the evidence actually used by the model [7]. Intervention-based studies further test whether final predictions change when either input bias signals or model-generated CoT are altered, modified, or removed. Turpin et al. showed that model explanations may “appear plausible but do not truly reflect the factors that caused the prediction” [5]. Lanham et al. tested whether final answers depend on the generated reasoning process by modifying, truncating, or removing CoT [9]. Recent work has also examined whether models report prompt signals that influence their answers in verbalized reasoning [4], or has used internal activations and computational graphs for white-box verification [6]. Such white-box methods complement the black-box diagnostic perspective adopted in this paper: white-box approaches can directly observe internal computation, but usually require access to model weights, whereas this paper tests graph faithfulness under API-only access through structural interventions and output verification. Together, these studies show that answer correctness, explanatory coherence, and reasoning faithfulness are distinct properties. GIF transfers this observation from verbalized reasoning to serialized graph reasoning: answer correctness, responsiveness to graph changes, and trace–answer consistency are all necessary but insufficient indicators of graph faithfulness.

## 2.2 Graph Serialization and Positional Sensitivity

Graphs themselves usually do not have a fixed linear order, whereas LLMs receive token sequences. When a graph is converted into an edge list, adjacency list, JSON representation, or natural-language description, its nodes and edges acquire positional, ordering, and formatting information that is absent from the graph structure itself. Prior work has shown that LLM graph reasoners often lack serialization invariance: even when the graph structure remains unchanged, merely changing node identifiers, edge order, or descriptive format can significantly alter model predictions [1]. Similar effects have also been observed in other settings: information in the middle of long contexts is more likely to be overlooked [37], option order in multiple-choice questions can affect answers [16], and item position in ranked lists or recommendation tasks can influence model judgments [13,14]. Counterfactual evaluation provides an important means of identifying such shortcuts. CofCA constructs counterfactual multi-hop question-answering examples to reduce the influence of parametric memory and factual co-occurrence, thereby testing whether models can reason based on the provided context [30]. In contrast, CofCA primarily controls at the level of input evidence: by changing counterfactual content, it tests whether models rely on the provided context rather than parametric memory or factual co-occurrence. This paper instead focuses on input-representation control: in graph tasks, by controlling position, order, and endpoint placement, we test whether models rely on graph topology rather than textual positional cues. These two directions constitute complementary intervention dimensions for LLM reasoning faithfulness: the former weakens surface correctness caused by parametric memory and factual co-occurrence, while the latter isolates positional and ordering shortcuts introduced by textual serialization. Even if a model’s answer changes with a counterfactual graph, this change may arise from node content, edge order, endpoint position, or other information that changes simultaneously when the graph structure is modified, rather than from the graph topology itself. The model may merely be responding to changes in the serialized text rather than to topological transformations of the graph. Therefore, observing answer changes before and after graph intervention is still insufficient to establish graph faithfulness. This paper further distinguishes genuine sensitivity to graph structure from surface sensitivity induced by endpoint position through position-controlled serialization.

## 2.3 Graph Reasoning and Tool-Augmented Reasoning

A large body of work enhances LLM reasoning through explicit graph structures. One line of work generates or retrieves relational paths from knowledge graphs [22], performs multi-hop reasoning along given paths [24], constructs explicit graph-based reasoning processes [25], or guides answer generation with path constraints [26]. Another line formulates reasoning as graph search [19], constrained decoding, or tool use [23], enabling models to gradually obtain neighbors, relations, or other structured information during reasoning. Other work uses knowledge-graph paths as training or reward signals to improve compositional reasoning and path alignment [28]. However, the fact that graph structure is provided, retrieved, or generated does not mean that a model necessarily reasons faithfully along the graph. A model may rely on non-structural cues such as node position, edge order, or candidate preferences, and it may also generate a path that leads to the answer while containing illegal transitions. Some prior work has begun to move beyond pure answer accuracy. Most closely related to our work is a line of evaluation that goes beyond pure answer accuracy: a prediction is counted as correct only if both the answer is correct and the predicted path is reachable [29]. While this strengthens the connection between answers and paths, it still does not sufficiently isolate positional cues introduced by serialization, nor does it fully distinguish between “a path can lead to the answer” and “every step generated by the model corresponds to a real edge in the input graph.”

In summary, research on CoT faithfulness shows that reasoning that appears plausible is not necessarily the reasoning that actually drives the prediction, and that interventions are needed to test whether models are genuinely constrained by a particular type of evidence. This paper inherits this intervention-based methodology, but shifts the object of analysis from natural-language explanations themselves to external graph structures. Research on graph serialization shows that once a graph is converted into a token sequence, changes in node identifiers, edge order, and formatting can significantly affect model outputs. Research on graph reasoning and tool-augmented reasoning shows that explicit graph structures or tool interfaces can improve reasoning performance. Bringing these three lines of work together reveals the central gap addressed by GIF: in serialized graph reasoning, responsiveness to graph changes and path–answer self-consistency are only necessary behavioral signals; neither is sufficient on its own to prove that a model faithfully follows the input graph structure. More importantly, the two types of failure signals are not independent under different prompt and topology conditions. This paper further finds that path-level failures on branching graphs under structured output may themselves be a disguised form of answer-level positional dependence, with both sharing the same underlying mechanism.

---
# 3 Task and Method

$$
\begin{aligned}
&\underbrace{(T,G,s,k)}_{\text{输入任务与图}}
\;\longrightarrow\;
\underbrace{(G_1,G_2)}_{
\substack{
\text{构造反事实图对}\\
y_1\neq y_2
}}
\\[6pt]
&\xrightarrow{\;\sigma\in\Sigma_{\mathrm{pos}}\;}
\underbrace{x_{\sigma}}_{
\substack{
\text{位置控制序列化}\\
\text{first / middle / last/ decoy-last}\\
}}
\;\xrightarrow{\text{LLM输出答案或路径}}\;
(\hat y,\hat p)
\\[8pt]
&\longrightarrow
\left\{
\begin{aligned}
&\text{答案层假忠实性：GFI}
\\[8pt]
&\text{符号路径验证：Verify}(\hat p;G,s,k)
\longrightarrow
\left\{
\begin{aligned}
&\text{路径层假忠实性：}\Delta_{\mathrm{illegal}}
\\[6pt]
&(\mathrm{FailureHop},\mathrm{ErrorType})
\\[-1pt]
&\qquad\longrightarrow
\text{verifier-retry 成本—收益分析}
\end{aligned}
\right.
\end{aligned}
\right.
\end{aligned}
$$
GIF 研究序列化图推理中的结构忠实性诊断。令 $R$ 表示一个不可直接观测的理想事件：模型预测在因果上受输入图结构控制；令 $C_\alpha$ 表示一个可观测事件：在序列化配置 $\alpha$ 下，模型能够对反事实图干预作出正确响应。若模型忠实地遵循图结构，则应有 $R\Rightarrow C_\alpha$；反之则不成立，即 $C_\alpha\not\Rightarrow R$。对此现象，GIF 关注两类必要但非充分的行为信号：第一，答案随图干预改变并不蕴含答案忠实于图，即$\text{答案随图干预变化}\not\Rightarrow\text{答案忠实于图结构}$，因为正确终点可能恰好处于显著文本位置，从而使模型通过位置捷径产生表面正确的反事实响应；第二，路径导向正确答案并不蕴含路径忠实于图（要求模型同时输出路径与答案），即$\text{路径与答案一致}\not\Rightarrow\text{路径在图上合法}$，因为模型可能生成一条末节点与自身答案一致、但包含非法边的轨迹。
## 3.1 任务设定

给定有向图 $G = (V,E)$，其中 $V$ 为节点集合，$E\subseteq V\times V$ 为有向边集合，给定起点 $s\in V$ 与跳数 $k$，任务要求模型从 $s$ 出发，沿图中合法有向边移动 $k$ 跳后到达正确答案 $y$。其中 $y$ 称为黄金答案，对应路径称为黄金路径 $p^*$；对于第 $j$ 个样本，在结构化输出设置下，模型还需要输出预测路径 $\hat p^{(j)}=(\hat v_0^{(j)},\hat v_1^{(j)},\ldots,\hat{v}_{m_j}^{(j)})$  与预测答案 $\hat y^{(j)}$，其中 $m_j$ 可能不等于 $k$。由于 LLM 接受的是线性化文本，定义序列化函数 $\sigma_\alpha(G)$ 将图 $G$ 映射为模型可接受的文本序列，其中 $\alpha$ 表示节点标号、边顺序、语法格式和位置控制策略等配置。在答案层，构造共享相同起点 $s$ 和跳数 $k$ 反事实图对 $(G_1,G_2)$，但两张图的正确 $k-hop$ 终点不同，即 $G_1:s\xrightarrow{k\text{ hops}}y_1，G_2:s\xrightarrow{k\text{ hops}}y_2$，其中 $y_1\neq y_2$。为控制图规模、节点命名和干扰分布等非结构线索，反事实图对除改变正确终点所必需的结构差异外，尽可能共享节点池和干扰结构；同时为降低节点语义、常识联想与参数记忆的影响，实验使用随机符号节点，并通过无图先验控制（prior-only）和候选项限定的先验控制（candidate-only prior controls）检查无图条件下的答案先验与候选偏好。在路径层，符号验证器检查模型输出预测路径 $\hat p$ 的起点、长度以及每次节点跳转是否对应输入图中的真实边。为简化路径正确性判据，使判断 $\hat{p}$ 是否合法等价于判断 $\hat{p}$​ 是否等于 $p^*$ ，本文采用唯一合法路径设定。定义从起点 $s$ 出发的所有合法 $k$-hop 路径集合为 $\mathcal W(G,s,k)=\left\{(v_0,\ldots,v_k)\mid v_0=s,\ \forall i\in\{0,\ldots,k-1\},\ (v_i,v_{i+1})\in E\right\}$，则 $\mathcal W(G,s,k)=\{p^*\}$，其中 $p^*=(v_0^*,v_1^*,\ldots,v_k^*)$ 为唯一黄金路径，满足 $v_0^*=s$ 且 $v_k^*=y$，唯一性由符号枚举程序验证。此时 $\hat{p}\in\mathcal{W}(G,s,k)\iff\hat{p}=p^\ast$，$\mathrm{PathValid}^{(j)}=1\iff\mathrm{PathGoldExact}^{(j)}=1$。此外，定义任务级准确率为 $\mathrm{Accuracy}^{(j)}=\mathbb{1}\!\left[\hat y^{(j)}=y^{(j)}\right]$，用于区分模型答案是否与黄金答案一致，作为后续答案层指标 Raw GIS 与 PC-GIS 的基础。
## 3.2 答案层：GFI 与位置控制

为诊断答案层假忠实性，GIF 在反事实图对上检验模型答案是否随图结构改变。对于答案层第 $j$ 个反事实图对 $(G_1^{(j)},G_2^{(j)})$，在序列化条件 $\alpha$ 下定义 $C_{\alpha}^{(j)}=\mathbb{1}\!\left[\hat y_{1,\alpha}^{(j)}=y_1^{(j)}\land \hat y_{2,\alpha}^{(j)}=y_2^{(j)}\right]$。反事实图对可以检验模型答案是否随图结构变化，却无法排除序列化位置捷径。例如，两张图的正确终点均恰好出现在边列表末尾，则 LLM 直接输出最后出现的节点也会产生表面正确的反事实响应。为分离这种表面敏感性，定义答案层的核心诊断指标图跟随能力虚胖（Graph-Following Inflation，GFI），用于衡量模型在默认序列化下通过反事实检验，但在改变终点位置后失败的样本比例：$\mathrm{GFI}^{(j)}=\mathrm{Raw GIS}^{(j)}-\mathrm{PC-GIS}^{(j)}$，其中 Raw GIS（原始图干预敏感性）表示模型在默认 endpoint-last 序列化下，是否能在两张反事实图上分别输出正确终点，即 $\mathrm{RawGIS}^{(j)}=C_{\mathrm{last}}^{(j)}$；PC-GIS（位置控制图干预敏感性）表示模型是否能在 endpoint-first、endpoint-middle、endpoint-last 和 decoy-last 四种位置控制条件下均做出正确响应，即 $PC-GIS^{(j)}=\prod_{\alpha\in\Sigma} C_{\alpha}^{(j)}$。由于 endpoint-last 包含在位置控制集合中，因此 $\mathrm{PC-GIS}^{(j)}\leq\mathrm{Raw GIS}^{(j)}$。同时为减少单个样本偶然误差，本文对所有样本级 $\mathrm{GFI}^{(j)}$ 取平均，在数据集层面定义 $\mathrm{GFI}=\frac{1}{N}\sum_{j=1}^{N}\mathrm{GFI}^{(j)}$。较高 GFI 表明模型表现出的表面敏感性很可能依赖终点位置捷径，本文将这种现象称为图跟随虚胖；较低 GFI 则必须结合 Raw GIS 解释，当 Raw GIS 本身接近零时，较低 GFI 应理解为模型已无法稳定响应图干预，而非位置依赖消失。为直接测量模型是否被“最后出现节点”这一显著文本位置吸引，GIF 还报告干扰项后置终点锚定率（decoy-last Endpoint Anchoring Rate，EAR），记 $d^{(j)}$ 为第 $j$ 个样本中被放置在序列末尾的诱饵节点，则 $\mathrm{EAR}_{\mathrm{decoy}}^{(j)}=\mathbb{1}\!\left[\hat{y}_{\mathrm{decoy}}^{(j)}=d^{(j)}\right]$。需要强调的是，GFI 不能单独解读。两个模型可能具有相同 GFI 但完全不同的失败结构：高 Raw GIS + 低 PC-GIS + 高 decoy-last EAR 指向稳定的位置策略利用（如 §5.2 chain-4hop 上 DS-Pro 与 DS-Flash 的对比）；低 Raw GIS + 低 PC-GIS + 低 EAR 则可能是图干预敏感性整体崩塌（如 §5.2 Qwen 在长链上的现象），并非位置依赖消除。本文在 §5、§6 报告 GFI 时同步报告 Raw GIS、PC-GIS、EAR 分项以支持联合解读。
## 3.3 路径层：$\Delta_{\mathrm{illegal}}$ 与符号路径验证

要求模型输出结构化路径能够提高推理过程的可见性，但不能保证路径中的每一步都对应输入图中的真实边。模型可能生成一条末节点与答案一致、形式完整，却包含非法跳转的路径。为区分“文本自洽”与“图上合法”，引入路径层的核心诊断指标自洽但非法路径率（Self-Consistent but Illegal Trace Gap，$\Delta_{\text{illegal}}$）。该指标衡量较弱的表面自洽检验 TAC 与更严格的结构检验 $\mathrm{TAC}\land\mathrm{PathValid}$ 之间的差距，定义为 $\Delta_{\text{illegal}}^{(j)}=\mathrm{TAC}^{(j)}\left(1-\mathrm{PathValid}^{(j)}\right)$[^1]，其中 TAC（轨迹-答案一致性）检查模型自己生成的路径与答案是否内部自洽，即 $\mathrm{TAC}(\hat{p},\hat{y})^{(j)}=\mathbb{1}\!\left[\hat{y}^{(j)}=\hat v_m^{(j)}\right]$；PathValid（路径合法性）则用于检查模型生成的路径是否在输入图上结构合法，即 $\mathrm{PathValid}^{(j)}=\mathbb{1}\!\left[\hat p^{(j)}\in\mathcal{W}\!\left(G^{(j)},s^{(j)},k\right)\right]$。为衡量模型是否完成路径层任务，进一步定义黄金路径完全匹配（PathGoldExact）$\mathrm{PathGoldExact}\ (\hat{p},p^*)=\mathbb{1}\!\left[\hat{p}=p^*\right]$，用于衡量模型是否完成任务 。在本文的唯一合法路径设定下，PathValid 与 PathGoldExact 等价（见 §3.1）。

为进一步区分结构忠实性与任务正确性，讨论场景 A（$\hat{y} = y$，模型答案正确）与场景 B（$\hat{y} \neq y$，模型答案错误）两种情况。场景 A 是结构化输出的最危险情形，表示模型虽然得到正确答案，却通过了一条图上非法的轨迹；场景 B 是路径层假忠实性的纯粹形式，表示模型围绕错误答案生成了一个内部自洽但非法的轨迹，两种都属于路径自洽但非法，都会被 $\Delta_{\text{illegal}}$ 捕捉到。为区分两者，引入 Accuracy 对 $\Delta_{\mathrm{illegal}}$ 进行分解。$A^{(j)}=\mathrm{Accuracy}^{(j)}\mathrm{TAC}^{(j)}\left(1-\mathrm{PathValid}^{(j)}\right)$，要求同时满足 $\hat y^{(j)}=y^{(j)},\hat v_{m_j}^{(j)}=\hat y^{(j)},\mathrm{PathValid}^{(j)}=0$；$B^{(j)}=\left(1-\mathrm{Accuracy}^{(j)}\right)\mathrm{TAC}^{(j)}\left(1-\mathrm{PathValid}^{(j)}\right)$，要求同时满足 $\hat y^{(j)}\neq y^{(j)},\hat v_{m_j}^{(j)}=\hat y^{(j)},\mathrm{PathValid}^{(j)}=0$，两种场景正好互斥地覆盖全部自洽但非法的预测路径，因此 $\Delta_{\mathrm{illegal}}^{(j)}=A^{(j)}+B^{(j)}$。在数据集层面定义$\Delta_{\mathrm{illegal}}^{\mathrm{correct}}=\frac{1}{N}\sum_{j=1}^{N}A^{(j)},\Delta_{\mathrm{illegal}}^{\mathrm{wrong}}=\frac{1}{N}\sum_{j=1}^{N}B^{(j)}$，因此$\Delta_{\mathrm{illegal}}=\Delta_{\mathrm{illegal}}^{\mathrm{correct}}+\Delta_{\mathrm{illegal}}^{\mathrm{wrong}}$（详见 Appendix B.2）。

| 场景                  | $\mathrm{Accuracy}$ | $\mathrm{TAC}$ | $\mathrm{PathValid}$ | $\Delta_{\mathrm{illegal}}$ | PathGoldExact |
| ------------------- | :-----------------: | :------------: | :------------------: | :-------------------------: | ------------- |
| 完全正确                |          1          |       1        |          1           |              0              | 1             |
| 场景 A：答案正确但<br>路径非法  |          1          |       1        |          0           |              1              | 0             |
| 场景 B：答案错误且<br>路径非法  |          0          |       1        |          0           |              1              | 0             |
| TAC=0（含答案与路径末节点不一致） |        0 或 1        |       0        |          任意          |              0              | 任意            |
类似地，$\Delta_{\text{illegal}}$ 同时需要联合解读：在场景 A / B 分解之外，与 TAC、PathValid 分项数据共同报告。定位失败发生位置，定义首次失败跳数（FailureHop）：$\mathrm{FailureHop}^{(j)}=\min\left\{i\in\{1,\ldots,k\}:(\hat v_{i-1}^{(j)},\hat v_i^{(j)})\notin E^{(j)}\right\}$，仅对输出格式可解析、起点正确且路径长度足以执行逐边检查的样本定义；若所有边均合法，则记为 pass。ErrorType 进一步区分格式错误、起点错误、长度错误、非法边和轨迹—答案不一致，并按照预先规定的优先级进行归类。FailureHop 与 ErrorType 用于后续失败模式分析，而不作为整体忠实性的核心指标。
## 3.4 统一诊断框架

答案层与路径层的诊断共享同一形式，即先检验模型是否通过一个较弱的表面判据，再检验其是否通过更严格的结构判据，二者之差刻画能通过表面检验但不能通过结构检验的假忠实性。一般地，可写为 $\text{假忠实性缺口}=\Pr(\text{通过表面检验})-\Pr(\text{通过结构检验})$。

为形成统一诊断框架，首先需要统一三种 prompt 接口（direct_minimal，jsoncot_basic，jsoncot_strict）。本文按照三档输出接口的约束强度，在序数轴上定义输出约束强度 $\lambda$，通过调整 $\lambda$，测量到的忠实性 gap 从答案层过渡到路径层。规定 $\lambda=0$ 表示 direct_minimal（只要答案），$\lambda=1$ 表示 jsoncot_strict（要整条合法路径），jsoncot_basic（要求输出路径，但结构约束弱于 strict）对应的 $\lambda_{\mathrm{basic}}$ 位于 $(0,1)$ 中间某处，可通过最大似然估计，具体设计留作 future work。

在给定接口强度 $\lambda$、任务深度 $k$ 与同源竞争强度 $d$ 下，GIF 的统计量可归结将模型输出的概率质量分解到互斥且可符号验证的出口，并检验该分解如何随 $\lambda$、$k$ 与 $d$ 改变。不同实验之间的差别主要体现在两个方面。第一，分解单位不同。答案层以最终输出为单位；路径层则进一步检查显式轨迹是否满足起点、长度、逐边合法性与终点一致性要求；第二，条件化方式不同。E1–E7 的实验条件由任务生成器外生指定，每个设计格的样本数在模型作答前已经确定，因此跨格比较属于干预对比。相反，流向分析中的人群成员资格由模型在参照接口下的实际行为定义，属于对配对输出的观察性再分类。

令样本集合 $S(\lambda,k,d)$ 表示在接口 $\lambda$、深度 $k$、出度 $d$ 下的全部样本，总样本数为 $N=|S(\lambda,k,d)|$。随后将不同参数下的 $S$ 按照以下顺序，互斥分类到以下四个子样本。各子样本均以全体样本数 $N$ 为分母。第一，模型输出 Correct，即 $S_{\mathrm{correct}}(\lambda,k,d)=\begin{cases}\left\{j\in S(0,k,d):\hat{y}_{0,k,d}^{(j)}=y_{k,d}^{(j)}\right\},&\lambda=0,\\[4pt]\left\{j\in S(\lambda,k,d):\hat{y}_{\lambda,k,d}^{(j)}=y_{k,d}^{(j)}\wedge\mathrm{PathValid}_{\lambda,k,d}^{(j)}=1\right\},&\lambda>0.\end{cases}$；第二，模型输出诱饵节点 $z$，则子样本划入 anchor，即 $S_{\mathrm{anchor}}(\lambda,k,d)=\left\{j\in S(\lambda,k,d):\hat{y}_{\lambda,k,d}^{(j)}=z_{k,d}^{(j)}\right\}$；第三，模型输出的轨迹末节点与最终答案一致，但完整路径未通过结构合法性检验，且没有划入 anchor，则样本划入 illegal，即 $S_{\mathrm{illegal}}(\lambda,k,d)=\left\{j\in S(\lambda,k,d):\mathrm{TAC}_{(\lambda,k,d)}^{(j)}=1 \land \mathrm{PathValid}_{(\lambda,k,d)}^{(j)}=0 \land j\notin S_{\mathrm{anchor}}(\lambda,k,d)\right\}$；第四，其余样本归入 other，由互斥性与穷尽性可得 $S_{\mathrm{other}}(\lambda,k,d)=S(\lambda,k,d)\setminus\left(S_{\mathrm{anchor}}(\lambda,k,d)\cup S_{\mathrm{illegal}}(\lambda,k,d)\cup S_{\mathrm{correct}}(\lambda,k,d)\right)$。上述四类出口在全体样本中的无条件比例记为无条件出口分解，比例为 $\pi_e(\lambda,k,d)=\frac{\left|S_e(\lambda,k,d)\right|}{N(\lambda,k,d)}$，其中 $e \in \{correct,anchor,illegal,other\}$。对于在 $\lambda=0$ 的 minimal 接口下，模型不输出显式轨迹，因此 illegal 出口的支撑为空，产生无法统计的结构性零点，特别约定相应单元格记为 N/A。为使用统一的答案判据比较不同接口，定义答案错误率 $T_{\mathrm{ans}}(\lambda,k,d)=\Pr\left(\hat y_{\lambda,k,d}^{(j)}\neq y_{k,d}^{(j)}\right)=\frac{1}{N(\lambda,k,d)}\sum_{j\in S(\lambda,k,d)}\mathbb{1}\left[\hat y_{\lambda,k,d}^{(j)}\neq y_{k,d}^{(j)}\right]$，用作诊断错误总量如何随 $\lambda$、$k$ 与 $d$ 改变；同时定义当前接口 $\lambda$ 下未满足成功要求的样本比例为 $T_{\mathrm{full}}(\lambda,k,d)=1-\pi_{\mathrm{correct}}(\lambda,k,d)$，由于不同 $\lambda$ 对应的成功要求不同，因此 $T_{\mathrm{full}}$ 仅作为辅助诊断指标。当 $T_{\mathrm{full}}(\lambda,k,d)>0$ 时，失败样本内部的出口构成定义为 $\boldsymbol{\varphi}(\lambda,k,d)=\left(\frac{\pi_{\mathrm{anchor}}}{T_{\mathrm{full}}},\frac{\pi_{\mathrm{illegal}}}{T_{\mathrm{full}}},\frac{\pi_{\mathrm{other}}}{T_{\mathrm{full}}}\right)$。其中，$T_{\mathrm{ans}}$ 描述错误总量，$\boldsymbol{\pi}$ 描述全体样本的无条件出口分布，$\boldsymbol{\varphi}$ 描述完整失败样本内部的结构构成。若 $T_{\mathrm{full}}=0$，则 $\boldsymbol{\varphi}$ 不定义，相应单元格记为 N/A。

随后令 $U$ 表示本文研究的两类通过表面检验，但未通过结构检验的忠实性缺口，即$U(\lambda,k,d) = \pi_{\mathrm{anchor}}(\lambda,k,d) + \pi_{\mathrm{illegal}}(\lambda,k,d)$。当 $\lambda = 0$ 时，此时对应于终点锚定率，即 $U(0,k,d) = \Pr(\hat y=z\mid \lambda=0,k,d) = \mathrm{EAR}_{\mathrm{decoy}}(k,d) = \frac{\sum_{j\in S(0,k,d)}\mathbb{1}\left[\hat y_{0,k,d}^{(j)} = z_{k,d}^{(j)}\right]}{N(0,k,d)}$$；$$\lambda\in\{\lambda_{\mathrm{basic}},1\}$ 时，$U(\lambda,k,d)=\pi_{\mathrm{anchor}}(\lambda,k,d)+\pi_{\mathrm{illegal}}(\lambda,k,d)$。将二者相减，得到接口间的观测净差异 $\Delta U(k,d) = U(1,k,d)-U(0,k,d)$，若 $\Delta U(k,d)<0$，则 strong interface 下观察到的假忠实量低于 minimal 下观察到的，反之相反。若 $\Delta U(k,d)$ 随深度仅出现一次稳定变号，则将首次由负转为非负的深度记为$k^{\dagger} = \min\left\{k:\Delta U(k,d)\geq 0\right\}$。于是，在观测意义上，$k<k^{\dagger}$ 对应强接口对假忠实出口总质量的净压制；$k\geq k^{\dagger}$ 对应强接口下观察到的假忠实出口总质量不再低于弱接口。若 $\Delta U$ 随 $k$ 多次变号或不存在稳定穿越，则不以单一 $k^{\dagger}$ 概括结果，而直接报告各深度的 $\Delta U(k,d)$ 及置信区间。需要强调，$\Delta U$ 首先是两个可观察出口集合之间的接口对比。将 anchor 与 illegal 进一步解释为同一潜在失败状态在不同接口下的表现形式，需要额外的跨接口测量一致性假设。下一节的逐样本流向追踪为这种解释提供样本层面的支持，但不单独识别不可观测状态的同一性。

无条件出口分布还可在离散设计网格上表示为概率向量 $\boldsymbol{\pi}(\lambda,k,d)=\left(\pi_{\mathrm{anchor}},\pi_{\mathrm{illegal}},\pi_{\mathrm{correct}},\pi_{\mathrm{other}}\right)$，其各分量非负且和为 $1$。对于具有相同出口定义的两个设计格 $g_1$ 与 $g_2$，定义离散差分向量 $\Delta\boldsymbol{\pi}=\boldsymbol{\pi}(g_2)-\boldsymbol{\pi}(g_1)$ 由于 $\sum_e\Delta\pi_e=0$，负分量表示相应出口的量减少，正分量表示增加；其总变差距离 $\mathrm{TV}(g_1,g_2)=\frac{1}{2}\left\|\Delta\boldsymbol{\pi}\right\|_1$ 刻画出口分布的总体重排强度。该差分向量只描述边际出口比例的净变化，不能识别具体样本从哪个出口转向哪个出口。对于共享同一 sample_id 总体的两个条件，进一步定义出口转移矩阵$P_{f\rightarrow e}=\Pr\left(j\in S_e(g_2)\mid j\in S_f(g_1)\right)$，其中 $g_1,g_2$ 为基准条件，$f$ 表示样本在基准条件下的原始出口，$e$ 表示同一个样本在目标条件下的新出口，$f,e\in\{\mathrm{anchor},\mathrm{illegal},\mathrm{correct},\mathrm{other}\}$，$P_{f\to e}$ 表示原来属于 $f$ 的样本中，有多大比例后来进入 $e$。于是得到一个 $4\times4$ 的转移矩阵
$P=\begin{pmatrix}P_{A\to A}&P_{A\to I}&P_{A\to C}&P_{A\to O}\\P_{I\to A}&P_{I\to I}&P_{I\to C}&P_{I\to O}\\P_{C\to A}&P_{C\to I}&P_{C\to C}&P_{C\to O}\\P_{O\to A}&P_{O\to I}&P_{O\to C}&P_{O\to O}\end{pmatrix}$，因为一个原始出口中的样本，在新接口下必须落入某个出口，因此每一行的和为 $1$，即$\sum_e P_{f\to e}=1$。每一行代表流向分析，下文状态迁移表对应此矩阵的 anchor 行 $(P_{A\to A},P_{A\to I},P_{A\to C},P_{A\to O})$，即 $(\pi_{\mathrm{anchor}\mid A},\pi_{\mathrm{illegal}\mid A},\pi_{\mathrm{corrected}\mid A},\pi_{\mathrm{other}\mid A})$。若将 $\boldsymbol{\pi}(g_1)$ 记为行向量，则有 $\boldsymbol{\pi}(g_2)=\boldsymbol{\pi}(g_1)P,\Delta\boldsymbol{\pi}=\boldsymbol{\pi}(g_1)(P-I)$。因此，差分向量刻画出口分布的净重排方向，转移矩阵保留逐样本的来源—去向信息；前者不能替代后者。本文仅在两端具有相同出口定义时计算完整差分向量，主要用于固定接口下沿 $k$ 或 $d$ 的相邻格比较。minimal 与带路径接口之间因 illegal 出口不可直接同口径观测，跨接口关系主要通过预定义的 $U$ 对比与逐样本转移分析刻画。

无条件分解可以揭示每个接口下的出口比例，但不能判断 strict 下的 illegal 是否主要来自 minimal 下的 anchor。为此，本文利用基于 sample_id 配对的样本，对其在不同接口下的输出进行流向分析，得到状态迁移表。迁移在本文中表示同一样本在不同接口下的出口变化，不等同于对不可观测内部状态的直接识别，也不单独构成随机干预意义上的机制证明。给定接口强度 $\lambda$、任务深度 $k$ 与同源竞争强度 $d$，$\lambda=0$ 下定义答案层锚定样本集合 $A=\{j:\hat{y}_0^{(j)}=z^{(j)}\}$，占比为 $p_A(k,d)=\frac{\left|S_{\mathrm{anchor}}(0,k,d)\right|}{N(k,d)}$，其中 $N(k,d)$ 表示在深度 $k$、出度 $d$ 的条件下，跨接口配对分析的总样本数。对于每个 $j\in A$，考察其在接口 $\lambda\in\{\lambda_{\mathrm{basic}},1\}$ 下的输出，按上述互斥出口重新分类，分为 $\pi_{\mathrm{anchor}\mid A}(\lambda)$（新 prompt 下仍然答案层锚定），$\pi_{\mathrm{illegal}\mid A}(\lambda)$（新 prompt 下迁移至路径层非法），$\pi_{\mathrm{corrected}\mid A}(\lambda)$（新 prompt 下修复），$\pi_{\mathrm{other}\mid A}(\lambda)$，四者和为 1 且均以 $|S_{\mathrm{anchor}}(0,k,d)|$ 为分母。计算方法为 $\pi_{\mathrm{illegal}\mid A}(\lambda,k,d)=\Pr\!\left(j\in S_{\mathrm{illegal}}(\lambda,k,d)\mid j\in S_{\mathrm{anchor}}(0,k,d)\right)=\frac{\left|S_{\mathrm{anchor}}(0,k,d) \cap S_{\mathrm{illegal}}(\lambda,k,d)\right|}{\left|S_{\mathrm{anchor}}(0,k,d)\right|}$。

对于 illegal，无条件分解与状态迁移表的关系为 $\pi_{\mathrm{illegal}}(\lambda,k,d)=p_A(k,d)\,\pi_{\mathrm{illegal}\mid A}(\lambda,k,d)+\left[1-p_A(k,d)\right]\pi_{\mathrm{illegal}\mid \neg A}(\lambda,k,d)=M_{A\rightarrow I}+M_{\neg A\rightarrow I}$，其中$p_A(k,d)\,\pi_{\mathrm{illegal}\mid A}(\lambda,k,d)$ 称为迁移成分 $M_{A\rightarrow I}(\lambda,k,d)$，表示 minimal 下曾锚定的样本在当前接口下进入 illegal 样本；$\left[1-p_A(k,d)\right]\pi_{\mathrm{illegal}\mid \neg A}(\lambda,k,d)$ 称为新发成分 $M_{\neg A\rightarrow I}(\lambda,k,d)$，表示 minimal 下未锚定的样本在当前接口下进入 illegal 样本。$M_{A\rightarrow I}$ 占 $\pi_{\mathrm{illegal}}(\lambda,k,d)$ 的比重称为 $m_{A\rightarrow I}(\lambda,k,d)$，即 $m_{A\rightarrow I}(\lambda,k,d)=\frac{M_{A\rightarrow I}(\lambda,k,d)}{\pi_{\mathrm{illegal}}(\lambda,k,d)}$。为比较 $A$ 中样本在强约束下主要被修复还是主要转入 illegal，定义 $\Delta_{\mathrm{I-C}\mid A}(\lambda,k,d)=\pi_{\mathrm{illegal}\mid A}(\lambda,k,d)-\pi_{\mathrm{corrected}\mid A}(\lambda,k,d)$，若 $\Delta_{\mathrm{I-C}\mid A}$ 显著大于 0，则在全部曾经锚定的样本中，转入 illegal 多余 corrected；若 $\Delta_{\mathrm{I-C}\mid A}$ 显著小于 0，则表示 corrected 多于 illegal；$\Delta_{\mathrm{I-C}\mid A}\approx0$ 表示二者平衡。其他样本分类的操作类似。

如果在接口 $\lambda\in\{\lambda_{\mathrm{basic}},1\}$ 下，$\Delta_{\mathrm{I-C}\mid A}(1,k,d)$ 随 $k$ 增大从负变正，并且只发生一次变号，则定义 $k^*=\min\left\{k:\Delta_{\mathrm{I-C}\mid A}(1,k,d)\geq 0\right\}$ 表示对 $\lambda = 0$ 下锚定的样本，在新接口 $\lambda$ 下，$k$ 取何值时，结果翻转。需要注意的是，$k^*$ 分母为 $A$，比较的是 illegal 与 corrected 的条件差异何时变号；而 $k^\dagger$ 分母为 $N$，比较的是 $\lambda\in\{\lambda_{\mathrm{basic}},1\}$ 与 $\lambda = 0$ 的总体 $U$ 差异何时变号。
## 3.5 Verifier-retry 的有限预算吸收过程建模（Absorbing-Process Modeling of Verifier-Retry under a Finite Retry Budget）

Verifier-retry 对同一任务最多调用模型 $5$ 次（为统一符号，下文用 $K_{\max}$ 代指），但与 §3.4 的跨接口流向表不同的是，verifier-retry 在连续轮次中重复施加相同的验证—反馈—重采样操作。因此，本文在一阶 Markov 工作假设下，将其逐轮状态变化表示为一个有限状态吸收过程，并对跨轮次时齐性进行经验诊断。所有转移核均在固定的模型、拓扑、深度与 retry protocol 内分别估计，不跨模型、拓扑或深度合并，即 $\text{model}\times\text{dataset/topology}\times k\times\text{retry protocol}$。时齐性诊断同样在各实验格内部进行，以避免不同难度条件在后续风险集中的组成变化制造伪轮次效应。

令第 $j$ 个样本在第 $t$ 次模型调用后的状态为 $X_t^{(j)}$，状态空间定义为 $\mathcal{X}=\{\mathsf{P},\mathsf{F}_{\mathrm{illegal}},\mathsf{F}_{\mathrm{format}},\mathsf{F}_{\mathrm{struct}}\}$，其中 $\mathsf{P}$ 表示输出通过全部 verifier 检查，系统停止重试；$\mathsf{F}_{\mathrm{format}}$ 表示输出不可解析或不满足规定的 schema；$\mathsf{F}_{\mathrm{illegal}}$ 表示格式、起点与长度检查通过，但路径中至少包含一条输入图中不存在的边；$\mathsf{F}_{\mathrm{struct}}$ 表示输出可解析，但首个失败项为起点错误、长度错误或轨迹—答案不一致。为保证状态互斥，本文按照固定的首错优先级归类 $\text{format/schema}\succ\text{wrong start}\succ\text{wrong length}\succ\text{illegal edge}\succ\text{TAC mismatch}\succ\text{pass}$，例如，当同一输出同时包含非法边与 TAC 不一致时，由于逐边合法性检查先于 TAC 检查，该输出被归入 $\mathsf{F}_{\mathrm{illegal}}$。一旦进入 $\mathsf{P}$，系统不再调用模型，因此 $\mathsf{P}$ 为吸收态。在本文唯一黄金路径设定下，$X_t^{(j)}=\mathsf{P}\Longrightarrow\mathrm{PathGoldExact}_t^{(j)}=1\land\mathrm{Accuracy}_t^{(j)}=1$；反之不一定。本文同时报告吸收样本中的任务准确率，作为 verifier 实现与离线指标之间的一致性审计。

样本 $j$ 的首次理论吸收时间为 $\tau_j=\inf\{t\geq1:X_t^{(j)}=\mathsf{P}\}$，实验最多观察至 $K_{\max}$ 次模型调用。若样本在预算内未通过，则其吸收时间在 $K_{\max}$ 处右删失，计算时约定 $\tau_j=+\infty$。在最多允许 $K\leq K_{\max}$ 次模型调用时，经验累计吸收率定义为 $A(K)=\frac{1}{N}\sum_{j=1}^{N}\mathbf{1}[\tau_j\leq K]$（即本文报告的 adaptive pass@$K$），对于未在预算 $K$ 内吸收的样本，其指示函数取值为 $0$。有限预算下的模型平均调用次数为 $C(K)=\frac{1}{N}\sum_{j=1}^{N}\min(\tau_j,K)$，对于在预算 $K$ 内未吸收的样本，上式将其调用次数记为 $K$；累计延迟、token 使用量与货币成本分别在成本分析中报告，不混入该定义。令瞬态状态集合为 $\mathcal{X}_{T}=\{\mathsf{F}_{\mathrm{illegal}},\mathsf{F}_{\mathrm{format}},\mathsf{F}_{\mathrm{struct}}\}$。对于当前失败状态 $f\in\mathcal{X}_{T}$ 和下一轮状态 $e\in\mathcal{X}$，定义第 $t$ 轮的风险集计数为 $n_t^{\mathrm{risk}}(f)=\sum_{j=1}^{N}\mathbf{1}[X_t^{(j)}=f,\ t<K_{\max}]$，并定义观测转移计数为 $n_t(f\rightarrow e)=\sum_{j=1}^{N}\mathbf{1}[X_t^{(j)}=f,\ X_{t+1}^{(j)}=e]$，其中，风险集仅包含在第 $t$ 次调用后仍处于失败状态、尚未耗尽调用预算的样本。对应 API failure、超时或缺失响应的样本，令观察指标 $O_{j,t+1}=\mathbf{1}[\text{第 }t+1\text{ 轮响应可观察}]$，转移计数为 $n_t(f\rightarrow e)=\sum_j\mathbf{1}[X_t^{(j)}=f,\ \tau_j>t,\ O_{j,t+1}=1,\ X_{t+1}^{(j)}=e]$，分母为 $n_t(f)=\sum_j\mathbf{1}[X_t^{(j)}=f,\ \tau_j>t,\ O_{j,t+1}=1]$，而非自动算作 $\mathsf{F}_{\mathrm{format}}$，并报告覆盖率。合并各轮后的经验转移概率估计为 $\widehat{P}_{fe}=\frac{\sum_{t=1}^{K_{\max}-1}n_t(f\rightarrow e)}{\sum_{t=1}^{K_{\max}-1}n_t^{\mathrm{risk}}(f)}$。三个瞬态状态对应的观测转移块记为 $[\widehat{\mathbf{Q}}\ \widehat{\mathbf{r}}]$，其中，$\widehat{\mathbf{Q}}$ 为失败状态之间的 $3\times3$ 瞬态子矩阵，表示不同失败类型之间的逐轮迁移；$\widehat{\mathbf{r}}$ 为进入吸收态的 $3\times1$ 概率列，表示不同失败类型在下一轮通过验证的概率。加入吸收态的确定性自循环后，完整的 $4\times4$ 吸收链转移矩阵为 $\widehat{\mathbf{P}}=\begin{pmatrix}\widehat{\mathbf{Q}}&\widehat{\mathbf{r}}\\\mathbf{0}^{\top}&1\end{pmatrix}$，由于吸收态 $\mathsf{P}$ 的确定性自循环行由规则规定，因此真正需要从数据里估计的是三个失败状态对应的 $3\times4$ 转移块，即 $\widehat{\mathbf{K}}=[\widehat{\mathbf{Q}}\ \widehat{\mathbf{r}}]=\begin{pmatrix}\widehat{p}_{\mathrm{illegal}\to\mathrm{illegal}}&\widehat{p}_{\mathrm{illegal}\to\mathrm{format}}&\widehat{p}_{\mathrm{illegal}\to\mathrm{struct}}&\widehat{p}_{\mathrm{illegal}\to\mathrm{P}}\\\widehat{p}_{\mathrm{format}\to\mathrm{illegal}}&\widehat{p}_{\mathrm{format}\to\mathrm{format}}&\widehat{p}_{\mathrm{format}\to\mathrm{struct}}&\widehat{p}_{\mathrm{format}\to\mathrm{P}}\\\widehat{p}_{\mathrm{struct}\to\mathrm{illegal}}&\widehat{p}_{\mathrm{struct}\to\mathrm{format}}&\widehat{p}_{\mathrm{struct}\to\mathrm{struct}}&\widehat{p}_{\mathrm{struct}\to\mathrm{P}}\end{pmatrix}$。为避免比例数字掩盖实际分母规模，所有稀疏转移格同时报告计数与比例。上述状态模型采用一阶 Markov 工作假设，即在给定当前 verifier 状态后，下一轮状态不再显式依赖更早的重试历史 $\Pr(X_{t+1}=e\mid X_t=f,X_{t-1},\ldots,X_1)=\Pr(X_{t+1}=e\mid X_t=f)$。该假设不意味着模型内部生成过程本身是 Markov 的，而是表示当前粗粒度 verifier 状态被视为预测下一轮状态所需的主要可观测信息。由于 $\mathsf{F}_{\mathrm{struct}}$ 合并了起点错误、长度错误和轨迹—答案不一致等细粒度失败类型，所得过程应被解释为粗粒度的经验状态模型，而非对模型内部生成机制的精确结构性刻画。

合并不同轮次估计单一转移核，还隐含转移概率不随尝试轮次变化的时齐性假设，即 $\Pr(X_{t+1}=e\mid X_t=f)=P_{fe}$ 不随 $t$ 改变。为诊断该假设，本文分别估计首次转移核 $\widehat{\mathbf{P}}^{(1\rightarrow2)}$ 与后续转移核 $\widehat{\mathbf{P}}^{(2+)}$，其中 $\widehat{\mathbf{P}}^{(1\rightarrow2)}$ 仅使用第一次调用到第二次调用之间的状态转移，$\widehat{\mathbf{P}}^{(2+)}$ 则合并第二次及以后调用产生的状态转移。对于每个失败状态 $f$，定义两组转移行之间的总变差距离为 $\mathrm{TV}_f=\frac{1}{2}\sum_{e\in\mathcal{X}}\left|\widehat{P}_{fe}^{(1\rightarrow2)}-\widehat{P}_{fe}^{(2+)}\right|$。本文采用 sample-level cluster bootstrap 构造 $\mathrm{TV}_f$ 的 $95\%$ 置信区间，以保留同一样本跨轮状态转移之间的依赖关系。若所有具有充分转移支持的状态行，其 $\mathrm{TV}_f$ 的 $95\%$ 置信区间上界均低于预先规定的分析容忍阈值 $\delta=0.10$，则合并不同轮次估计单一转移核；否则不使用单一时齐核进行长期外推，并分别报告分轮转移结果。对于转移计数不足的状态行，本文将时齐性诊断标记为不确定，而不将其视为支持时齐性，同时在附录报告 $\delta=0.05$ 与 $\delta=0.15$ 下的敏感性分析。需要强调的是，该诊断检验的是跨轮次时齐性，而不单独证明一阶 Markov 假设成立。仅当时齐性诊断支持合并转移核，且估计的瞬态子矩阵满足 $\rho(\widehat{\mathbf{Q}})<1$ 时（保证失败状态集合中不存在永不离开的封闭类，并使基本矩阵具有有限的吸收链解释），进一步计算基本矩阵 $\mathbf{N}=(\mathbf{I}-\widehat{\mathbf{Q}})^{-1}$，其中 $\rho(\widehat{\mathbf{Q}})$ 表示 $\widehat{\mathbf{Q}}$ 的谱半径。基本矩阵中的元素 $N_{fe}$ 表示，从失败状态 $f$ 出发，在最终吸收前预计访问失败状态 $e$ 的次数。给定当前已处于失败状态 $f$，无限预算下的期望剩余模型调用次数为 $\mathbb{E}[T_{\mathrm{remaining}}\mid X_1=f]=(\mathbf{N}\mathbf{1})_f$，包括从当前失败状态出发所执行的后续模型调用，直至首次进入吸收态 $\mathsf{P}$。相应地，从失败状态 $f$ 出发最终进入吸收态的概率可写为 $\mathbf{B}=\mathbf{N}\widehat{\mathbf{r}}$。在只有一个吸收态 $\mathsf{P}$ 的情况下，若 $\rho(\widehat{\mathbf{Q}})<1$，则理论上应有 $\mathbf{B}=\mathbf{1}$。该结果表示在时齐吸收链模型和无限重试预算下，所有瞬态状态最终都会以概率 $1$ 进入通过态。然而，本文实际采用有限调用预算 $K_{\max}$，且无限预算外推依赖一阶 Markov、时齐性和最终吸收等额外假设。因此，正文中的主要成本比较仍以经验累计吸收率 $A(K)$、经验调用次数 $C(K)$、累计延迟以及 token 成本为主。基本矩阵及其派生量仅作为附录中的模型化补充，不替代有限预算下的经验结果，也不用于将预算内未通过的样本直接解释为永不通过。除一阶 Markov 性与轮次时齐性外，将分格样本的转移计数合并为单一核并用于多步迭代，还隐含样本在该实验格内共享可比较的转移规律。若不同样本具有未观测的固定难度或修复倾向，则风险集会随轮次逐渐富集于困难样本，合并核可能仅代表轮次特定的边际混合，而不具有稳定的个体级转移含义。本文不以该吸收链刻画样本级 frailty；样本异质性通过经验累计吸收率 $A(K)$ 对均质独立重试基线 $H_{base}(K)=1-[1-A(1)]^K$ 的偏离，以及控制当前失败状态后的剩余历史依赖，在 §6 中另行分析。其中 $A(1)$ 表示首轮总体通过率。该基线假设所有实例在每一轮均具有与总体首轮通过率相同的成功概率，并且不同轮次之间相互独立。

[^1]：朴素差值 $\text{TAC} - \text{PathValid}$ 在 PathValid=1 且 TAC=0 时取 −1，使 $\Delta_{\text{illegal}}$ 失去 [0,1] 区间语义；乘积形式则始终非负且与 GFI 的代数结构对称。

英文版：
# 3 Task and Method
$$
\begin{aligned}
&\underbrace{(T,G,s,k)}_{\text{input task and graph}}
\;\longrightarrow\;
\underbrace{(G_1,G_2)}_{
\substack{
\text{construct a counterfactual graph pair}\\
y_1\neq y_2
}}
\\[6pt]
&\xrightarrow{\;\sigma\in\Sigma_{\mathrm{pos}}\;}
\underbrace{x_{\sigma}}_{
\substack{
\text{position-controlled serialization}\\
\text{first / middle / last}\\
\text{decoy-last}
}}
\;\xrightarrow{\text{LLM outputs an answer or path}}\;
(\hat y,\hat p)
\\[8pt]
&\longrightarrow
\left\{
\begin{aligned}
&\text{answer-level false faithfulness: GFI}
\\[8pt]
&\text{symbolic path verification: Verify}(\hat p;G,s,k)
\longrightarrow
\left\{
\begin{aligned}
&\text{path-level false faithfulness: }\Delta_{\mathrm{illegal}}
\\[6pt]
&(\mathrm{FailureHop},\mathrm{ErrorType})
\\[-1pt]
&\qquad\longrightarrow
\text{verifier-retry cost--benefit analysis}
\end{aligned}
\right.
\end{aligned}
\right.
\end{aligned}
$$
GIF diagnoses structural faithfulness in serialized graph reasoning. Let $R$ denote an ideal but not directly observable event, namely that a model prediction is causally governed by the input graph structure. Let $C_\alpha$ denote an observable event, namely that the model correctly responds to a counterfactual graph intervention under serialization configuration $\alpha$. If the model faithfully follows the graph structure, then $R\Rightarrow C_\alpha$ should hold; however, the converse does not hold, i.e., $C_\alpha\not\Rightarrow R$. GIF therefore focuses on two necessary but insufficient behavioral signals. First, an answer changing under a graph intervention does not imply that the answer is faithful to the graph, i.e., $\text{answer changes under graph intervention}\not\Rightarrow\text{answer is faithful to the graph structure}$, because the correct endpoint may happen to occupy a salient textual position, allowing the model to produce a superficially correct counterfactual response through a positional shortcut. Second, a path leading to the correct answer does not imply that the path is faithful to the graph, i.e., $\text{path--answer consistency}\not\Rightarrow\text{path is valid on the graph}$, because in settings where the model is required to output both a path and an answer, it may generate a trace whose final node agrees with its own answer while still containing illegal edges.

## 3.1 Task Setup

Given a directed graph $G=(V,E)$, where $V$ is the node set and $E\subseteq V\times V$ is the directed edge set, together with a start node $s\in V$ and a hop count $k$, the task requires the model to start from $s$, traverse legal directed edges in the graph for exactly $k$ hops, and reach the correct answer $y$. The answer $y$ is referred to as the gold answer, and the corresponding path is referred to as the gold path $p^{*}$. For the $j$-th instance, under the structured-output setting, the model must additionally output a predicted path $\hat p^{(j)}=(\hat v_0^{(j)},\hat v_1^{(j)},\ldots,\hat v_{m_j}^{(j)})$ and a predicted answer $\hat y^{(j)}$, where $m_j$ may differ from $k$.

Because an LLM receives a linearized textual representation, we define a serialization function $\sigma_\alpha(G)$ that maps graph $G$ to a textual sequence accepted by the model, where $\alpha$ specifies configurations such as node labels, edge order, syntactic format, and position-control strategy. At the answer level, we construct a counterfactual graph pair $(G_1,G_2)$ that shares the same start node $s$ and hop count $k$, but has different correct $k$-hop endpoints, namely $G_1:s\xrightarrow{k\text{ hops}}y_1$ and $G_2:s\xrightarrow{k\text{ hops}}y_2$, where $y_1\neq y_2$. To reduce the influence of node semantics, commonsense associations, and parametric memory, the experiments use random symbolic node identifiers and employ a graph-free prior control, termed prior-only, and a candidate-restricted prior control, termed candidate-only prior controls, to examine answer priors and candidate preferences in the absence of graph structure.

At the path level, a symbolic verifier checks the start node, length, and every node transition in the predicted path $\hat p$, verifying whether each transition corresponds to an actual edge in the input graph. To simplify the path-correctness criterion so that determining whether $\hat p$ is valid is equivalent to determining whether $\hat p$ equals $p^{*}$, we adopt a unique-valid-path setting. We define the set of all legal $k$-hop paths from start node $s$ as $\mathcal{W}(G,s,k)=\left\{(v_0,\ldots,v_k)\mid v_0=s,\ \forall i\in\{0,\ldots,k-1\},\ (v_i,v_{i+1})\in E\right\}$

We require $\mathcal{W}(G,s,k)=\{p^{*}\}$. Here, $p^{*}=(v_0^{*},v_1^{*},\ldots,v_k^{*})$ is the unique gold path satisfying $v_0^{*}=s$ and $v_k^{*}=y$, and its uniqueness is verified through symbolic enumeration. Under this setting,$\hat p\in\mathcal{W}(G,s,k)\iff\hat p=p^{*}$ and $\mathrm{PathValid}^{(j)}=1\iff\mathrm{PathGoldExact}^{(j)}=1$.In addition, we define task-level answer accuracy as $\mathrm{Accuracy}^{(j)}=\mathbb{1}!\left[\hat y^{(j)}=y^{(j)}\right]$,which indicates whether the model’s predicted answer matches the gold answer and serves as the basis for the subsequent answer-level metrics Raw GIS and PC-GIS.

## 3.2 Answer-Level Diagnosis: GFI and Position Control

To diagnose answer-level false faithfulness, GIF tests whether model answers change with the graph structure using counterfactual graph pairs. For the $j$-th answer-level counterfactual graph pair $(G_1^{(j)},G_2^{(j)})$, under serialization condition $\alpha$, we define  $C_{\alpha}^{(j)}=\mathbb{1}!\left[\hat y_{1,\alpha}^{(j)}=y_1^{(j)}\land \hat y_{2,\alpha}^{(j)}=y_2^{(j)}\right]$.  
A counterfactual graph pair can test whether the model answer changes with the graph structure, but it cannot rule out positional shortcuts introduced by serialization. For example, if the correct endpoints of both graphs happen to appear at the end of their respective edge lists, an LLM that simply outputs the last-mentioned node would still exhibit a superficially correct counterfactual response.

To separate such superficial sensitivity from structural sensitivity, we define the core answer-level diagnostic Graph-Following Inflation (GFI), which measures the proportion of instances that pass the counterfactual test under the default serialization but fail after the endpoint position is changed:  
$\mathrm{GFI}^{(j)}=\mathrm{Raw\ GIS}^{(j)}-\mathrm{PC\text{-}GIS}^{(j)}$.  
Here, Raw GIS, or Raw Graph Intervention Sensitivity, indicates whether the model outputs the correct endpoints for both counterfactual graphs under the default endpoint-last serialization and is defined as  
$\mathrm{RawGIS}^{(j)}=C_{\mathrm{last}}^{(j)}$.PC-GIS, or Position-Controlled Graph Intervention Sensitivity, indicates whether the model responds correctly under all four position-controlled conditions—endpoint-first, endpoint-middle, endpoint-last, and decoy-last—and is defined as $\mathrm{PC\text{-}GIS}^{(j)}=\prod_{\alpha\in\Sigma_{\mathrm{pos}}} C_{\alpha}^{(j)}$. Because endpoint-last is included in the position-control set, $\mathrm{PC\text{-}GIS}^{(j)}\leq\mathrm{Raw\ GIS}^{(j)}$.

To reduce noise from individual instances, we average the instance-level values and define the dataset-level metric as $\mathrm{GFI}=\frac{1}{N}\sum_{j=1}^{N}\mathrm{GFI}^{(j)}$. A high GFI indicates that the model’s apparent sensitivity is likely driven by an endpoint-position shortcut, a phenomenon we refer to as graph-following inflation. A low GFI must, however, be interpreted jointly with Raw GIS: when Raw GIS is already close to zero, a low GFI indicates that the model has lost the ability to respond reliably to graph interventions rather than that its positional dependence has disappeared. To directly measure whether the model is attracted to the salient textual position occupied by the last-mentioned node, GIF also reports the decoy-last Endpoint Anchoring Rate (EAR). Let $d^{(j)}$ denote the decoy node placed at the end of the serialized input for the $j$-th instance; then  
$\mathrm{EAR}_{\mathrm{decoy}}^{(j)}=\mathbb{1}!\left[\hat y_{\mathrm{decoy}}^{(j)}=d^{(j)}\right]$.

It is important to emphasize that GFI should not be interpreted in isolation. Two models may have the same GFI while exhibiting entirely different failure structures. High Raw GIS, low PC-GIS, and high decoy-last EAR indicate reliable exploitation of a positional strategy, as in the comparison between DS-Pro and DS-Flash on chain-4hop in Section 5.2. In contrast, low Raw GIS, low PC-GIS, and low EAR may indicate an overall collapse of graph-intervention sensitivity, rather than the disappearance of positional dependence, as observed for Qwen on long chains in Section 5.2. Therefore, when reporting GFI in Sections 5 and 6, this paper also reports Raw GIS, PC-GIS, and EAR to support joint interpretation.

## 3.3 Path-Level Diagnosis: $\Delta_{\mathrm{illegal}}$ and Symbolic Path Verification

Requiring the model to output a structured path improves the visibility of its reasoning process, but it does not guarantee that every step in the path corresponds to a real edge in the input graph. A model may generate a formally complete path whose final node agrees with the answer while still containing illegal transitions. To distinguish textual self-consistency from graph validity, we introduce the core path-level diagnostic Self-Consistent but Illegal Trace Gap, denoted by $\Delta_{\mathrm{illegal}}$. This metric measures the gap between the weaker surface-consistency test TAC and the stricter structural test $\mathrm{TAC}\land\mathrm{PathValid}$:  
$\Delta_{\mathrm{illegal}}^{(j)}=\mathrm{TAC}^{(j)}\left(1-\mathrm{PathValid}^{(j)}\right)$[1](https://chatgpt.com/c/6a35623d-9160-83e8-9680-7a9ef6288973#user-content-fn-1).  
Here, TAC, or Trace–Answer Consistency, checks whether the path and answer produced by the model are internally consistent:  
$\mathrm{TAC}(\hat p,\hat y)^{(j)}=\mathbb{1}!\left[\hat y^{(j)}=\hat v_{m_j}^{(j)}\right]$.  
PathValid checks whether the generated path is structurally valid on the input graph:  
$\mathrm{PathValid}^{(j)}=\mathbb{1}!\left[\hat p^{(j)}\in\mathcal W!\left(G^{(j)},s^{(j)},k\right)\right]$.  
To measure whether the model completes the path-level task, we further define exact gold-path matching, termed PathGoldExact:$\mathrm{PathGoldExact}(\hat p,p^*)=\mathbb{1}\!\left[\hat p=p^*\right]$.Under the unique-valid-path setting adopted in this paper, PathValid and PathGoldExact are equivalent, as discussed in Section 3.1.

To further distinguish structural faithfulness from task correctness, we consider two cases: Case A, in which $\hat y=y$ and the model answer is correct, and Case B, in which $\hat y\neq y$ and the model answer is incorrect. Case A is the most concerning outcome under structured output because the model obtains the correct answer while providing a trace that is invalid on the graph. Case B is a pure form of path-level false faithfulness, in which the model constructs an internally consistent but illegal trace around an incorrect answer. Both cases are self-consistent but illegal and are therefore captured by $\Delta_{\mathrm{illegal}}$. To distinguish them, we use Accuracy to decompose $\Delta_{\mathrm{illegal}}$:  
$A^{(j)}=\mathrm{Accuracy}^{(j)}\mathrm{TAC}^{(j)}\left(1-\mathrm{PathValid}^{(j)}\right)$,  
which requires  
$\hat y^{(j)}=y^{(j)}$, $\hat v_{m_j}^{(j)}=\hat y^{(j)}$, and $\mathrm{PathValid}^{(j)}=0$;  
$B^{(j)}=\left(1-\mathrm{Accuracy}^{(j)}\right)\mathrm{TAC}^{(j)}\left(1-\mathrm{PathValid}^{(j)}\right)$,  
which requires  
$\hat y^{(j)}\neq y^{(j)}$, $\hat v_{m_j}^{(j)}=\hat y^{(j)}$, and $\mathrm{PathValid}^{(j)}=0$.  
The two cases disjointly cover all self-consistent but illegal predicted paths, such that  
$\Delta_{\mathrm{illegal}}^{(j)}=A^{(j)}+B^{(j)}$.  
At the dataset level, we define  
$\Delta_{\mathrm{illegal}}^{\mathrm{correct}}=\frac{1}{N}\sum_{j=1}^{N}A^{(j)}$,  
$\Delta_{\mathrm{illegal}}^{\mathrm{wrong}}=\frac{1}{N}\sum_{j=1}^{N}B^{(j)}$,  
and therefore  
$\Delta_{\mathrm{illegal}}=\Delta_{\mathrm{illegal}}^{\mathrm{correct}}+\Delta_{\mathrm{illegal}}^{\mathrm{wrong}}$.

|Case|$\mathrm{Accuracy}$|$\mathrm{TAC}$|$\mathrm{PathValid}$|$\Delta_{\mathrm{illegal}}$|
|---|:-:|:-:|:-:|:-:|
|Fully correct|1|1|1|0|
|Case A: correct answer butillegal path|1|1|0|1|
|Case B: incorrect answer andillegal path|0|1|0|1|
|TAC = 0 (answer $\neq$ predicted path end node)|0 or 1|0|arbitrary|0|

Similarly, $\Delta_{\mathrm{illegal}}$ also requires joint interpretation. Beyond the Case A/B decomposition, it should be reported together with TAC and PathValid, and interpreted alongside the FailureHop distribution and reverse-reachability evidence in Section 6. A single $\Delta_{\mathrm{illegal}}$ value only indicates the proportion of self-consistent but illegal paths; it does not by itself distinguish whether the model deviates during forward traversal or instead performs local rationalization around an answer anchor. To localize where a failure first occurs, we define FailureHop as $\mathrm{FailureHop}^{(j)}=\min\left\{i\in\{1,\ldots,k\}\mid(\hat v_{i-1}^{(j)},\hat v_i^{(j)})\notin E^{(j)}\right\}$.FailureHop is defined only for instances whose outputs are parseable, whose start nodes are correct, and whose path lengths are sufficient for edge-by-edge inspection; if every edge is valid, the instance is recorded as pass.ErrorType further distinguishes format errors, start-node errors, length errors, illegal-edge errors, and trace–answer inconsistencies according to a predefined priority order. FailureHop and ErrorType are used for subsequent failure-pattern analysis rather than as core measures of overall faithfulness.

## 3.4 Metric Roles and Unified Diagnostic Structure

GIF is organized around two core metrics: GFI at the answer level and $\Delta_{\mathrm{illegal}}$ at the path level. The two metrics share a unified diagnostic structure in which a model passes a weaker surface-level test but fails a stricter structural test. GFI compares the default counterfactual response with the response obtained after position control, whereas $\Delta_{\mathrm{illegal}}$ compares path–answer self-consistency with the joint satisfaction of path–answer self-consistency and path validity. The remaining metrics serve distinct roles: Raw GIS, PC-GIS, TAC, and PathValid are components of the two core metrics; EAR provides direct evidence of final-position anchoring; PathGoldExact measures task-level path correctness; and FailureHop and ErrorType localize structural failures. All of these metrics are defined in the main text, but to avoid presenting them with equal visual prominence and imposing unnecessary cognitive load, subsequent sections report them selectively according to the relevant research question. In verifier-retry settings, pass@$K$ and cumulative latency are used to evaluate the cost–benefit trade-off of symbolic verification with retry; their definitions and experimental details are provided in Section 4. As discussed in Sections 3.2 and 3.3, when reporting GFI or $\Delta_{\mathrm{illegal}}$, this paper also reports the corresponding component metrics to support joint interpretation.

## Footnotes

1. The naive difference $\mathrm{TAC}-\mathrm{PathValid}$ takes the value $-1$ when $\mathrm{PathValid}=1$ and $\mathrm{TAC}=0$, causing $\Delta_{\mathrm{illegal}}$ to lose its $[0,1]$ range interpretation; the multiplicative form is always nonnegative and algebraically symmetric with GFI. [↩](https://chatgpt.com/c/6a35623d-9160-83e8-9680-7a9ef6288973#user-content-fnref-1)


---
# 4 Experimental Setup

本节说明实验如何保证 GIF 诊断具有可解释性。第 3 节已经定义 GFI、$\Delta_{\mathrm{illegal}}$、TAC、PathValid 等指标，本节不再重复指标含义，而是说明合成图、序列化控制、prompt 条件、模型调用和统计方法如何共同排除替代解释。除特别说明外，所有非重试主实验均采用确定性解码；verifier-retry 仅在首次尝试失败后引入轻微采样扰动，以降低模型反复生成相同结构错误的概率。
## 4.1 实验设计原则：受控识别与诊断有效性

第 3 节定义了 GIF 关注两类假忠实性现象，并分别以 GFI 和 $\Delta_{\mathrm{illegal}}$ 度量。本节进一步明确这些核心主张所依赖的实验设计，随后说明各项控制如何排除替代解释，使实验结果能够支持这些主张。为提高上述指标能够刻画相应失败机制的纯净度，实验设计需满足三项要求：第一，排除答案先验、候选偏好和节点语义造成的混淆；第二，分离图结构变化与文本位置变化对模型答案的影响；第三，将答案自洽性与路径合法性作为两个独立属性检验。

为落实这些要求，本文从五个维度施加控制：第一，节点语义与答案先验控制。为隔离参数记忆、常识联想、事实共现和节点语义对模型预测的影响，所有节点均采用随机符号标识。同时，实验还设置了 prior-only 和 candidate-only prior controls，前者完全不提供图结构，后者只提供候选节点集合，用于检查模型在无图条件下是否对特定答案或候选节点存在系统性偏好。上述控制结果单独报告，不与图任务结果混合计算；第二，位置变量控制。对于每一反事实图对，实验构造前、中、后以及诱饵节点四种序列化。不同序列化版本保持节点集合、边集合、起点、跳数、黄金终点不变，仅改变终点相关边和诱饵节点在文本序列中的位置。前三种序列化用于检验模型是否能够在终点位置变化后仍保持图干预响应；诱饵节点用于检验模型是否被最后出现的诱饵节点吸引；第三，区分路径合法性与任务正确性。路径合法性判断模型生成的轨迹 $\hat p$ 是否满足输入图结构 $G$ 约束，由 $\mathrm{PathValid}^{(j)}$ 衡量。任务正确性分两个层级，答案层任务正确性判断 $\hat y$ 是否等于黄金答案 $y$，由 $\mathrm{Accuracy}^{(j)}=\mathbb{1}[\hat y^{(j)}=y^{(j)}]$ 衡量；路径层任务正确性判断 $\hat{p}$ 是否恰好等于黄金路径 $p^*$，由 $\mathrm{PathGoldExact}^{(j)}$ 衡量。三者通过两个引理联系：引理 1（唯一路径设定下的路径等价性）：当 $|\mathcal W(G,s,k)|=1$ 时， $\mathrm{PathValid}=1\iff\mathrm{PathGoldExact}=1$，这一等价把“路径合法性”与“路径层任务正确性”绑定，使 PathValid 同时承担两种诊断职能；引理 2（任务正确性的层级连接）：$\mathrm{PathGoldExact}\land\mathrm{TAC}\Longrightarrow\mathrm{Accuracy}$，当 $\mathrm{PathGoldExact}=1$ 时，$\hat p^{(j)}=p^{*(j)}$，因此 $\hat v_{m_j}^{(j)}=y^{(j)}$，当 $\mathrm{TAC}=1$ 时，$\hat y^{(j)}=\hat v_{m_j}^{(j)}$，二者结合即可推出 $\hat y^{(j)}=y^{(j)}$。这一蕴含说明当模型路径完全等于黄金路径且其声明的答案等于路径末节点时，答案必然等于黄金答案。$\Delta_{\mathrm{illegal}}$ 和 GFI 的设计都源自这条因果链：$G\longrightarrow\mathcal{W}(G,s,k)=\left\{p^{*}\right\}\longrightarrow p^{*}\longrightarrow v_k^{*}=y$，即图结构 $G$ 唯一决定黄金路径集合 $\mathcal{W}$（在唯一性约束下退化为单元素集），黄金路径 $p^{*}$ 决定其末节点 $v_k^{*}$，末节点即为黄金答案 $y$。模型若忠实遵循图结构，则 $G$ 决定的下头属性 $\mathcal{W}(G,s,k)$ 都应被模型预测命中；若模型通过非结构线索预测，则会在某个环节暴露不一致。GFI 检查模型生成的 $\hat y​$ 是否真的跟随 $G\rightarrow y$；$\Delta_{\mathrm{illegal}}$ 检查模型生成的 $\hat p$ 是否真的属于 $G\rightarrow\mathcal{W}(G,s,k)$；第四，拓扑与难度控制。拓扑方面，链式图用于隔离答案层位置捷径，其线性结构使模型取“最后出现的节点”成为可行的位置捷径；分叉图要求模型维护当前节点的同时在每一步选择后继节点，使状态追踪失败和非法跳转变得可观测。难度控制方面，chain-4hop 与 branching-4hop 构成相同跳数下的拓扑对照，branching-12hop 作为长路径困难设置，其余跳数作为推理深度扫描放入附录，分析不同失败形态随路径长度变化的趋势；第五，诊断可见性控制。GIF 的核心指标依赖于模型在给定任务上的表现处于非饱和、非崩塌的可观测区间，实验设计的关键是使模型能力与任务难度形成适当配对，若模型表现接近满分（或完全失败），诊断信号消失，难以在行为数据中区分失败类别归因（GFI 还是 $\Delta_{\mathrm{illegal}}$ 占主导）与FailureHop 分布形态。DS-Pro thinking 在 branching-12hop 上 PathGoldExact 已达 99.93%（§5.4.1），诊断信号进入天花板效应区间；而非思考模式下的 PathGoldExact  为 55.37%，因此 DS-Pro 在 branching-4hop 的能力分层分析中使用使用非思考模式。为与 DS-Pro 形成对比，DS-Flash 也使用非思考模式。

## 4.2 合成图构造（Synthetic Graph Construction）

合成图的目的不是模拟真实知识图谱的全部语义与结构复杂度，而是提供具有较高内部效度的诊断测试床：在其他因素受控的条件下，分别操纵序列化位置并验证逐边路径合法性。因此，本文实验能够回答 GFI 与 $\Delta_{\mathrm{illegal}}$ 所针对的机制性问题；这些失败模式能否直接外推到真实知识图谱、工具调用图或开放世界图任务，则属于外部效度问题，并在局限性部分讨论。合成图中每个基础样本由起点 $s$、任务跳数 $k$、唯一黄金路径 $p^*$ 和黄金终点 $y$ 构成。对于每个基础样本，生成器先构造一个反事实图对 $(G_1,G_2)$，再构造四种位置控制序列化。因此，一个基础样本在单一模型和单一图任务 prompt 下展开为 $2\times4=8$ 次模型调用，每个数据集（dataset）包含 N=200 个基础样本共 $200 \times 8 = 1600$ 次调用。其中 prior-only 和 candidate-only prior 属于额外控制条件，不计入 8 次图任务调用。

| 拓扑类型（Topology） | 模型（MODEL）                                 | 跳数 / 路径长度（Hop length） |                                           每个模型/提示下的调用次数（Queries per model/prompt）                                           |
| :------------: | ----------------------------------------- | :-------------------: | :-------------------------------------------------------------------------------------------------------------------------: |
|     Chain      | GPT；DS-Flash；Qwen                         |       3 / 5 / 6       |                                                    direct_minimal: 1600                                                     |
|     Chain      | GPT；DS-Flash / Pro；Qwen                   |           4           |     direct_minimal: 1600<br><br>jsoncot_basic（仅 DS-Flash；Qwen）: 1600<br><br>jsoncot_strict（仅DS-Flash / Pro; Qwen）: 1600     |
|   Branching    | GPT；DS-Flash / Pro；Qwen                   |           4           | direct_minimal: 1600<br><br>jsoncot_basic（GPT; DS-Flash; Qwen）: 1600<br><br>jsoncot_strict（GPT; DS-Flash / Pro; Qwen）: 1600 |
|   Branching    | GPT；DS-Flash；Qwen                         |      6 / 8 / 10       |                             direct_minimal: 1600<br>jsoncot_basic: 1600<br>jsoncot_strict: 1600                             |
|   Branching    | GPT; DS-Flash / Pro（Pro 含 thinking）; Qwen |          12           |     direct_minimal（仅 GPT; DS-Flash; Qwen）: 1600<br><br>jsoncot_basic（DS-Flash; Qwen）: 1600<br><br>jsoncot_strict: 1600      |
表注：
1. 本表未统计 `prior_only`、`candidate_only_prior` 和 `verifier-retry`的调用次数。
2. 表中 `DS-Pro` 若未特别标注 `thinking`，均指 `DS-Pro no-thinking`。
3. 表中调用次数为计划调用量；实际结果可能因超时、解析失败或 API 返回异常等原因减少。
4. GPT-5.4-mini 结果来自第三方中转服务，因此本文按由服务商路由的模型配置（provider-routed model configuration）报告结果，不将其等同于官方 API 配置。
5. 非 retry 主实验和 retry 第一次尝试均采用确定性解码（$temperature=0$）；若输出未通过符号验证器，为降低模型重复生成相同错误结构的概率，后续尝试使用 $temperature=0.3$。多次独立运行以降低 decoding randomness。

### 4.2.1 节点编码：随机符号节点

为实施 §4.1 所述的节点语义控制，每个节点使用 `J9E9`、`W6S2` 等固定长度的随机字母-数字组合标识。节点标识不携带实体类别、关系语义或自然语言含义，并在同一基础样本的反事实图对和不同序列化版本间保持一致。随机符号不能单独排除所有 token 偏好，因此实验同时使用 prior-only 和 candidate-only prior controls 检查模型是否对特定符号节点或候选位置表现出稳定偏好。完整控制结果单独报告，不与图任务结果混合计算。

---
### 4.2.2 拓扑设计：链式图与分叉图

本文使用链式图和分叉图两类拓扑，分别侧重答案层位置依赖与路径层错误传播的诊断；所有图在进入实验前均需通过符号程序验证。

链式图用于诊断答案层位置捷径，以唯一合法 $k-hop$ 路径为主干，从起点 $s$ 出发，对所有非终点黄金路径节点，生成器保证每步至多只有一个后继节点能够继续通向完整合法 $k-hop$ 路径，即 $|\mathcal N^+(v_i)|=1$，其中 $\mathcal{N}^{+}(v_i)=\{u\mid (v_i,u)\in E\}$ 表示节点 $v_i$ 的出邻居集合。同时可加入干扰节点或干扰边，干扰边起点不在合法路径上，模型从 $s$ 出发沿合法边走不会在合法路径上遇到“可选下一步”，只是边列表里多了一些无关噪声。实验以 chain-4hop 作为主实验设置，并使用 chain-3/5/6 hop 构成推理深度扫描，用于考察弱提示下不同模型的序列化位置依赖（GFI）是否随推理深度变化。由于链式图不包含真正的分支选择，在已知当前状态 $v_i$ 的条件下，无信息均匀选择基线下 $\Pr(\text{局部选择正确}\mid v_i)=\frac{1}{|\mathcal N^+(v_i)|}=1$，表示 chain 中不存在同源候选竞争，并不意味着模型必然能够正确维护当前状态，也不排除模型绕过路径追踪而直接抽取答案。因此，在 answer-only 设置下，若模型表现为 Raw GIS 高但 PC-GIS 低，二者之差可以较为直接地量化终点位置线索对表面性能的贡献，而不易受到同源分支选择错误的混淆。

分叉图在链式图基础上加入结构性分叉：干扰边的起点可以是合法路径上的节点，模型从 $s$ 出发会看到多个看似合法的下一跳，但仅有一条能完成合法 $k-hop$ 路径，其他的通向终止分支。终止分支 $e_{\mathrm{term}}$ 指从黄金路径节点 $v_i$ 出发，指向非黄金后继节点 $u$ 的合法边，即 $e_{\mathrm{term}}=(v_i,u)\in E$。其中 $u$ 称为终止节点，满足 $\mathcal{N}^{+}(u)\cap\{v_{i+1},\ldots,v_k\}=\varnothing$，模型一旦走到该节点就没有黄金路径可以继续走（在唯一黄金路径设定下，进入非黄金节点后不存在能够在剩余预算内重新拼接成合法 $k$-hop 黄金终点路径的 exact-budget 返回路径）。因此，在本实验经过唯一性验证的图中，本文使用局部条件 $\mathcal{N}^{+}(u)\cap V(p^*)=\varnothing$ 标记直接返回黄金路径被排除的终止分支。所有的终止分支组成终止分支集合 $\mathcal{E}_{\mathrm{term}}=\left\{(v_i,u)\in E\ \middle|\ i\in\{0,\ldots,k-1\},\ u\notin p^*,\ \mathcal{N}^{+}(u)\cap p^*=\varnothing\right\}$。随着同源候选竞争被引入，链式 endpoint-last 条件下“局部候选唯一”与“黄金后继唯一”的行为对齐被打破，位置敏感的局部检索不再必然产生正确结果。在行为层面，位置依赖由 answer-only 条件下的全局终点偏好扩展为 path-output 条件下的候选内位置偏好，但这并不预设二者由完全相同的内部机制实现。本文以 branching-12hop 作为主要困难设置，用于诊断长程状态维护、分支选择与非法轨迹，并以 branching-4hop 作为短路径参照。在 `direct_minimal` 条件下，比较 branching-4hop 与 chain-4hop，以考察引入同源候选竞争后答案层位置依赖的变化；由于该比较同时改变边数与输入长度，因此将其解释为拓扑条件条件下的整体行为差异，而不作为分叉变量的因果效应估计；在 `jsoncot_strict` 条件下，比较采用相同生成规则的 branching-4hop 与 branching-12hop，以刻画路径深度、分叉机会和上下文长度共同增加时的错误变化，而不将差异单独归因于路径深度。

汇聚图不纳入本文主实验，原因是汇聚图存在多条合法路径到达同一黄金终点，在该设定下，$\mathrm{PathValid}$ 不再等价 $\mathrm{PathGoldExact}$，引入多路径等价与多路径歧义消解问题。$\mathrm{PathValid}$ 与 $\Delta_{\mathrm{illegal}}$ 本身可以直接扩展至多路径设置；参考路径一致性则应由单一路径精确匹配改为成员检验 $\hat{p}\in\mathcal{P}^{*}$。汇聚图与多路径设定下的诊断扩展留作未来工作。

### 4.2.3 反事实图对、位置控制序列化与符号图验证

每张图分别被实例化为四种文本序列化条件，其中前三种序列化保持抽象图 $G=(V,E)$ 不变，仅改变边或节点在文本中的排列位置；而 decoy_last 则在相同基础图上加入一条由黄金答案指向诱饵节点的控制边 $y\rightarrow d$，使 $d$ 位于 $k+1$ hop，并将该边置于文本末位。符号验证保证该增广不改变从 $s$ 出发的唯一合法 $k$-hop 路径及其黄金终点，即诱饵节点 $d$ 必须不同于黄金答案，且不能成为从 $s$ 出发恰好移动 $k$ 跳后的合法终点。

| 序列化条件           | 实例化方式               | 诊断目的                 |
| --------------- | ------------------- | -------------------- |
| endpoint-first  | 将黄金终点相关边或节点信息置于序列前部 | 检验终点前置时模型是否仍能正确响应    |
| endpoint-middle | 将黄金终点相关边或节点信息置于序列中部 | 检验中间位置信息是否被忽略        |
| endpoint-last   | 将黄金终点相关边或节点信息置于序列末部 | 作为默认序列化条件并计算 Raw GIS |
| decoy-last      | 将错误诱饵节点相关信息置于序列末部   | 检验末位节点锚定并计算 EAR      |
所有样本进入模型评测前由独立符号程序验证。除上文四种文本序列化、§3.1 声明的图结构与反事实配对约束，以及黄金路径唯一性约束外，验证器还需核验：第一，chain 的非终点黄金路径节点不存在同源候选竞争；Branching 的指定分叉点包含预定数量的同源候选，且非黄金候选通向不能完成合法 $k-hop$ 路径的终止分支；第二，任务元信息与已验证图实例的一致性，确保 start node、hop count、gold path、gold answer、graph id 和反事实 pair id 未发生错配。只有通过全部结构与序列化检查的样本才进入最终评测。

## 4.3 模型与提示（Models and Prompt Interfaces）

本文使用五类 prompt 评估五个模型-推理模式配置，其中前三类 prompt 用于图任务实验，后两类用于 prior controls：

| Model             | Reasoning configuration        | Experimental role                                  |
| ----------------- | ------------------------------ | -------------------------------------------------- |
| DeepSeek-V4-Flash | thinking disabled              | 主要评测配置，用于答案位置依赖、路径合法性及 verifier-retry 分析           |
| DeepSeek-V4-Pro   | thinking disabled              | 与 Flash 进行同系列模型能力对照                                |
| DeepSeek-V4-Pro   | thinking enabled               | 推理模式消融；仅用于已实际覆盖的 branching-12hop jsoncot_strict 设置 |
| Qwen Max          | thinking disabled              | 跨模型系列行为对照                                          |
| GPT-5.4-mini      | provider default configuration | 跨模型系列行为对照                                          |
本文所有干预均通过 prompt 字符串实施，但这并不意味着它们在机制上属于同一类。为避免将所有变化笼统称为 prompt effect，本文区分两类干预：输入表征干预与响应契约干预。第一类是输入表征干预（input-representation intervention），改变模型所看到的证据内容或排列方式，例如 endpoint_first/middle/last 以及 decoy_last；边的文本顺序；同源候选顺序；final edge 的位置、重复次数和距离；Chain / Branching；full graph / oracle current state；branch factor，作用是回答模型看到了什么，以及这些信息怎样被序列化。第二类是响应契约干预（response-contract intervention），改变要求模型生成什么，例如 answer-only 与 path+answer、是否要求固定 JSON schema、是否要求恰好输出 $k+1$ 个节点、是否强调逐边合法、是否要求答案等于路径末节点、是否允许 NO_LEGAL_CONTINUATION，以及 JSON 字段顺序，作用是回答模型必须以什么形式提交预测。

| Prompt               | 输出形式                   | 与上一层的主要差异              |
| -------------------- | ---------------------- | ---------------------- |
| direct_minimal       | JSON，仅 `answer` 字段     | answer-only 接口         |
| jsoncot_basic        | JSON，`path` + `answer` | 增加显式路径字段，不明确逐边合法性等约束   |
| jsoncot_strict       | 相同 JSON schema         | 明确起点、长度、逐边合法性及轨迹-答案一致性 |
| prior_only           | JSON，仅答案，无图            | 无图答案先验                 |
| candidate_only_prior | JSON，仅答案，候选节点集合但无边     | 候选集合及顺序偏好              |
direct_minimal：仅要求模型输出包含最终预测终点的 answer 字段，不要求生成路径，因此主要用于答案层指标 Raw GIS、PC-GIS、GFI 和 EAR。该条件仍要求返回 JSON，以保持解析接口一致；jsoncot_basic：在相同 JSON 输出要求下增加 path 字段，但不显式列出路径起点、节点数量、逐边合法性和轨迹-答案一致性等约束。该设置使 TAC、PathValid、PathGoldExact、$\Delta_{\mathrm{illegal}}$ 和 FailureHop 的计算成为可能，同时用于检验增加显式路径输出本身如何改变模型行为；jsoncot_strict：使用与 jsoncot_basic 相同的输出 schema，但进一步明确要求路径从 $s$ 开始、包含恰好 $k+1$ 个节点、每次跳转对应输入图中的真实边，且答案等于路径末节点。这些要求仅属于提示层约束，并不保证模型实际遵循；所有输出均由独立符号验证器重新检查。其中 direct_minimal 与 jsoncot_basic 的比较属于输出接口消融，而  jsoncot_basic 与 jsoncot_strict 的比较更接近路径约束强度消融。prior_only 与 candidate_only_prior：不提供完整图结构，用于检查无图答案先验和候选集合偏好。两类控制结果于附录单独报告，不参与 Raw GIS、PC-GIS 或 GFI 的计算。
## 4.4 因果机制干预实验

详见 Appendix B 

| 实验                                             | 检验目标                                                                                                                                                                                    | 干预因素                                                                                                                                                                                        | 控制项                                                                                                                                                                                                                                                                                                                                                                                                                                        | 设计                                                                                                                                                                         | 主指标 / 判据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1：输出接口 $z$ 是否在行为层改变模型对两条等价终止分支的相对位置偏好         | 总体行为层：$z$ 是否改变整体选择行为，即是否更易命中终止分支 $D$、更易提交后位分支 $L$、更易选黄金后继 $C_{\mathrm{gold}}$。  <br><br>条件层：在已命中 A / B（$D=1$）的子样本内，$z$ 是否改变 A / B 间的前位/后位偏好。                                            | 1）主干预：输出接口 $z \in \{answer\_only=\{\text{"answer"}\},path\_answer=\{\text{"path"},\text{"answer"}\}\}$<br><br>2）位置干预：A/B 在边列表中的文本先后 $R \in \{\text{branch-a-early},\text{branch-b-early}\}$ | 单一图结构，起点 $v_0$ 固定三候选边（$e_g$、A、B）；$a,b$ 无后继、局部角色等价；A/B 身份按边字符串词典序固定，再映射为 $c_{\mathrm{early}}/c_{\mathrm{late}}$ 以剥离身份与位置。<br>两个位置条件间仅交换 A / B 文本顺序，图结构、序列化长度、非目标边顺序全不变。<br>每格报告调用数、有效输出数、命中数、配对共同有效 cluster 数。                                                                                                                                                                                                                              | $2\times2$ 配对干预：位置 $R$（2）$\times$ 接口 $z$（2），每格 $N=400$。<br>配对逻辑：交换 A/B 位置 $\rightarrow$ 检验选择是否跟随相对位置；切换接口 $\rightarrow$ 检验接口对位置偏好的调制。$N=1600$/模型                           | $\Pr(L=1)$，配对 $RD_L^{\mathrm{total}}$<br><br>接口是否改变等价终止分支的相对位置偏好                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| E2：固定终止状态下输出约束对响应类型分布的影响                       | 互斥表把“进入终止分支”的上游过程与“进入后的终止响应”的下游过程耦合在一起。<br>E2 固定上游的终止分支选择过程，诊断在同一零出度终止状态下，exact-length 要求与逐边合法性要求如何改变显式退出、非显式停止与非法续写之间的分配。                                                              | $T=(L,V)\in\{0,1\}^2$，其中<br>$L=\mathbb{1}\{\text{施加 exact-length 要求}\}$，<br>$V=\mathbb{1}\{\text{施加逐边合法性要求}\}$。                                                                             | 固定终止前缀 $\tilde p=(v_0,\ldots,v_{10},u^0_{11})$，距离规定长度差 $1$ hop，任何续写均违反图结构。<br>同一图实例跨模型、跨四个前缀条件完全相同。终止分支身份 $C$ 按图实例哈希预先确定，先于 prompt 条件生成，且与 $T$ 独立；样本层近似平衡，估计量层对 $C$ 对称化平均。<br>四条件共用相同 endpoint-middle 图文本、边顺序、固定前缀与 schema，且均允许 $\texttt{NO\_LEGAL\_CONTINUATION}$，仅改变是否施加两项输出约束。                                                                                                                                                          | $2\times2$ 因子设计：$L$（2）$\times$ $V$（2）。每条件 $N=200\times2=400$（反事实图），共 $N=1600$/模型。                                                                                          | 终止响应 $Y_{\mathrm{term}}\in\{R,S,I,O\}$ 的格点分配 $p^r(t)$；<br>主结果变量为 $R/S/I$，主估计量为 $RD_{\mathrm{len}}^{\mathrm{total},r}$ 与 $RD_{\mathrm{edge}}^{\mathrm{total},r}$。                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| E3：终点边 $e_k$ 的文本位置是否因果影响最后分叉点选中黄金后继的概率（全局位置效应） | 在最后分叉点 $v_{k-2}$，检验终点边 $e_k=(v_{k-1},y)$ 在边列表中的文本位置，是否因果改变模型选中正确后继 $v_{k-1}$ 的概率。                                                                                                       | 终点边 $e_k$ 的插入位置 $\mathrm{pos}\in\{\mathrm{first},\mathrm{center},\mathrm{before},\mathrm{after},\mathrm{last},\mathrm{canonical}\}$；六种位置互异，$\mathrm{canonical}$ 为原位对照。                      | 先对原图整体 shuffle 一次，再移除终点边 $e_k$，并保持 fixed body 内部相对顺序不变；六个位置条件除 $e_k$ 外共享同一固定正文，以排除整体 shuffle 带来的图文本顺序差异。代码核验六个位置互异，若不满足则丢弃并重新抽取；多 shuffle seed 取平均，用于将特定 shuffle 实现下的位置耦合边际化。主分析仅统计正确到达 $v_{k-2}$ 的样本，即 $C_{j,s,\mathrm{pos}}=1$，避免将早期路径错误归因于终点边位置；六个条件均使用 jsoncot_strict。                                                                                                                                                                 | 六位置单因子配对设计：$J=400$ 个 sample_id，每个 sample_id 在 $K$ 个 shuffle seed 下执行 $6$ 个位置条件，共 $400\times K\times6=2400K$ 次调用/模型。<br>主实验采用 $4$-hop，以降低正确前缀到达率过低导致的幸存者效应；$6$-hop 作为稳健性检验。 | 双差分效应：$\Delta_{\mathrm{net}}(m)=\Delta_{\mathrm{support}}(m)-\Delta_{\mathrm{dead}}(m)$。<br>各 arm 内先相对 $M=1$ 求差，再相减；同时报告 $\Delta_{\mathrm{dead}}(m)$ 的方向，并结合 $P_{\mathrm{support}}(1),P_{\mathrm{support}}(m),P_{\mathrm{dead}}(1),P_{\mathrm{dead}}(m)$ 检查天花板/地板效应。<br>若 $\Delta_{\mathrm{net}}(m)>0$ 且 $\Delta_{\mathrm{dead}}(m)>0$，则支持 graph-role-sensitive 频次锚定；若 $\Delta_{\mathrm{net}}(m)\approx0$，则更接近一般 token-frequency salience；若两者均弱，则说明模型基本不受重复影响。                                                                                                                                                             |
| E4：终点边重复次数是否因果影响最后分叉选择（频次锚定）                   | 检验终点边 $e_k$ 在边列表中的出现次数（edge multiplicity）是否偏离去重图语义零基线，并被模型用作最后分叉选择的非结构线索。<br>同时通过对照设计排除“读取噪声 + 冗余纠错”的竞争假设。                                                                              | 主干预为重复次数 $M\in\{1,2,3,4\}$。<br>arm 表示被操纵边的图角色：support arm 重复终点边 $e_k$，dead arm 重复终止分支 $e_{\mathrm{dead}}^{A}$。<br>结构对照为重复方式 $R\in\{\mathrm{clustered},\mathrm{evenly\text{-}dispersed}\}$。  | 沿用 E3 六位置控制样本作为 $M=1$ 基线；在同一 $(j,s,\mathrm{pos})$ 单元内，除被操纵边的重复次数 $M$ 外，其余边及相对顺序保持不变。<br>去重后图结构与自然语言语义不变，重复边不携带额外图语义；所有条件均使用 jsoncot_strict。<br>evenly-dispersed 条件按等宽分区中心插入重复边，并用 $\mathrm{Span}$ 描述分布形态。<br>主分析仅统计正确到达 $v_{k-2}$ 的样本。去重指令实验 $Z$ 作为附录缓解项，不进入主矩阵，也不计入主调用数。                                                                                                                                                                  | clustered 条件：$J=400\times K$ 个 seed $\times 6$ 个位置 $\times 4$ 个 $M$。<br>support/dead 两个 arm 各 $9600K$，合计 $19200K$。<br>evenly-dispersed 结构对照为 $1600K$；全实验共 $20800K$ 次调用/模型。 | clustered 条件：$J=400\times K$ 个 seed $\times 6$ 个位置 $\times 4$ 个 $M$。<br>support/dead 两个 arm 各 $9600K$，合计 $19200K$。<br>evenly-dispersed 结构对照为 $1600K$；全实验共 $20800K$ 次调用/模型。                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| E5：长路径低 accuracy 归因于前段状态维护还是终端局部决策困难           | 拆分长 branching-$k$ hop 低 accuracy 的两类困难：<br>a 类为状态维护失败，即链变长后，模型在到达 $v_{k-2}$ 前已经偏离黄金路径；<br>b 类为 $v_{k-2}$ 处的局部决策困难，包括终端约束，即 horizon 与终点对齐。<br>在唯一黄金路径设定下，horizon 与终点对齐在信息层面等价，因此合称为终端约束。 | $2\times2$ 诊断轴：状态维护负担（完整前缀 vs oracle 空降 $v_{k-2}$）$\times$ 终端约束（原始 2-hop 终端窗口 vs 后续延展窗口弱化）。<br>四个任务条件为 $T\in\{\mathrm{F},\mathrm{R},\mathrm{O\text{-}local},\mathrm{O\text{-}full}\}$。      | R/O 系列均保持 $v_{k-2}$ 局部候选窗口逐字一致、候选数量一致、$v_{k-1}$ 相对位置一致、$e_{\mathrm{dead}}^{A,B}$ 一致、shuffle 规则一致，并通过补充/调整干扰边对齐边列表元素数量；四个条件均使用 jsoncot_strict。<br>task 系列从 $v_0$ 出发，未抵达 $v_{k-2}$ 的样本记为 $0$；O 系列从 $v_{k-2}$ 出发，因此无幸存者效应。                                                                                                                                                                                                                    | $2\times2$ 边界设计，按 sample_id 配对。<br>task 系列沿用主实验 hop 扫描；当 $v_{k-2}$ 前缀到达率低于 $0.5$ 时，幸存者效应显著，因此该 hop 不进入主诊断矩阵，仅作为辅助结果报告。                                                     | 四个 cell 概率为 $p_F,p_R,p_{\mathrm{local}},p_{\mathrm{full}}$。<br>状态维护效应：$\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}on}}=p_{\mathrm{local}}-p_F$，$\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}off}}=p_{\mathrm{full}}-p_R$。<br>终端约束效应：$\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}on}}=p_R-p_F$，$\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}off}}=p_{\mathrm{full}}-p_{\mathrm{local}}$。<br>交互项：$\Delta_{\mathrm{interaction}}=(p_R-p_F)-(p_{\mathrm{full}}-p_{\mathrm{local}})$。<br>若两个 $\Delta_{\mathrm{state}}$ 均较大，则状态维护负担主导；若两个 $\Delta_{\mathrm{terminal}}$ 均较大，则终端约束主导。估计量解释为诊断性边界对比，而非无偏因果识别。 |
| E6：同源候选竞争与 chain 的退化 branching 诊断              | 控制黄金节点出度 $d$，考察同源候选竞争对局部黄金后继选择率的边际影响；当 $d=1$ 时退化为 chain，用于检验 chain 是否可视为 branching 的无竞争极限。该实验还与 E2 协同刻画 $e_{\mathrm{dead}}$ 如何在接口约束下被传导为非法路径。                                           | 主干预为出度 $d\in\{1,2,3,4,5\}$。<br>$d=1$ 为 chain 退化条件，$d=3$ 为 canonical 主实验条件，$d\in\{2,4,5\}$ 用于出度扫描。<br>接口条件为 $\{\texttt{direct\_minimal},\texttt{jsoncot\_basic},\texttt{jsoncot\_strict}\}$。 | 采用 keyed 确定性排序：用 SHA256 根据边内容与样本种子生成稳定 key，并按 key 升序排列，避免依赖列表长度的 random shuffle；从而保证跨 $d$ 条件下共有边的相对顺序一致，新增同源分支也按同一 key 规则插入确定位置。<br>边数守恒与竞争纯度守恒之间存在折中，因此同步报告 $q_{\mathrm{reach}}$ 的实测稳定性，审计出度操纵是否额外改变背景干扰强度。<br>另设 deletion-only reshuffle control：等量删除背景边并用多 seed 重排，但不改变出度，用于验证 $q_{\mathrm{reach}}$ movement 不是由背景边删除或重排本身造成。<br>$k$ 根据 E5 的 $q_{\mathrm{reach}}(k)$ 与 $p_F^{\mathrm{cond}}(k)$ 选择，避免地板/天花板效应，并采用 reach-conditioned 分析。 | $5\times3$ 因子设计：出度 $d$（5）$\times$ 接口（3），每格 $N=800$，共 $12000$ 次调用/模型。<br>按 sample_id 聚类 bootstrap 计算 $95\%$ CI；未到达局部决策点的样本记为 $0$。                                           | 前 $k-1$ hop 成功率：$p_{k-1}(d)=q_{\mathrm{reach}}(d)\cdot p^{\mathrm{cond}}(d)$。<br>拟合 $p(d)=\alpha+\beta(d-1)+\gamma(d-1)^2$，其中 $\alpha$ 表示 chain 退化基线，$\beta$ 表示同源竞争的边际代价，$\gamma$ 表示该代价是否随 $d$ 加速：$\gamma<0$ 为凹形/超线性崩溃，$\gamma\approx0$ 为线性稀释，$\gamma>0$ 为凸形/地板效应。<br>若 $q_{\mathrm{reach}}$ 与 $p^{\mathrm{cond}}$ 方向相反，导致 $p(d)$ 非单调，则 $\alpha/\beta/\gamma$ 仅作描述统计，机制结论改由 $p^{\mathrm{cond}}(d)$ 的拟合给出。                                                                                                                                                                                                                  |
| E7：同源候选竞争对答案层位置锚定与路径层非法续写的联合诊断                 | 用同一最小干预操纵 $v_{k-2}$ 的出度，将 direct_minimal 下的答案层锚定与 jsoncot_strict 下的终止/非法路径联系起来。<br>注入同源终止竞争后，检验末位诱饵 $z$ 的位置捷径是否被瓦解，以及该结构化显式路径约束是否会转化为 dead-branch / illegal continuation。               | 主干预为 $v_{k-2}$ 的局部出度 $d\in\{1,2,3,4\}$，其中 $d=1$ 退化为原始 chain。<br>接口条件为 $\{\texttt{direct\_minimal},\texttt{jsoncot\_basic},\texttt{jsoncot\_strict}\}$，并按图角色分配。                              | 三图反事实 $G=(G_1,G_2,G_3)$：$G_1,G_2$ 为 m-chain 处理图，仅改变 $v_{k-2}$ 的局部出度；$G_3$ 为原始 chain 对照。<br>注入的每个同源候选 $v_{\mathrm{dead}}^{(k-2)}$ 均为零出度节点，且不指向黄金节点，保证唯一黄金路径条件在 $d\ge2$ 时仍成立。<br>随 $d$ 增加等量删除背景干扰边以保持边数守恒，不重新 random shuffle，而采用 E6 相同的 keyed 排序规则保证实验可比。<br>$G_2/G_3$ 用于答案层诊断，使用 direct_minimal；$G_1$ 用于路径层诊断，使用 jsoncot。所有条件按 sample_id 配对。                                                                                                | 三图 × 出度 × 接口的稀疏矩阵设计，按答案层 / 路径层角色裁剪条件组合。<br>每个 cell $N=800$；单个 hop 共 $3200$ 次调用。<br>覆盖 $4/5/6$-hop，合计 $9600$ 次调用/模型。                                                        | G2 主指标为 $\mathrm{SS\text{-}EAR}=\mathbb{1}[\hat y(G_2)=z]$。<br>条件层指标为 $\mathrm{SS\text{-}EAR}_{\mathrm{cond}}=\Pr(\hat y(G_2)=z\mid \mathrm{EAR}(G_3)=1)$，配对瓦解率为 $\Delta_{\mathrm{EAR}}^{\mathrm{SS,cond}}=1-\mathrm{SS\text{-}EAR}_{\mathrm{cond}}$。<br><br>G1 侧报告 $p_{\mathrm{cond}}(d)$、$p_{\mathrm{dead}}(d)$ 与 $p_{\mathrm{illegal}}(d)$，并与 E2/E5/E6 结果衔接。<br>若 $\mathrm{SS\text{-}EAR}_{\mathrm{cond}}\approx1$，说明答案层位置捷径不被同源竞争动摇；若显著低于 $1$，说明同源竞争瓦解位置锚定，并激活路径遍历。                                                                                                                                                    |

## 4.5 评估与统计（Evaluation and Statistics）

指标的定义与诊断动机已在 §3 给出，本节只规范计算流程，使报告数字可被独立复现。

§4.3 介绍了实验所使用的 prompt 形式。本节进一步说明如何解析模型所返回的 JSON 输出，并将其中的 answer 与 path 字段转换为可计算检验的预测答案与路径对象。在 direct_minimal 条件下，prompt 仅要求模型返回包含 answer 字段的 JSON 对象；在 jsoncot_basic 与 jsoncot_strict 条件下，模型返回包含 path（节点序列）和 answer（单个节点标识符）的 JSON 对象。共享解析器首先去除首尾空白及 Markdown 代码围栏，并尝试将完整响应解析为 JSON 对象。完整解析失败时，提取首个可完整解码的 JSON 对象。此时，若主对象前后存在非空文本，则记录 extra_text；若主对象前后剩余文本中包含至少一个可完整解码的 JSON 对象，则额外记录 multiple_json_objects。二者仅作为格式审计字段，不影响 JSON 解析或 schema 判定；完整解析成功时，解析器进一步执行与提示接口对应的 schema 检查，direct_minimal 要求 answer 为非空字符串；jsoncot_basic 与 jsoncot_strict 在此基础上还要求 path 为仅包含非空字符串节点的列表（这里的“非空字符串”均指去除首尾空白后长度仍大于零）。字段缺失、字段类型错误、path 中出现 null，以及空字符串或非字符串节点，均记为 schema_failure。仅同时满足 JSON 解析与 schema 检查的输出记为 parse_ok 并进入相应字段的有效子集，即 $\text{格式有效}=\mathbb{1}\{\text{JSON 解析成功}\land \text{模式检查通过}\}$。json_parse_failure、schema_failure 与后续符号验证发现的结构错误分别统计，不相互替代。

§ X 中用于路径结构检查与 verifier-retry 反馈的符号验证器仅接收任务公开信息 $(G,s,k)$ 与模型输出 $(\hat p,\hat y)$。通过使用输入图的边集合 $E$ 检查模型已经生成的节点转移是否合法，但不访问黄金答案 $y$、黄金路径 $p^*$，也不计算或提供正确下一跳。验证器据此计算 PathValid 与 TAC，而不判断预测答案是否等于黄金答案；Accuracy 与 PathGoldExact 由离线评分器在评测阶段另行计算。在 verifier-retry 设置中，反馈仅包含违反的约束类型，并可指出首次失败的 hop 及模型已经生成的非法边；反馈不包含合法替代边、可选邻居集合、黄金终点或黄金路径。因此，该反馈不构成 oracle leakage。模型在生成文本中自行声称某条边合法，也不会影响符号验证结果。本文按阶段分别统计 json_parse_failure、schema_failure 与符号结构错误；只有通过前两阶段且具备可检查节点转移的输出，才进入 illegal_edge_at_hop_i 统计。

本文以 sample_id（即一个基础样本）作为最小独立重采样单位，共享主实验 sample_id 池。统计分析在各 model–dataset–prompt setting 内进行；同一 sample_id 下的 $G_1/G_2$、四种序列化条件以及同一因果机制实验中的全部处理条件，在重采样时整体保留，以避免将样本内重复观测误作独立数据。Raw GIS、PC-GIS 与 GFI 均在 $G_1/G_2 \times 4$ 种序列化结果齐全的共同配对子集上计算。GFI 在样本级定义为 $\mathrm{RawGIS}^{(j)}-\mathrm{PCGIS}^{(j)}$，随后进行聚类 bootstrap。跨模型对比仅在所有被比较模型及所有对比组成项均有效的共同 `sample_id` 集合上估计，并同步报告全部调用数、有效调用数和共同 cluster 数。

非 retry 主实验与六个因果机制实验均使用 $B=10{,}000$ 次 percentile cluster bootstrap。95% 置信区间取 bootstrap 分布的第 $2.5$ 与第 $97.5$ 百分位。Verifier-retry 首先在每个任务内将多次 attempt 汇总为 $pass@K$、累计延迟和最终失败状态，再在独立 run 内按 `sample_id` 重采样；不同 run 的均值与波动另行报告，不将 attempt 当作独立样本。主要二元效应以 risk difference 及其 95% CI 报告。

预先规定的主要对比与探索性分析分开报告；FailureHop、ErrorType、ErrorSource 及逐 hop 分解视为机制探索，不作为独立 confirmatory 检验。本文不单独依赖 $p$ 值，而联合报告效应量、95% CI、全部调用数、有效子集和 cluster 数。对于 risk difference，CI 包含 $0$ 表示当前数据下效应方向不确定。需要注意的是，若 chain 与 branching 条件并非由同一基础样本配对生成，则该比较应报告为非配对的拓扑条件对比，而不表述为 paired causal contrast。

# 5 Results

本节按引言中提出的四条主要发现组织实验结果。§5.1 首先报告先验控制，用于排除答案先验与候选偏好对后续图任务结果的污染；随后，§5.2–§5.5 分别对应四条主要发现：弱提示下的答案层位置捷径、结构化输出下的路径层非法轨迹、答案层锚定错误向路径层的迁移，以及两类缓解机制的成本—收益。

## 5.1 无图先验与候选偏好控制（Graph-Free Controls for Answer Priors and Candidate Preferences）

结果如表 X 所示：

| Dataset                |              Random baseline | DS-Flash prior-only | Qwen Max prior-only | DS-Flash candidate-only | Qwen Max candidate-only |
| ---------------------- | ---------------------------: | ------------------: | ------------------: | ----------------------: | ----------------------: |
| `chain_4hop_N200`      | $\frac{1}{22}\approx 4.55\%$ |            $0.00\%$ |            $0.00\%$ |                $2.50\%$ |                $3.00\%$ |
| `branching_4hop_N200`  | $\frac{1}{28}\approx 3.57\%$ |            $0.00\%$ |            $0.00\%$ |                $3.50\%$ |                $1.50\%$ |
| `branching_12hop_N200` | $\frac{1}{84}\approx 1.19\%$ |            $0.00\%$ |            $0.00\%$ |                $0.25\%$ |                $0.00\%$ |

DeepSeek-V4-Flash 和 Qwen Max 在表中三个 prior-only 条件下的准确率均为 0%；在 candidate-only prior 条件下，两种模型的准确率在数值上均与相应的均匀候选随机基线相当或更低。该结果表明，在不给出图结构信息时，两种模型没有表现出能够稳定识别黄金终点的答案先验或候选列表偏好。因此，主实验中观察到的正确响应、位置依赖和非法路径现象，不能由答案先验或候选集合偏好单独解释。

**Finding 1.** 在不给出图结构信息时，两种模型未表现出能够稳定识别黄金终点的答案先验或候选列表偏好。因此，后续实验中的正确响应、位置依赖与非法路径现象不能由这些无图偏好单独解释。
## 5.2 弱提示下链式图容易诱发答案层位置捷径（Weak prompts induce answer-level positional shortcuts）

本节报告答案层的位置依赖实验结果，包括各模型在不同链长和拓扑上的 GFI （位置虚胖）长度扫描，实验揭示了三个主要发现：

| Dataset | Model        | Accuracy | RawGIS | GFI    | EAR    |
| ------- | ------------ | -------- | ------ | ------ | ------ |
| 3hop    | DS-Flash     | 48.00%   | 92.50% | 92.50% | 81.25% |
| 4hop    | DS-Flash     | 29.50%   | 68.50% | 68.50% | 63.75% |
| 5hop    | DS-Flash     | 26.19%   | 62.50% | 62.50% | 75.75% |
| 6hop    | DS-Flash     | 25.03%   | 67.17% | 67.17% | 77.50% |
| 3hop    | GPT-5.4-mini | 70.61%   | 86.43% | 82.41% | 44.25% |
| 4hop    | GPT-5.4-mini | 47.38%   | 71.00% | 71.00% | 58.25% |
| 5hop    | GPT-5.4-mini | 39.19%   | 63.00% | 63.00% | 66.25% |
| 6hop    | GPT-5.4-mini | 32.83%   | 46.73% | 46.73% | 62.00% |
| 3hop    | Qwen Max     | 77.00%   | 85.50% | 73.50% | 24.00% |
| 4hop    | Qwen Max     | 46.06%   | 38.00% | 38.00% | 19.75% |
| 5hop    | Qwen Max     | 19.06%   | 4.00%  | 4.00%  | 9.00%  |
| 6hop    | Qwen Max     | 13.69%   | 3.00%  | 3.00%  | 10.50% |
第一，chain graphs 在默认 endpoint-last 序列化下通常表现出较高的 Raw GIS，但 PC-GIS 几乎始终接近 0，因此 GFI 与 Raw GIS 基本重合。表明，在 direct_minimal 弱提示下，模型表现出的图干预敏感性大多无法在终点位置变化后保持，而主要依赖默认序列化中的终点位置线索。DeepSeek-V4-Flash 在 chain-3hop 至 chain-6hop 上的 PC-GIS 均为 0；GPT-5.4-mini 仅在 chain-3hop 上保留 4.02% 的 PC-GIS，在 4–6 hop 上均降至 0；Qwen Max 在 chain-3hop 上保留 12.00% 的 PC-GIS，但从 chain-4hop 开始同样降至 0。由此可见，三种模型在默认位置下表现出的反事实响应，大部分不能通过位置控制后的严格检验；第二，三种模型呈现出不同的深度响应模式。DeepSeek-V4-Flash 表现出稳定而强烈的位置依赖，其 chain-3hop 至 chain-6hop 的 GFI 始终维持在 62.50%–92.50%，且 PC-GIS 全程为 0，说明默认序列化下的 Raw GIS 几乎完全无法在位置控制后保持。GPT-5.4-mini 的 GFI 随链长增加从 82.41% 单调下降至 46.73%，但 PC-GIS 仅在 chain-3hop 上为 4.02%，其余深度均为 0，说明其位置依赖随深度逐渐减弱，却没有转化为稳定的位置不变图跟随能力。Qwen Max 的 GFI 从 73.50% 降至 38.00%，并在 chain-5hop 和 chain-6hop 上进一步降至 4.00% 和 3.00%。但这一低 GFI 与 Raw GIS 的同步崩塌共同出现，因此应解释为默认图干预敏感性本身已经丧失，而不是位置依赖被消除；第三，decoy-last EAR 为末位诱饵锚定提供了更直接的行为证据。DeepSeek-V4-Flash 在四种链长上的 decoy-last EAR 为 63.75%–81.25%，说明其经常直接选择被置于序列末位的错误诱饵节点。GPT-5.4-mini 的 EAR 为 44.25%–66.25%，表明即使 GFI 随深度下降，模型仍持续受到末位线索影响。Qwen Max 的 EAR 则从 24.00% 下降至长链条件下约 9%–11%；但这一变化与其 Raw GIS、GFI 和准确率的同步下降共同出现，因此不能被解释为模型已经获得更忠实的图推理能力，而更接近整体任务敏感性的崩塌。

**Finding 2.** 在 direct_minimal 弱提示下，chain graphs 中较高的原始图干预敏感性大多无法通过终点位置控制，并伴随显著的末位诱饵锚定，表明默认序列化下的表面成功主要受到位置捷径支撑。
## 5.3 结构化输出暴露分支图中的非法轨迹问题

本节报告路径层的非法轨迹实验结果，包括各模型在不同链长和拓扑上的轨迹—答案一致率（TAC）、路径合法率（PathValid）以及自洽但非法轨迹率 $\Delta_{\mathrm{illegal}}$ 以及长度扫描，branch下的位置依赖等流向分析放在 §6 Analysis 中。实验揭示了三个主要发现：

| Dataset | Model        | TAC     | PathValid | $\Delta_{\mathrm{illegal}}$ | $\Delta_{\mathrm{illegal}}^{\mathrm{correct}}$ | $\Delta_{\mathrm{illegal}}^{\mathrm{wrong}}$ |
| ------- | ------------ | ------- | --------- | --------------------------- | ---------------------------------------------- | -------------------------------------------- |
| 4-hop   | DS-Flash     | 100.00% | 88.25%    | 11.75%                      | 0.00%                                          | 11.75%                                       |
| 6-hop   | DS-Flash     | 99.69%  | 78.56%    | 21.12%                      | 0.50%                                          | 20.62%                                       |
| 8-hop   | DS-Flash     | 99.69%  | 66.12%    | 33.56%                      | 1.06%                                          | 32.50%                                       |
| 10-hop  | DS-Flash     | 99.69%  | 56.75%    | 42.94%                      | 1.69%                                          | 41.25%                                       |
| 12-hop  | DS-Flash     | 99.31%  | 48.50%    | 50.81%                      | 1.69%                                          | 49.12%                                       |
| 4-hop   | GPT-5.4-mini | 95.99%  | 71.62%    | 24.37%                      | 0.06%                                          | 24.31%                                       |
| 6-hop   | GPT-5.4-mini | 93.68%  | 43.62%    | 50.06%                      | 0.00%                                          | 50.06%                                       |
| 8-hop   | GPT-5.4-mini | 90.64%  | 23.49%    | 67.15%                      | 0.06%                                          | 67.09%                                       |
| 10-hop  | GPT-5.4-mini | 88.42%  | 12.20%    | 76.22%                      | 0.00%                                          | 76.22%                                       |
| 12-hop  | GPT-5.4-mini | 79.88%  | 8.06%     | 71.81%                      | 0.06%                                          | 71.75%                                       |
| 4-hop   | Qwen Max     | 100.00% | 68.12%    | 31.87%                      | 0.56%                                          | 31.31%                                       |
| 6-hop   | Qwen Max     | 100.00% | 54.19%    | 45.81%                      | 2.69%                                          | 43.12%                                       |
| 8-hop   | Qwen Max     | 100.00% | 34.75%    | 65.25%                      | 4.62%                                          | 60.62%                                       |
| 10-hop  | Qwen Max     | 99.75%  | 15.75%    | 84.00%                      | 4.19%                                          | 79.81%                                       |
| 12-hop  | Qwen Max     | 99.19%  | 8.38%     | 90.81%                      | 4.94%                                          | 85.88%                                       |

第一，较高的 TAC 并不意味着模型生成了图上合法的路径。DeepSeek 和 Qwen 在全部深度上的 TAC 均接近 100%，但 PathValid 随深度持续下降。以 12-hop 为例，DeepSeek 的 TAC 为 99.31%，PathValid 仅为 48.50%；Qwen 的 TAC 为 99.19%，PathValid 仅为 8.38%。GPT 的 TAC 也始终明显高于 PathValid，在 12-hop 上二者分别为 79.88% 和 8.06%。这表明模型通常能够使生成路径的末节点与自身答案保持一致，但这种文本层面的自洽性并不能保证路径中的每一次跳转都存在于输入图中；第二，自洽非法轨迹随推理深度 kkk 显著增加。DeepSeek 的 $\Delta_{\mathrm{illegal}}$ 从 4-hop 的 11.75% 逐步上升至 12-hop 的 50.81%；Qwen 从 31.87% 上升至 90.81%。GPT 从 24.37% 上升至 76.22%，在 12-hop 时为 71.81%。总体而言，随着分叉图深度增加，PathValid 下降的速度显著快于 TAC，从而造成越来越大的 $\Delta_{\mathrm{illegal}}$。需要说明的是，GPT 在 12-hop 上的缺口略低于 10-hop，并不是 PathValid 恢复，而是 TAC 本身从 88.42% 进一步下降至 79.88%；第三，$\Delta_{\mathrm{illegal}}$​ 主要由答案错误且路径非法的样本构成。在 12-hop 上，DeepSeek 的 $\Delta_{\mathrm{illegal}}^{\mathrm{wrong}}$​ 为 49.12%，而 $\Delta_{\mathrm{illegal}}^{\mathrm{correct}}$​ 为 1.69%；GPT 分别为 71.75% 和 0.06%；Qwen 分别为 85.88% 和 4.94%。因此，大多数自洽非法样本同时表现为答案错误、路径末节点与模型答案一致，但路径未能通过逐边合法性验证。答案正确但路径非法的样本比例相对较低，不过在 DeepSeek 和 Qwen 的深层条件下仍可观察到。

**Finding 3.** 结构化路径输出并不保证图结构忠实性。在分叉图上，模型可以维持很高的 TAC，同时 PathValid 随深度快速下降；到 12-hop 时，三种模型的 $\Delta_{\mathrm{illegal}}​$ 分别达到 50.81%、71.81% 和 90.81%。因此，TAC 只能证明路径与模型自身答案相互一致，不能替代逐边图合法性验证。

## 5.4 失败形态会随接口约束、拓扑结构与推理深度发生迁移


单个指标或同一指标族沿 λ,k,d\lambda,k,dλ,k,d 的变化放 §5；把 GFI、EAR、Accuracy、路径合法性和流向分析组合成机制解释放 §6。





## 5.5 两类缓解机制：内部预算推理（思考模式） vs 外部符号反馈（verifier-retry）

### 5.5.1 内部缓解：thinking 完全解决任务，代价是时延成本高

首先比较 DeepSeek-V4-Pro 的 no-thinking 与 thinking 模式。 $\texttt{branching-12hop} + \texttt{jsoncot\_strict}$ 的设置下 DeepSeek-V4-Pro 通过思考模式解决了所有任务（1 个因 timeout 未返回有效结果），已完成样本的准确率、路径合法性和 PathGoldExact、Raw GIS 均几乎达到满分，GFI 接近 0，表明在足够高的内部推理预算下，思考模式可以同时消除答案层位置依赖和路径层非法轨迹同时；也证明了 branching-12hop 任务本身并非不可解，强模型在高推理预算下可以接近完美完成。当然，这一漂亮且美好的成绩伴随显著的时延成本，DeepSeek-V4-Pro no-thinking 的平均时延约为 2.4 秒，而思考模式的平均时延为 46.7 秒，约为前者的 19 倍。因此，认为 thinking 更适合理解为高推理预算下的高性能参考而非处理任务的默认方案。

Table X. DeepSeek-V4-Pro under no-thinking and thinking modes on branching-12hop + `jsoncot_strict`. All rows use the same model; only the inference mode differs. Metrics except latency are reported in percentages.

| Mode        | Accuracy |   Raw GIS |   PC-GIS |      GFI | PathGoldExact | Avg. latency |
| ----------- | -------: | --------: | -------: | -------: | --------------: | -----------: |
| no-thinking | $56.3\%$ |  $25.5\%$ |  $2.0\%$ | $23.5\%$ |        $55.3\%$ |         2.4s |
| thinking    | $99.9\%$ | $100.0\%$ | $99.5\%$ |  $0.5\%$ |        $99.9\%$ |        46.7s |
no-thinking 模式下的失败不是简单格式问题，DeepSeek-V4-Pro 的 Accuracy 为 56.3%，PathGoldExact 为 55.3%，PC-GIS 仅为 2.0%，表明模型存在明显的路径非法和图状态追踪失败。Thinking 可以缓解该问题，但它是一种高成本的内部缓解机制。
### 5.5.2 外部缓解：verifier-retry 可以部分缓解，但受模型能力限制

| Model    | 8-hop $A(1)$ | 8-hop $A(5)$ | 8-hop $H_{base}(K)$ | 12-hop $A(1)$ | 12-hop $A(5)$ | 12-hop $H_{base}(K)$ |
| -------- | ------------ | ------------ | ------------------- | ------------- | ------------- | -------------------- |
| DeepSeek | 66.5%        | 85.5%        | 99.6%               | 48.6%         | 70.6%         | 96.4%                |
| GPT      | 25.8%        | 61.9%        | 77.4%               | 6.9%          | 24.5%         | 30.1%                |
| Qwen     | 33.4%        | 67.0%        | 86.9%               | 8.1%          | 33.4%         | 34.6%                |

第一，单次生成失败后，继续提供 verifier 反馈并重新采样，确实能够使部分样本在任务预算内转为通过，但随着 $k$ 的增加，更深的分叉图显著降低了最终通过率，并削弱了首轮失败样本在后续轮次中被救回的比例。在 8-hop 上，DeepSeek、GPT 和 Qwen 的首轮通过率分别为 66.5%、25.8% 和 33.4%，五轮累计通过率分别达到 85.5%、61.9% 和 67.0%；在 12-hop 上，三种模型的首轮通过率分别降至 48.6%、6.9% 和 8.1%，五轮累计通过率仅为 70.6%、24.5% 和 33.4%。从 8-hop 增加至 12-hop 后，DeepSeek-V4-Flash、GPT-5.4-mini 和 Qwen Max 的五轮通过率分别下降 14.8、37.4 和 33.6 个百分点。进一步以首轮失败样本为分母，五轮预算内的救回比例分别由 8-hop 的 56.7%、48.7% 和 50.5% 降至 12-hop 的 42.8%、18.9% 和 27.5%。这表明 $k$ 的增加不仅压低模型第一次生成合法路径的概率，也降低了 verifier 反馈在后续轮次中将失败样本转入通过状态的效率；


第二，经验累计吸收率 $A(K)$ 低于 independent baseline，且不同模型的偏离程度明显不同。记第 $t$ 轮条件吸收率为 $h_t$，分母为实际进入第 $t$ 轮的剩余实例，表示进入该轮的剩余实例中，在本轮通过 verifier 的比例，即 $h_t=\Pr(\tau=t\mid\tau\ge t)$。在 12-hop 上，DeepSeek 的条件吸收率由首轮的 48.6% 降至第五轮的 6.6%；Qwen 的条件吸收率在第二轮短暂上升，但随后逐步下降；GPT 则在各轮维持较低且相对平坦的水平。8 / 12 hop 的逐轮条件吸收率见 Appendix X。若所有实例在各轮均共享同一固定成功概率，则条件吸收率不应随风险集组成发生如此明显的变化。因此 $A(K)$ 与 $H_{base}(K)$ 的偏离表明均质独立假设不足以刻画实际 retry 过程。一种可能解释是实例级成功概率存在异质性：若模型对某个实例 $i$ 具有较高通过倾向，则更可能在前几轮被吸收，使后续风险集的组成发生变化。为检验这一解释能在多大程度上重现实测曲线，本文构造了异质性校正的独立重试基线 $J(K)$，并比较均质独立基线 $H(K)$、实际累计吸收率 $A(K)$、异质性校正后的独立基线 $J(K)$。

为诊断下降原因，判断上述偏离是否与不同具体实例 $i$ 的成功概率异质性有关，本文进一步比较了三个独立 run 的首轮结果与 Beta-Binomial 相对于均质 Binomial 的过散度检验。三个独立 run 的零假设为其首轮条件吸收率 $h_1​$ 相同，Cochran’s Q 检验的最小 $p$ 值为 0.34，未检测到显著的 run 间首轮总体通过率差异，排除结果主要由某次 run 整体更强或更弱造成；过散度检验表明所有模型均拒绝均质 Binomial 假设。而利用 Beta-Binomial 拟合数据，假设每个实例 $i$ 都有自己的潜在首轮成功概率 $p_i$，令 $p_i\sim\operatorname{Beta}(\alpha,\beta)$，结果显示拟合显著更好，且不同模型呈现不同的异质性分布形态：DeepSeek 呈现出最强的两极化结构，其 $p_i$ 大量集中于接近 0 或接近 1 的区域；GPT 和 Qwen 在深层条件下则主要表现为强烈偏低成功率端，表明不同实例的成功倾向不同。完整的拟合参数、似然比检验和首轮成功计数分布见 Appendix X。基于上述结果，本文使用三个 run 的首轮成功计数拟合 Beta-Binomial 分布，并据此构造异质性校正的独立重试基线 $J(K)$。该基线允许不同实例具有不同但固定的成功概率，同时假设在给定实例成功概率后，各轮尝试条件独立。

与使用总体首轮通过率构造的均质独立基线 $H(K)$ 相比，实际 verifier-retry 曲线 $A(K)$ 在全部六个设置中均位于 $H(K)$ 与 $J(K)$ 之间。

例如，在 DeepSeek 12-hop 条件下，$H(5)=96.4%$，$A(5)=70.6%$，而 $J(5)=54.7%$，其 95% 置信带见 Appendix X。因而，各设置均满足 $H(K)>A(K)>J(K)$，并有 $H(K)-J(K)\geq H(K)-A(K)$。

这一结果说明，仅引入实例级成功概率异质性，便足以产生不小于实测幅度的均质 iid 基线虚高。因此，实际累计通过率低于 $H(K)$，不能直接解释为 verifier 反馈无效或重试过程存在负作用。相当一部分偏离可以由风险集分选和实例成功倾向异质性解释。

不过，这一分解属于描述性模型比较，而不是对真实机制的唯一识别。

另一方面，$A(K)>J(K)$ 表明，实际 verifier-retry 的表现优于“每个实例保持固定成功概率的独立重复采样”基线。这一方向与 verifier 反馈产生净正修正的解释一致，但不能仅凭 $A(K)$ 与 $J(K)$ 的差异将其解释为严格的因果效应，因为这一比较仍可能受到潜在成功概率分布设定误差以及未建模轮次依赖的影响。

Qwen 12-hop 提供了一项不依赖 $J(K)$ 数值估计的方向性证据。首轮失败后的第二轮条件通过率为 9.6%，高于总体首轮通过率 8.1%，差值为 1.5 个百分点，bootstrap 95% 置信区间为 $[0.1,2.9]$，单侧检验的 $p$ 值为 0.018。

在仅包含固定实例成功概率和风险集选择效应的模型下，首轮失败会使剩余风险集进一步向低成功概率实例集中，因此第二轮条件通过率原则上不应高于总体首轮通过率。尽管这一反转幅度较小，其方向仍表明，反馈后的第二轮生成并非首轮生成机制的简单重复。

稳定失败池中的后续吸收结果提供了进一步补充。即使将分析限定在三个 run 的首轮尝试均失败的实例中，一条实际 retry 轨迹仍能够在 $K\leq5$ 的预算内吸收其中相当一部分实例。DeepSeek 在 8-hop 和 12-hop 条件下的吸收率分别为 53.8% 和 40.8%，GPT-5.4-mini 在 12-hop 条件下的吸收率则为 16.9%。

这一结果本身不能单独证明反馈具有因果效应，因为成功概率较低但非零的实例，也可能在后续独立采样中偶然通过。但它至少说明，三次首轮尝试均失败并不等同于该实例在当前协议下不可修复，反馈重试仍能够从这一稳定失败池中恢复部分样本。

综合而言，在本文考察的 $K\leq5$ 范围内，retry 收益的饱和主要受到模型—实例成功倾向分布的约束，而不只是由调用预算本身决定。成功倾向较高的实例会在早期被迅速吸收，剩余风险集则逐轮向低成功概率实例集中。Verifier 反馈可能进一步修复其中一部分稳定失败实例，但这一修复比例随模型和任务深度而异，并在更深设置中总体下降。

需要强调的是，现有诊断尚不能将稳定的实例级成功倾向，与单条 retry 轨迹内部由反馈历史、上下文延续或错误骨架复用造成的额外轮间依赖完全分离。要区分这两类机制，需要在相同实例上加入不保留前轮上下文的 fresh-context 独立重采样对照。本文将这一对照实验留作后续工作。

第四，**非法轨迹是 retry 环中最具有持续性的失败状态，其黏性随深度增加，而从非法状态直接转入通过状态的比例同步下降。** 在 8-hop 上，DeepSeek-V4-Flash、GPT-5.4-mini 和 Qwen Max 从非法状态再次进入非法状态的比例分别约为 58.9%、62.5% 和 80.9%；在 12-hop 上分别上升至 75.3%、66.8% 和 90.9%。与此同时，从非法状态直接进入 verifier pass 的比例分别由 19.9%、13.6% 和 16.6% 降至 12.3%、4.9% 和 7.9%。

|Model|8-hop illegal→illegal|12-hop illegal→illegal|8-hop illegal→Pass|12-hop illegal→Pass|
|---|---|---|---|---|
|DeepSeek-V4-Flash|58.9%|75.3%|19.9%|12.3%|
|GPT-5.4-mini|62.5%|66.8%|13.6%|4.9%|
|Qwen Max|80.9%|90.9%|16.6%|7.9%|

Qwen Max 的 12-hop 结果最为突出：约 90.9% 的非法输出在下一轮仍然非法，而只有约 7.9% 转入 pass。此外，其结构失败转入非法状态的比例约为 93.5%，说明 verifier 反馈通常没有将结构性失败直接推向通过状态，而是使其转化为形式更完整、但仍包含非法边的路径输出。

非法状态黏性的模型排序与最终累计通过率排序并不一致。12-hop 上，非法自环比例为 Qwen Max >>> DeepSeek-V4-Flash >>> GPT-5.4-mini，而五轮累计通过率为 DeepSeek-V4-Flash >>> Qwen Max >>> GPT-5.4-mini。这表明首轮任务能力与失败后的可修复性是两个不同维度：前者反映模型能否直接完成任务，后者反映模型一旦进入非法轨迹后，符号反馈能否将其从该状态中拉出。Verifier-retry 的最终效果由初始状态分布和失败状态持续性共同决定，不能由单一转移概率解释。

格式错误并不是主要限制。DeepSeek-V4-Flash 和 Qwen Max 几乎没有形成具有充分支持的格式错误状态行，GPT-5.4-mini 的格式错误也只占较小比例。因此，retry 的主要瓶颈并非 JSON 或 schema 遵循，而是逐边合法性和跨步状态维护。完整转移核、分轮时齐性诊断、状态覆盖率和低支持标记见 Appendix X。

第五，**不同模型获得预算内通过结果所需的调用成本差异显著。** 在 12-hop、五轮预算下，DeepSeek-V4-Flash 平均使用约 2.59 次调用达到 70.6% 的累计通过率；Qwen Max 和 GPT-5.4-mini 平均分别使用约 4.21 和 4.42 次调用，但累计通过率仅为 33.4% 和 24.5%。

|Model|A(5)A(5)A(5)|C(5)C(5)C(5)|C(5)/A(5)C(5)/A(5)C(5)/A(5)|
|---|---|---|---|
|DeepSeek-V4-Flash|70.6%|2.59|约 3.7|
|Qwen Max|33.4%|4.21|约 12.6|
|GPT-5.4-mini|24.5%|4.42|约 18.0|

将未通过任务消耗的调用一并计入后，不同模型每个预算内吸收样本对应的调用次数最高相差约五倍。这说明 retry 的实际调用成本不仅取决于最大预算，也取决于模型能否在前几轮进入通过状态。

与 §5.5.1 的 thinking mode 相比，verifier-retry 通过多次非思考调用和外部符号反馈形成另一种成本—效果位置；thinking mode 则通过单次高预算推理获得更高的路径正确率。由于两类实验部分采用不同底座模型，该比较仅用于描述经验 trade-off，而不解释为严格的处理效应。

> **Finding X.** Verifier-retry 相对于单次非思考调用提高了有限预算内获得合法路径的概率，但其最终效果和后续救回效率均随图深度增加而下降。多数设置的逐轮通过概率随轮次衰减，且 18 个模型—深度—运行设置中仅 GPT-5.4-mini 的 12-hop 三个 run 满足预设的时齐性标准。与此同时，非法轨迹在更深分叉图上表现出更高的跨轮持续性和更低的下一轮通过概率。上述结果与剩余风险集逐渐富集于困难任务的解释一致，并表明增加外部重试预算能够缓解部分失败，但不能稳定补足底座模型缺失的图状态追踪能力。













与 thinking 不同，verifier-retry 不依赖单次高预算的内部推理，而是在模型生成路径后进行外部符号检查。该实验同样在 $\texttt{branching-12hop} + \texttt{jsoncot\_strict}$ 上运行，最大重试次数为 $K=5$。Table X 显示 pass@K 曲线，DeepSeek-V4-Flash 从 pass@1 的 48.6% 提升到 pass@5 的 70.6%，累计延迟为 4.42 秒；Qwen Max 从 pass@1 的 8.4% 提升到 pass@5 的 33.4%，累计延迟为 12.12 秒。

Table X.Verifier-retry results on branching-12hop + `jsoncot_strict`. pass@K is reported as a percentage, and Latency@5 denotes the average cumulative latency at retry budget (K=5). Increasing the retry budget improves pass@K for both models, but the gains saturate and depend strongly on the base model: DeepSeek-V4-Flash reaches 70.6% pass@5 with 4.42s latency, whereas Qwen Max reaches only 33.4% despite a higher 12.12s latency.

| Model                     |   pass@1 |   pass@2 |   pass@3 |   pass@4 |   pass@5 | Latency@5 |
| ------------------------- | -------: | -------: | -------: | -------: | -------: | --------: |
| DeepSeek-V4-Flash + retry | $48.6\%$ | $59.0\%$ | $64.4\%$ | $68.6\%$ | $70.6\%$ |    4.42 s |
| Qwen Max + retry          |  $8.4\%$ | $17.2\%$ | $24.1\%$ | $29.8\%$ | $33.4\%$ |   12.12 s |
结果表明外部 verifier-retry 可以提高路径合法通过率，收益受模型能力限制。DeepSeek-V4-Flash 的 pass@1 已接近 50%，说明模型在首次尝试时就已经能生成相当比例的合法路径，增加重试预算后，pass@K 进一步上升，表明外部结构反馈和重新生成可以提高通过率；相比之下，Qwen Max 的 pass@1 只有 8.4%，即使经过 5 次重试也只达到 33.4%，说明 verifier-retry 提高的是多次尝试下的通过率，而不是直接提升 LLM 单次成功生成合法路径的能力，当模型本身初始通过率就较低时，单纯增加重试次数存在明显的收益上限。这也是 pass@K 曲线呈现边际收益递减（DeepSeek-V4-Flash 从 $K=1$ 到 $K=2$ 提升 10.4 个百分点，但从 $K=4$ 到 $K=5$ 只提升 2.0 个百分点；Qwen Max 也呈现类似饱和趋势）的重要解释之一。通过分析 FailureHop 分布可知，verifier 可以发现错误，也可以提醒 LLM 错误在哪里，但后续修复仍依赖模型自身的图状态追踪能力，前几次 retry 主要处理“可修正或随机失误”类的样本，剩下的失败样本往往卡在同一 hop 或同类非法边，说明特定分叉位置存在稳定的状态追踪瓶颈，因此继续增加 K 新增通过数会越来越少。

总体来看，thinking 和 verifier-retry 代表两种不同的缓解路线。Thinking 是内部缓解，通过增加模型自身推理预算显著降低答案层和路径层失败，但时延成本高；Verifier-retry 是外部缓解，通过符号验证和重试缓解非法路径问题，成本更低，也更模块化，但其上限由底座模型的图跟随能力决定。需要注意的是，在本文的唯一 $k$-hop 合法路径设定下，verifier pass 与 PathGoldExact 在结果上等价：任何从起点出发、长度正确、逐边合法且 answer 等于路径末节点的输出，都必须对应唯一黄金路径 $p^*$。不过，verifier 本身并不使用 gold path ，它只检查输出路径的结构合法性。思考模式与 verifier retry 二者不存在完全替代，DeepSeek-V4-Pro thinking 以 46.7 秒达到 99.9% 的 PathGoldExact，而 DeepSeek-V4-Flash verifier@5 以 4.42 秒达到 70.6% 的路径通过率。从效果维度比较，都使用”最终路径正确率“标准，思考模式单次的 $\texttt{PathGoldExact} ≈ 99.9\%$，而 verifier pass@5 的 PathGoldExact 仅为 71%（DS-Flash）和 34%（Qwen），效果上 thinking 完胜，这是思考模式作为高性能参考可以预见的结果；从成本维度比较，都使用"平均延迟"标准，thinking 的单次推理约耗时约为 46.7s，而 verifier@5 累计耗时约 4.5s（Flash，5次重试的累计），其中通过 verifier-retry 提供结构错误提醒，并给予模型多次重新生成的机会，加上 DeepSeek-V4-Flash 本身生成速度较快，所以重复尝试的总时延成本相对较低，这正是 verifier-retry 的主要优势——以较低的外部检查和重试成本，提高多次尝试下获得合法路径的概率；从同时看效果和成本的性价比维度比较，thinking 用 46.7s，verifier 用 1/10 的时间达到了 thinking 约 71% 的效果（0.71/0.999≈71%），因此一个合理的定位是 verifier 并非是思考模式的上位或等价替代，而是用极低成本逼近 thinking 的大部分效果。

**Finding 4.** 面对 branching graphs 上的轨迹层虚假忠实性，thinking 和 verifier-retry 提供了两类互补缓解机制：thinking 更强但更慢，verifier-retry 更便宜但只能部分缓解非法路径。Verifier-retry 可以在较低延迟下部分缓解非法路径，但其可达到的上限仍由底座模型自身的图跟随能力决定。（Against trace-level spurious faithfulness on branching graphs, thinking and verifier-retry offer complementary mitigation strategies. Thinking substantially improves path correctness but incurs high latency, while verifier-retry provides a cheaper repair mechanism that only partially corrects illegal traces.Verifier-retry partially repairs illegal paths at lower latency, but its ceiling is determined by the base model’s graph-following ability.）
## 5.5 结果汇总（Summary of Results）

第 5.2 至 5.4 节报告了本文的主要代表性结果。本节不再引入新的消融实验，而是从提示形式（prompt interface）、拓扑与路径长度（topology-depth）、推理预算（reasoning budget）和 verifier 预算（verifier budget）四个角度总结前述结果及附录中的补充统计。

| 扫描维度（Scan axis）    | 主要结论（Main takeaway）                                                                                                                                                                                                                                                     | 完整结果（Full results） |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| Prompt interface   | `direct_minimal` 最容易暴露答案层位置捷径；`jsoncot_basic` 和 `jsoncot_strict` 能显著缓解 chain graphs 上的 shortcut，但不能保证 branching graphs 上的路径合法性。`jsoncot_basic` 与 `jsoncot_strict` 的对比进一步表明，更强格式约束并不自动等价于更强图合法性，prompt interface 本身是控制图忠实性的关键变量。                                         | Appendix A.1       |
| Topology and depth | Chain depth scan 显示答案层失败具有模型差异：DeepSeek-V4-Flash 在 chain 3–6 hop 上持续保持较高 Raw GIS / GFI，而 Qwen Max 随 hop 增长从较高 Raw GIS 转为 near-zero sensitivity collapse。Branching depth comparison 显示路径层非法性随分叉深度放大：branching-4hop 已出现非零 illegal-trace gap，branching-12hop 则将该失败放大为主导机制。 | Appendix A.2       |
| Reasoning budget   | DeepSeek-V4-Pro thinking 在 branching-12hop 上几乎消除答案层和路径层失败，但平均延迟显著高于 no-thinking。这表明困难任务本身可解，但需要更高内部推理预算。                                                                                                                                                                | Appendix A.3       |
| Verifier budget    | pass@K 从 K=1 到 K=5 持续上升但边际收益递减；DeepSeek-V4-Flash 的提升和上限明显高于 Qwen Max，说明 verifier-retry 放大已有图跟随能力，而不是凭空创造图推理能力。                                                                                                                                                          | Appendix A.4       |

这些结果共同支持本文的机制解释：弱提示链式图主要暴露答案层位置虚胖，分叉图结构化输出主要暴露路径层自洽但非法，thinking 与 verifier-retry 分别从内部推理预算和外部结构反馈两个方向缓解失败，但成本和上限不同。

---
# 6 Analysis



## 详细错误状态放到 §6

这些类别：

- `LegalDeadStop`
- `EarlyDeadIllegalContinue`
- `LastBranchDeadIllegalContinue`
- `ImmediateIllegalTransition`
- `GoldSuffixShortcut`
- `TerminalIllegalBridge`

不是基础验证器错误，而是利用图结构和轨迹前缀进行的**二级机制分析**。应放到：

```
6.X Error-State Decomposition of Branching Failures
```

在那里先给操作定义，再报告占比。

`TerminalIllegalBridge` 最好暂时改成更中性的：

> `Terminal-Directed Illegal Jump`

中文：

> 指向终点的非法跳转

其操作定义可以是：

\[ (\hat v_{i-1},\hat v_i)\notin E \quad\land\quad \hat v_i\in\{y,\text{terminal-region nodes}\}. \]

它只描述非法边的方向，不声称由位置锚定造成。随后再结合 E3/E4 讨论位置干预是否提高或降低该类别，才能使用“位置锚定诱发”的因果解释。

所以最终边界应是：

> §3 定义指标；§4.4 定义通用验证与统计；§6 才定义并分析 Branching 的详细错误状态。

这样术语不会提前出现，也不会让验证器错误和机制解释混为一谈。
















第 5 节已经报告了主要实验结果：弱提示下 chain graphs 会暴露答案层位置捷径，使用结构化输出缓解可以，但又会在 branching graphs 上揭露路径层的非法轨迹，对此尝试了两种缓解方法，思考模式能力强但时延成本高，verifier-retry 可以提高 pass@K 但收益随 K 增加递减。本节继承黑箱诊断角度主要从三个方面进一步分析失败模式间的关系：第一，分析 TAC 与 $\Delta_{\text{illegal}}$ 的分布，刻画“自洽但非法”的轨迹缺口在哪些拓扑、路径长度和模型设置下最严重；第二，在 verifier-retry 框架下，pass@K 的收益结构如何与底座模型的失败结构相对应？具体而言，pass@K 相对独立重试零基线的偏离，是否与 FailureHop 在 hop 维度上的集中度一致；第三，随着 retry 次数增加，剩余失败样本在 hop 维度上的集中程度如何变化？repeated same-hop failure 是否揭示了 retry 饱和背后的数据层结构？总体而言，§6.1 分析 TAC 与 PathValid 的脱节，§6.2 使用独立重试零基线诊断 pass@K 饱和，§6.3 进一步用 FailureHop 与 repeated same-hop failure 定位剩余失败的集中性。
## 6.1 自洽但非法的轨迹缺口在哪些拓扑、路径长度和模型设置下最严重

图 X 展示了 TAC 与 $\Delta_{\text{illegal}}$ 的关系，其中虚线表示 $\Delta_{\text{illegal}}=\mathrm{TAC}$ 的上界，即所有自洽轨迹都为非法轨迹的极端情况。![[Pasted image 20260613135723.png]]分叉图结构下大多集中在高 TAC 区域，但 $\Delta_{\text{illegal}}$ 差异很大，部分模型在 TAC 接近 1 的同时具有较高的 $\Delta_{\text{illegal}}$，说明能稳定生成与答案一致的路径末节点但不能保证中间转移遵循输入图边；相比下链式图结构主要落在 $\Delta_{\text{illegal}}\approx 0$ 附近，说明结构化轨迹自洽通常也对应图上合法。该分布表明“自洽但非法”的轨迹缺口并不是所有结构化输出都会出现，主要集中在分叉图，尤其在更长路径和 no-thinking 设置下最严重。
## 6.2 pass@K 的收益结构如何与底座模型的失败结构对应

### 6.2.1 引入独立基线

§5.4 表明，verifier-retry 可以提高 pass@K，但提升幅度会随着 K 增加而迅速变小。本节进一步解释这一饱和现象。核心原因是：verifier-retry 提高的是多次尝试中至少一次通过符号检查的概率，而不是直接提升模型单次生成路径的能力。因此，当剩余失败样本具有稳定结构瓶颈时，继续增加 retry 预算会反复遇到相似错误，新增通过样本逐渐减少。为了区分“多次尝试带来的自然提升”和“结构性瓶颈导致的饱和”，引入一个简单的独立重试基线。若模型每次尝试都以固定概率 $p$ 生成通过 verifier 的路径，并且不同尝试近似独立，则最多尝试 $K$ 次的通过率应满足 $\mathrm{pass@}K = 1-(1-p)^K$，其中 $p$ 可由 pass@1 估计。需要注意的是，这个公式不是模型真实推理过程的理论假设，而是一个无效模型（null model），如果实测 pass@K 接近该曲线，说明 retry 更接近低成功率下的重复采样；如果实测 pass@K 明显低于该曲线，则说明剩余失败并不能通过独立重试自然解决，可能存在任务难度异质性或稳定结构瓶颈。表中 DS-Flash 的偏离随 K 单调扩大（K=2 时为 −14.5%，到 K=5 时为 −25.8%），说明随着重试次数增加，新加入的样本越来越受困于结构相关失败。

| 模型（Model）         | 重试预算 K | 实际 pass@K（Observed pass@K） | 独立重试零基线（Independent-retry null baseline） | 差值（Difference） |
| ----------------- | -----: | -------------------------: | ---------------------------------------: | -------------: |
| DeepSeek-V4-Flash |      1 |                   $48.6\%$ |                                 $48.6\%$ |        $0.0\%$ |
| DeepSeek-V4-Flash |      2 |                   $59.0\%$ |                                 $73.5\%$ |      $-14.5\%$ |
| DeepSeek-V4-Flash |      3 |                   $64.4\%$ |                                 $86.4\%$ |      $-22.0\%$ |
| DeepSeek-V4-Flash |      4 |                   $68.6\%$ |                                 $93.0\%$ |      $-24.4\%$ |
| DeepSeek-V4-Flash |      5 |                   $70.6\%$ |                                 $96.4\%$ |      $-25.8\%$ |
| Qwen Max          |      1 |                    $8.4\%$ |                                  $8.4\%$ |        $0.0\%$ |
| Qwen Max          |      2 |                   $17.2\%$ |                                 $16.1\%$ |       $+1.1\%$ |
| Qwen Max          |      3 |                   $24.1\%$ |                                 $23.2\%$ |       $+0.9\%$ |
| Qwen Max          |      4 |                   $29.8\%$ |                                 $29.6\%$ |       $+0.1\%$ |
| Qwen Max          |      5 |                   $33.4\%$ |                                 $35.5\%$ |       $-2.1\%$ |

**Table X.** Observed verifier-retry pass@K versus an independent-retry null baseline for DeepSeek-V4-Flash and Qwen Max. The null baseline assumes that each retry is an independent attempt with the same success probability as pass@1. DeepSeek-V4-Flash falls far below this baseline as K increases, indicating that retry failures are highly correlated and many hard cases remain hard across attempts. In contrast, Qwen Max closely follows the independent-retry baseline, suggesting that its retry gains are mostly consistent with simple repeated sampling rather than systematic repair.

### 6.2.2 两模型在诊断平面上的位置对比

两个模型呈现出明显不同的模式。Qwen Max 的 pass@K 基本贴合独立重试基线。以 pass@1 = 8.4% 估计，独立基线预测 pass@5 约为 35.5%，实测 pass@5 为 33.4%，两者接近，说明 Qwen Max 的 verifier-retry 提升主要可由低单次通过率下的多次尝试解释，模型单次生成合法路径的概率很低，但多试几次后，通过率会按照重复采样的方式缓慢上升。DeepSeek-V4-Flash 则明显低于独立重试基线。以 pass@1 = 48.6% 估计，独立基线预测 pass@5 约为 96.4%，但实测 pass@5 只有 70.6%，低约 25.8 个百分点。这说明 DeepSeek-V4-Flash 的剩余失败并不像独立重采样那样容易被 retry 消除。换言之，虽然它在首次尝试中已经能生成相当比例的合法路径，但那些首次失败的样本中，有相当一部分会在后续尝试中继续失败。

| 模型（Model）         | 一次尝试通过率（pass@1） | 实际 pass@5（observed pass@5） | 独立重试基线@5（independent baseline at K=5） |  差值（gap） |
| ----------------- | --------------: | -------------------------: | ------------------------------------: | -------: |
| DeepSeek-V4-Flash |        $48.6\%$ |                   $70.6\%$ |                              $96.4\%$ | -25.8 pp |
| Qwen Max          |         $8.4\%$ |                   $33.4\%$ |                              $35.5\%$ |  -2.1 pp |
这一差异说明 pass@K 的饱和并不能只用“尝试次数不够”解释。对于 Qwen Max，主要限制是单次通过率很低，因此 pass@K 随 K 缓慢上升；对于 DeepSeek-V4-Flash，主要限制则是剩余失败样本的相关性更强，继续 retry 往往不能产生独立的新机会。结合 FailureHop 与 repeated_same_hop 统计可以看到部分失败样本会反复卡在相同 hop 或相同错误类型上。这表明 verifier 能指出结构错误，也能提高多次尝试下获得合法路径的概率，但当模型反复在同一状态转移处失败时，外部反馈无法保证继续增加 K 就能线性提高通过率。

为进一步刻画 verifier-retry 的收益结构，将两个互补判据放在同一二维平面上。图 X 左侧首先比较了实测 pass@K 曲线与独立重试基线，独立重试零基线假设每次尝试都以 pass@1 估计出的固定概率 $p$ 独立通过 verifier，最多尝试 $K$ 次的预期通过率为 $1-(1-p)^K$，如果实测曲线贴近该基线则说明增加 retry 更接近于重复获得新的低概率机会；如果实测曲线明显低于该基线则说明后续 retry 并没有提供等价的独立新机会。图 X 右侧比较了 pass@K 相对独立重试零基线的偏离以及 FailureHop 在 hop 维度上的归一化熵。右图的横轴为平均独立基线缺口 $\bar{d}=\frac{1}{K_{\max}-1}\sum_{K=2}^{K_{\max}}\left(1-(1-p)^K-\mathrm{pass@}K\right)$，其中 $p$ 由 pass@1 估计，$\bar{d}$ 越大表示实测 pass@K 越低于独立重试基线，后续 retry 收益越不像独立重采样，剩余失败更可能受到相关错误或任务难度异质性的限制；纵轴为 FailureHop 的归一化熵，用于衡量失败跳数在路径中的分散程度，$H_{\mathrm{norm}}$ 越接近 1 说明失败越接近均匀分布；越接近 0，说明失败越集中在少数 hop。![[Pasted image 20260613153101.png]]图 X 中两个模型落在明显不同的位置，其中 Qwen Max 的实测 pass@K 与独立基线几乎重合，平均独立基线缺口接近 0，FailureHop 归一化熵为 0.927，最高单跳失败占比仅为 0.155。这一诊断解释了 verifier-retry 的饱和来源，Qwen 的 retry 收益形状接近独立重试基线，即低成功率下的重复采样，尽管单次通过率较低但每次 retry 仍提供一次重新尝试的低概率机会，从失败也可看出，较分散地出现在不同 hop（见 6.3 FailureHop）；相比下 DeepSeek-V4-Flash + retry 的实测 pass@K 从 K=2 开始明显低于独立基线，并且差距随 K 增大而扩大，说明饱和更多来自剩余失败的相关性或难度异质性，即首次失败后留下的样本不容易通过后续 retry 自然解决，后续剩余样本集中在更难修正的状态转移处，这些失败更可能反复暴露相似的状态转移错误（见 6.3 FailureHop）。从 DS-Flash 的平均独立基线缺口约为 0.217、FailureHop 归一化熵为 0.803、最高单跳失败占比为 0.370 且主要集中在 hop 2 也可看出剩余失败更集中，更偏离独立重试假设。![[Pasted image 20260613150247.png]]总而言之，上述协同分析表明 verifier-retry 的收益呈现出两层结构。第一层是 pass@K 随 retry 预算 $K$ 增加而上升。由于 pass@K 衡量的是模型在最多 $K$ 次尝试中至少一次生成通过 verifier 路径的概率，因此随着 $K$ 增加， pass@K 通常上升。这一点主要来自 pass@K 的定义本身。真正需要解释的是第二层：这种提升并不是线性的，而会随着 $K$ 增加呈现边际收益递减，可以从浅层和深层两个维度理解。表面上 pass@1 决定了模型单次尝试的基础通过率，也给出了独立重采样情形下 pass@K 应如何累计的参照。

图 X 中 Qwen Max 虽然 pass@1 较低，但其 pass@K 基本贴近独立重试零基线；尽管 Qwen 的绝对水平远低于 DS-Flash（33.4% vs 70.6%），但相对提升幅度$\Delta \approx 25$更大，说明后续 retry 更接近近似独立的低概率重采样，因此多次尝试仍能持续带来累计提升。且 Qwen 这种低 $p$ 下的独立基线下贴合独立基线带来的“更大相对提升”恰恰值得强调的。

### 6.2.3 反直觉观察:相对提升幅度差异 

首先需要进一步说明为什么独立重试零基线 $1-(1-p)^K$ 可以看作一种理想化参照。本文中 LLM 需要处理受控定长多跳图推理任务 $G=(V,E)$，每处理一个基础样本可以看作 LLM 抽样回答一次问题，则每次 retry 可以看作 LLM 重新抽样回答问题。然而，与人类不同的是，人类处理错误时会积累经验，在 retry 时更新策略；而通过 API 调用的 LLM 无法自发更新参数，且上下文记忆仅限于本轮对话反馈的信息，导致模型的图追踪能力不会随 retry 自发增强，每次 LLM 在参数层面与第一次抽样回答几乎完全等价（LLM 重试在参数层面不发生学习或更新；不同尝试之间的差异主要来自 verifier feedback 改变了上下文，以及 temperature=0.3 带来的采样随机性）。这使得”独立重采样“可以作为一个合理的理想化乐观参照，若所有样本难度相同且共享同一个单次通过率 $p$、每次抽样独立，则 pass@K 应满足 $1-(1-p)^K$。然而，真实任务的样本难度通常本身并不均匀，且 verifier 的提示可能使同一样本难度在时间上具有异质性，因此不同样本可能具有不同的单次通过率 $p_i$，本文采用的思路是使用整体 pass@1 估计出的统一 $p$ 计算 $1-(1-p)^K$。若实测 pass@K 明显低于该基线，则说明失败并非均匀随机分布，而可能是由于样本难度异质性或稳定结构瓶颈。需要注意的是，所有样本共享同一个成功概率 $p$ 这个关键假设在实际数据中可能会导致真实累计通过率低于用 $p$ 计算出的独立重试零基线。若样本难度均匀，用整体 pass@1 估计出的平均 $p$ 自然可以较好预测 pass@K；但若样本难度高度异质，例如一部分样本很容易、另一部分样本对模型而言几乎总是失败，那么真实 pass@K 更接近 $\frac{1}{N}\sum_i \left(1-(1-p_i)^K\right)$，而不是 $1-\left(1-\frac{1}{N}\sum_i p_i\right)^K$。这也把 pass@K 相对独立基线的偏离转化为对样本难度结构的诊断，若 pass@K 贴合基线（比如 Qwen），说明在该模型视角下样本难度近似均匀——大多数样本都处在“低概率但有可能‘的同一区间。这种情形下，错题之间没有强结构相关，通过重复尝试可以解决偶发失误、容易出错但可恢复的问题。此时 retry 类似在同一枚不公平硬币上反复抛掷，每次都有相同的小概率通过，但并不意味着 retry 在修复错误，而是意味着”样本对该模型而言基本均匀地难，多抛几次自然有几次走运通过“；若 pass@K 显著低于基线（DS-Flash），则说明该模型视角下样本难度强烈异质——一旦某些样本本身超过了模型当前的状态追踪能力，例如简单样本被首次抽样吸收完，那么剩余样本对该模型的当前能力构成稳定瓶颈，每次重试对这些样本仍是低概率，且这个低概率不会因为”看了反馈“而提升，因为 LLM 无法跨轮积累经验，模型并不会因为重复尝试就自然掌握正确转移。需要注意的是，由于 $1-(1-p)^K$ 关于 $p$ 是凹函数，根据 Jensen 不等式,若样本难度异质（$p_i$ 服从某个分布而非常数），则真实累计通过率 $\frac{1}{N}\sum_i\big(1-(1-p_i)^K\big) \leq 1 - \big(1-\tfrac{1}{N}\textstyle\sum_i p_i\big)^K = 1-(1-\bar{p})^K$，即任何样本难度异质性都会导致实测 pass@K 低于用平均 $p$ 估算的独立基线，这意味着实测 pass@K 低于独立基线既可能反映参数层面的 retry 不独立，也可能反映样本层面的成功概率分布并不均匀，剩余失败中存在一批稳定 hard cases，二者在数据层面不可严格区分。诊断含义是两种来源的合并表达。

对于低 $p$ 的数学性质，$p$ 小的时候 $K$ 从 1 到 5 的相对增长率确实更大（$0.084 \to 0.355$ 是 4.2 倍而 $0.486 \to 0.964$ 是 2 倍)。

| 模型       | 实测相对提升 | 独立基线预测 | 相对偏离  |
| -------- | ------ | ------ | ----- |
| Qwen Max | 2.98 倍 | 3.23 倍 | −7.7% |
| DS-Flash | 0.45 倍 | 0.98 倍 | −54%  |
Qwen 的 $pass@1 \to pass@5$ 相对提升幅度（2.98 倍）远大于 DS-Flash（0.45 倍），差值 2.527 在 paired bootstrap 检验下 95% CI [2.518, 2.536] ($p\approx 0$），这一观察的大部分确实可由低 $p$ 的数学性质解释。但去除这一数学性质后 Qwen 实测 2.98 而独立基线预测 3.23 ，偏离仅 7.7%（几乎完全贴合），retry 行为和独立重采样几乎不可区分；DS-Flash 实测 0.45 而独立基线预测 0.98 ，偏离 54%（实测只达到独立预测的一半），retry 明显低于独立重采样能给的提升。这一观察并非数据噪声，恰恰是两类失败结构的直接印证。两个模型相对各自独立预测的偏离方向一致（均为负），但偏离幅度差异巨大（Qwen 偏离 7.7% vs DS-Flash 偏离 54%）。这才是失败结构差异的关键证据——Qwen 的剩余失败在重试间近独立、retry 类似低 $p$ 下的多次重抽样；DS-Flash 的剩余失败在重试间强相关、retry 反复触发同一结构瓶颈，大量样本在多次尝试中持续失败。

![[Pasted image 20260613211318.png]]
图注：Qwen 的相对 retry 提升幅度显著高于 DS-Flash，run-level bootstrap CI 不跨 0；但由于Qwen 只有 2 个 paired runs，DS-Flash 是 3 runs，这个结果更适合作为描述性证据，而不是强统计结论。

### 6.2.4 机制解释:LLM 无状态调用 + 样本难度异质性

其次分析影响 DS-Flash 大量样本在多次尝试中持续失败的因素。上文已经说明 LLM 重试在参数层面不发生更新，那么一个合理的解释就应该来自样本层面——任务难度有异质性，对简单问题样本每次抽样都是简单的，每次 retry 都比较容易过，$p_i$ 很高；困难问题样本对每次抽样都是困难的，$p_i$ 很低甚至接近 0，对于公式里假设的统一 $p = 0.486$ 计算得 $pass@5 = 1-(1-0.486)^5 ≈ 96.4\%$，但正如前文所述，隐含假设是“每个样本每次都有 48.6% 的机会通过”，而 DS-Flash 的真实情况更接近一部分样本几乎必过，另一部分反复不过“（类似混合分布），这也解释了 DeepSeek-V4-Flash 为什么会明显低于独立重试基线。DS-Flash 的 pass@1 较高，说明它在首次尝试中已经解决了大量容易样本；但首次失败后留下的样本并不是同等难度的随机失败，而更可能集中在对该模型而言稳定困难的状态转移上。换言之，容易样本被较早吸收，剩余样本的 $p_i$ 很低，继续 retry 对这些样本仍然像重复面对同一个难题，因此 pass@K 会提前饱和。下一节的 FailureHop 分析将进一步检验这一点：如果这些 hard cases 主要集中在 hop 2 等分叉状态更新位置，那么 DS-Flash 低于独立基线就可以解释为样本难度异质性与结构瓶颈共同作用的结果。

同时，这也给出了一种 Qwen 贴合基线的解释，对于 Qwen 不是"没有难样本"，而是说明在 Qwen 的能力视角下，大多数样本都处于相近的低成功率区间（$p$ 仅为 8.4%，几乎所有样本都接近“不会”），由于样本间成功概率差异小，没有像 DS-Flash 那样形成明显的“易样本/难样本”分层，所以混合分布退化成一个统一的低 $p$ 分布（或类似均匀分布），每次 retry 都像在同一枚低成功率硬币上再次采样，多试几次自然会带来累计提升，反而和独立基线吻合，但这并不表示 verifier-retry 在系统性修复结构错误。
### 6.2.5 收尾

这也直接回答了为什么 verifier-retry 即便给定足够大的 $K$ 也无法替代思考模式，thinking 通过单次推理内部的多步搜索绕过状态追踪瓶颈，而 verifier-retry 只能给模型”再试一次“的机会，无法在参数层面给模型补充新的能力。这是两类缓解机制本质上的边界。下一节将进一步用 FailureHop 和 repeated same-hop failure 检验这一推测，分析这些剩余失败是否集中在少数状态转移位置上。
## 6.3 FailureHop 区分稳定瓶颈与低概率重采样

§6.2 表明，DeepSeek-V4-Flash 的 pass@K 明显低于独立重试零基线，而 Qwen Max 更接近该基线。本节进一步分析剩余失败样本的 FailureHop 分布，检验这种 pass@K 差异是否对应路径位置上的不同失败结构。图 X 进一步展开了每个 retry attempt 下逐 hop 的 FailureHop 分布。该图显示，剩余失败并不是均匀分布在整条路径上，而呈现出明显的“首尾较硬、中段较软”结构。对于 DeepSeek-V4-Flash，第 2 跳形成稳定主峰，失败占比在五次 attempt 中始终维持在 0.33–0.39 之间，几乎不被 retry 削弱；第 3 跳在 attempt 1 时也较高，但随后从 0.21 下降到约 0.11，说明该位置的一部分失败可以被 retry 修正；第 12 跳则从 0.12 上升到 0.17–0.18，形成稳定的终端转移小峰。中间 hop 的失败占比整体较低，说明 DS-Flash 的剩余失败并不是随机散布，而是集中在少数关键状态转移位置。
![[Pasted image 20260614004919.png]]
这一分布与 branching-12hop 的任务结构相对应。第 2 跳附近通常对应早期状态更新与分叉选择，模型需要在路径刚开始时维护当前节点并选择合法后继；终端 hop 则对应最后一次状态转移，模型不仅需要沿合法边移动，还必须在恰好 $k$ 跳处到达终点。需要强调的是，终端 hop 的失败不是 answer 总结错误，而是路径中最后一条边本身非法。因此，hop 12 的小峰应理解为终端转移位置上的 FailureHop，而不是输出总结阶段的错误。

相比之下，Qwen Max 的 FailureHop 分布更分散。它同样在早期 hop 有热点，但最高单 hop 占比从 attempt 1 的 0.27 下降到 attempt 5 的 0.16，第 2–4 跳的早期热区随 retry 被持续削平；与此同时，第 12 跳的占比从 0.04 上升到 0.14，中间 hop 的占比也更加均匀。这里的上升是剩余失败样本中的相对占比变化，并不意味着 retry 使终端错误的绝对数量增加。更合理的解释是，retry 优先削减了一部分可恢复失败，使剩余失败在相对比例上向更多 hop 分散。![[Pasted image 20260614165443.png]]

图 Y 进一步按错误类型拆解每个 FailureHop 上的失败来源。结果显示，剩余失败几乎全部来自“状态转移 / 图边跟踪失败”，而路径长度错误、起点错误和答案对齐错误占比很低。这说明 verifier-retry 后仍然存在的主要问题，不是模型无法生成格式正确的结构化输出，也不是 answer 与 path 末节点不一致，而是模型在生成路径时选择了输入图中不存在的边。

这一点也澄清了终端 hop 的含义。Hop 12 的失败不应解释为答案总结失败；如果模型主要在最终答案总结时出错，应表现为 answer 与 path 末节点不一致，即 TAC 下降。但图 Y 显示，hop 12 的主要错误类型仍然是状态转移 / 图边跟踪失败，说明错误发生在路径中的最后一次转移 $v_{11}\rightarrow v_{12}$，而不是 answer 字段的总结或对齐。因此，本文将 hop 12 的峰值描述为终端转移失败，而不是输出总结失败。

综合来看，图 X 和图 Y 共同为 §6.2 的 pass@K 诊断提供了路径层证据。DeepSeek-V4-Flash 低于独立重试零基线，对应的是第 2 跳主峰长期稳定、难以被 retry 削弱，且错误类型主要是状态转移 / 图边跟踪失败；Qwen Max 贴近独立重试零基线，对应的是早期热点逐渐被削平、FailureHop 分布更分散。换言之，DS-Flash 更像“容易样本被前几次 retry 吸收后，剩余失败反复卡在少数状态转移位置”；Qwen 则更像“整体成功率低，但失败位置更分散，多次采样仍能持续捞回一部分样本”。
## 6.4 Summary

本节解释了第 5 节结果背后的机制。第一，结构化输出能提高格式服从和 trace-answer consistency，但不能保证路径逐边合法。模型可以生成答案与路径末节点一致的轨迹，却仍然违反输入图的边约束；第二，verifier-retry 的提升会饱和，因为验证器只能发现错误，不能替代模型修复错误所需的状态更新能力。修复非法边本质上仍要求模型完成 $\hat{v}_{i+1}\in N^+(\hat{v}_i)$ 而这正是失败模型所缺乏的能力；第三，FailureHop 和 repeated same-hop failure 表明剩余错误是结构性的，而不是随机噪声。大量最终失败会反复卡在同一个 hop，说明模型在收到结构性反馈后仍难以越过同一状态转移瓶颈。因此，图推理评测不应只检查最终答案，也不应只检查推理轨迹是否与答案一致。对于路径类图任务，真正关键的是验证模型生成的每一步状态转移是否忠实于输入图结构。

---
# 7 Conclusion

本文提出一组面向序列化图推理的黑箱忠实性诊断方法，其核心由两个互补指标构成：Graph-Following Inflation（GFI）与 Illegal Trace Gap（ITG，即 TAC − PathValid）。其中，GFI 衡量模型在未控制位置时表现出的图跟随能力是否被 endpoint-position shortcut 虚高；ITG 则衡量模型生成的结构化轨迹是否虽然与答案自洽，却包含输入图中不存在的非法转移。二者分别对应答案层与路径层的两类假忠实性：前者揭示“答案随图改变不等于忠实图跟随”，后者揭示“轨迹自洽不等于路径合法”。

实验结果表明，仅依赖最终答案准确率或轨迹一致性都会系统性高估模型的图跟随能力：答案层 Raw GIS 可能被位置捷径虚胖，需要由 PC-GIS 与 GFI 揭示；路径层高 TAC 可能掩盖大量非法边，需要由 PathValid 与 $\Delta_{\text{illegal}}$) 揭示。Thinking 与 verifier-retry 等缓解机制的效用边界，也只有在同时观察 pass@K、路径合法性和失败位置结构时才能被精细刻画。这些发现对图推理评测提出了直接启示。我们建议未来涉及路径生成、图导航或知识图谱推理的评测，在最终答案准确率之外，至少引入三项诊断：第一，位置控制后的答案稳定性，即通过反事实图对与多位置序列化检验模型答案是否真的受图结构控制；第二，路径逐边合法性，即使用符号 verifier 检查模型生成轨迹是否由输入图中的真实边组成，而不是只检查路径与答案是否自洽；第三，失败位置与错误类型分析，即通过 FailureHop 和错误类型分布刻画模型究竟在哪些状态转移处失败。否则，模型可能在答案层呈现伪图敏感性，或在路径层生成自洽但非法的解释，从而造成对图结构跟随能力的系统性高估。

总体而言，本文的核心结论是：LLM 在图任务中“答案随图变”和“解释自洽”都不足以证明其真正沿图推理。GFI 与 ITG 分别揭示了答案层和路径层的两类假忠实性；只有结合反事实图干预、位置控制、路径合法性验证和失败模式分析，才能更可靠地区分真正的图结构跟随与表面忠实行为。


"本文总结主要发现如下：

(1) Chain + direct_minimal 暴露答案层位置捷径，多模型共有；

(2) 能力提升不自动消解位置依赖——在 chain + answer-only 设置下，DS-Pro 反而更稳定地利用 endpoint-last/decoy-last 位置线索；

(3) Chain + jsoncot_strict 让 GFI 归零，结构化路径输出能消除简单链式位置依赖；

(4) Branching + jsoncot_strict 揭示新的失败形态——形式自洽但图上非法的路径；这一形态由 FailureHop U 型 + 反向可达性富集共同支持，提示其机制是位置依赖在结构化输出下的'路径构造/反向合理化'，而非独立的新失败；

(5) Thinking 几乎完全解决任务但延迟高 19 倍；verifier-retry 便宜但其 pass@K 上限受底座模型状态追踪能力限制。

这些发现共同表明：**图忠实性评测不能依赖单一指标或单一提示设置**。GFI、Δillegal\Delta_{\text{illegal}} Δillegal​ 等核心诊断必须与 Raw GIS、PC-GIS、EAR、反向可达性等辅助证据联合解读，才能区分真实图忠实性与不同形态的位置依赖。"


---
# Limitations

1. E3 对每个图实例预先采样一个正文排列，并在六个终点边位置条件间固定该排列。该设计能够隔离给定随机序列化背景下终点边位置的配对效应，但未在同一图实例上重复采样多个正文排列。因此，本文不能完整估计位置效应对正文边排列的实例内敏感性；未来工作可通过多序列化重复，进一步检验位置效应与正文排列之间的交互。

2. 本文有若干限制。第一，本文使用合成符号图，而不是自然语言知识图谱或真实世界图结构。这一设计有助于削弱实体名称、常识关联和参数记忆带来的语义先验，使实验更集中地诊断模型是否依赖输入图结构；但它也意味着本文没有覆盖真实知识图谱中的实体语义、关系语义和世界知识先验，也没有系统比较边列表、邻接表或自然语言描述等不同序列化格式。语义先验与图结构约束的交互，以及不同图编码方式下 GFI 与 ITG 是否保持稳定，留作后续工作；第二，本文集中在 fixed-hop path following 任务，即从给定起点出发沿有向边恰好走 (k) 跳并输出终点。这一任务便于构造反事实图对、位置控制序列化和路径级符号验证，但并不覆盖所有图推理形式。可达性判断、最短路径、连通性判断、汇聚图、多合法路径、稠密图和 cycle 等拓扑可能引入不同失败模式。GFI 与 ITG 的核心思想可以扩展到这些设置，但反事实图构造、gold path 定义和 FailureHop 诊断需要相应调整；第三，本文采取黑箱行为诊断的方法论立场。我们通过反事实图干预、位置控制和路径级验证检验模型行为是否与图忠实性相一致，但不声称直接观察或证明模型内部形成了稳定的图状态表示。理想的图因果控制事件不可直接观测，本文指标只能提供可操作的行为层近似诊断；第四，verifier-retry 结果基于单一符号验证器和受控反馈设置。该验证器只返回结构性错误反馈，例如格式错误、路径长度错误、非法边所在 hop 或 trace-answer mismatch，不泄露 gold answer、gold path 或正确下一跳。反馈粒度、提示措辞、是否给出邻居集合、是否结合搜索式修复，以及 retry 是否引入跨轮记忆或参数更新，都可能改变 pass@K 曲线和独立重试零基线的适用性。因此，本文的 verifier-retry 结论应理解为 stateless API 调用和受控反馈条件下的诊断结果，而不是所有外部验证系统的最终上限；第五，本文的实验覆盖仍有限。主实验采用确定性解码，verifier-retry 使用少量独立 run，并主要覆盖 DeepSeek、Qwen 和 GPT-5.4-mini 等代表性模型配置；这些设置足以揭示位置捷径、自洽但非法路径、retry 饱和和 thinking 修复等主要趋势，但不足以完全刻画 run 间方差、temperature 敏感性和跨模型族稳定性。更多开源模型、闭源模型、工具增强模型和专门图推理模型仍需进一步评估。

---
# Appendix A. 完整消融与指标分母索引（Full Ablations and Metric Denominator Index）

本附录补充正文未展开的指标分母定义


完整消融结果、、错误状态归类与 verifier-retry 成本口径。正文 §3 定义核心指标的公式与诊断意义，§4.5 定义输出解析、符号验证和分母集合；本附录提供可核查的完整索引。

## A.1 指标分母与有效子集索引

## Metric Denominators and Effective Subsets

本文在显式定义的分母集合上报告所有指标。$N_{\mathrm{all}}$ 表示全部生成测试样本；$N_{\mathrm{returned}}$ 表示模型成功返回非空响应的样本；$N_{\mathrm{parseable}}$ 表示响应中可获得合法 JSON 对象的样本；$N_{\mathrm{parse_ok}}$ 表示同时通过 JSON 解析与对应 schema 检查的样本。对于路径层指标，$N_{\mathrm{parseable}}^{\mathrm{path}}$ 表示在要求路径输出的条件下同时具有可用 `path` 与 `answer` 字段的样本集合。

$N_{\mathrm{effective}}$ 表示某一指标在预先规定的解析条件和结构可用条件下可评价的样本集合，不包含基于结果正确性的事后筛选。$N_{\mathrm{paired}}$ 表示反事实图对中两侧均满足对应指标可评价条件的配对样本；$N_{\mathrm{paired}}^{\text{all-serial}}$ 表示反事实图对的两侧及四种序列化条件均满足对应可评价条件的配对样本。下表列出本文主要指标使用的分母与有效子集。

| 层级      | 指标                                             | 主报告分母                               | 定义见             | 正确计算口径                                                                  |
| ------- | ---------------------------------------------- | ----------------------------------- | --------------- | ----------------------------------------------------------------------- |
| 审计      | 请求是否成功返回（RequestSuccess）                       | $N_{\mathrm{task}}$                 | §4.4.4；§4.5     | API 正常返回率；timeout/异常单列                                                  |
| 审计      | 模型输出是否能被解析成要求的格式（ParseSuccess）                 | $N_{\mathrm{task}}$                 | §4.4.1；§4.5     | 输出通过 JSON/schema 解析的比例                                                  |
| 答案层     | Accuracy                                       | $N_{\mathrm{task}}$                 | §3.1；§4.4.1     | $\mathbb{1}[\hat y=y]$；解析失败计 0，不按正确性事后筛选                                |
| 答案层     | Raw GIS                                        | $N_{\mathrm{complete}}$ 个 sample_id | §3.2；§4.4.1     | endpoint-last 下 $G_1/G_2$ 均正确；当前代码只在八格完整样本上计算                           |
| 答案层     | PC-GIS                                         | 同一 $N_{\mathrm{complete}}$          | §3.2；§4.4.1     | $G_1/G_2$ × 四种序列化全部正确                                                   |
| 答案层     | GFI                                            | 同一 $N_{\mathrm{complete}}$          | §3.2；§4.4.1     | 逐样本计算 $RawGIS^{(j)}-PCGIS^{(j)}$，再平均                                    |
| 答案层     | EAR (decoy-last)                               | 完整样本中的两侧 decoy-last 调用              | §3.2；§4.4.1     | $\mathbb{1}[\hat y=d]$；只测末位诱饵复制                                         |
| 路径层     | PathPresent                                    | $N_{\mathrm{task}}$                 | §4.4.1          | 是否解析出非空路径；用于说明路径指标的可观测性                                                 |
| 路径层     | TAC-all                                        | $N_{\mathrm{task}}$                 | §3.3；§4.4.1     | $\mathbb{1}[\hat y=\hat v_m]$；无路径/解析失败计 0                               |
| 路径层     | PathValid-all                                  | $N_{\mathrm{task}}$                 | §3.3；§4.4.1     | 起点、$k+1$ 长度、逐边合法全部成立                                                    |
| 路径层     | PathGoldExact-all                              | $N_{\mathrm{task}}$                 | §3.3；§4.4.1     | $\mathbb{1}[\hat p=p^*]$；唯一合法路径下等于 PathValid                            |
| 路径层     | $\Delta_{\mathrm{illegal}}$-all                | $N_{\mathrm{task}}$                 | §3.3；§4.4.1     | 逐任务计算 $TAC(1-PathValid)$，再平均                                            |
| 路径层     | $\Delta_{\mathrm{illegal}}^{\mathrm{correct}}$ | $N_{\mathrm{task}}$                 | §3.3；§4.4.1     | $Accuracy \cdot TAC(1-PathValid)$                                       |
| 路径层     | $\Delta_{\mathrm{illegal}}^{\mathrm{wrong}}$   | $N_{\mathrm{task}}$                 | §3.3；§4.4.1     | $(1-Accuracy)TAC(1-PathValid)$                                          |
| 路径层敏感性  | TAC-given-path                                 | $N_{\mathrm{path}}$                 | 代码附加口径          | 仅用于敏感性分析，不能替代 TAC-all                                                   |
| 路径层敏感性  | PathValid-given-path                           | $N_{\mathrm{path}}$                 | 代码附加口径          | 排除无路径输出后的条件合法率                                                          |
| 路径层敏感性  | $\Delta_{\mathrm{illegal}}$-given-path         | $N_{\mathrm{path}}$                 | 代码附加口径          | 排除无路径输出后的条件非法率                                                          |
| 辅助      | ErrorType                                      | $N_{\mathrm{task}}$                 | §3.3；§4.4.1     | 互斥优先级：no-path、wrong-start、too-short/long、illegal-edge、TAC mismatch、none |
| 辅助      | FailureHop incidence                           | $N_{\mathrm{task}}$                 | §3.3；§4.4.1, §6 | 第一次非法边出现在 hop $i$ 的无条件发生率                                               |
| 辅助      | FailureHop distribution                        | 有明确非法边的任务                           | §6              | 条件于出现非法边后的 hop 构成；必须与 incidence 区分                                      |
| 机制      | ErrorSource                                    | $N_{\Delta}$                        | §6 新增           | TAC-invalid 轨迹的互斥来源分类；不同于验证器 ErrorType                                  |
| Retry   | pass@$K$                                       | 每个 run 的全部 retry 任务                 | §4.4.2；§5.4.2   | 前 $K$ 次至少一次通过 verifier；timeout 计失败                                      |
| Retry   | Latency@$K$                                    | 同一全部 retry 任务                       | §4.4.4；§5.4.2   | 成功则累计到首次通过；未通过则累计前 $K$ 次                                                |
| Retry   | AttemptsUsed@$K$                               | 同一全部 retry 任务                       | §4.4.2          | 截止首次通过或预算耗尽的实际尝试次数                                                      |
| Retry辅助 | Final ErrorType@$K$                            | $N_{\mathrm{fail}@K}$               | §4.4.2；§6.3     | 预算 $K$ 后仍失败者的最终错误构成                                                     |
| Retry辅助 | Final FailureHop@$K$                           | $N_{\mathrm{fail}@K}$ 中有非法边者        | §4.4.2；§6.3     | 剩余失败的首次非法边位置                                                            |
| Retry辅助 | repeated_same_error                            | 有多次可评价失败的最终失败任务                     | §4.4.2；§6.3     | 多次尝试是否反复出现同类错误                                                          |
| Retry辅助 | repeated_same_hop                              | 有多次非法边记录的最终失败任务                     | §4.4.2；§6.3     | 多次尝试是否反复卡在同一 hop                                                        |

注：
1. $\Delta_{\mathrm{illegal}}=\Delta_{\mathrm{illegal}}^{\mathrm{correct}}+\Delta_{\mathrm{illegal}}^{\mathrm{wrong}}$ 的公式定义见 §3.3；本表仅作为分母与有效子集索引
2. $N_{\mathrm{task}}$：某一实验设置下的全部任务数（timeout/解析失败记为失败）
3. $N_{\mathrm{complete}}$：G1/G2 × 四种序列化共 8 个结果齐全的基础 sample_id
4. $N_{\mathrm{path}}$：模型输出答案+输出可解析的非空路径的任务
5. $N_{\Delta}$：满足 $TAC=1 \land PathValid=0$ 的任务
6. $N_{\mathrm{fail}@K}$：retry 预算为预算 $K$ 且 $K$ 次后仍未通过 verifier 的任务

## A.2 错误状态与轨迹来源归类

## Error-State and Error-Source Assignment

本文区分验证器错误状态 ErrorType 与机制性轨迹来源 ErrorSource。ErrorType 描述输出在解析、schema 与符号验证管线中的失败阶段；ErrorSource 仅在 TAC 成立但 PathValid 失败的样本中进一步描述非法轨迹的可观测来源。二者不应合并。

### A.2.1 ErrorType 互斥归类

API 调用状态首先分为 `request_failure`、`timeout` 与 `returned`。仅对 `returned` 输出执行下列互斥 ErrorType 归类，按顺序自上而下匹配，匹配即止：

|优先级|ErrorType|定义|
|---|---|---|
|1|`json_parse_failure`|无法从返回文本中获得合法 JSON 对象|
|2|`schema_failure`|JSON 可解析，但未满足对应 prompt 接口的字段与类型要求|
|3|`wrong_start`|路径首节点不等于指定起点 $s$|
|4|`wrong_length`|路径节点数不等于 $k+1$|
|5|`illegal_edge_at_hop_i`|起点与长度正确，但第 $i$ 跳首次出现输入图中不存在的有向边|
|6|`trace_answer_mismatch`|路径每条边均合法，但预测答案不等于路径末节点|
|7|`valid`|路径满足起点、长度、逐边合法性与路径-答案一致性|

该优先级保证每条 returned 输出最多归入一个 ErrorType；JSON 解析失败与 schema 失败不会被重新归类为非法路径；FailureHop 仅在 `illegal_edge_at_hop_i` 类样本上定义。

### A.2.2 ErrorSource 轨迹来源分类

当本文进一步分析 TAC 成立但 PathValid 失败的样本时，主分母为 $N_{\mathrm{TAC\text{-}illegal}}=#{j:\mathrm{TAC}^{(j)}=1\land \mathrm{PathValid}^{(j)}=0}$。

|ErrorSource|分母|定义|
|---|---|---|
|`dead_branch_mediated`|$N_{\mathrm{TAC\text{-}illegal}}$|首次偏离黄金路径是一条合法终止分支边，随后才出现非法边或无法完成合法 $k$-hop 轨迹|
|`legal_dead_stop`|$N_{\mathrm{TAC\text{-}illegal}}$|合法进入终止分支后停止或无法继续完成合法 $k$-hop 轨迹|
|`early_dead_illegal_continue`|$N_{\mathrm{TAC\text{-}illegal}}$|早期进入终止分支后继续生成非法边|
|`last_branch_dead_illegal_continue`|$N_{\mathrm{TAC\text{-}illegal}}$|在最后分叉处进入终止分支后继续非法生成|
|`immediate_illegal_from_gold_prefix`|$N_{\mathrm{TAC\text{-}illegal}}$|模型仍位于黄金路径前缀时，下一跳直接生成非法边|
|`terminal_directed_jump`|immediate-illegal 子集|直接跳向预测答案或终点类节点|
|`gold_suffix_shortcut`|immediate-illegal 子集|直接跳到黄金路径后缀节点，表现为跳步捷径|
|`other_error_source`|$N_{\mathrm{TAC\text{-}illegal}}$|无法归入上述类别的其他轨迹形态|

这些类别仅描述轨迹失败的可观测形态，不直接构成因果机制断言；机制解释由 FailureHop、反向可达性等观察性证据，以及 E3/E4 中关于终点边位置与文本支持的干预实验共同支撑。

## A.3 因果干预 outcome 索引

## Causal Intervention Outcome Index

本节列出因果干预实验中使用的专用 outcome。它们用于分析特定实验条件下的局部选择、终止分支行为、oracle-state 增益和文本支持效应，不作为 §3 的核心诊断指标。

|实验|Outcome|分母|备注|
|---|---|---|---|
|E1 响应契约 × 同源候选顺序|late-choice given dead hit|选择终止分支的有效样本|在终止分支选择中，是否偏向后位候选|
|E1 响应契约 × 同源候选顺序|gold local choice|可解析局部分支选择的样本|在同源候选中是否选择黄金后继|
|E2 终止分支 × 长度 / 退出接口|explicit no-legal|E2 returned 且可解析输出|是否显式输出无合法延续|
|E2 终止分支 × 长度 / 退出接口|stop|E2 可解析路径样本|进入终止分支后是否停止|
|E2 终止分支 × 长度 / 退出接口|illegal continue|E2 可解析路径样本|进入终止分支后是否继续生成非法边|
|E2 终止分支 × 长度 / 退出接口|complete-but-illegal|E2 可解析路径样本|是否补足长度但路径非法|
|E3 final edge 位置|gold-prefix survival|E3 可评估路径样本|是否保持黄金路径前缀直到目标分叉前|
|E3 final edge 位置|last-branch failure|E3 可评估路径样本|是否在最后关键分叉处失败|
|E4 final edge 重复 / 距离|gold-prefix survival|E4 可评估路径样本|用于比较文本支持与距离效应|
|E4 final edge 重复 / 距离|last-branch failure|E4 可评估路径样本|用于比较文本支持与距离效应|
|E5 oracle-state|oracle-state accuracy gain|full graph 与 oracle-state 配对有效样本|oracle-state 相对 full graph 的 Accuracy 增益|
|E5 oracle-state|dead-branch hit|E5 可解析局部分支选择样本|是否选择终止分支|
|E6 终止分支行为分解|dead-branch selection|E6 可解析局部分支选择样本|是否选择非黄金终止分支|
|E6 终止分支行为分解|legal-dead-stop|E6 中进入终止分支的样本|合法进入终止分支后是否停止|
|E6 终止分支行为分解|illegal-continue-after-dead|E6 中进入终止分支的样本|进入终止分支后是否继续非法生成|
|E6 终止分支行为分解|refusal|E6 returned 输出|是否拒绝或声明无法继续|

## A.4 完整消融结果

## Full Ablation Results

正文仅报告最能支撑主要发现的设置。本节补充完整消融表，包括 prompt 接口、拓扑深度、推理预算和因果干预 outcome。所有表格均应同时报告样本数或有效分母，避免将不同有效集上的数值直接混读。

### A.4.1 Prompt-interface Ablation

|Model|Topology|Hop|Prompt / Response Contract|Accuracy|TAC|PathValid|PathGoldExact|$\Delta_{\mathrm{illegal}}$|$N_{\mathrm{effective}}^{\mathrm{ans}}$|$N_{\mathrm{parseable}}^{\mathrm{path}}$|
|---|---|--:|---|--:|--:|--:|--:|--:|--:|--:|
|DeepSeek-V4-Flash|branching|12|direct_minimal||||||||
|DeepSeek-V4-Flash|branching|12|jsoncot_basic||||||||
|DeepSeek-V4-Flash|branching|12|jsoncot_strict||||||||
|Qwen Max|branching|12|direct_minimal||||||||
|Qwen Max|branching|12|jsoncot_basic||||||||
|Qwen Max|branching|12|jsoncot_strict||||||||

### A.4.2 Topology-depth Ablation

|Model|Prompt / Response Contract|Topology|Hop|Accuracy|Raw GIS|PC-GIS|GFI|EAR|PathGoldExact|$\Delta_{\mathrm{illegal}}$|$N_{\mathrm{paired}}$|
|---|---|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|
|DeepSeek-V4-Flash|direct_minimal|chain|3|||||||||
|DeepSeek-V4-Flash|direct_minimal|chain|4|||||||||
|DeepSeek-V4-Flash|direct_minimal|branching|4|||||||||
|DeepSeek-V4-Flash|jsoncot_strict|branching|12|||||||||
|Qwen Max|direct_minimal|chain|4|||||||||
|Qwen Max|jsoncot_strict|branching|12|||||||||

### A.4.3 Reasoning-budget Ablation

|Model|Mode|Topology|Hop|Prompt|Accuracy|PathGoldExact|$\Delta_{\mathrm{illegal}}$|Latency|$N_{\mathrm{all}}$|
|---|---|---|--:|---|--:|--:|--:|--:|--:|
|DeepSeek-V4-Pro|no-thinking|branching|12|jsoncot_strict||||||
|DeepSeek-V4-Pro|thinking|branching|12|jsoncot_strict||||||
|GPT-5.4-mini|no-thinking|branching|12|jsoncot_strict||||||

### A.4.4 Causal Intervention Outcomes

|Experiment|Model|Condition|Outcome|Value|Denominator|
|---|---|---|---|--:|--:|
|E1|DeepSeek-V4-Flash|answer-only|late-choice given dead hit|||
|E1|DeepSeek-V4-Flash|path+answer|late-choice given dead hit|||
|E1|DeepSeek-V4-Flash|answer-only|gold local choice|||
|E1|DeepSeek-V4-Flash|path+answer|gold local choice|||
|E2|DeepSeek-V4-Flash|length-on|explicit no-legal|||
|E2|DeepSeek-V4-Flash|length-on|illegal continue|||
|E3|DeepSeek-V4-Flash|final-edge-first|last-branch failure|||
|E4|DeepSeek-V4-Flash|final-edge-repeat-3|last-branch failure|||
|E5|DeepSeek-V4-Flash|oracle-state|oracle-state accuracy gain|||
|E6|DeepSeek-V4-Flash|dead-branch|illegal-continue-after-dead|||

## A.5 Verifier-retry 预算与成本消融

## Verifier-retry Budget and Cost Ablation

Verifier-retry 评估的是固定调用预算内的系统级修复能力。除特别说明外，pass@$K$ 以 $N_{\mathrm{all}}$ 为分母；timeout、request failure 或 no return 均计为未通过，而不从分母中剔除。

|Model|Topology|Hop|Prompt|$K$|pass@$K$|retry recovery rate|verified accuracy|verified path-validity|cumulative latency|calls per task|calls per solved task|
|---|---|--:|---|--:|--:|--:|--:|--:|--:|--:|--:|
|DeepSeek-V4-Flash|branching|12|jsoncot_strict|1||||||||
|DeepSeek-V4-Flash|branching|12|jsoncot_strict|3||||||||
|DeepSeek-V4-Flash|branching|12|jsoncot_strict|5||||||||
|Qwen Max|branching|12|jsoncot_strict|1||||||||
|Qwen Max|branching|12|jsoncot_strict|3||||||||
|Qwen Max|branching|12|jsoncot_strict|5||||||||

若报告 token、credit 或 monetary cost，应在同表或相邻表中给出：

| Model             | Topology  | Hop | Prompt         | $K$ | total calls | total tokens | total credits / cost | cost per task | cost per solved task |
| ----------------- | --------- | --: | -------------- | --: | ----------: | -----------: | -------------------: | ------------: | -------------------: |
| DeepSeek-V4-Flash | branching |  12 | jsoncot_strict |   5 |             |              |                      |               |                      |
| Qwen Max          | branching |  12 | jsoncot_strict |   5 |             |              |                      |               |                      |
## A.6 机制分析指标

本节补充路径层机制分析指标。ErrorType 与 ErrorSource 应分开报告：ErrorType 是验证器观察到的结构错误类型，例如格式错误、长度错误、起点错误或非法边；ErrorSource 是机制性轨迹来源，用于解释非法路径为何产生，例如进入终止分支后继续生成、反向命中预测答案前沿，或最后分支处选择错误候选。

机制分析至少报告以下项目：

|指标|统计对象|说明|
|---|--:|---|
|Reverse-reachability hit|非法路径|检查非法路径是否命中预测答案的反向可达前沿|
|Reverse-reachability enrichment|非法路径|与随机或结构基线比较反向可达命中的富集程度|
|Dead-branch-mediated rate|非法路径|非法轨迹是否由进入终止分支后继续生成介导|
|LegalDeadStop rate|终止分支轨迹|到达终止分支后停止而非非法续写|
|EarlyDeadIllegalContinue rate|终止分支轨迹|早期进入终止分支后仍继续生成非法边|
|LastBranchDeadIllegalContinue rate|终止分支轨迹|最后分支处进入终止分支后仍继续非法续写|
|ErrorSource distribution|非法路径|互斥机制性来源分布|

这些机制指标不替代 PathValid、PathGoldExact 或 (\Delta_{\mathrm{illegal}})，而是用于解释路径层失败的来源。

## A.7 实验 outcome 清单

表 A2 列出每个实验单元需要报告的 outcome。正文只报告主要趋势和关键对比，本附录保留完整 outcome，便于复查。

|实验单元|必报 outcome|备注|
|---|---|---|
|answer-only 主实验|Accuracy、Raw GIS、PC-GIS、GFI、EAR|用于判断答案层是否随图改变以及是否受位置锚定影响|
|path-output 主实验|TAC、PathValid、PathGoldExact、(\Delta_{\mathrm{illegal}})、FailureHop|用于判断路径自洽是否等于结构合法|
|prompt-interface 消融|Accuracy、TAC、PathValid、PathGoldExact、(\Delta_{\mathrm{illegal}})|比较 direct_minimal、jsoncot_basic、jsoncot_strict|
|topology-depth 消融|Accuracy、GFI、EAR、PathValid、FailureHop|比较 chain 与 branching，以及不同 hop depth|
|verifier-retry 消融|pass@K、PathValid、PathGoldExact、latency@K、timeout rate|报告性能收益与推理成本|
|机制分析|ErrorType distribution、ErrorSource distribution、Reverse-reachability、Dead-branch metrics|ErrorType 与 ErrorSource 不合并|

## A.8 完整结果表

本节放置正文未展示的完整表格，包括所有模型、所有 topology-depth 条件、所有 prompt-interface 条件与所有 retry budget 条件下的完整结果。

建议按以下顺序排列：

1. prompt-interface 消融完整表；
    
2. topology-depth 消融完整表；
    
3. reasoning-budget 消融完整表；
    
4. verifier-retry budget 完整表；
    
5. ErrorType distribution；
    
6. ErrorSource distribution；
    
7. 机制分析补充表。
    

正文中只引用关键表格编号，例如：

> 完整分母索引见 Appendix A.1；完整消融结果见 Appendix A.2–A.5；机制分析补充结果见 Appendix A.6。



---

# Appendix B. 因果机制干预实验

## B.1 输出接口与等价分支顺序

E1 关注两个层次的问题：首先，E1 在所有有效输出上检验输出接口 $z$ 是否改变模型的整体选择行为，包括模型是否更容易命中终止分支 $D$、是否更容易提交后位终止分支 $c_{\mathrm{late}}$，以及是否更容易选择黄金局部后继 $c_{\mathrm{gold}}$。E1 主实验采用 4hop；稳健性实验采用 6hop（每个 cell 调用 200 个样本），实验矩阵如下。

首先在全部分配调用（N = 400）中区分输出是否可解析，定义可解析性指标 $P$（即 parseable 简写）为第 $j$ 次调用 API 成功，且返回响应可被解析为 JSON，即$P_j=\mathbb{1}\{\mathrm{api\_ok}_j=1\land \mathrm{json\_parse\_ok}_j=1\}$；其次关注 $P = 1$ 时，能否从可解析输出中提取候选节点 $\hat c$，即 $O=\mathbb{1}[\hat{c}\text{ 可从输出中定义}]$；当 $O=1$ 时，为将两种接口下的可观察输出统一描述，定义候选节点 $\hat c$，需要注意的是，$\hat c$ 是从模型外部输出中读取的行为指标，而非模型内部真实机制的直接观测。对于 answer_only 接口，$\hat c$ 定义为模型输出的答案节点，即 $\hat c = \hat y$；对于 path_answer 接口，若模型从起点 $v_0$ 出发且至少包含一个后续节点 $\hat v_1$，则 $\hat c$ 定义为路径第一跳节点（即生成路径的第二个节点），即 $\hat c = \hat v_1$；在此基础上，进一步将 $\hat c$ 映射为三个二值指标。第一，若 $\hat c$ 落入两条终止分支 A / B 之一，则命中终止分支（$D = \mathbb{1}\!\left[\hat{c} \in \{c_{\mathrm{early}}, c_{\mathrm{late}}\}\right] = 1$）；第二，若 $\hat c$ 等于当前序列化条件下文本位置较晚的终止分支目标节点，则选择无条件后位（$L = \mathbb{1}\!\left[\hat{c} = c_{\mathrm{late}}\right] = 1$）；第三，若 $\hat c$ 等于黄金局部后继节点，则选择黄金局部（$C_{gold} = \mathbb{1}\!\left[\hat{c} = c_{\mathrm{gold}}\right] = 1$）。对于无法从可解析输出中提取候选节点 $\hat c$ 的样本，即 $O=0$ 时，不再定义 $\hat c$，仅将该样本保留在主分析分母中，并令 $D=0$、$L=0$、$C_{gold}=0$。上述条件作为主分析，分母为所有可解析输出，即 $P = 1$；此外还报告条件于 $O=1$ 的敏感性分析，分母为可解析且能读出 $\hat c$ 的所有输出，即 $P=1\land O=1$，用于排除主分析由于可能的 $O=0$ 样本太多，导致不再主要反映“模型选前位/后位”，而是反映“模型有没有给出可观察的选择”的问题。

本实验还额外报告辅助诊断的身份指标模型 $H_A$，表示模型这次选中的候选节点是否为 A 分支的目标节点 $a$，即 $H_A = 1[ĉ = a]$，用于区分模型偏好“相对前位/后位”与模型偏好某个固定分支身份 A / B。具体比较 A 在前时，模型在命中 A / B 后选择 A 的比例 $\Pr(H_A=1\mid D=1,z,\text{branch-a-early})$ 与 A 在后时，模型在命中 A / B 后选择 A 的比例 $\Pr(H_A=1\mid D=1,z,\text{branch-b-early})$。定义 $\Delta_A(z)=\Pr(H_A=1\mid D=1,z,A\prec B)-\Pr(H_A=1\mid D=1,z,B\prec A)$，由于 $\Delta_A(z)\approx 0$ 可能对应固定身份偏好或无明显偏好，本文同时报告两种位置条件下的原始比例。

| 情况  | $\Delta_A(z)$ | 两个原始概率      | 解读    |
| --- | ------------: | ----------- | ----- |
| 1   |          $>0$ | A 早时高，B 早时低 | 偏相对前位 |
| 2   |          $<0$ | A 早时低，B 早时高 | 偏相对后位 |
| 3   |   $\approx 0$ | 两格都接近 1     | 固定偏 A |
| 4   |   $\approx 0$ | 两格都接近 0     | 固定偏 B |
| 5   |   $\approx 0$ | 两格都接近 0.5   | 无明显偏好 |
由于输出接口 $z$ 会影响 $D$，因此在总体行为层，E1 的主指标为无条件后位选择率 $\Pr\!\left(L=1\right)$（由于 $L=1 \Rightarrow D=1$，因此无条件后位提交率 $\Pr(L=1)$ 等价于联合概率 $\Pr(D=1\land L=1)$；下文按需互换使用），用于检验所有有效输出中，模型有多大比例直接提交了后位终止分支。同时报告 $\Pr\!\left(C_\mathrm{gold} = 1\right)$，判断模型是否更容易选择黄金局部后继；$\Pr\!\left(D=1\right)$ ，判断接口是否改变模型陷入终止分支的总体倾向。总体层主要因果估计量是 $RD^{\mathrm{total}}_{X}=\frac{1}{2}\sum_{r\in\{A\to B,\;B\to A\}}\left[\Pr(X=1\mid Z=\mathrm{path\_answer},R=r)-\Pr(X=1\mid Z=\mathrm{answer\_only},R=r)\right]$，用于判断不管 A / B 谁在前，path_answer 相比 answer_only 是否整体改变了 X=1 的概率。其中 $X$ 表示待考察的二值结果变量，可取 $X=L$、$X=D$ 或 $X=C_{\mathrm{gold}}$，分别表示是否选择相对后位终止分支、是否命中两条终止分支之一，以及是否选择黄金局部后继。

其次，E1 希望检验当模型可观测选择已经命中终止分支 A / B（即 $D=1$）时，输出接口是否会改变模型在两条结构等价终止分支 A / B 间前位/后位偏好。由于只关注位于终止分支的子样本内，输出接口 $z$ 会如何影响 $L$，因此条件层主指标为条件后位选择率 $\Pr\!\left(L=1 \mid D=1\right)$；为检验输出接口是否改变前位/后位选择比例，本文进一步报告在命中终止分支的条件下，后位选择率的接口差异 $RD^{cond}_{L|D}$，定义为 path_answer 下的条件后位选择率减去 answer_only 下的条件后位选择率，即 $RD^{\text{cond}}_{L \mid D} = \Pr(L=1 \mid D=1, \text{path\_answer}) - \Pr(L=1 \mid D=1, \text{answer\_only})$。若 $RD^{cond}_{L|D}<0$，则表示在已经命中两条终止分支之一的输出中，path_answer 比 answer_only 更少选择后位终止分支，即其选择构成更接近前位偏好；若 $RD^{cond}_{L|D}>0$，则表示 path_answer 相比 answer_only 更偏向后位终止分支；同时报告辅助诊断指标 $\Pr\!\left(D=1\right)$ ，区分模型更容易进入终止分支，还是进入终止分支后更倾向选择后位。由于 D 是处理后的行为变量，$RD^{\mathrm{cond}}_{L\mid D}$ 不解释为 principal-stratum causal effect，仅作为终止分支命中输出内部选择构成的描述性分解。接口的总体因果效应由无条件配对 RD 识别，包括后位终止分支提交率 $\Pr(D=1\land L=1)$、终止分支命中率 $\Pr(D=1)$ 以及黄金分支提交率 $\Pr(C_{\mathrm{gold}}=1)$。

本实验设定每个图实例从起点 $v_0$ 出发，存在三种候选边。第一，正确（黄金）边：$e_g=(v_0,v_1)$；第二，终止分支 A：$e_A=(v_0,a)$；第三，终止分支 B：$e_B=(v_0,b)$。其中节点 $a$，$b$ 均无后继边，且 A / B 的局部结构角色等价。为避免分支身份与文本位置混淆，本文按边字符串词典序为两条终止分支固定身份，记词典序较小者为 A，较大者为 B。A / B 仅用于固定终止分支身份并构造位置交换条件，主要统计量不以模型选 A 还是 B 为条件，而是在每个序列化条件下将 A / B 映射为 $c_{\mathrm{early}}$ 与 $c_{\mathrm{late}}$（$c_{\mathrm{early}}$ 与 $c_{\mathrm{late}}$ 仅指 A / B 两条终止分支之间的相对前后位置），通过模型选择“当前文本中位置靠前的那个”还是“位置靠后的那个”计算主要统计量。同时，对于 $\Pr(D=1\land L=1)$、$\Pr\!\left(D=1\right)$  以及 $\Pr\!\left(C_{gold} = 1\right)$，为说明分母构成与条件指标的稳定性，本文同时报告每个条件下的全部调用数、有效输出数、终止分支命中数，以及用于配对统计的共同有效 cluster 数。为排除分支身份、图结构、图序列化长度和非目标边顺序等替代解释，E1 采用 2 × 2 的配对干预设计，两个位置条件间，除 A / B 两条边在文本中的先后顺序被交换，其他输入内容均不变。

| 输出接口        | branch-a-early | branch-b-early |
| ----------- | -------------: | -------------: |
| answer_only |        400 次调用 |        400 次调用 |
| path_answer |        400 次调用 |        400 次调用 |
表注：
1. 两个输出接口之间的 schema 文本及其长度差异属于接口处理本身
2. 400 次调用即 400 个样本（N = 400），每个样本 $2 \times 2$ 配对干预。

实验仅交换 A 与 B 在边列表中的文本位置，生成 branch-a-early 与 branch-b-early 两种情况，其中 branch-a-early 指 A / B 两条终止分支里相对更早的那条；branch-b-early 同理。随后在该位置干预下切换两种输出接口：answer_only 要求模型仅输出 {"answer": "..."}；path_answer 要求模型输出 {"path": [...], "answer": "..."}。由此形成文本位置 × 输出接口的 2 × 2 实验组合。该实验的设计逻辑是，若模型偏好固定分支身份，则 A / B 位置交换后模型仍应持续选择同一分支；若模型受文本位置影响，则被选中的分支应随 A / B 的位置交换而改变；若输出接口改变位置偏好，则两种接口下的条件后位选择率会出现系统差异。本文将结果解释为行为层面符合后位显著性驱动的终点抽取（recency-driven endpoint extraction）与类首个匹配的下一跳边检索（first-match-like next-edge retrieval）的差异，而不强断言模型内部真实执行了“末位 token 抽取”或“从前向后扫描”等具体算法。总体而言，E1 识别的是输出接口 $z$ 对等价终止分支整体选择行为的影响，而非模型内部算法的直接证据。无条件指标 $\Pr(L=1)$、$\Pr(D=1)$ 和 $\Pr(C_{\mathrm{gold}}=1)$ 用于刻画总体行为变化；条件后位选择率 $\Pr(L=1\mid D=1)$ 及 $RD^{cond}_{L|D}$ 仅描述命中两条终止分支之一的输出中，选择相对前位/后位的构成，不解释为独立条件因果效应。A / B 位置交换用于检验选择是否跟随两条终止分支之间的相对文本位置，而非固定分支身份。因此，本文仅将该结果解释为当前受控分叉图与边列表序列化条件下，输出接口会在行为层面改变模型在两条等价终止分支之间的相对位置偏好。

## B.2 预先指定的终止前缀、长度要求与合法性要求

在 §4.2.2 终止分支集合的基础上，定义零出度终止分支集合 $\mathcal{E}_{\mathrm{term}}^{0}$（上角标表示节点出度为 $0$）为下一跳节点没有任何出邻居的集合，即 $\mathcal{E}_{\mathrm{term}}^{0}=\left\{(v_i,u^0_{i+1})\in\mathcal{E}_{\mathrm{term}},i\in\{0,\ldots,k-2\}\ \middle|\ \mathcal{N}^{+}(u^0_{i+1})=\varnothing\right\}$，其中 $u^0$ 不存在任何出邻居集合，称为零出度终止节点。原始 branching-12hop 路径的 ErrorSource 互斥分解表（简称互斥表）对所有已观察到的 $\mathrm{TAC}=1\land \mathrm{PathValid}=0$ 的路径进行穷尽且互斥的错误来源分解，在具有足够错误样本的非思考模型与提示配置中，有 $97.3\%$-$99.7\%$ 的自洽但结构无效轨迹是先沿合法边进入终止分支，表明非法路径错误并非任意幻觉，而是有稳定状态链，模型首先沿图中合法边偏离黄金路径抵达零出度终止节点 $u^0$，随后产生终止状态响应 $Y_{\mathrm{term}}$。然而，原始 branching-12hop 路径同时包含两个过程：上游的终止分支选择过程，即模型是否沿图中合法但非黄金边进入终止分支；下游的终止状态响应过程，即模型抵达零出度终止节点后，在 exact-length 要求与逐边合法性要求下，选择产生哪种零出度终止状态响应，这正是 E2 关注的。互斥表给出的是两个过程耦合下观测到的整体 ErrorSource 分布，无法区分模型在抵达同一零出度终止状态后，终止状态响应差异究竟来自终止状态处理本身的局限，还是 exact-length 要求与逐边合法性要求的作用。为此，E2 通过给定模型终止前缀 $\tilde{p}=(v_0,\ldots,v_{10},u^0)$ 固定上游的终止分支选择过程，并在保持图、前缀和终止分支身份不变的条件下，控制是否施加 exact-length 要求与逐边合法性要求，诊断同一零出度终止状态的终止响应差异的来源。互斥表关注 $\mathrm{TAC}=1\land \mathrm{PathValid}=0$ 路径的整体 ErrorSource 分布，即无效轨迹主要经由哪些前置错误环节产生；E2 关注 exact-length 要求和逐边合法性要求如何改变终止响应类型，二者在诊断层次上互补。由于 E2 固定的是最后一个可分叉位置后的零出度终止状态，因此其直接解释范围是模型抵达该状态后的终止响应机制，应与原始数据中的 ErrorSource 分解联合解释：后者回答模型如何进入终止分支；E2 则回答进入终止分支后，不同输出约束如何改变停止、退出与非法续写之间的分配。将 exact-length 要求和逐边合法性要求映射为两个二值指标，$\mathrm{L}=\mathbb{1}\{\text{施加 exact-length 要求}\}$ $\mathrm{V}=\mathbb{1}\{\text{施加逐边合法性要求}\},\quad T=(\mathrm{L},\mathrm{V})\in\{0,1\}^2$
。在 E2 设定下，主分析估计量为 $p^r(t)=\Pr(Y_{\mathrm{term}}=r\mid \tilde p,T=t)$，其中 $t=(\ell,v)\in\{0,1\}^2$，$r\in\{\mathsf R,\mathsf S,\mathsf I,\mathsf O\}$。

对于请求成功且输出可解析的调用，互斥表已经给出了可观测 ErrorSource，在 E2 的固定前缀设定下，上游错误来源影响被消除， ErrorSource 上游类别退化，只剩下游相关类别可被观测，本文据此重新定义互斥且穷尽的终止状态响应变量 $Y_{\mathrm{term}}\in\{R,S,I,O\}$，互斥性、穷尽性依赖判定逻辑保证。其中，$R$：显式报告无合法后继边（显式停止）；$S$：输出固定前缀后停止（非显式停止）；$I$：输出固定前缀后非法续写；$O$：其他不合规响应。需要注意的是，$\mathsf O$ 总体很低，最高约 $1.5\%$，表明 $\mathsf R/\mathsf S/\mathsf I$ 对可解析终止响应的覆盖已经接近完整。由 PathValid 定义 $\mathrm{PathValid}^{(j)}=\mathbb{1}\!\left[\hat p^{(j)}\in\mathcal{W}(G^{(j)},s^{(j)},k)\right]=\mathbb{1}\!\left[\mathrm{PathStartOK}^{(j)}\land \mathrm{PathLengthOK}^{(j)}\land \mathrm{PathEdgesValid}^{(j)}\right]$ 可知 $\mathrm{PathValid}=0$ 的失败模式至少有两类：逐边非法，存在某条边 $(\hat v_j,\hat v_{j+1})\notin E$；长度不足，所有边都合法，但 $|\hat p|-1<k$。E2 的四类终止响应进一步讲其分解：长度不足拆分为 $\mathsf{R}$ / $\mathsf{S}$；逐边非法拆分为 $\mathsf{I}$。E2 的方法论贡献在于将固定零出度终止状态后的可观测响应分解为显式退出、非显式停止、非法续写以及其他不合规响应，并估计输出约束对这些响应概率的因果效应，使 $\mathrm{PathValid}$ 作为复合指标内部的失败模式从隐藏状态转化为可观测且可独立操纵的概念。$T=(\mathrm{L},\mathrm{V})$ 两个干预变量分别作用于不同子空间之间的转移概率，从而支持对每种失败成因的因果效应做独立识别。

对每个 branching-12hop 图实例，在最后一个可分叉节点 $v_{10}$ 处，生成器提供黄金边 $e_g$ 以及两条结构等价的终止分支 A / B。实验在两个终止分支中预选一个用于构造固定前缀 $\tilde{p}^{(j)}=(v_0^{(j)},\ldots,v_{10}^{(j)},u_{11}^{0,(j)})$，原任务要求模型完成 $12$ hops，固定前缀距离满足规定长度还差 $1$ hop，而模型继续生成任何从 $u_{11}^{0,(j)}$ 出发的边都必然违反图结构，若模型此时继续生成节点，则记其在 $u_{11}^{0,(j)}$ 后预测的第一个新节点为 $\hat u_{12}^{(j)}$。同一图实例跨模型、跨四个 E2 条件固定前缀完全相同；不同样本 $j$ 或 G1 / G2 图和固定前缀不同。实验采用 $2\times2$ 因子设计，为避免终止分支身份与处理条件混淆，E2 在生成处理条件之前，根据图实例标识的哈希值确定所使用的零出度终止分支，并在同一图实例的四个处理条件中保持不变。该选择先于 prompt 条件生成完成，因此与后续是否施加 exact-length 要求和逐边合法性要求独立；在样本层面，两个候选终止分支的使用频率近似平衡。四个处理条件使用相同的 keyed shuffle 图文本、边顺序、固定前缀和输出 schema，仅改变是否施加 exact-length 要求与逐边合法性要求。

E2 从与主实验共享的基础图池中选取 400 个独立的 branching-12hop 图实例，每个实例对应一个单独的 sample_id，不再构造反事实图对。候选实例首先接受与模型输出无关的静态结构验证：图中须存在唯一黄金 12-hop 路径；倒数第二个决策节点须同时具有黄金后继与零出度终止后继；选定终止后继后形成的固定前缀须由 11 条合法边组成，但其末节点不存在任何合法后继。通过验证的实例构成合格样本池，随后使用预先固定的随机种子从中无放回抽取 400 个实例。所有资格检查与样本抽取均在模型调用前完成，不使用模型在主实验或其他实验中的答案、错误类型或路径行为。对于每个入选实例，四个处理条件共享相同的图、固定前缀、边顺序与输出格式，仅改变 exact-length 要求与逐边合法性要求。实验采用 $2\times2$ 因子设计，且四个条件均允许模型输出 NO_LEGAL_CONTINUATION。因此，即使在 length-on × legality-on 条件下，模型也不必通过伪造边来满足长度要求，而可以显式报告当前不存在合法后继边。每个条件包含 $400$ 个图实例，每个模型共执行 $400\times4=1600$ 次调用。

| 输出约束       | edge-validity-off                               | edge-validity-on                                  |
| ---------- | ----------------------------------------------- | ------------------------------------------------- |
| length-off | 不要求 CONTINUED 路径补满至原任务规定的 12 hops；也不额外声明逐边合法性要求 | 不要求 CONTINUED 路径补满至 12 hops；但若继续生成，则每次转移均必须属于 $E$ |
| length-on  | 要求 CONTINUED 路径补满至原任务规定的 12 hops；但不额外声明逐边合法性要求  | 同时要求 CONTINUED 路径补满至 12 hops，且每次转移均必须属于 $E$       |
§ 3.1 中已经定义模型输出的预测路径为 $\hat p^{(j)}=(\hat v_0^{(j)},\hat v_1^{(j)},\ldots,\hat{v}_{m_j}^{(j)})$；预测答案为 $\hat y^{(j)}$，其中 $m_j$ 可能不等于 $k$。在此基础上进一步定义 E2 的主指标，固定前缀 $\tilde p$​ 严格前缀于模型输出 $\hat p^{(j)}$ 记为 $\tilde p ≺ \hat p^{(j)}$，即 $\tilde p ≺ \hat p^{(j)} \iff \hat p_{0:11}^{(j)} = \tilde p^{(j)} \land |\hat p^{(j)}| > |\tilde p^{(j)}|$，其中 $\hat p_{0:11}^{(j)} = \tilde p^{(j)}$ 为模型输出路径从下标 0 到下标 11 的子序列全等于固定前缀 $\tilde p^{(j)}$，$|\hat p^{(j)}| > |\tilde p^{(j)}|$ 表示模型输出路径的总长度严格大于 12。首先定义显式退出 $R$ 表示模型使用实验提供的显式退出选项，声明从终止节点不存在合法后继边，即 $\mathbb{1}\{Y_{\mathrm{term}}^{(j)}=\mathsf R\}=\mathbb{1}\{\widehat{\mathrm{status}}_j=\texttt{NO\_LEGAL\_CONTINUATION}\}$；其次定义非显式停止 $S$ 表示模型完整保留固定前缀且不再生成下一节点，但没有使用 NO_LEGAL_CONTINUATION 显式退出，即 $\mathbb{1}\{Y_{\mathrm{term}}^{(j)}=\mathsf S\}=\mathbb{1}\{R^{(j)}=0 \land \tilde p^{(j)} = \hat p^{(j)}\}$；最后定义非法续写 $I$ 表示模型完整保留固定前缀后继续生成，即 $\mathbb{1}\{Y_{\mathrm{term}}^{(j)}=\mathsf I\}=\mathbb{1}\{R^{(j)}=0 \land \tilde p^{(j)} ≺ \hat p^{(j)}\}$；未被上述类别覆盖的输出定义为其他不合规响应，即 $\mathbb{1}\{Y_{\mathrm{term}}^{(j)}=\mathsf O\} = 1-R^{(j)}-S^{(j)}-I^{(j)}$，包括修改或遗漏固定前缀、使用错误起点、返回不兼容 schema，以及其他无法解释为显式退出、前缀停止或前缀后续写的输出。此外，在非法续写 $I$ 中进一步区分两类不同类型失败：第一，乱凑型。模型只是为了继续写，随便生成节点，即 $|\hat p|\neq k+1$，其中 $k$ 是任务要求的 hop；第二，自洽型。$|\hat p| = k+1$，且末节点等于模型声明的答案 $\hat u_{12}^{(j)} =\hat y^{(j)}$（TAC = 1），记为 $F$。这种失败的迷惑性最强，表面自洽，只有逐边合法性出错。其中，F 进一步分为场景 A 和场景 B：满足 $F^A=\mathbb 1[I=1\land|\hat p|=k+1 \land \hat u_{12}^{(j)} = \hat y = y]$，即 § 3.3 中定义的场景 A；进一步定义形式完整但图上非法的续写为 以及场景 B，记 $F^B=\mathbb 1[I=1\land|\hat p|=k+1 \land \hat u_{12}^{(j)} = \hat y \neq y]$，两者满足 $|F|=|F^A|+|F^B|=\left|\left\{j:Y_{\mathrm{term}}^{(j)}=\mathsf I\land \mathrm{TAC}^{(j)}=1\land|\hat p|=k+1\right\}\right|$。

主分析中主要结果变量为 $R$、$S$、$I$，并额外报告 $F$，辅助结果变量为 $F^A$、$F^B$，作为 $F$ 场景内部的对应描述性细分指标,在每个 $T$ 条件下报告其绝对数与可解析占比，但不估计 $T$ 对 $F^A/F^B$ 比例的因果效应，因为模型在 no-target 设定下无法感知黄金答案 $y$，该比例不进入 $T$ 的因果路径。并将 $O$ 作为不合规响应审计项，分母为所有可解析输出，即 $P=1$；此外还报告条件于 $Y \neq \mathsf{O}$ 的敏感性分析，分母为可解析且能归入 $\mathsf{R}/\mathsf{S}/\mathsf{I}$ 三类核心终止响应的所有输出，即 $P=1 \land Y \neq \mathsf{O}$，用于排除主分析由于可能的 $\mathsf{O}$ 样本过多导致不再主要反映模型在终止状态下选择哪种终止响应，而是反映模型是否给出可分类的终止响应的问题。本文同时报告每格全部调用数、请求成功数、可解析响应数，以及各出口类别的有效计数。为避免因处理条件影响解析成功率而产生事后选择，全部调用口径作为主要审计口径；基于可解析响应的条件比例作为行为分解结果，并与解析率联合报告。API timeout 或无返回单独归为技术失败，不解释为模型选择了某种出口。此外，$P$ 本身可能受到处理条件 $T$ 影响。DS-Flash 在 length-off 条件下解析失败率为 $31\%-39\%$，显著高于其他模型与条件。因此,E2 同时在全调用口径报告 request failure、parse failure 与各条件下的 $N_{\mathrm{effective}}$，使读者能够独立评估主分析的 $P=1$ 子样本是否对处理条件的处理效应估计构成隐性筛选。

定义辅助变量 $C\in\{A,B\}$ 表示每个图实例中使用了两条终止分支中的哪一条，尽管样本层面终止分支身份近似平衡（§B.2 实验设计部分），为消除有限样本下身份不平衡造成的残余偏差，估计量层显式对 $C$ 做对称化平均，与 §A 中 E1 对边顺序 $R$ 的处理对称。在处理条件 $t$ 下，所有可解析输出中终止响应类别为 $r$ 的比例记为基础格点概率，即 $p^r(\ell,v,c)=\Pr(Y_{\mathrm{term}}=r\mid \mathrm{Parseable}=1,\ L=\ell,\ V=v,\ C=c)$；为消除两条终止分支身份的影响，记对 $C$ 平均后的格点概率为 $\bar p^{r}(\ell,v)=\frac{1}{2}\sum_{c\in\{A,B\}}\Pr(Y_{\mathrm{term}}=r\mid L=\ell,V=v,C=c)$；为分析 exact-length 要求对响应类别 $Y$ 变化的总体影响，固定 $V$ 计算总体主效应 $\mathrm{RD}^{\mathrm{total}}$，即 $RD_{\mathrm{L}}^{\mathrm{total},r}=\frac{1}{2}\sum_{V\in\{0,1\}}\left[\bar p^r(1,V)-\bar p^r(0,V)\right]$；随后，固定 $\mathrm{V}=v$​，分析是否存在逐边合法性要求两种情况下的 exact-length 对非法续写的局部效应，即 $RD_{\mathrm{L}\mid V=v}^{\mathrm{cond},r}=\bar p^{r}(1,v)-\bar p^{r}(0,v)$，其中 $v\in\{0,1\}$；最后，计算交互效应 $\mathrm{INT}^{r}$，分析逐边合法性要求是否改变了 exact-length 要求的效应，$\mathrm{INT}^{r}=RD_{\mathrm{L}\mid V=1}^{\mathrm{cond},r}-RD_{\mathrm{L}\mid V=0}^{\mathrm{cond},r}$，正值表示在开启逐边合法性要求时，exact-length 要求更容易增加响应类型 $r$ 的概率；负值相反。逐边合法性要求同理，为节省篇幅不再赘述。
排除 endpoint-first / center / adjacent 导致的整体 shuffle 与不同图文本正文差异的影响
## B.3 固定正文与终点边位置

每个 branching-$k$ hop 图记为一个  $\mathrm{sample\_id\ j}$  ，E3 检验图 $\mathrm{G}$ 中终点边 $e_{k}=(v_{k-1},y)$ 在边列表中的文本位置是否因果影响模型在最后分叉点 $v_{k-2}$ 选择正确后继黄金节点 $v_{k-1}$ 的概率，即是否存在全局文本位置效应。该实验首先对原始 branching 整体 shuffle 一次，随后仅移除终点边 $e_{k}$ 并保持 body 内部不同边相对顺序不变，最后在六个互异位置重新插入终点边 $e_{k}$ 得到六种终点边位置控制样本，简称控制样本，其中 canonical 插入原位作为对照。通过代码对每个图实例和 shuffle seed（简记为 $\mathrm{s}$） 进行核验保证六个控制样本满足互异位置约束，若六个条件下 $e_{k}$ 位置不互异，则丢弃并重新抽取，之后重复上述过程，直至符合要求的 shuffle 达到 $K$；若六个位置互异，则核验通过。除 $e_{k}$ 外，六个条件共享同一固定正文顺序，从而排除整体 shuffle 导致的不同图文本顺序差异的影响。为降低状态追踪难度对实验结果的干扰，主实验采用 4 hop，并以 6 hop 作为稳健性检验。因为在原始主实验中，模型正确到达最后分支前缀的概率在 4-hop 明显较高（0.843-0.932）；6-hop 对较弱模型仍约为 0.5 以上；8-hop 开始 GPT 与 Qwen 的 $\mathrm{C_{j,g,s,pos}}$ 值跌破 0.5，此时机制诊断样本的幸存者效应显著。对每个固定 shuffle seed，六个条件下 fixed body 的内部相对顺序保持一致，风险差 $\mathrm{RD}_{t}$ 反映 $e_k$ 位置由 canonical 移到 $t$ 所引起的总效应（包含 $e_k$​ 自身绝对位置变化、以及 fixed body 中各边因 $e_k$​ 占位变化而产生的绝对位置平移）。对 $K$ 个独立 shuffle seed 取平均后，特定 shuffle 实现下的位置耦合被 marginalize 为 shuffle 分布上的平均效应。实验具体插入位置如下表。

| 条件                         | $e_{k}$ 的插入位置 | 操作说明                                          |
| -------------------------- | ------------- | --------------------------------------------- |
| global-first（简称 first）     | 边列表第一行        | 将终点边 $e_{k}$ 插入整个边列表第一个位置                     |
| center                     | 固定正文中央的可用位置   | 将终点边 $e_{k}$ 插入整个边列表的中心插入槽                    |
| adjacent-before（简称 before） | $e_{k-1}$ 正前方 | 将终点边 $e_{k}$ 插入边列表中 $e_{k-1}$ 正前面（index 距离为1） |
| adjacent-after（简称 after）   | $e_{k-1}$ 正后方 | 将终点边 $e_{k}$ 插入边列表中 $e_{k-1}$ 正后面（index 距离为1） |
| global-last（简称 last）       | 边列表最后一行       | 将终点边 $e_{k}$ 插入整个边列表最后一个位置                    |
| canonical                  | 不变            | 保持 full-shuffle 原位                            |
六种控制版本除 $e_{k}$ 的位置外，其他条件完全相同，且均使用 jsoncot_strict。本主实验共 $\mathrm{J}=400$ 个独立 sample_id（稳健性实验 200 个），每个 $\mathrm{sample\_id}$ 分别执行六个位置条件，共有 $400 \times 6 \times K = 2400K$ 次调用。为避免将早期路径错误归因于终点边干预，主实验只统计模型正确到达黄金路径的倒数第三个节点 $v_{k-2}$（生成器限制 $v_{k-2}$ 为最后一个存在同源边的分叉节点；稳健性实验同理）。主分析有效样本量约为各条件调用数乘以对应 $\Pr(\mathrm{C_{j,s,pos}}=1)$，并在结果中报告实际满足 $\mathrm{C_{j,s,pos}}=1$ 的样本数。令固定前缀 $\tilde{p}^{(j)}_{0:k-2}$ 表示模型前 $k-2$ hop 沿黄金路径，即 $\tilde{p}^{(j)}_{0:k-2}=(v_0^{(j)},\ldots,v_{k-2}^{(j)})$，同时定义指示变量正确前缀保持率（Correct prefix indicator）记为 $\mathrm{C_{j,s,pos}}$ 用于判别模型是否正确抵达 $v_{k-2}$，即 $\mathrm{C_{j,s,pos}}=\mathbb 1[\tilde p_{0:k-2}\prec\hat p]$；定义指示变量 $D_\mathrm{j,s,pos}=\mathbb 1[\hat v_{k-1}^{(j,s,pos)}\text{ 未输出 } \lor \hat v_{k-1}^{(j,s,pos)}\neq v_{k-1}^{(j)}]$ 判别边 $e_{k-1}=(v_{k-2},v_{k-1})$ 的成功率是否受 $e_{k}$ 文本位置因果影响，其中 $\mathrm{pos} \in \{\mathrm{first}, \mathrm{center}, \mathrm{before}, \mathrm{after}, \mathrm{last}, \mathrm{canonical}\}$。主分析统计量为 $\Pr(D_\mathrm{pos}=1\mid \mathrm{C}=1,T=t), t \in \mathrm{T} = \mathrm{pos}\setminus\{\mathrm{canonical}\}$，分别计算五个位置与 canonical 配对做配对风险差，即 $\mathrm{RD}_{t}=\Pr(D_{t}=1\mid C_{t}=1)-\Pr(D_{\mathrm{canonical}}=1\mid C_{\mathrm{canonical}}=1)$，同时报告在下一节点选择同源 dead-end 分支的比例作为辅助统计指标。使用 Cochran's Q 检验 $\Pr(\mathrm{C_{j,s,pos}}=1)$ 在六个控制样本下的一致性（$p > 0.05$ 视为无显著差异）作为稳健性诊断，如果检验显著，则再两两 McNemar 检验并用 Holm 校正；同时报告六条件下 $\Pr(\mathrm{C_{j,s,pos}}=1)$ 的具体数值和最大差值，联合判据完整逻辑见下表。

| 最大差值$\Delta_C=max_{pos} \Pr(C=1) - min_{pos} \Pr(C=1)$ | Q 检验结果   | 判定                            |
| ------------------------------------------------------ | -------- | ----------------------------- |
| ≤ 2 pp                                                 | 任意 p 值   | 实质影响可忽略，selection bias 风险低    |
| 2 pp–5 pp                                              | p > 0.05 | 差异不显著，通常可接受，但需要报告数值           |
| 2 pp–5 pp                                              | p ≤ 0.05 | 存在一定 selection bias 风险，需要谨慎解释 |
| > 5 pp                                                 | 任意 p 值   | 在 Limitations 中报告，并限制因果解释强度   |
定义共同幸存集 $\mathcal{S}=\{(j,s): \mathrm{C_{\mathrm{{j,s,pos}}}}=1,\forall pos\}$，在 $\mathcal{S}$ 上重算 $\mathrm{RD}_{t}^{\mathrm{hard}}=\Pr(D_t^{(j,s)}=1\mid (j,s)\in\mathcal{S})-\Pr(D_{\mathrm{canonical}}^{(j,s)}=1\mid (j,g,s)\in\mathcal{S})$，Cluster bootstrap 按 $\mathrm{sample\_id}$ 聚类（$95\%$ CI）。若 $\mathrm{RD}_{t}^{\mathrm{hard}}$ 与主分析 $\mathrm{RD}_{t}$ 在方向和量级上一致，则主结论不依赖于条件间的样本组成差异，selection bias 风险可控。若不一致，则在 Limitations 中报告该差异并限定主结论的因果解释强度。

## B.4：终点边文本支持

主实验 GFI 已经验证了模型存在位置锚定现象，E4 则希望进一步检验模型是否存在边出现频次锚定现象（简称频次锚定）。在理想的简单图观察者零基线下，边列表首先被恢复为去重后的有向边集合 $\mathrm{E}$。因此，同一条边重复出现不会改变 $\mathrm{E}$，也不会增加新的图结构信息。换言之，重复边在图语义上不提供额外证据。E4 并不假设 LLM 的 token processing 满足该理想条件；相反，本实验正是检验模型是否偏离这一去重图语义零基线，即是否会把边出现频次这一序列化层面的非结构线索用于最后分叉选择。同时合成图排除可解释的自然语言的语义因素，重复无法传达语义信息。

每个 branching-$k$ hop 图记为一个  $\mathrm{sample\_id\ j}$ ，E4 检验图 $\mathrm{G}$ 中终点边 $e_{k}=(v_{k-1},y)$ 在边列表中的总出现次数（edge multiplicity，简记为重复次数 $\mathrm{M}$），是否因果影响模型在最后分叉点 $v_{k-2}$ 选择后继黄金节点 $v_{k-1}$ 的概率，即是否存在频次锚定。本实验沿用 § B.3（实验 E3） 的六个控制样本作为基线（$\mathrm{M = 1}$），首先改变边 $e_{k}$ 的重复次数 $\mathrm{M}\in\{1,2,3,4\}$，得到四个频次控制样本；为排除读取噪声（漏读、错读或注意力分散）+ 冗余纠错这一竞争假设，即模型重复观察 $e_k$​ 后能进行更准确解析，而非真的对频次敏感，设计结构对照样本。以原始 branching 整体 shuffle 后得到的样本为底图，在 $v_{k-2}$ 的两条等价终止分支 $e^{\mathrm{A}}_{\mathrm{dead}}$ 与 $e^{\mathrm{B}}_{\mathrm{dead}}$ 中任选一条（本实验选择 $e^{\mathrm{A}}_{\mathrm{dead}}$），对应的零出度终止节点为 $v_{\mathrm{dead}}^{\mathrm{0}}$，修改其重复次数 $\mathrm{M}\in\{1,2,3,4\}$。以 $\Delta_{\mathrm{dead}}(m) = \Pr(\hat v_{k-1}=v_{\mathrm{dead}}^{\mathrm{0}} \mid M^A=m) - \Pr(\hat v_{k-1}=v_{\mathrm{dead}}^{\mathrm{0}} \mid M^A=1)$ 作为冗余纠错的主统计指标，表示模型是否更容易选中终止分支 $e^{\mathrm{A}}_{\mathrm{dead}}$。如果模型因为 $e^{\mathrm{A}}_{\mathrm{dead}}$ 重复次数更多就更容易选中该终止分支，即 $\Delta_{\mathrm{dead}}(m) > 0$，则模型读取能力噪声假设不成立，因为冗余纠错假设重复只能帮助模型更准确识别黄金边，失败率应降低（或不变），而不应该出现模型更偏向终止分支。同时为区别重复带来的 token-level 频次激活效应以及整个边列表变长带来的任务难度增加，定义冗余纠错的辅助统计指标 $\Delta_{\text{support}}(m)=\Pr(\hat{v}_{k-1}=v_{k-1}\mid M_k=m)-\Pr(\hat{v}_{k-1}=v_{k-1}\mid M_k=1)$，计算 $\Delta_{\mathrm{net}}(m)$ = $\Delta_{\text{support}}(m)-\Delta_{\mathrm{dead}}(m)$ = $\left[\Pr(\hat{v}_{k-1}=v_{k-1}\mid M_k=m)-\Pr(\hat{v}_{k-1}=v_{k-1}\mid M_k=1)\right]-\left[\Pr(\hat{v}_{k-1}=v_{\mathrm{dead}}^{0}\mid M_A=m)-\Pr(\hat{v}_{k-1}=v_{\mathrm{dead}}^{0}\mid M_A=1)\right]$，先分别在各自内部做相对 $\mathrm{M}=1$ 的变化 $\Delta_{\text{support}}(m)$ 与 $\Delta_{\mathrm{dead}}(m)$，由于分别在各自 arm 内部的任务是同一个目标（选中 support / dead），且模型自身的分支辨识能力、样本基础难度和节点初始选择偏好都由模型参数与样本结构决定，在不同 $\mathrm{M}$ 下完全相同，因此二者相减后降低了非 $\mathrm{M}$ 相关的混淆，剩下的应理解为终点边 $e_{k}$ 重复引起的额外效应；随后做 $\Delta_{\text{support}}(m)-\Delta_{\mathrm{dead}}(m)$，在 support arm 与 dead arm 具有相同 $\mathrm{M}$ 变化的条件下，进一步降低一般激活强度和任务难度变化等共同副作用的影响；最后为排除天花板 / 地板效应，本文同时报告 $P_{\mathrm{support}}(1),\ P_{\mathrm{support}}(m);\ P_{\mathrm{dead}}(1),\ P_{\mathrm{dead}}(m)$。如果 $\Delta_{\mathrm{net}}(m)\approx 0$，则 support 和 dead 对重复的反应强度接近，更像一般 token-level frequency salience；如果 $\Delta_{\mathrm{net}}(m)>0$ 显著（在 $\Delta_{\mathrm{dead}}(m)>0$ 前提下），则重复 $\mathrm{support}$ 的正向效应强于 $\mathrm{dead}$ 的负向效应，模型对重复对象的图角色敏感（graph-role-sensitive frequency anchoring）；如果 $\Delta_{\mathrm{dead}}(m)\le 0$ 且 $\Delta_{\mathrm{support}}(m)>0$，则模式与冗余纠错预测相容；如果两者均不显著，则频次效应弱，模型不受重复影响。E4 仍然关注图忠实性问题，且为黑盒诊断，因此设置对照实验的目的不是对该现象进行模型内部状态转移分析，而是排除模型读取能力噪声，区分不忠实性与冗余纠错现象。实验完整矩阵如下。

| $\mathrm{R}$                | pos 条件                     | $e_{k}$ 的插入位置                                 | 频次控制样本                          | 频次对照样本                                |
| --------------------------- | -------------------------- | --------------------------------------------- | ------------------------------- | ------------------------------------- |
| $\mathrm{clustered}$        | global-first（简称 first）     | 将终点边 $e_{k}$ 插入整个边列表第一个位置                     | 重复终点边 $e_{k}$ 出现次数 $\mathrm{M}$ | 重复终止分支 $\mathrm{A}$ 出现次数 $\mathrm{M}$ |
| $\mathrm{clustered}$        | center                     | 将终点边 $e_{k}$ 插入整个边列表的中心插入槽                    | 重复终点边 $e_{k}$ 出现次数 $\mathrm{M}$ | 重复终止分支 $\mathrm{A}$ 出现次数 $\mathrm{M}$ |
| $\mathrm{clustered}$        | adjacent-before（简称 before） | 将终点边 $e_{k}$ 插入边列表中 $e_{k-1}$ 正前面（index 距离为1） | 重复终点边 $e_{k}$ 出现次数 $\mathrm{M}$ | 重复终止分支 $\mathrm{A}$ 出现次数 $\mathrm{M}$ |
| $\mathrm{clustered}$        | adjacent-after（简称 after）   | 将终点边 $e_{k}$ 插入边列表中 $e_{k-1}$ 正后面（index 距离为1） | 重复终点边 $e_{k}$ 出现次数 $\mathrm{M}$ | 重复终止分支 $\mathrm{A}$ 出现次数 $\mathrm{M}$ |
| $\mathrm{clustered}$        | global-last（简称 last）       | 将终点边 $e_{k}$ 插入整个边列表最后一个位置                    | 重复终点边 $e_{k}$ 出现次数 $\mathrm{M}$ | 重复终止分支 $\mathrm{A}$ 出现次数 $\mathrm{M}$ |
| $\mathrm{clustered}$        | canonical                  | 保持 full-shuffle 原位                            | 重复终点边 $e_{k}$ 出现次数 $\mathrm{M}$ | 重复终止分支 $\mathrm{A}$ 出现次数 $\mathrm{M}$ |
| $\mathrm{evenly-dispersed}$ | N/A                        | $\mathrm{M}$ 个出现实例在边列表允许槽位中尽量均匀分布             | N/A                             | N/A                                   |
设计结构对照样本，令分散重复作为对照组。定义二值变量 $\mathrm{R}$ 表示重复方式，其中 $R\in\{\mathrm{clustered},\mathrm{evenly-dispersed}\}$，$\mathrm{clustered}$ 表示被操纵边的 $\mathrm{M}$ 次出现连续的放在一起，$\mathrm{evenly-dispersed}$ 表示被操纵边的 $\mathrm{M}$ 次出现在边列表允许槽位中尽量均匀分布，上述实验条件记为 $\mathrm{clustered}$ 条件下的频次控制样本。$\mathrm{evenly\text{-}dispersed}$ 条件以原始 branching 整体 shuffle 后得到的样本为底图，设插入 $\mathrm{M}$ 个出现实例后的边列表总行数为 $\mathrm{L}$，即 $\max\{\text{edge-list index}\}=\mathrm{L}$。首先，从边列表抽出终点边 $e_{k}=(v_{k-1},y)$，将边列表划分为 $\mathcal{B} = M$ 个区域，每个区域宽度为 $\omega=\frac{\mathrm{L}}{M}$。其中，第 $b$ 个区域定义为 $\mathcal{B}_b=\left\{i\in\{1,\ldots,\mathrm{L}\}:\left\lfloor(b-1)\cdot\omega\right\rfloor<i\le \left\lfloor b\cdot\omega\right\rfloor\right\}$，其中 $b=1,\ldots,M$，$\left\lfloor b\Omega\right\rfloor$ 简记为 $\ell_b$。随后，在每个区域 $\mathcal{B}_b$ 的区域中心 $p_b=\left\lfloor\frac{\ell_{b-1}+1+\ell_b}{2}\right\rfloor$ 处插入一条终点边 $e_{k}$，得到 $\mathrm{evenly\text{-}dispersed}$ 条件下的频次控制样本。为描述重复边在文本序列中的分布形态，本文采用 Span 衡量重复边最早与最晚出现位置之间的归一化跨度。定义 $\mathrm{Span}=\frac{p_M-p_1}{L-1}$，其中 $p_1$ 为重复边第一次出现的位置，$p_M$ 为重复边最后一次出现的位置，$L$ 为插入 $M$ 次出现后的最终边列表总行数。理想图遍历器看到一次 $e_i$ 后就会将其加入边集合 $E$，对重复边去重后，六个条件对应完全相同的有向图。若模型忠实地遍历并恢复去重后的图结构，则在图结构、起点、跳数与边位置模板保持不变时，边重复次数 $\mathrm{M}$ 不应系统性改变输出；若模型不忠实于图，则分四种情况：由于集中重复能提高同一局部块的候选显著性，如果集中重复强，分散重复弱，则说明模型行为更符合逐步游历过程中依赖局部文本块检索候选边的机制；由于分散重复能增强全局可检索性和多位置命中率，如果分散重复强，集中重复弱，则说明模型更可能在全局文本序列中进行多位置检索或证据聚合；如果两者都强，则 frequency salience 和 global retrievability 都在起作用；如果两者都弱，则该模型不太受边重复影响

在同一 $(j,  s, \text{pos})$ 单元内,除被操纵边的重复次数 $\mathrm{M}$ 外，其余边及其相对顺序保持不变，且均使用 jsoncot_strict。本实验共 $\mathrm{J}=400$ 个独立 sample_id， $\mathrm{clustered}$ 条件下，每个 $\mathrm{sample\_id}$ 在 $\mathrm{K}$ 个独立 shuffle seed 下执行 6 $\mathrm{pos}$ × 4 $\mathrm{M}$ 全因子设计，频次控制样本调用数 $400 \times 6 \times 4 \times K = 9600K$；频次对照样本采用相同的 6 × 4 全因子,调用数 $9600K$，E4 主实验总调用数 $9600K + 9600K = 19200K$。$\mathrm{evenly-dispersed}$ 条件下的结构对照样本有 $400K \times 4 = 1600K$ 次调用，全实验共 $19200K + 1600K = 20800K$ 次调用。为避免将早期路径错误归因于终点边干预，主实验只统计模型正确到达黄金路径的倒数第三个节点 $v_{k-2}$（生成器限制 $v_{k-2}$ 为最后一个存在同源边的分叉节点；稳健性实验同理）。作为 Appendix 中的补充缓解实验，本文另设指令对照样本，指令为 duplicate-edge instruction $\mathrm{Z} \in \{0,1\}$。其中 $\mathrm{Z=0}$ 表示使用主实验 prompt，$\mathrm{Z=1}$ 表示使用下列 prompt。该 instruction 条件不进入 E4 主矩阵，也不计入主实验调用数；其目的仅是检验显式去重规则是否能够缓解主实验中观察到的频次锚定现象。prompt指令如下：
```
Graph interpretation rules:
1. Treat the edge list as a simple directed graph.
2. If the same directed edge appears multiple times, treat it as a single edge.
3. Duplicate edge rows do not indicate higher importance, larger weight, or additional evidence for the correct path.
```
## B.5：长路径失败的状态维护与终端分支诊断

为诊断有向图 $G = (V,E)$ 在 jsoncot_strict 设置下，导致长路径中 branching-$k$ hop 出现低 accuracy 现象的两类可能困难：a 类困难是模型能力不足导致状态维护失败。随着路径变长，模型需要维护的决策链长度也随之增加，这使得模型更可能在抵达最后分支节点 $v_{k-2}$ 前就已经偏离黄金路径；b 类困难是局部决策困难，最后分支节点 $v_{k-2}$ 属性特殊。模型在之前的分叉点处主要面对同源边候选竞争，然而在 $v_{k-2}$ 节点处，模型额外面对两个挑战，紧张的剩余 hop 数约束（horizon）与终点对齐约束（由于 $v_{k-1}$ 直接通向黄金答案 $y$，不存在终点对齐困难，因此终点对齐约束主要作用于节点 $v_{k-2}$ 处）。可以证明，在本文 § 3 的唯一黄金路径 $\mathcal W(G,s,k)=\{p^*\}$ 设定下，horizon 与终点对齐约束在任务信息层面构成等价约束，换言之，二者在任务信息层面不可作为两个独立因素分离，因此之后统称为终端约束。证明如下，沿用 § 3.1 的任务路径集合定义。记 $\mathcal W(G,v_i,r)$ 为从节点 $v_i$ 出发、长度为 $r$、终点为 $y$ 的合法有向任务路径集合。若任务满足唯一黄金路径条件 $\mathcal W(G,s,k)=\{p^*\}$，其中 $p^*=(v_0=s,v_1,\ldots,v_k=y)$，则对任意黄金路径节点 $v_i$，都有 $\mathcal W(G,v_i,k-i)=\{(v_i,v_{i+1},\ldots,v_k)\}$。因此，在给定当前状态为 $v_i$ 的条件下，剩余 hop 数 $k-i$ 与该剩余预算下的合法终点 $y$ 共同指向同一个唯一后缀，因此，剩余预算约束与终点对齐约束在任务信息层面不可作为两个独立因素分离。由上述可知，b 类困难包括同源候选竞争与终端约束，其中同源候选竞争于 E6 / E7 两个实验中诊断。branch 主实验的 accuracy 矩阵如下：

| model               | 4-hop | 6-hop | 8-hop | 10-hop | 12-hop |
| ------------------- | ----- | ----- | ----- | ------ | ------ |
| GPT-5.4-mini        | 71.7% | 43.6% | 23.6% | 12.3%  | 8.3%   |
| Qwen-Max            | 68.7% | 56.9% | 39.4% | 20.1%  | 13.8%  |
| DS-Flash            | 88.2% | 79.1% | 67.2% | 58.4%  | 50.2%  |
| DS-Pro non-thinking | 87.3% | N/A   | N/A   | N/A    | 56.4%  |
主实验虽然进行了 hop 长度扫描，但仅凭不同 hop 下的 $\mathrm{{\Delta_{Accuracy}}}$ 无法诊断上述两类困难。其中，a 类困难无法诊断的原因在于路径长度 $k$ 与局部决策次数天然耦合，即当 $k$ 增加时，额外决策次数 $\mathrm{\Delta k}$ 同时增加。在理想情况下，在每个黄金节点 $v_i$ 处选中黄金后继的概率为常数 $p$，则 Accuracy 曲线天然呈现 $p^k$ 的几何衰减。此时，Accuracy 随 hop 增加而下降，可能是由于自身数学特性，也可能是由于模型在靠后的 $v_i$ 处出现状态维护能力退化，即真的出现了 a 类困难。且实际中 $p$ 可能并非常数，这就引入更多需要考虑变量；同时，直接比较不同 $k$ hop 曲线各自在 $v_{k-2}$ 处选中黄金后继的概率，会引入幸存者效应，即​ hop 数越长，抵达 $v_{k-2}$ 的前缀难度越高，该 $k$ hop 类别的幸存子集越少（例如 8 hop 中 GPT / Qwen 通过前缀的比例已跌破 0.5）。因此跨 $k$ hop 比较的是不同筛选强度下的非配对子集，存在误差。对此，E5 不再比较最终 Accuracy，而是比较四种条件下 $v_{k-2}$ 处选中黄金后继的诊断性 $2\times2$ 边界设计，用 $\mathrm{T}$ 代表不同任务设定。需要强调的是，E5 并非严格的单因素因果消融。O-local / O-full 在移除前缀状态维护负担的同时也改变了输入分布与有效搜索空间；R-task 并非完全移除终端约束，而是将原始 2-hop 终端窗口替换为后续延展窗口。因此，E5 的估计量应解释为诊断性边界对比，而非对终端约束因果效应的无偏识别。四个实验设计如下：

第一个实验，将原始 branching-$k$ hop 主实验定义为完整状态基线实验（Full-task baseline，F-task），同时存在 a 类与 b 类两类困难。第二个实验，目标替换任务（Target-Replacement Task，R-task）实验，设计目的为对比终端约束对 $v_{k-2}$ 处黄金后继选择概率的影响。首先使用相同生成规则（即 1 条 $e_{\mathrm{gold}}$ + 2 条 $e_{\mathrm{dead}}^{A,B}$）生成一定长度的 $z$ 链，即 $z_1\to z_2\to\cdots\to z_{k}$，放置于原始 branching-$k$ hop 的 $v_{k-1}$ 节点与 $v_{k}$ 节点之间，得到 $v_{0}\to\cdots\to v_{k-2}\to v_{k-1}\to z_1\to\cdots\to z_k\to y$。随后需要保证边列表元素数量相同，补充指向非黄金节点的干扰边，干扰边生成规则沿用原始 branching 的干扰边生成规则；需要缓解 $v_{k-2}$ 节点决策环境差异导致的干扰，应保证 $v_{k-2}$ 处的局部候选窗口逐字一致、候选数量一致、$v_{k-1}$ 的相对位置一致、$e_{\mathrm{dead}}^{A,B}$ 一致、shuffle 顺序一致，下列实验沿用此设定。对于 task 系列实验，定义 cell-level 主统计量为 $p_F$ 与 $p_R$，未抵达 $v_{k-2}$ 的样本记为 0。$p_F$ 表示任务设定为 $F$ 时，模型从 $v_0$ 出发，成功抵达 $v_{k-2}$ 且下一跳选中 $v_{k-1}$ 的概率，即 $p_F=\Pr(\text{前 }k-2\text{ hop 正确} \land \hat v_{k-1}=v_{k-1} \mid \mathrm{T = F} )$；$p_R$ 表示任务设定为 $R$ 时，模型从 $v_0$ 出发，成功抵达 $v_{k-2}$ 且下一跳选中 $v_{k-1}$ 的概率，即 $p_R=\Pr( \text{前 }k-2\text{ hop 正确} \land \hat v_{k-1}=v_{k-1} \mid \mathrm{T = R} )$。同时定义幸存者版本统计量用于辅助诊断，$p_F^{\mathrm{cond}}=\Pr(\hat v_{k-1}=v_{k-1}\mid \mathrm{T}=F \land \text{前 }k-2\text{ hop 正确})$；$p_R^{\mathrm{cond}}=\Pr(\hat v_{k-1}=v_{k-1}\mid \mathrm{T}=R \land \text{前 }k-2\text{ hop 正确})$。其中 $p_F= \Pr(\text{前 } k-2 \text{ hop 正确}) \times \Pr(\hat v_{k-1}=v_{k-1}\mid \text{前 } k-2 \text{ hop 正确}) = q_{\mathrm{reach}} \times p_F^{\mathrm{cond}}$，其中 $q_{\mathrm{reach}}$ 称为到达率，作为辅助诊断指标，表示模型沿黄金路径正确走完前 $k-2$ 跳、抵达 $v_{k-2}​$ 的概率。前者衡量长序列状态维护失败，后者衡量幸存样本中的终端局部决策困难（表 X）。该切片不依赖 oracle 干预，因而不引入输入分布或搜索空间的改变，也不声称识别两类机制的纯因果效应，而用于刻画主要失败质量在前段状态维护与终端局部选择之间的分布。

第三个实验，理想信息条件下的局部任务（Oracle-local，O-local），以 $v_{k-2}$ 为起点，要求模型完成剩余 $2$ hop 后缀并选中黄金答案。通过在 branching-$k$ hop 主实验中，仅保留图 $G$ 中 $v_{k-2}$ 节点之后的（包含 $v_{k-2}$）黄金节点 $v_i$，以及 $v_i$ 对应的两个同源终止分支节点，保证边列表元素数量相同，干扰边数量调整至对应水平。由于此时抵达最终分支节点 $v_{k-2}$ 的路径前缀被移除，因此前段状态维护负担被卸载，但同时也缩小了输入图的有效搜索范围；第四个实验，理想信息条件下的全部任务（Oracle-full，O-full），对于 R-task 中得到的长链，仅节选 $v_{k-2}\to v_{k-1}\to z_1\to\cdots\to z_k\to y$ 及其对应终止分支部分（z 链相同长度），为保证边列表元素数量相同，干扰边数量调整至对应水平。由于 O 系列实验直接从 $v_{k-2}$ 出发，因此不存在幸存者效应，因此可以定义主统计量为 $p_{\mathrm{local}}$ 与 $p_{\mathrm{full}}$。$p_{\mathrm{local}}$ 表示模型从 $v_{k-2}$ 出发，下一步选中黄金后继 $v_{k-1}$ 的概率，即 $p_{\mathrm{local}} = \Pr(\hat v_{k-1} = v_{k-1} \mid \mathrm{T = local} \land start = v_{k-2})$；$p_{\mathrm{full}}$ 表示模型从 $v_{k-2}$ 出发，下一步选中黄金后继 $v_{k-1}$ 的概率，即 $p_{\mathrm{full}}=\Pr(\hat v_{k-1}=v_{k-1}\mid \mathrm{T = full} \land start=v_{k-2})$。

| 条件 $\mathrm{T}$ | 起点 /幸存者效应     | 状态维护负担 | 终端约束（horizon + 终点对齐） | 后续延展负担 | 主统计量 / 辅助统计量                  |
| --------------- | ------------- | ------ | -------------------- | ------ | ----------------------------- |
| F-task          | $v_{0}$ / 有   | 有      | 有                    | 无      | $p_F$ / $p_F^{\mathrm{cond}}$ |
| R-task          | $v_{0}$ / 有   | 有      | 弱化                   | 有      | $p_R$ / $p_R^{\mathrm{cond}}$ |
| O-local         | $v_{k-2}$ / 无 | 无      | 有                    | 无      | $p_{local}$ / N/A             |
| O-full          | $v_{k-2}$ / 无 | 无      | 弱化                   | 有      | $p_{full}$ / N/A              |
对于 a 类困难，由实验矩阵可知，应对比 F-task & O-local 样本，以及 R-task & O-full 样本。将状态维护效应定义为基于 sample_id 配对的（下同）oracle 当前状态与完整前缀状态之间的差异 $\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}on}}$ 与 $\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}off}}$。其中，$\Delta_{\mathrm{state}}^{\mathrm{terminal-on}}$ 表示原始终端约束存在时，状态维护负担造成的局部选择损失，即 $\Delta_{\mathrm{state}}^{\mathrm{terminal-on}} = p_{\mathrm{local}} - p_{F}$；$\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}off}}$ 表示终端约束被弱化时，状态维护负担造成的局部选择损失，即 $\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}off}}=p_{\mathrm{full}}-p_R$。$\Delta_{\mathrm{state}}$ 应解释为移除前缀状态维护与空降当前状态后的总恢复效应，而不是固定样本状态下的纯状态维护消融。若两个 $\Delta_{\mathrm{state}}$ 均较大，说明状态维护负担在两类终端环境下都会显著降低局部后继选择概率。

对于 b 类困难中的终端约束效应，由实验矩阵可知，应对比 F-task & R-task 样本，以及 O-local & O-full 样本。将终端约束效应定义为定义为弱化终端窗口后，$v_{k-2}$ 处黄金后继选择概率的提升 $\Delta_{\mathrm{terminal}}^{\mathrm{state-on}}$ 与 $\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}off}}$。其中，$\Delta_{\mathrm{terminal}}^{\mathrm{state-on}}$ 表示有前缀状态维护负担时，弱化终端约束带来的第一跳提升，即 $\Delta_{\mathrm{terminal}}^{\mathrm{state-on}}=p_{\mathrm{R}}-p_{\mathrm{F}}$；$\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}off}}$ 表示无前缀状态维护负担时，弱化终端约束带来的第一跳提升，即 $\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}off}}=p_{\mathrm{full}}-p_{\mathrm{local}}$。若两个 $\Delta_{\mathrm{terminal}}$ 均较大，说明终端位置约束本身带来稳定额外困难。

由 $2\times2$ 结构，状态维护效应之差与终端约束效应之差两种算法等价（算术恒等）：$\Delta_{\mathrm{interaction}}=\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}on}}-\Delta_{\mathrm{terminal}}^{\mathrm{state\text{-}off}}=\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}on}}-\Delta_{\mathrm{state}}^{\mathrm{terminal\text{-}off}}$。即 $\Delta_{\mathrm{interaction}}=(p_R-p_F)-(p_{\mathrm{full}}-p_{\mathrm{local}})$。由于二者相减可能包含状态维护与终端窗口的真实交互与 task 系列独有的可达性筛选效应，因此不将其理解成干净的因果交互。若 $\Delta_{\mathrm{interaction}}$ 显著偏离 0，说明终端约束效应依赖前缀状态维护负担，或等价地，状态维护效应依赖终端窗口是否存在。若上述效应均较小，则低 accuracy 更可能来自其他机制或任务构造因素。
## B.6：同源候选数量与输出约束

E6 检验 chain 是否可以看作没有同源终止分支候选竞争（简称同源竞争）的退化 branching 情形，通过控制出度，考察同源竞争是否降低选中黄金后继的概率。通过与 E2 协同诊断，刻画同源竞争如何影响模型选中 $e_{dead}$ ，以及 $e_{dead}$ 如何在接口的作用下进一步呈现为非法路径的传导机制。

令 $D$ 表示每个 branch $k$ hop 黄金节点的出度候选集合，$d \in D\{1,2,3,4,5\}$：$d = 1$ 时只保留黄金路径，此时 branching 退化为 chain，$d = 3$ 时为主实验设定，记为 canonical，$d = \{2,4,5\}$ 留作扫描，用于揭露演化趋势。为保证边列表元素数量一致，E6 使用位置守恒的确定性重排。为避免 edge list 内容差异导致的跨 $d$ 排序结果不可比，E6 不使用依赖列表长度的 random shuffle，而采用 keyed 排序。对每条边 $e$ 被赋予确定性分数 $\mathrm{score}(e)=\mathrm{SHA256}(\mathrm{seed}_{\mathrm{sample}}\Vert \mathrm{src}(e)\to \mathrm{dst}(e))$。边列表按 $\mathrm{score}(e)$ 升序排列。由于 $\mathrm{score}(e)$ 仅依赖边的内容标识与样本种子，任意在多个 $d$ 条件下共有的边，其相对顺序在所有 $d$ 下保持一致。$d$ 增大时，新增的同源终止分支按同一公式插入确定位置；为保持总边数守恒而删除的背景干扰边，不改变其余边的相对顺序。同时，对于边数守恒（边列表元素数量守恒，则调整 $d$ 必然影响干扰边数量，进而影响黄金节点处竞争纯度）与竞争纯度守恒（干扰边数量不变，则调整 $d$ 比如影响边列表元素数量）问题，本实验采用折中处理，即增加背景干扰边总量，使得边数守恒下调整 $d$ 引发的干扰边变化占比较低，同时，背景干扰边总量太多会加重 Lost-in-the-Middle，进而压低 $q_{\mathrm{reach}}$，并且放大 position effect ，因此干扰边具体数量判据为 $q_{\mathrm{reach}}$ 实测稳定性：在固定 $d$ 下进行仅删除重排控制（deletion-only reshuffle control），用于检验 E6 的效果是来自新增同源候选竞争，还是改变 distractor edges 引发的文本环境改变。通过随机删除与主实验相同数量的背景 distractor edges，并用多个 seed 重新 shuffle 边列表，但不改变任何路径节点的出度与同源候选集合，之后诊断该操作导致的 $q_{\mathrm{reach}}$ 波动。如果显著小于主 E6 中跨 $d$ 的 $q_{\mathrm{reach}}$ 变化，则说明主结果中的 $q_{\mathrm{reach}}$ 变化不是由背景边删除/重排单独解释。诊断指标如下：第一，主实验跨 d 的 $\Delta q_{reach}$ 变化，即 $\Delta q_{reach}=\max_d q_{\mathrm{reach}}(d)-\min_d q_{\mathrm{reach}}(d)$；第二，删除 / 重排控制中的 $q_{\mathrm{reach}}$ 波动。首先统计 deletion-only control 下，$q_{\mathrm{reach}}$ 的平均随机波动，即 $\mathrm{SD}_{q,\mathrm{delete}}=\operatorname{SD}_s\left(q_{\mathrm{reach}}^{\mathrm{delete}}(s)\right)$。其实统计在所有 deletion-only seed 里，$q_{\mathrm{reach}}$ 最大波动范围，即 $\mathrm{Range}_{q,\mathrm{delete}}=\max_sq_{\mathrm{reach}}^{\mathrm{delete}}(s)-\min_s q_{\mathrm{reach}}^{\mathrm{delete}}(s)$。如果 $\rho_{\mathrm{range}}=\frac{\mathrm{Range}_{q,\mathrm{delete}}}{\Delta q_{\mathrm{main}}} \ll 1$ 或 $\rho_{\mathrm{sd}}=\frac{\mathrm{SD}_{q,\mathrm{delete}}}{\Delta q_{\mathrm{main}}} \ll 1$，则 deletion-only reshuffle control 的影响不足以解释主实验中的 $q_{\mathrm{reach}}$ movement；第三，删除重排扰动占比，衡量 deletion-only reshuffle control 造成的 $q_{\mathrm{reach}}$ 波动，占主 E6 跨 $d$ 变化的多大比例，即 $\rho_{\mathrm{delete}}=\frac{\max_s q_{\mathrm{reach}}^{\mathrm{delete}}(s)-\min_s q_{\mathrm{reach}}^{\mathrm{delete}}(s)}{\max_d q_{\mathrm{reach}}(d)-\min_d q_{\mathrm{reach}}(d)}$。如果 $\rho_{\mathrm{delete}}\ll 1$，就说明删除背景边不是主要来源。

要注意的是，随调整 $d$ 同步调整的 $v_i​$ 出现次数是任务结构的自然产物，因为模型不知道干扰边不包括黄金节点这条生成规则，无法区分 $v_i​$ 后继多是因为是黄金节点还是碰巧干扰边都包含该节点。为排除地板效应与天花板效应，参考 E5 给出的 $q_{\mathrm{reach}}(k)$ 以及 $p_F^{cond}​(k)$ 选取实验采用的 $k$ hop，随后分别结合三种接口因子：direct_minimal（仅输出答案，不纳入 reach-conditioned 主指标）、jsoncot_basic（输出路径与答案，但不逐条强调约束）、jsoncot_strict（要求正确起点、恰好 $k+1$ 个节点、逐边合法和答案等于路径末节点），观察 prompt 对同源竞争的影响，形成 $5\times3$ 因子设计，每格调用 $800$ 次，共 $12000$ 次.

|   出度 d | $k$ hop 下条件名称 | direct_minimal | jsoncot_basic | jsoncot_strict |    小计 |
| -----: | ------------- | -------------: | ------------: | -------------: | ----: |
|      1 | 无竞争 （chain）   |            800 |           800 |            800 |  2400 |
|      2 | 低出度扫描         |            800 |           800 |            800 |  2400 |
|      3 | 标准设定（主实验）     |            800 |           800 |            800 |  2400 |
|      4 | 高出度扫描         |            800 |           800 |            800 |  2400 |
|      5 | 更高出度扫描        |            800 |           800 |            800 |  2400 |
| **合计** | —             |           4000 |          4000 |           4000 | 12000 |
全样本下按照 `sample_id` 聚类估计 95% bootstrap CI，未到达局部决策点的样本记为 0。主要统计指标为前 $k-1$ hop（$v_{k}$ 处无同源竞争，退化为 chain）成功率 $p_{k-1}(d) = q_{\mathrm{\text{reach k-1}}}(d)\times p_{k-1}^{\mathrm{cond}}(d)$ 以及抵达率 $q_{\mathrm{reach}}(d)$。通过比较前 $k-1$ hop 成功率在不同出度下的变化 $\Delta p = p(d+1)-p(d)$，诊断同源竞争的代价。同时利用数据拟合曲线 $p(d)=\alpha+\beta \cdot (d-1)+\gamma \cdot (d-1)^2+\varepsilon_d$，其中 $d-1$ 用于中心化，此时截距 $\alpha$ 表示 $d = 1$ 时（完全无同源竞争）的 $p(d)$，即退化基线 chain；斜率 $\beta = \frac{\partial p(d)}{\partial d}$ 表示 $p(d)$ 随 $d$ 的变化率，即同源竞争的边际代价，为避免数学属性影响诊断，报告 $p(d)$ 的随机基线$p_{\mathrm{random}}(d) = (\frac{1}{d})^{k-1}$，计算超出随机倍数 $v(d) = \frac{p(d)}{p_{\mathrm{random}}(d)}$，含义为 $p(d)$ 比模型随机猜测强多少倍；曲率 $\gamma = \frac{\partial^2 p}{\partial d^2}$ 表示下降是匀速还是变速，对应同源竞争代价是否随 $d$ 变化。$p(d)$ 的曲率在 $p(d)-p_{\mathrm{random}}(d)(d>1)$ 条件下计算，其中相减用于去除自身数学特性的影响；不计算 $d=1$ 因为存在 chain 到 branching 的跃迁成本。结果分为三种情况：第一，$\gamma > 0$ 凸曲线，对应地板效应；第二，$\gamma < 0$ 凹曲线，表示竞争的超线性恶化，即竞争不是线性稀释，而是存在崩溃点；第三，$\gamma \approx 0$ 表示竞争是线性稀释，每加一条候选代价恒定。若 $p(d)$ 因 $q_{\mathrm{reach}}$ 与 $p^{\mathrm{cond}}$ 反向变动而非单调，则 $\alpha/\beta/\gamma$ 仅作描述统计，机制结论由 $p^{\mathrm{cond}}(d)$ 上的对应拟合给出。

抵达率 $q_{\mathrm{reach}}(d)$ 用于诊断随调整 $d$ 同步调整的 $v_i​$ 出现次数引发的到达率变化，与 $p^{\mathrm{cond}}(d)$ 协同分析。$p^{\mathrm{cond}}(d)$ 表示 $v_{k-1}$ 的局部正确率，即 $p^{\mathrm{cond}}=\Pr(\hat v_{k-1}=v_{k-1}\mid \text{前 }k-2\text{ hop 正确})$。出度越大，黄金节点在文本中越突出，模型越容易抵达 $v_{k-2}$，此时抵达率 $q_{\mathrm{reach}}(d)$ 上升；但抵达后，更大的出度使模型选中 $e_{dead}$，此时 $p_{\mathrm{cond}}\downarrow$ 且 $p_{\mathrm{dead}}\uparrow$。其中，两者反向变化揭示了 d 的双刃剑效应，证明到达和选择是两个独立机制，即 E5 中的 a 类（状态维护 / 到达）与 b 类（局部选择）困难之间的分解；且 $q_{\mathrm{reach}}$ 的变化可用于揭示 $p_{\mathrm{cond}}$ 跨 $d$ 的幸存者偏差，即 $q_{\mathrm{reach}}$ 上升导致幸存子集随 $d$ 扩大，高 $d$ 子集混入了更多原本到不了的弱样本，导致 $p_{\mathrm{cond}}$ 的真实下降被低估，即保守偏差。

本实验的辅助统计指标为 accuracy、错误率 $p_{\mathrm{error}} = 1 - p^{\mathrm{cond}}$ 以及 first / last candidate preference。其中错误率 $p_{\mathrm{error}}$ 可进一步分解为两个互斥子类，终止分支 $p_{\mathrm{dead}}(d) = Pr(\hat v_{k-1} = v^{dead}_{k-1} \mid D = d \land \text{前 } k-2 \text{ hop 正确})$ 与图外非法跳转率 $p_{\mathrm{illegal}}(d) = Pr( \text{illegal jump} |D = d \land \text{前 } k-2 \text{ hop 正确})$，满足 $p^{\mathrm{cond}}(d) + p_{\mathrm{dead}}(d) + p_{\mathrm{illegal}}(d) + p_{\mathrm{other}}(d) = 1$，其中 $e_{\mathrm{dead}}$ 子集的后续行为由 E2 继续分析。由于第 $k$ hop 不存在同源竞争，因此第 $k$ hop 的准确率不纳入主统计量，而是由 accuracy 与 $p_{k-1}(d)$ 协同进行分析。额外报告  first / last candidate preference，用于区分同源竞争与候选边在文本中的 first / last 位置，定义为 $p_{\mathrm{first}}(d)=\Pr(\mathrm{selected\ rank}=1 \mid \mathrm{observed\ local\ candidate\ choice})$ 以及 $p_{\mathrm{last}}(d)=\Pr(\mathrm{selected\ rank}=d \mid \mathrm{observed\ local\ candidate\ choice})$。
## B.7：同源候选竞争对位置锚定的瓦解

E7 的目的是用同一个最小干预，将答案层锚定与路径层非法联系起来，通过操纵 $v_{k-2}$ 节点的局部出度，从 direct_minimal 中的答案层锚定，到 jsoncot 中的终止/非法路径，二者共同构成联合诊断。$G = (G_1,G_2,G_3)$ 构造方法是，对于同一 chain $k$ hop，$G_3$ 使用原始链式图，与 $d$ 无关；$G_1,G_2$ 则仅控制 $v_{k-2}$ 节点的出度 $d$，称为修改后的链式图（modified chain，m-chain），且对于在 $v_{k-2}$ 注入的每个同源候选 $v_{k-2}^{dead,d}$，应满足零出度 $\mathcal{N}^{+}(v_{k-2}^{dead,d})=\varnothing$ 与不指向黄金节点，从而 $|\mathcal{W}(G,s,k)|=1$ 在 $d\ge 2$ 下仍成立，其中 $d \in D\{1,2,3,4\}$，且当 $d = 1$ 时退化为原始 chain。全部的同源候选构成同源候选集合，即 $Z_{\mathrm{dead}}(v_{k-2})=\{v_{k-2}^{dead,d}\}$。$G_1$ 的末节点为黄金答案，$G_2$、$G_3$ 末节点则是在黄金答案后加一个 decoy last 节点 $z$；$G_1$ 用于诊断 b 类困难中的同源竞争，$G_2,G_3$ 用于计算同源竞争对位置捷径瓦解的影响。随着 $d$ 增加，减少相应干扰边数量保持边列表元素总量不变，最后重新 shuffle 一次，为保证跨 E 实验结果具有可比性，均使用同一 $\mathrm{keyed_{k,d,E}}$ 进行 shuffle（即 E6 标准） 。E7 覆盖 4 / 5 / 6 hop 以及（direct_minimal、jsoncot_basic、jsoncot_strict）这三种 prompt 接口，$G_2/G_3$ 用于检测答案层，direct_minimal 足够；$G_1$ 用于检测路径层，必须依赖显式路径，故需 jsoncot。构造反事实图对 $G = (G_1,G_2,G_3)$，其中 $G_1,G_2$ 为 m-chain，$G_3$ 为原始 chain；对于某一 $k$ hop 图，每个 cell 包含 $800$ 次调用，共 $800 \times 4 = 3200$ 次调用，在三种 hop 下，全实验共 $3200 \times 3 = 9600$ 次调用。对于单一模型的完整单一 hop 的实验矩阵如下。

| 图 $G$ | $v_{k-2}$ 出度 | 末位节点                  | 角色                    | direct_minimal | jsoncot_basic | jsoncot_strict |
| ----- | -----------: | --------------------- | --------------------- | -------------- | ------------- | -------------- |
| $G_1$ |           改变 | 黄金节点 $y$ 位于 $k$ hop   | 诊断 b 类困难中的同源竞争        | 0              | 800           | 800            |
| $G_2$ |           改变 | 诱饵节点 $z$ 位于 $k+1$ hop | 提供 SS-EAR，诊断位置捷径怎么被瓦解 | 800            | 0             | 0              |
| $G_3$ |           不变 | 诱饵节点 $z$ 位于 $k+1$ hop | 原始 EAR 基线             | 800            | 0             | 0              |
需要注意的是，由于 decoy last 位于 $k+1$ hop，因此 $G_3$ 中 EAR 揭示的是 hop 约束与末位显著节点间的竞争（模型要选 $z$ 必须违背恰好 $k$ 跳的约束），而 $G_2$ 在 $G_3$ 的基础上引入了同源终止分支，因此测量的是同源终止分支对末尾显著节点的瓦解作用，而 $G_1$ 由于检测的是 $v_{k-1}$ 处的正确率（即 b 类困难中的同源竞争），关闭了 decoy 通道，从而降低了末尾显著节点对实验指标的干扰。

$G_1$ 的主指标为 $p^{\mathrm{cond}}(d)$ 和 $p_{\mathrm{error}}(d)$，其中 $p^{\mathrm{cond}}(d)$ 表示$v_{k-1}$ 的局部正确率，即 $p^{\mathrm{cond}}(d)=\Pr(\hat v_{k-1}=v_{k-1}\mid D=d  \land \text{前 }k-2\text{ hop 正确})$；$p_{\mathrm{error}}$ 的两个子指标为：终止分支 $p_{\mathrm{dead}}(d) = Pr(\hat v_{k-1} \in Z_{\mathrm{dead}}(v_{k-2}) \mid D = d \land \text{前 } k-2 \text{ hop 正确})$ 与图外非法跳转率 $p_{\mathrm{illegal}}(d) = Pr( \text{illegal jump} |D = d \land \text{前 } k-2 \text{ hop 正确})$。指标及分析在 E2 / E5 / E6 中已作详细说明，此处不再赘述。

$G_2$ 的主统计指标为同源竞争下的 EAR（Same-Source Endpoint Anchoring Rate，SS-EAR），即 $\mathrm{SS\text{-}EAR} = \mathbb{1}[\hat y_{G_2} = z^{(j)}]$。条件层指标仅在 $\mathrm{EAR}_{G_3} = 1$（即已出现位置捷径）的子集上计算，即 $\mathrm{SS\text{-}EAR}^{\mathrm{cond}}=\Pr\left(\hat{y}_{G_2}=z \mid \mathrm{EAR}_{G_3}=1\right)$，若 $\mathrm{SS\text{-}EAR}^{\text{cond}} \approx 1$，则位置捷径显著，模型更侧重直接使用末尾节点，而不论图中是否存在局部同源终止候选；若 $\mathrm{SS\text{-}EAR}^{\text{cond}} < 1$ 显著，则同源竞争瓦解位置捷径，同源终止候选成为局部可达但非 $k$-hop 合法答案的竞争项，激活了路径遍历。依据 sample_id 进行配对，计算 $\mathrm{SS\text{-}EAR}^{\mathrm{cond}}$ 与 $\mathrm{EAR}^{\mathrm{cond}}$ 之差为瓦解率，即 $\mathrm{\Delta EAR}_{\mathrm{ss}}^{\mathrm{cond}} = \mathrm{EAR}_{G_3}^{\mathrm{cond}} - \mathrm{SS\text{-}EAR}_{G_2}^{\mathrm{cond}} = 1-\mathrm{SS\text{-}EAR}_{G_2}^{\mathrm{cond}}$，正值表示加同源边后锚定减弱，反之相反。同时，为避免幸存者效应，同时报告全样本下的 SS-EAR 与 $\mathrm{\Delta EAR}_{\mathrm{ss}}$（不符合 $\mathrm{EAR}_{G_3}^{\mathrm{j}} = 1$ 的样本记 0），即 $\mathrm{\Delta EAR}_{\mathrm{ss}}^{\mathrm{(j)}} = \mathrm{EAR}_{G_3}^{\mathrm{(j)}} - \mathrm{SS\text{-}EAR}_{G_2}^{\mathrm{(j)}}$，之后取平均减少误差，$\mathrm{\Delta EAR}_{\mathrm{ss}} = \frac{∑_j ΔEAR_{ss}^{(j)}}{N}$。报告全实验 95% 置信度下的 bootstrap CI。

综上，$G_3$ 负责测量原始 decoy-last EAR；$G_2$ 负责在保持末位诱饵节点 $z$ 的同时，额外注入同源终止竞争，测量位置锚定是否被瓦解；$G_1$ 负责关闭 decoy-last 通道，仅保留同源终止竞争，用显式路径输出测量该结构是否转化为 dead-branch choice 与 illegal continuation。


---

# Appendix A Full Ablations

Appendix A 提供 prompt-interface、topology-depth、reasoning-budget 和 verifier-retry budget 的完整消融结果。正文 §5.6 只总结主要趋势，本附录报告逐项表格、补充图和额外分析，以支持第 5 节的主要结论。

## A.1 Prompt-interface ablation

本节比较三种 prompt interface：

```
direct_minimal
jsoncot_basic
jsoncot_strict
```

`direct_minimal` 是 answer-only 接口，主要暴露答案层位置捷径。`jsoncot_basic` 要求模型输出结构化路径和答案，使路径级诊断成为可能。`jsoncot_strict` 进一步要求严格 JSON、固定路径长度、起点一致和 answer-path consistency。

这一消融用于回答：

> 显式路径输出和更强格式约束是否真的提高图忠实性？

完整结果应报告 direct_minimal、jsoncot_basic 和 jsoncot_strict 在 chain 与 branching 设置下的 Accuracy、Raw GIS、PC-GIS、GFI、EAR、TAC、PathValid、PathGoldExact 和 (\Delta_{\text{illegal}})。

特别需要保留 basic vs strict 的对比。若 jsoncot_basic 在某些设置下具有更高单点正确率，但 jsoncot_strict 具有更强格式稳定性或更低位置污染，应明确报告这一 trade-off。该对比说明 prompt interface 不是简单的“越严格越好”，而是在答案正确率、位置稳健性、格式服从和路径合法性之间产生不同权衡。

## A.2 Topology-depth ablation

本节报告 chain depth scan 与 branching depth comparison。

Chain depth scan 包括：

```
chain_3hop_N200
chain_4hop_N200
chain_5hop_N200
chain_6hop_N200
```

该扫描用于检验答案层位置虚胖是否随 hop 长度变化，以及不同模型是否呈现不同失败机制。需要突出两类趋势：DeepSeek-V4-Flash 在 chain 3–6 hop 上持续保持较高 Raw GIS / GFI，说明其 answer-only 行为稳定受 endpoint-position shortcut 影响；Qwen Max 则随 hop 增长从较高 Raw GIS 迅速转为 near-zero sensitivity collapse，说明它在长链上不只是抗位置偏置，而是逐渐失去对图干预的敏感性。

Branching depth comparison 包括：

```
branching_4hop_N200
branching_12hop_N200
```

branching-4hop 作为中间难度设置，用于观察从简单 chain 到困难 branching-12hop 之间的过渡。branching-4hop 已经开始出现非法路径差距，而 branching-12hop 将该差距进一步放大，说明路径层失败并不是 JSON 输出格式造成的偶然现象，而是来自分叉结构中的持续状态追踪困难。

## A.3 Reasoning-budget ablation

本节比较 DeepSeek-V4-Pro no-thinking 与 DeepSeek-V4-Pro thinking。

该消融只在关键困难设置上运行：

```
branching_12hop_N200 + jsoncot_strict
```

报告指标包括 Accuracy、Raw GIS、PC-GIS、GFI、TAC、PathValid、PathGoldExact、(\Delta_{\text{illegal}})、平均延迟、中位延迟和 p95 延迟。

该消融用于回答：

> 更高内部推理预算是否能消除答案层和路径层失败？

结果显示，thinking 几乎解决 branching-12hop，但平均延迟显著高于 no-thinking。这说明任务本身可解，no-thinking 失败主要来自有限推理预算下的状态追踪不足，而不是数据集不可解或评估器过严。

## A.4 Verifier-retry budget ablation

本节报告 verifier-retry 在不同 retry budget 下的结果：

```
K=1,2,3,4,5
```

核心指标包括：

```
pass@1
pass@2
pass@3
pass@4
pass@5
latency@K
final PathValid
final PathGoldExact
final FailureHop distribution
final ErrorType distribution
repeated_same_hop
```

该消融用于回答：

> 外部符号验证是否能以较低成本修复非法路径？这种修复是否随 K 增加持续提升？

结果显示，pass@K 随 K 增加而上升，但边际收益递减。DeepSeek-V4-Flash 的提升幅度和最终上限明显高于 Qwen Max，说明 verifier-retry 的收益依赖底座模型已有的图跟随能力。对于 K 次后仍失败的样本，FailureHop 和 repeated_same_hop 分析进一步表明，剩余错误常常集中在稳定结构瓶颈上，而不是随机噪声。




---
# 附录
**Chain 图**  
有 `gold path + 背景干扰边`，但背景干扰边的起点不是 gold path 上的节点，所以模型如果从 `start` 沿合法边走，根本不会在 gold path 上遇到“可选下一步”。  
也就是说它只是边列表里多了一些无关噪声，不形成状态追踪中的分叉。

形式上像：

```
S -> A -> B -> C -> Y

X1 -> X2
M3 -> M4
Q7 -> Q8
```

从 `S` 出发，每一步只有一个合法下一跳。

**Branching 图**  
不仅有 `gold path + 背景干扰边`，还会从 gold path 节点额外接出 dead-end branch：

```
S -> A -> B -> C -> Y

S -> D1
A -> D2
B -> D3
```

这时模型从 `S` 或中间节点出发，会看到多个看似合法的下一跳；但只有一条能完成 exactly-hop gold path，其他是 dead-end。这个才是结构性分叉。

所以关键区别不是“有没有额外边”，而是：

|类型|干扰边是否从 gold path 节点出发|是否制造路径选择压力|
|---|---|---|
|Chain|否|否，只是文本噪声|
|Branching|是|是，模型必须在每一步选对边|
![[newplot (2).png]]



![[Pasted image 20260610002334.png]]

![[Pasted image 20260610002354.png]]
















![[Pasted image 20260609230122.png]]
![[Pasted image 20260610002119.png]]
![[Pasted image 20260610002654.png]]

![[Pasted image 20260609230203.png]]




![[Pasted image 20260614005006.png]]



---
### 🎯 第一梯队 · 定义 GIF 自身 / 直接动机（4 篇，最核心）

[1]**图没变，字变了：LLM 图推理的序列化不变性危机**（Lost in Serialization）— GIF 最近邻，序列化不变性危机 = 引言第 2–3 段的直接动机。
[2]. **图帮文本去噪，文本帮图复活**（TGS-RAG）— GFI = Raw GIS − PC-GIS 精确表述、pilot 数字、"生成层留空"缺口、真实 RAG testbed。
[3]. **别让大模型手算图：图推理应从复现算法转向设计算法**（Simple-RTC）— "工程绕过 vs 诊断显微镜"的对照，metrics 语境。

### 🔬 第二梯队 · 忠实性谱系（Related Work 主战场，9 篇）

[4]. **思维链会招供，最终答案会闭嘴**（Lie to Me, Young 2026）— verbalization vs structural faithfulness 的关键区分；跨家族 roster 借用源。
[5]. **思维链不忠实：模型如何把偏见包装成推理**（Turpin 2023）— 行为干预测忠实性的思想祖先。
[6]]. **给 CoT 推理拍 X 光：用计算图验证**（CRV）— 把 GIF 定位成黑盒/任务无关诊断框架；pilot 规模小→定位为 diagnostic benchmark 的依据。
[7]. **忠实性不是"看起来合理"：NLP 解释该如何证明自己没撒谎** — faithfulness 的严格定义来源，写 intro/定义节必引。
[8]. **会解释，不代表真会想：大模型忠实思维链为何困难**（Hardness of Faithful CoT）— "忠实本身就难"的理论背书。
[9]. **大模型的"推理小作文"可信吗？**（Measuring Faithfulness in CoT）— 忠实性测量方法谱系。
[10]. **先有答案，再编理由：真实场景中的 CoT 不忠实**（CoT in the Wild）— 真实场景不忠实的证据。
[11]. **偏见进了答案，却没进思维链**（Bias & CoT Faithfulness, VLLM）— 偏置注入法的旁证。
[12]. **模型嘴里的推理，未必是脑中的理由：CoT 忠实性失真** — 忠实性失真证据。⚠️ 这篇 frontmatter 的"论文题目"写成了 FiDeLiS，疑似复制错，建议核对。

### 📍 第三梯队 · 位置偏置/序列化（支撑 endpoint anchoring，5 篇）

[13]. **排名别看位置：InvariRank** — "注意力泄漏 ≫ 位置编码"的机制分解；endpoint anchoring 升级到机制归因的钩子。
[14]. **排在前面就更香：RISE** — 位置偏置量化框架；给你第二条反锚定干预臂（逐跳单步选择）。
[15]. **把图塞进 KV：Graph-KV** — 强制串行化→位置偏置/Lost-in-the-Middle，序列化弊端的总论来源。
[16]. **选项换个座位，模型换个答案**（MCQ 选项顺序敏感）— 位置敏感的跨任务旁证。
[17]. **顺序一换，模型就乱：LLM 输入排列敏感性** — 排列敏感跨 benchmark 证据。⚠️ frontmatter"论文题目"写成 Graph-constrained Reasoning，明显是复制错，建议修。

### 🧭 第四梯队 · 图推理方法（被诊断的"增强路线" + 数据/testbed，11 篇）

[18]. **图结构不是文本：LLM 需要导航**（GraphWalk）— 底座（合成图生成器）+ 反例 + 弹药三合一。
[19]. **边看边走：SoG**（Search-on-Graph）— 最佳真实世界 testbed。
[20]. **把计划画成图：用 GNN 抓任务规划错接漏步**（GNNVerifier）— 真实图数据源（TaskBench/UltraTool）。
[21]. **路径别全塞：KGQA 需要看几跳历史？**（BPC）— 行为冗余的层级互补论据。
[22]. **让大模型沿着图走：知识图谱把推理从"表演"拉回"路径"**（RoG）— 忠实 KG 推理代表方法。
[23]. **别让大模型硬算图：拆成三步调用图工具**（GCR, Graph-constrained Reasoning）— 标题自带"faithful reasoning"，正面对照。
[24]. **别让 CoT 瞎想：让大模型顺着知识图谱走**（Follow the Path）— KG 路径推理。
[25]. **让大模型沿着图走：Graph-CoT** — RAG 从找文本升级到走关系。
[26]. **给大模型一张地图：FiDeLiS** — 忠实 KGQA 约束路径。
[27]. **边想边查图：用知识图谱逐步补脑**（Graph-Augmented Reasoning）— 迭代式 KG 检索。
[28]. **KG 当奖励模型：路径对齐训练组合推理** — 下游意义/安全攸关迁移的研究故事（"忠实诊断是前置必要条件"）。
[29]. **LLM 真的会看懂图吗？**（Revisiting Graph Reasoning Ability）— 图推理失败的实证弹药。

### 📐 第五梯队 · 多跳评测/反事实基准（3 篇）

[30]. **别让模型背答案：CofCA** — 反事实多跳、抗记忆虚胖，与你"反事实图构造"是姊妹方法。
[31]. **Omanic：一步步检查多跳推理** — step-wise 多跳评测。
[32]. **答案对了，不代表会推理**（多维度推理步评估）— 推理质量评测维度。

### 🛰 第六梯队 · 验证/Agent（弱关联，按需，3 篇）

[33]. **把推理拆成图：GoV 用 DAG 验算** — DAG 拓扑序验证骨架。
[34]. **把多条思路织成一张图：GraphReason** — 图结构验证法。
[35]. **从"会调用"到"会纠错"：工具增强大模型的元验证与反思学习** — 工具增强忠实性，关联 verify-retry 那条实验线。
[36]. **Thinking While Listening**（FSRM）— 最弱，仅"递归架构能自发学结构但是否忠实仍需诊断"一句。
[37]. Lost in the Middle



