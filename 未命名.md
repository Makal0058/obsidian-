大模型越来越多地被用作图推理器：研究者把图结构序列化成文本，再让模型沿路径、选关系或回答受图约束的问题。可是，较高的答案正确率，或者答案看起来会随着图干预而变化，并不一定说明模型真的忠实使用了图结构；模型也可能只是利用了图文本中的序列化线索，尤其是终点节点出现位置。本文提出 **Graph-Intervention Faithfulness，图干预忠实性**，作为一个诊断型评测框架，用来检验模型答案和推理轨迹究竟是受显式图结构因果控制，还是被位置捷径劫持。GIF 通过成对图干预和位置控制版本进行比较，包括 endpoint-first、endpoint-middle、endpoint-last 和 decoy-last，并报告 Raw GIS、PC-GIS、GFI、终点锚定率、路径合法性、轨迹-答案一致性和失败跳数等指标。在多跳符号图任务上，实验发现：只要求模型输出答案时，Raw GIS 可以看起来很高，但一旦控制终点位置，图跟随能力会完全塌陷，暴露出严重的“图推理虚胖”。相反，要求模型输出结构化路径与答案可以显著降低终点锚定；但更难的分叉图仍会暴露状态追踪失败。进一步结果显示，外部符号验证加重试可以用远低于 thinking 模式的延迟，恢复相当一部分推理收益。这说明忠实图推理不仅取决于模型能力，也取决于提示接口设计和外部可验证机制。