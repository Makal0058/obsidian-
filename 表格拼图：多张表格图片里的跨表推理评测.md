> [!info] 基本信息
> - **论文题目**：MTabVQA: Evaluating Multi-Tabular Reasoning of Language Models in Visual Space
> - **中文题目**：MTabVQA：评测语言模型在视觉空间中的多表格推理能力
> - **期刊/会议**：Findings of ACL: EMNLP 2025
> - **年份**：2025
> - **Tag**： #视觉语言模型 #多表推理 #视觉问答 #基准测试 #指令微调 #表格理解 #多跳推理 #监督微调 #强化学习后训练 #数据构造 #OCR #结构化数据

---
```table-of-contents
```
---
# 一、研究背景（前人做到哪一步）

已有工作的推进路径大致分三条线

## 1.1 文本/HTML格式的单表QA

这是最成熟的方向。WTQ、WikiSQL、Spider、BIRD等benchmark已经把单表的自然语言问答和text-to-SQL做得相当充分，数据规模从10k到80k不等。模型输入是文本或HTML表格，任务是生成SQL或直接回答。这条线基本已经饱和

---
## 1.2 文本格式的多表QA

Spider和MMQA把问题扩展到跨表推理，但仍然依赖文本/结构化表示。MMQA（2025）是这条线最新的代表，支持multi-table + multi-hop，但输入依然不是图像

---
## 1.3 图像格式的单表QA

MMTab（2024）是这条线的标志性工作，把表格渲染成图片，做了大规模（23K表/49K QA对）的benchmark，推动了VLM在表格图像上的能力评估。但它只覆盖单表场景

---
## 1.4 前人留下的空白

"图像格式 × 多表 × 多跳推理"这个交叉点没有人做过。现有工作要么是多表但非视觉，要么是视觉但单表，MTabVQA填的就是这个格子

---
## 二、拟解决的问题

**核心问题一句话：** VLM能否从多张表格图片中提取信息、跨表关联、完成多跳推理？

**具体拆解为三个子能力的缺失：**
## 2.1 视觉解析能力

表格以图片形式出现时，模型需要先"看懂"表格——识别表头、列类型、单元格内容、合并单元格等视觉结构。现有OCR+LLM的pipeline方案存在错误累积、丢失视觉布局信息（如列对齐、合并行）的问题，论文希望模型直接从像素层面处理

---
## 2.2 跨表信息关联能力

多张表之间存在外键关系，回答问题需要先在表A中找到中间结果，再用这个结果去表B中查找，即join操作的视觉化版本。这要求模型能同时维护多张图片的语义并建立跨图连接

---
## 2.3 多跳推理能力

最终答案不直接存在于任何一张表中，需要聚合、比较、计数、排名等操作叠加在跨表检索之上。这是对模型综合推理能力的考验，不是简单的信息检索

---
## 2.4 问题的现实动机

约24%的PDF文档是纯图像格式，其中的表格数据对文本模型完全不可见。Web页面上的表格也大量以图片形式存在。随着VLM被部署为视觉Agent去浏览屏幕、操作文档，这个能力缺口会直接影响实际系统的可用性

---
# 三、创新点与贡献

## 3.1 核心创新点
### 3.1.1 交叉点创新（定位层面）

填补了**视觉格式 × 多表 × 多跳推理**这一评估空白
不是对已有方向的增量改进，而是在任务定义层面开辟了新的评估维度
### 3.1.2 数据构造方法（工程层面）

- 从SQL join查询出发，保证multi-table reasoning的必要性有逻辑依据，而非人工拼凑
- 图关系采样算法（Algorithm 1）：BFS遍历外键关系图，采样时保留跨表行级关联，避免采样后表间关系断裂
- 10种视觉风格随机渲染，模拟真实文档的视觉多样性（颜色/字体/边框/斑马纹等）
- 两路QA生成策略并行：SQL转自然语言 + Taxonomy引导LLM生成，覆盖不同推理类型
- 三LLM多数投票 + 人工复核的验证流水线，确保multi-table necessity

---
## 3.2 具体贡献

| 贡献项 | 内容 | 规模 |
|--------|------|------|
| MTabVQA Benchmark | 多表视觉QA评估集 | 3,745 QA对 / 8,499张表图 |
| MTabVQA-Instruct | 指令微调训练集 | 15,853条 / 33,464张表图 |
| TableVision | 基于Qwen2.5-VL-7B的微调模型 | LoRA rank=128 |
| 系统性评测 | 覆盖主流开源+闭源VLM的zero-shot基线 | 7个模型 |
| 后训练对比实验 | CoT / GRPO / SFT三种策略的受控对比 | — |

---
## 3.3 关键实验结论（支撑贡献的证据）

### 3.3.1 能力天花板仍然很低

GPT-4.1 zero-shot仅达到37.0% EM / 61.7% F1，开源最强Gemma-3-12B仅11.8% EM，说明benchmark难度设置合理，未被轻易解决
### 3.3.2 SFT >> GRPO >> CoT

- Zero-shot baseline：2.8% EM / 22.9% F1
- CoT prompting：3.0% EM / 24.5% F1（几乎无提升）
- GRPO：13.1% EM / 46.5% F1
- SFT（LoRA）：28.0% EM / 55.9% F1

> GRPO提升有限的原因：exact match作为reward信号过于稀疏，难以捕捉视觉多表推理的细粒度质量
### 3.3.3 数据多样性 > 数据量

- MultiTabQA子集（10,990条）整体F1仅30.2%，低于Spider子集（2,395条）的65.2%
- 原因：MultiTabQA为纯文本多表QA数据，与视觉多表场景存在domain shift
- Full Instruct Set（多源混合）达到最高68.2% F1，验证多样性的决定性作用
### 3.3.4 Domain对齐效应

在Spider数据上训练的模型在MTabVQA-Spider子集上F1最高（64.3%），在MiMo+ATIS上训练的模型在对应子集上表现最好，说明in-domain对齐强于跨域泛化，fine-tuning数据来源的选择至关重要
### 3.3.5 小模型微调可超越大模型zero-shot

TableVision（7B微调）在MTabVQA-Query子集上以49.8% EM超过GPT-4.1的34.2% EM，整体F1（68.2%）也超过GPT-4.1（61.7%），验证targeted instruction tuning的高效性。

---
## 3.4 局限性（作者自述）

- 仅覆盖英文数据
- 表格视觉复杂度仍为合成风格，缺少扫描件噪声、嵌入图表等真实场景
- 人工验证仅有单一标注员，未做inter-annotator agreement检验
- GRPO的reward function设计粗糙，存在改进空间

---
## 四、方法与模型

## 4.1 整体框架

MTabVQA的构建分为三个阶段，形成完整的数据生产流水线：

```
数据源选择与SQL分析
        ↓
关系采样 + 视觉渲染
        ↓
QA对生成 + 多级验证
```

---
## 4.2 数据来源与选择逻辑

### 4.2.1 为什么优先选text-to-SQL数据集

SQL join查询天然包含多表关联逻辑，可以直接作为multi-table reasoning必要性的结构化依据，
避免人工设计跨表关系时的主观性
### 4.2.2 数据源分布：

| 数据源 | 用途 | QA规模 |
|--------|------|--------|
| Spider | Benchmark + Instruct | 1,048 + 2,395 |
| QFMTS | Benchmark主体 | 2,456 |
| BIRD | Instruct为主 | 1,572 |
| ATIS | 两者均有 | 112 + 384 |
| MiMoTable | 两者均有 | 129 + 512 |
| MultiTabQA | 仅Instruct | 10,990 |
### 4.2.3 训练/评估严格分离

Benchmark来自各数据源的dev/test split，
Instruct来自train split，
保证评估时模型面对的是完全未见过的表结构和实例。

---
### 4.3 关系采样算法

### 4.3.1 问题背景：

原始表可能有数百行，直接渲染会导致图片过大、视觉信息密度失控
简单随机采样会破坏表间的外键关联，使得跨表推理在采样后的数据上无法成立
### 4.3.2 解决方案：图关系感知采样（Algorithm 1）

```
输入：数据库D，外键关系集R，最大行数Nmax=50
输出：各表的采样行子集S

1. 以度最高的表为起点seed table，随机采样至多Nmax行
2. BFS遍历外键关系图
3. 对每个相邻未处理表：
   - 找出与已采样行存在外键匹配的行（GetLinkedRows）
   - 从这些关联行中采样至多Nmax行
4. 重复直到所有关联表处理完毕
```
### 4.3.3 核心保证

采样后的跨表行级关联完整保留，multi-hop推理路径在子集上仍然成立

---
## 4.4 视觉渲染

### 4.4.1 工具链

`dataframe_image`（selenium/matplotlib后端）+ 自定义Pillow脚本
### 4.4.2 视觉多样性设计（10种主题随机分配）

| 变化维度 | 具体内容 |
|----------|----------|
| 颜色方案 | 表头背景色（绿/蓝/深灰等）、斑马纹交替行色 |
| 字体 | serif/sans-serif切换、加粗表头、字号调整 |
| 边框样式 | 实线/虚线/无边框/不同粗细 |
| 布局 | 多表并排/垂直堆叠/混合排列 |
| 单元格间距 | padding大小变化 |
### 4.4.3 设计意图

模拟真实PDF文档和网页中表格的外观多样性，强迫模型具备鲁棒的视觉解析能力，而非依赖固定格式的OCR捷径

---
## 4.5 QA对生成

### 4.5.1 SQL转自然语言
```
流程：
多表SQL查询
    → 在采样子集上执行，获得ground-truth答案
    → Gemini Flash 2.0将SQL转换为自然语言问题
    → 产出：(问题, 答案, 可验证的SQL逻辑依据)
```
保证答案的确定性和可核验性。

### 4.5.2 Taxonomy引导生成
```
预定义问题类型分类体系（14类）：
Match-Based Fact Checking（22.7%）
Descriptive Analysis（18.1%）
Aggregation（17.0%）
Ranking（17.0%）
Counting（10.6%）
Multi-hop Fact Checking（9.6%）
Comparison（2.6%）
Other（2.4%）

流程：
给定采样表 + 问题类型 + few-shot示例
    → LLM生成问题、答案、推理步骤（结构化JSON）
    → 要求必须跨≥2张表
```
补充SQL路径无法覆盖的多样推理类型

---
## 4.6 验证流水线
### 4.6.1 自动验证（三LLM多数投票）

```
验证维度：
1. 问题是否合法且需要multi-hop推理
2. 答案是否基于表格数据准确
3. tables_used字段是否正确（≥2张表）
4. 推理步骤是否逻辑自洽

通过标准：
- 多数LLM判定valid
- 确认使用多表
- 平均分 ≥ 7.0 / 10
```
### 4.6.2 人工验证

通过自动验证的QA对进入Streamlit标注界面，人工核查答案正确性，重点审查复杂边界case

---
## 4.7 后训练策略对比

### 4.7.1 实验设置

- 基础模型：Qwen2.5-VL-3B
- 训练数据：MTabVQA-Instruct Spider子集（2,395条）
- 评估集：MTabVQA-Spider

### 4.7.2 三种策略

1. **CoT Prompting（无参数更新）**

在prompt中要求模型先输出chain_of_thought再给答案，输出格式为包含`chain_of_thought`和`data`两个字段的JSON

**结果：** 几乎无提升（EM 2.8% → 3.0%）

2. **GRPO（强化学习后训练）**

```
框架：EasyR1
训练步数：270 steps
Reward函数（复合）：
  - 内容正确性：EM + F1加权
  - 结构格式分：是否包含<think>和<answer>标签（二值）
  - JSON格式分：<answer>内是否为合法JSON（二值）

关键超参：
  - KL系数：0.01
  - 每个prompt生成n=5个候选
  - 学习率：1e-6
  - 优化器：AdamW (BF16)
```

**结果：** EM 13.1% / F1 46.5%，有改善但未超SFT

3. **SFT with LoRA（监督微调）**
```
适配方法：LoRA
rank：128
基础模型：Qwen2.5-VL-7B（TableVision用此配置）
         Qwen2.5-VL-3B（对比实验）
```

**结果：** EM 28.0% / F1 55.9%（3B），TableVision（7B）达到43.4% EM / 68.2% F1

---
## 4.8 模型评估设置

### 4.8.1 评估指标

- **EM（Exact Match）**：主指标，严格字符串匹配，适合表格事实性答案
- **F1**：词级别重叠，捕捉部分正确和语义相似
- **Precision / Recall**：辅助分析模型错误模式
### 4.8.2 Zero-shot评估格式

所有模型统一要求输出结构化JSON：
```json
{"data": [["ans1", "ans2"], ["ans3", "ans4"]]}
```
temperature=1.0，top-p=1.0
### 4.8.3 GPT-4.1在ATIS上的典型失败模式

Recall=86.3%但Precision=30.0%，EM仅6.3%

模型找到了正确信息，但把答案嵌入冗长自然语言回复中，无法通过严格JSON格式的EM评估。
→ 说明proprietary模型的指令遵从能力（格式控制）是评估瓶颈之一，不完全是推理能力问题

---
# 五、实验与结论（数据说明了什么、有什么局限性）

## 5.1 Zero-Shot基线评测
### 5.1.1 结论

视觉多表推理对当前所有VLM都构成显著挑战

| 模型类别 | 代表模型 | Overall EM | Overall F1 |
|----------|----------|-----------|-----------|
| 开源最弱 | LLaVA-OV-7B | 2.1% | 18.4% |
| 开源最强 | Gemma-3-12B | 11.8% | 40.1% |
| 闭源 | Gemini-2.0-Flash | 34.1% | 59.3% |
| 闭源最强 | GPT-4.1 | 37.0% | 61.7% |
| 微调模型 | TableVision（ours） | 43.4% | 68.2% |
### 5.1.2 数据说明了什么

- 开源与闭源之间存在断崖式差距（11.8% vs 37.0% EM），7B规模的开源模型在zero-shot下几乎无法完成这个任务
- GPT-4.1未能超过50% EM，说明benchmark难度合理，不存在"顶级模型轻松解决"的天花板问题
- Qwen2.5-VL-7B（7.8% EM）微调后变成TableVision（43.4% EM），同一个底座提升了5倍以上，说明性能瓶颈在训练数据而非模型架构
### 5.1.3 子数据集的差异揭示的问题

GPT-4.1在各子集的EM：
```
MTabVQA-Spider：49.0%   ← 最高，SQL风格问题匹配GPT训练分布
MTabVQA-Query：34.2%
MTabVQA-MiMo：20.2%
MTabVQA-ATIS：6.3%      ← 异常低
```
ATIS子集的recall=86.3%但precision=30.0%，说明模型"知道答案"但无法按格式输出，是指令遵从问题而非推理能力问题

这提示用EM评估闭源模型时，格式控制能力会系统性低估其真实推理水平

---
## 5.2 后训练策略对比

### 5.2.1 结论

SFT是当前最有效的提升路径，GRPO改善有限，CoT几乎无效

```
Zero-shot → CoT → GRPO → SFT
EM:  2.8%    3.0%   13.1%  28.0%
F1: 22.9%   24.5%   46.5%  55.9%
```
### 5.2.2 各策略的数据解读

1. **CoT为何失败：**

在没有参数更新的情况下，让3B模型"先思考再回答"，模型本身的视觉解析和跨表推理能力并未增强，prompt格式的改变无法弥补能力的根本缺失

CoT对大模型有效，对能力不足的小模型是空操作

2. **GRPO为何不及SFT：**

- Reward信号过于稀疏：多表推理的答案通常是列表型结构，exact match要求完全匹配，中间推理步骤的质量得不到奖励
- 视觉token处理的特殊性：图像特征的梯度传播路径比纯文本更复杂，PPO族算法在多模态设置下本身就不稳定
- 270步训练量相对有限，GRPO需要更多exploration才能收敛
- format reward（结构标签和JSON合法性）很快饱和，后期主要靠EM/F1信号，但这两个信号噪声大

3. **SFT为何有效：**

直接告诉模型"给定这些表图，问题X的答案是Y"，监督信号密集且明确，LoRA rank=128提供了足够的参数容量，在2395条高质量数据上就能显著改变模型行为

---
## 5.3 微调数据来源对泛化的影响

### 5.3.1 结论

数据多样性和domain对齐比数据量更重要

| 训练子集 | 样本量 | Overall F1 |
|----------|--------|-----------|
| Zero-shot | 0 | 35.1% |
| MiMo+ATIS | 896 | 40.0% |
| Spider | 2,395 | 65.2% |
| MultiTabQA | 10,990 | 30.2% |
| Full Instruct | 15,853 | 68.2% |
### 5.3.2 最反直觉的结果

MultiTabQA用了10,990条数据，比Spider多4.6倍，但整体F1（30.2%）甚至低于zero-shot baseline（35.1%）

### 5.3.3 原因分析

MultiTabQA是纯文本多表QA数据，训练后模型学会了文本多表推理的模式，但这个模式与"从图片中解析表格再推理"的范式存在根本性的domain shift，大量文本格式训练反而破坏了模型原有的视觉解析能力。

→ **负迁移**，而非无效迁移

### 5.3.4 Domain对齐效应（子集内最佳）

```
Spider训练 → Spider评估：F1 59.2%（子集最高）
MiMo+ATIS训练 → ATIS评估：F1 46.5%（子集最高）
MiMo+ATIS训练 → MiMo评估：F1 39.7%（子集最高）
```
in-domain对齐强于cross-domain泛化，Full Instruct之所以最强，是因为它同时覆盖了所有domain

---
## 5.4 推理类别的性能分布

### 5.4.1 结论

文本检索类任务强，数值计算类任务弱

GPT-4.1和Gemini-2.0-Flash在各类别的F1：

```
Descriptive Analysis：0.73 / 0.72   ← 最强
Multi-hop Fact Checking：0.71 / 0.66
Match-Based Fact Checking：0.66 / 0.68
Ranking：0.55 / 0.56
Aggregation：0.57 / 0.54
Counting：0.47 / 0.40               ← 最弱
```
### 5.4.2 数据说明了什么

VLM在"找到什么"上表现尚可，在"算出什么"上明显退化

Counting任务要求精确枚举，不允许任何遗漏或重复，对视觉解析的完整性要求极高，当前模型的视觉OCR错误会直接传播到计数结果。这是VLM在结构化数值推理上的系统性弱点，不特定于某个模型

---
## 5.5 综合结论

```
1. 任务本身：视觉多表推理是当前VLM的显著能力边界，
   即使GPT-4.1也未能超过40% EM

2. 提升路径：SFT是当前最可靠的方法，
   GRPO的潜力受限于reward设计，需要更细粒度的过程奖励

3. 数据策略：负迁移风险真实存在，
   数据来源的视觉模态对齐是首要考虑因素，优先级高于数据量

4. 评估方法论：EM指标对冗长输出的惩罚会系统性低估闭源模型能力，
   F1作为辅助指标必要但不充分

5. 小模型潜力：7B微调模型可超越GPT-4.1，
   说明这个任务的瓶颈在训练数据分布而非模型规模
```

---
## 5.6 局限性
### 5.6.1 数据层面

- 仅覆盖英文，无多语言泛化能力评估
- 表格为合成渲染，缺少扫描噪声、嵌入图表、不规则合并单元格等真实复杂场景
- 单一标注员验证，无inter-annotator agreement数据，质量上界不确定
### 5.6.2 实验层面

- GRPO训练步数有限（270步），结论不排除训练不充分的影响
- reward function设计简单，GRPO潜力未被充分挖掘
- TableVision评估在自建benchmark上，存在数据构造偏差的自我验证风险
### 5.6 评估层面

- EM指标对格式敏感，GPT-4.1在ATIS上的异常已揭示这一问题
- 人工验证规模有限，LLM验证的系统性偏差未被量化
- 14个推理类别的边界定义主观性较强，类别间存在重叠可能