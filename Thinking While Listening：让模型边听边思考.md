> [!info] 基本信息
> - **论文题目**：Thinking While Listening: Fast–Slow Recurrence for Long-Horizon Sequential Modeling
> - **中文题目**：边听边想：用于长序列建模的快慢递归模型
> - **期刊/会议**：ICML 2026
> - **年份**：2026
> - **Tag**：#循环神经网络 #长序列建模 #潜空间推理 #快慢动力学 #序列状态追踪 #测试时计算扩展 #自组织表示 #OOD泛化

---
```table-of-contents
```
---
# 一、研究背景（前人做到哪一步）

## 1.1 问题的根源：长序列状态追踪

循环神经网络在长序列上维持内部状态本身就是公认难题（Bengio et al., 1994；Pascanu et al., 2013），现代 Transformer 同样没有解决这个问题（Tay et al., 2021；Liu et al., 2023；Sinha et al., 2025）。核心矛盾是：模型必须一边处理新观测，一边保持对过去信息的连贯表示。

---
## 1.2 Latent Recurrent Modeling 的兴起

近期出现了一类在**隐状态（latent space)** 中迭代运行**循环块（recurrent block**)的方法（Geiping et al., 2025；Miyato et al., 2025；Gladstone et al., 2025 等）。基本范式是：给定固定输入 $O$，从随机噪声 $X_0$ 出发，反复执行$$X(t+1) = R(X(t), \text{Encoder}(O))$$其中：
$X(t)$：$t$时刻的 latent state，维度 $K \times D$（$K$个latent token，每个 $D$ 维），就是模型的**内部思维状态**
$X(t+1)$：下一步更新后的 $latent state$
$R(\cdot, \cdot)$：recurrent block，共享权重的神经网络，负责执行一步 latent 更新
$O$：外部观测（输入），静态不变
$\text{Encoder}(O)$：把观测编码成与 latent 同维度的条件信号，注入到$R$里

利用重复迭代让 latent state 自组织、收敛到稳定表示。这类方法的动机通常是 test-time compute scaling，但其本质能力是对 latent space 中的信息进行迭代精炼和聚类组织

---
## 1.3 前人方法的共同瓶颈

上述方法都设计给**静态输入**，应用到序列时有一个结构性缺陷：每处理一个新观测就重置 latent state，靠把历史 latent summary 拼到 context 里来保留过去信息。这导致两个问题：
1. Context 长度随序列增长，引入长上下文失效问题
2. 没有机制让 latent reasoning 在观测之间持续演化

代表性工作的具体处理方式：

| 方法        | 机构/作者               | 问题                                             |
| --------- | ------------------- | ---------------------------------------------- |
| Coconut   | Hao et al., 2024    | 每轮重新初始化，历史靠 context 累积                         |
| Looped LM | Zhu et al., 2025    | Latent 在 sequence 维度增长                         |
| Perceiver | Jaegle et al., 2021 | 有 RNN-style latent update，但无更快的 reasoning loop |
| ACT       | Graves, 2017        | Fast/slow 分离，但 latent 不跨轮延续                    |
| HRM / TRM | Wang et al., 2025   | Fast-slow 耦合，但只处理静态输入                          |
| CTM       | Darlow et al., 2025 | 可处理时序输入，但每轮重置辅助变量                              |

---
## 1.4 前人留下的空白

没有一个方法能同时做到：Latent state **不重置**地跨越整个序列演化，同时在每个观测间隔内进行多步 recurrent reasoning，且 context 大小固定不增长，这就是本文的切入点

---
# 二、拟解决的问题（为什么现有方法不行）

现有方法无法在处理长序列时，同时保持固定大小的记忆和连续的内部推理
## 2.1 现有方法为什么不行：

### 2.1.1 Transformer / SSM 类

只沿时间轴单向更新，每个观测只过一遍网络，没有在 latent space 里反复"思考"的机会。遇到需要长程回溯的任务（比如记住序列开头的括号）直接崩

**注：**
1. **Transformer：** 2017年提出，现在最主流的序列模型，GPT、Claude、Gemini 底层都是它。核心机制是**注意力机制（Attention）**——处理序列时，每个位置都和所有其他位置算一次相关性，决定**我应该关注哪里**，问题是：$$ \begin{cases} 计算量是O(n^2)，序列长度翻倍，计算量翻四倍 \\没有真正的记忆，靠把所有历史都放在 context 里来记住过去\\context\ 一长就开始失忆（Lost\ in\ the\ Middle 问题）\end{cases} $$2. **SSM（State Space Model，状态空间模型）：** 更新的方向，代表作是 Mamba（2023）。核心思路是把序列建模变成一个控制理论里的状态方程：$$h_t = Ah_{t-1} + Bx_t，y_t​=Ch_t​$$其中：$h_t$是隐状态、$x_t$是输入、$y_t$是输出。每步只看当前输入和上一步状态，不需要存所有历史

优点：$\begin{cases}  计算量是O(n)，比 \ Transformer\ 快很多 \\理论上可以处理无限长序列\end{cases}$

问题：$\begin{cases} 隐状态维度固定，信息压缩有损失\\长程依赖还是会丢\end{cases}$
### 2.1.2 Latent recurrent 类（Geiping、Gladstone 等）

能在 latent space 里迭代思考，但设计给静态输入，硬套到序列上就变成：**每个观测单独处理，思考完重置**、**历史靠 context 堆积**、**序列一长，context 撑不住**

**注：**
2024年底才兴起的新方向，是这篇论文的直接前身。**基本思路：** 不是每个观测过一遍网络，而是**对同一个观测反复跑同一个网络块**，让 latent state 慢慢收敛
```
输入 O
    ↓
X(0) = 随机噪声
    ↓
X(1) = R(X(0), Encoder(O))
    ↓
X(2) = R(X(1), Encoder(O))
    ↓
...
    ↓
X(T) = 最终表示 → 解码输出
```

同一个网络 $R$、一个输入 $O$ ，跑 $T$ 次

**为什么这样有效：** 直觉上类似"想清楚再说"——不是看一眼就给答案，而是反复琢磨，每次迭代都在整理信息，最后收敛到一个稳定的表示。理论上有动力系统的支撑：反复迭代会让 latent token 自发聚类，形成有结构的表示

**代表性工作：**

| 论文                         | 机构       | 特点                                   |
| -------------------------- | -------- | ------------------------------------ |
| Geiping et al., 2025       | Maryland | 最早系统提出，动机是 test-time compute scaling |
| Miyato et al., 2025（AKOrN） | 松尾研      | 用 Kuramoto 振荡器，有能量函数，自组织性最强          |
| Gladstone et al., 2025     | 多校联合     | 基于 Transformer 的实例化，能量最小化框架          |
| Hao et al., 2024（Coconut）  | Meta     | 在语言模型上做，latent 空间里的 chain-of-thought |

**共同缺陷：** 都是给**静态输入**设计的。应用到序列时的处理方式：

```
O_1 → 迭代T次 → X_1(T) → 重置 → O_2 → 迭代T次 → X_2(T) → ...
                    ↓                           ↓
               存进context               存进context
```

每个观测独立处理，历史靠 context 堆积，序列一长就撑不住

**和这篇论文的关系：** 这篇论文就是把 latent recurrent 从**静态输入**扩展到**动态序列输入**——不重置，不堆 context，让 latent state 一直活着，新观测进来只是修改它。这一步看起来简单，但之前没人做到。
### 2.1.2 HRM / TRM（Wang et al.）

有 fast-slow 双速动力学，结构上最接近，但只能处理静态输入，根本没考虑序列场景

**注：**
HRM 灵感来自**神经科学**：大脑皮层不同区域有不同的时间尺度$\begin{cases}低级皮层（感觉区）：响应快，处理具体细节 \\高级皮层（前额叶）：响应慢，处理抽象概念\end{cases}$

HRM 把这个发现搬进神经网络：设计两个耦合的动力学过程，一快一慢，分别处理不同抽象层次的信息

**TRM** 是 HRM 的改进版，解决了 HRM 训练时的一些不稳定问题，其他思路基本一样

|     | HRM                          | TRM                       |
| --- | ---------------------------- | ------------------------- |
| 全称  | Hierarchical Reasoning Model | Truncated Reasoning Model |
| 作者  | Wang et al., 2025            | Jolicoeur-Martineau, 2025 |
| 关系  | 原版                           | HRM 的扩展                   |

和 FSRM 的关系：结构上最接近 FSRM，都有 fast-slow 耦合动力学，**但有一个关键区别：** HRM/TRM 只在**一个固定输入**上做 fast-slow 推理，处理完就结束。FSRM 让这个过程在整个序列上持续运行，latent state 全程不重置

| | HRM / TRM | FSRM |
|---|---|---|
| 输入类型 | 静态输入 | 动态序列输入 |
| latent 是否跨观测延续 | 否 | **是** |
| 能处理长序列吗 | 不能 | 能 |

### 2.1.2 CTM（Darlow et al.）

能处理时序输入，也有 fast recurrence，但每轮开始时辅助变量重置，latent 的连续性被打断。输出从神经元同步模式读，不是直接从 latent 读，信息损失

**注：**
核心思路是把神经网络的每个 latent token 看成一个**神经元**，神经元之间有同步强度，模型的输出不是从 latent 直接读出来的，而是从**神经元的同步模式**读出来的

>类比：不是听每个人说了什么，而是看所有人有没有同时点头——从集体行为里读出答案。

和 FSRM 最像的地方：

- 有 fast recurrence，在每个时间片段内跑多步
- 可以处理时序输入，不只是静态输入
- 也用动力系统的语言描述模型行为

关键区别：CTM 每开始新一轮时，辅助变量重新初始化——本质上还是把序列切成一段一段处理，latent 的连续性在段与段之间断掉了

| | CTM | FSRM |
|---|---|---|
| 输出来源 | 神经元同步模式 | 直接从 latent 读出 |
| 辅助变量 | 每轮**重置** | **不重置** |
| latent 连续性 | 被打断 | 全程保持 |

### 2.1.2 ACT（Graves, 2017）

最早分离 fast/slow 的工作，但 fast loop 结束后 latent 不直接延续，下一轮的初始状态是前一轮轨迹的集成重建，不是真正的持续演化

**注：**
核心思路是普通 RNN 每个输入走固定步数，ACT 让模型**自己决定每个输入要想多久**。简单的输入少想几步，复杂的输入多想几步，动态分配计算量
```
输入 x
    ↓
想一步 → 够了吗？→ 够了 → 输出
    ↓ 不够
想一步 → 够了吗？→ 够了 → 输出
    ↓ 不够
...
```
"够了吗"由模型自己判断，不是人工设定

和 FSRM 像的地方：

- 有 fast loop 和 slow input 的分离
- fast loop 在同一个输入上跑多步
- 比普通 RNN 多了"反复思考"的机制

关键区别：
ACT是fast loop 跑完之后，把整条轨迹加权平均成一个新的初始状态，再开始下一轮。这个操作相当于把思考过程"摘要"了一遍，信息有损失，而且连续性被打断
FSRM 直接把 $X(T)$ 作为下一轮的 $X(0)$，没有任何中间处理

| | ACT | FSRM |
|---|---|---|
| fast loop 结束后 latent 怎么办 | **不直接延续** | **直接延续** |
| 下一轮初始状态从哪来 | 前一轮所有轨迹的集成重建 | 就是上一轮结束时的状态 |
| latent 连续性 | 间接传递，有损失 | 完整保留 |
历史地位：ACT 是2017年的工作，比 latent recurrent reasoning 这个方向早了将近十年。当时没有自组织机制，也没有 Kuramoto 振荡器这些工具，所以只能做到这一步

> 思路对了，工具不够，所以没走到终点。FSRM 站在它肩膀上，补上了剩下的路

---
## 2.2 缺口:

没有任何方法同时满足以下三点：
1. Latent state 跨越整个序列**不重置**，持续演化
2. 每个观测间隔内进行**多步 recurrent reasoning**
3. Context 大小**固定不增长**

三个条件缺一不可——缺第1条**思考被打断**，缺第2条**推理不充分**，缺第3条**序列一长就崩**

---
# 三、创新点与贡献

## 3.1 核心创新：Fast-Slow 耦合动力学

把 latent recurrent reasoning 和 sequential observation 解耦成两个时间尺度，同步运行：$$ \begin{cases} Slow loop：观测\ O_s\ 按序列节奏进来，编码成条件信号\ C(t)\\  Fast loop：latent\ state\ X(t)  在每个观测间隔内跑 \ T 次更新，且全程不重置\end{cases} $$
关键方程：$$X(t+1) = \Pi\left(X(t) + \gamma F(X(t), C(t);\theta)\right)$$latent state 每走一小步，用神经网络 $F$ 综合自身状态和外部观测，算出一个更新方向，走一小步，再拉回球面上。重复 $T$ 次

其中：
$X(t)$：当前时刻 $t$ 的 latent state，模型的**内部思维状态**，是一个矩阵，$K$ 个 token，每个 $D$ 维
$X(t+1)$：下一时刻的 latent state，这一步更新完之后的结果
$\theta$：神经网络 $F$ 的参数，所有时间步**共享同一套**，这就是"recurrent"的含义
$F(X(t), C(t);\theta)$：神经网络，参数是 $\theta$，吃两个输入 $\begin{cases} X(t)：当前自己的状态\\C(t)：当前观测的编码（外部信号） \end{cases}$，输出一个**应该往哪个方向走**的增量
$\gamma$：步长，控制每次更新走多大。类似学习率，是可学习的参数，初始值设为 $0.1$
$X(t) + \gamma F(\cdot)$：当前状态加上一个小增量——就是往前走一步
$\Pi(\cdot)$：归一化操作，$\Pi(x) = \dfrac{x}{\|x\|}$，把向量投影回单位球面上。作用是防止 latent state 越走越大或越走越小，强制每个 token 始终待在单位球面上。这也是 Kuramoto 振荡器的几何结构——所有振子在球面上转，不会跑飞

看起来和前人只差一点——$X(t)$不重置、$C(t)$跟着慢时间轴走。但这一点带来了本质差异：latent state 第一次拥有了跨越整个序列持续演化的能力

---
## 3.2 贡献：

### 3.2.1 架构层面

提出 FSRM（Fast-Slow Recurrent Model），第一个在固定大小 latent space 内实现连续序列推理的框架。Context 不增长，latent 不重置，两个问题同时解决
### 3.2.2 理论层面

继承 AKOrN（Miyato et al., 2025）的能量函数解释，latent 更新天然对应能量下降，模型的"置信度"和"组织程度"可以用一个标量追踪，有可解释性
### 3.2.3 涌现结构（Emergent Structure）

模型在没有任何人工监督的情况下，自发在 latent space 里编码出了任务的内在结构：

- Dyck 任务：第一层按括号类型组织，第二层按栈深度组织
- DoorKey 任务：latent 轨迹自发围绕"捡钥匙""开门""发现终点"形成聚类

这不是设计出来的，是学出来的——说明 fast-slow 耦合确实在逼迫模型提炼结构性信息
### 3.2.4 实验层面

- Dyck-(30,5)：训练长度 ≤40，测试到 $10^5$，~90% 精度；GPT-5.1 / Claude Opus 4.5 / Gemini 3.0 Pro 在几百 token 后全崩
- 迷宫 OOD：19×19 训练，39×39 测试，~60% 精度，所有 baseline 接近 0
- MiniGrid RL：zero-shot OOD 迁移，全面优于 LSTM、Mamba-2、Transformer-XL
### 3.2.5 消融验证

权重共享（recurrence）是关键——去掉后 OOD 从 0.61 跌到 0.51，继续加深层数反而跌到 0.45。参数量只有 baseline 的 1/10，但 OOD 更强，排除了**小模型防过拟合**的解释

---
## 3.3 总结

用 fast-slow 耦合动力学，第一次让固定大小的 latent state 在长序列上真正连续地思考，同时意外发现模型会自发学出任务的内在结构

---
# 四、方法与模型

把**思考**和**观察**分成两个速度不同的过程，让思考比观察快 $T$ 倍，同时思考过程全程不中断
## 4.1 整体架构：Fast-Slow 耦合

两个过程同时运行，但节奏不同：

|  | Slow Process | Fast Process |
|---|---|---|
| 负责什么 | 接收外部观测 | 内部 latent 推理 |
| 节奏 | 每来一个观测走一步 | 每个观测间隔走 $T$ 步 |
| 对应变量 | $C(t)$ | $X(t)$ |
| 会不会重置 | 随观测更新 | **全程不重置** |

慢过程给快过程提供条件信号，快过程在观测之间自己反复迭代，把信息整理成结构化的表示

---
## 4.2 完整动力学方程

$$\begin{cases} C(t) = \text{Encoder}(O_s), \quad s = \lfloor t/T \rfloor \\ X(t+1) = \Pi\left(X(t) + \gamma F(X(t), C(t);\theta)\right) \end{cases}$$

其中：
$O_s$：第 $s$ 个观测，慢时间轴
$s = \lfloor t/T \rfloor$：快时间 $t$ 除以 $T$ 取整，得到当前对应哪个观测
$C(t)$：观测编码，在同一个观测内保持不变
$X(t)$：latent state，每个快时间步都在更新
$F(\cdot)$：神经网络，综合 $X(t)$ 和 $C(t)$ 算更新方向
$\gamma$：步长，可学习，初始 $0.1$
$\Pi(x) = \dfrac{x}{\|x\|}$：投影回单位球面，防止状态跑飞
$\theta$：所有时间步共享同一套参数（这就是 recurrent）

---
## 4.3 核心组件 $F$：AKOrN

$F$ 的具体实现用的是 Miyato et al., 2025 的 AKOrN，基于 Kuramoto 振荡器：$$F(X, C)_i = \Omega_i x_i + \text{Proj}_{x_i}\left(J(X,C)_i\right)$$每个 latent token 是球面上的一个点，$F$ 告诉它往哪个方向移动——既受自身旋转驱动，又被其他 token 和外部观测拉着走，最终自发聚类

其中：
$x_i$：第 $i$ 个 latent token
$\Omega_i x_i$：反对称矩阵驱动的自转项，让每个 token 在球面上自己转
$J(X,C)_i$：attention-based 交互项，让所有 token 互相影响，同时把观测 $C$ 加进来
$\text{Proj}_{x_i}(v) = v - (v^\top x_i)x_i$：把更新方向投影到切空间，确保更新后还在球面上

---
## 4.4 能量函数：可解释性的来源

$$E(X) = -\frac{1}{2}\sum_i x_i^\top J(X,C)_i$$这个标量反映 latent token 的组织程度：
$$\begin{cases} 能量低 → token 聚集，表示收敛到稳定结构\\ 能量高 → token 分散，表示刚接收到新信息还没整理好 \end{cases}$$
实验中能量轨迹直接对应任务结构——来新括号时 spike，稳定后对应括号类型；第二层能量对应栈深度

---
## 4.5 两层扩展（Dyck 任务）

Dyck 任务有层次结构，用两层 fast process：$$\begin{cases} X^{(0)}(t) = C(t)\\ X^{(\ell)}(t+1) = \Pi\left(X^{(\ell)}(t) + \gamma F\left(X^{(\ell)}(t), X^{(\ell-1)}(t)\right)\right) \end{cases}$$
第一层：条件信号是原始观测编码，追踪 **token 类型**
第二层：条件信号是第一层输出，追踪 **栈深度**

分工自发涌现，没有人工监督

---
## 4.6 训练与推理

- **参数**：$\theta$、$\gamma$、Encoder、Decoder 一起端到端训练
- **预测**：每个慢时间步 $s$ 结束时，用 $\text{Decoder}(X(sT))$ 读出输出
- **RL 任务**：用 PPO 训练，Encoder 部分加卷积层处理图像观测，fast loop 最后 $5$ 步做梯度截断提升效率

---
## 4.7一句话总结

让一个不会重置的 latent state 以比观测快 $T$ 倍的节奏持续迭代，用 Kuramoto 振荡器驱动自组织，慢慢把序列信息提炼成结构化的内部表示

---
# 五、实验与结论（数据说明了什么、有什么局限性）
## 5.1 三个任务的设计逻辑

实验选题不是随机的，三个任务对应同一个核心能力的三种考法：

| 任务              | 考什么                | 难点                    |
| --------------- | ------------------ | --------------------- |
| Egocentric Maze | 从长序列里提取有用信息，忽略无用信息 | 死路信息是干扰，必须选择性记忆       |
| Dyck 括号         | 无限长序列上的精确状态追踪      | 必须记住序列最开头的括号          |
| MiniGrid RL     | 稀疏奖励下的长程决策         | 没有逐步监督信号，必须自己发现哪些信息重要 |

三个任务都要求模型**边接收信息边整理信息**，不能存全部历史

---

## 5.2 数据说明了什么

### 5.2.1 Dyck 括号任务

- 训练长度 $\leq 40$，测试到 $10^5$
- FSRM 全程保持 $\sim 90\%$ 精度
- GPT-5.1 / Claude Opus 4.5 / Gemini 3.0 Pro：给了算法描述，几百个 token 后全部崩溃

**说明**：LLM 的 context 机制根本不适合这类**状态追踪任务**，不是参数量或提示词的问题，是结构性缺陷。FSRM 泛化到训练范围四个数量级之外，靠的是真正学会了任务结构，不是记忆

### 5.2.2 Egocentric Maze 任务

- 训练：$19 \times 19$ 迷宫
- 测试：$39 \times 39$ 迷宫（OOD）
- FSRM：$\sim 60\%$ 精度
- 所有 baseline（LSTM / Mamba / Transformer / Looped TF / S5 / CTM）：接近 $0$

**说明**：不是FSRM特别强，是其他方法根本没有泛化能力。$60\%$ 在 OOD 上已经是量级差距

### 5.2.3 MiniGrid 强化学习任务

- 训练最简单配置，zero-shot 迁移到更大更复杂的环境
- FSRM 在 LavaCrossing / DoorKey / MultiRoom 全面优于或持平 LSTM、Mamba-2、Transformer-XL
- 能量轨迹显示：agent**沮丧**时（视野突变）能量spike，关键事件（捡钥匙/开门/发现终点）在 latent 空间自发形成聚类

**说明**：fast-slow 耦合在没有逐步监督的情况下同样有效，模型自己学会了哪些事件值得记住

---
## 5.3 消融实验说明了什么

### 5.3.1 权重共享（Recurrence）是关键

| 配置 | OOD 精度 |
|------|---------|
| 共享权重 $T=5$ | $0.612$ |
| 不共享 $T=5$（5层） | $0.509$ |
| 不共享 $T=10$（10层） | $0.445$ |

加深层数反而变差。说明 OOD 泛化来自**反复用同一套规则整理信息**，不是靠堆参数

### 5.3.2 Fast loop 迭代次数 $T$ 的影响

- $T$ 越大，OOD 精度单调提升，$T=8$ 之后饱和
- 推理时调大 $T$ 不会显著损害性能

说明模型对 $T$ 的变化有鲁棒性，fast-slow 耦合的时间比例不需要精确对齐

### 5.3.3 参数量不是原因

- FSRM 参数量约为 baseline 的 $\frac{1}{10}$
- 把 FSRM 加宽到 256 channels，OOD 进一步提升至 $0.727$

排除"小模型防过拟合"的解释，泛化能力来自架构设计本身

---
## 5.4 涌现结构：意外发现

模型在没有任何人工监督的情况下，自发在 latent space 里编码出任务的内在结构：
### 5.4.1 Dyck 任务：

- 第一层能量：每来新 token 就 spike，稳定在该 token 类型对应的水平 → 追踪**符号类型**
- 第二层能量：对 token 类型不敏感，随栈深度变化 → 追踪**栈深度**
- PCA 可视化：latent 轨迹形成弹簧状流形，按栈深度排列，长序列上保持稳定
### 5.4.2 DoorKey 任务：

latent 轨迹自发围绕"捡钥匙""开门""发现终点"形成与轨迹无关的聚类。**说明**：fast-slow 耦合在逼迫模型提炼结构性信息，不是记住具体轨迹，而是学会了任务的抽象规律。

---
## 5.5 局限性

### 5.5.1只在小规模合成任务上验证

没有在语言、视频、机器人等真实场景测试，泛化能力是否保持未知
### 5.5.2 推理速度慢

$T=5$ 时约为 Mamba / Transformer 的 $3\times$ 推理时间，原因是：
1. Double-loop 结构本身的计算开销
2. 没有针对性的 CUDA kernel 优化，竞品有高度优化的底层实现
### 5.5.3 对随机初始化敏感

Figure 9 中误差棒较大，说明结果在不同随机种子下有波动。根本原因是反复复用同一层权重导致的梯度不稳定，和 RNN 的梯度消失/爆炸是同类问题

---
## 5.6. 结论

Fast-slow 耦合动力学是一个有效的序列推理机制：让 latent state 以比观测更快的节奏持续迭代，不重置、不堆 context，模型自发学会提炼任务的内在结构，在长序列 OOD 泛化上大幅超越现有方法

**三个核心发现：**
1. 连续 latent 演化 + 固定 context 可以同时实现
2. 权重共享（recurrence）是泛化能力的来源，不是参数量
3. 涌现结构是 fast-slow 耦合的副产品，不需要额外设计
