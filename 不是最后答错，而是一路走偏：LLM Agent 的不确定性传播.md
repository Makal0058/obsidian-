 [!info] 基本信息
> - **论文题目**：Uncertainty Propagation on LLM Agent
> - **中文题目**：大语言模型智能体中的不确定性传播
> - **期刊/会议**：ACL 2025 Long Paper
> - **年份**：2026年3月
> - **主题**： #LLM不确定性估计 #多步Agent #不确定性传播 #情境感知 #隐马尔可夫模型 #RAG置信度 #可解释决策
---
```table-of-contents
```
---
# 一、研究背景（前人做到哪一步）

## 1.1 LLM Agent的发展

LLM被集成进Agent系统后，能够处理复杂的多步决策任务。早期工作（WebGPT）尝试让LLM主动与环境交互，后续工作将LLM用于代码生成、多Agent协作（CAMEL、AutoGen）等场景。ReAct框架将**推理**和**行动**统一，成为决策型Agent的主流范式，本文也基于此构建

---
### 1.2 LLM不确定性估计的现有方法

现有方法均聚焦于单步输出的不确定性，主要分三类：

1. **基于熵**：Predictive Entropy将图像领域的预测熵扩展到语言模型，Semantic Entropy进一步引入语义等价性，Normalized Entropy解决了输出长度带来的偏差

2. **基于似然**：直接用输出序列的概率估计置信度

3. **基于自评估**：让LLM自己判断答案是否正确（P(True)），不需要访问内部logits，适用于黑盒模型

---
### 1.3 前人留下的空缺

上述方法均为单步设计，直接用于Agent有两个根本缺陷：

第一，只看最后一步的不确定性，忽略了中间步骤的累积效应

第二，忽略了Agent与环境的动态交互过程，观测质量和推理轨迹的偏离都会影响最终决策的可靠性。

UALA做了一定改进，但仍是单步估计，没有跨步传播的机制

---
# 二、拟解决的问题（为什么现有方法不行）

## 2.1 核心矛盾

不确定性在多步过程中是累积的，而不是孤立发生在某一步。现有方法只看最后一步，相当于只看考试最后一道题对没对，忽略了前面所有步骤的推理质量

---
### 2.2 逐类分析为什么不行

1. **基于熵的方法（Predictive Entropy、Semantic Entropy）**：设计用于单步QA，输入问题输出答案，没有中间推理过程。直接用在Agent上，只能拿到最后一步的熵值，丢失了所有中间步骤的信息

2. **基于似然的方法（Normalized Entropy、Likelihood）**：同上，估计的是最终输出序列的概率，对推理链路中发生了什么完全不感知

3. **基于自评估的方法（P(True)）**：让模型自己判断答案对不对，但Agent本身就可能过度自信，自评估的可靠性存疑，且同样只在最后一步做

4. **UALA**：往前走了一步，在最后一步之前加了不确定性估计，但本质上还是单步方法，没有跨步传播机制

---
### 2.3 根本原因

多步Agent的问题在于，错误往往不是在最后一步产生的，而是在中间某步推理跳跃或检索偏离之后悄悄积累的。等到最后一步再看，模型可能对错误答案也表现出很高的置信度，如Figure 1所示——每步单独看不确定性都很低，但最终答案是错的。只看最后一步，这种情况完全无法识别

---
# 三、创新点与贡献

## 3.1 核心创新

现有方法把Agent的不确定性等同于最后一步输出的不确定性，SAUP的核心创新是把这个问题重新定义为：**整条推理链路的加权不确定性聚合问题**

---
### 3.2 具体贡献

**第一，提出SAUP框架**

第一个系统性地将不确定性传播引入多步LLM Agent的框架。不依赖特定的单步估计方法，任何现有的单步不确定性估计技术都可以作为backbone插入，兼容性强

**第二，设计情境权重估计机制**

引入Inquiry Drift和Inference Gap两个代理指标，量化每步的情境质量，将其转化为权重。设计了三种权重估计方案：

- 位置代理（SAUP-P）：越靠近最终答案权重越大
- 距离代理（SAUP-D）：直接用Drift和Gap的距离作为权重
- 学习代理（SAUP-HMMD）：将(Da, Do)序列输入CHMM，用隐状态作为权重，效果最强

**第三，实验验证**

在HotpotQA、MMLU、StrategyQA三个数据集上，使用LLaMA3-8B、LLaMA3-70B、GPT-4o三个模型验证，AUROC相比最强baseline最高提升20%。即使最简单的均匀传播（不加权重）也显著优于只看最后一步的方法

---
# 四、方法与模型

## 4.1 整体框架

两个核心问题：每步的不确定性 $U_i$ 怎么算，每步的权重 $W_i$ 怎么定。最终聚合公式：
$$U_{\text{agent}} = \sqrt{\frac{1}{N}\sum_{i=1}^{N}(W_i \cdot U_i)^2}$$
其中：
**$U_{\text{agent}}$：** 整个Agent的最终不确定性分数，这是我们想要的输出
**$N$：** Agent总共走了多少步，比如ReAct走了5轮思考-行动-观察，N=5
**$W_i$：** 第i步的情境权重，反映这步对整体不确定性的贡献有多大。偏离越严重权重越高
**$U_i$：** 第i步的单步不确定性，用normalized entropy估计，就是这步LLM输出有多不确定
**$W_i \cdot U_i$：** 这步的加权不确定性，权重和不确定性相乘，两者都高才会对整体产生大影响。
**$(W_i \cdot U_i)^2$：** 平方是为了放大较大值的影响，一步严重偏离会被显著放大，而不是被其他正常步骤平均掉
**$\frac{1}{N}\sum_{i=1}^{N}$：** 对N步求平均，就是把所有步骤的加权不确定性平方加起来再除以N
**$\sqrt{\cdot}$：** 最外层开根号，把平方还原回原来的量纲，这整体就是均方根（RMS）

总结：对每步的加权不确定性求均方根，权重大的步骤对最终分数影响更大

---
## 4.2 单步不确定性估计

每步输出包含两部分：Thinking（$T_n$）和Action（$A_n$），两者分别算熵再相加：$$U_n = U_n^T + U_n^A$$
其中：
**$U_n$：** 第n步的总不确定性，由Thinking和Action两部分相加得到
**$U_n^T$：** 第n步Thinking部分的不确定性，即LLM生成思考过程时有多不确定
**$U_n^A$：** 第n步Action部分的不确定性，即LLM生成动作指令时有多不确定

每部分用带长度归一化的预测熵估计：$$U_n^R = \mathbb{E}_{R_n}\left[\frac{1}{|R_n|}\sum_{a_i} -\log P(a_i \mid Q, Z_{n-1}, a_0, \cdots, a_{i-1})\right]$$其中：
**$U_n^R$：** 第n步某一部分（T或A）的不确定性，R是占位符，代指T或A
**$R_n$：** 第n步的LLM输出序列，可以是$T_n$（思考文本）或$A_n$（动作文本）
**$|R_n|$：** 输出序列的token数量，用来做长度归一化
**$a_i$：** 输出序列中第i个token
**$Q$：** 原始问题，即用户输入
**$Z_{n-1}$：** 前n-1步的完整轨迹记录，包含所有历史的思考、动作、观察
**$a_0, \cdots, a_{i-1}$：** 当前序列中第$\ i$ 个token之前已经生成的所有token，即生成 $a_i$ 时的上文
**$P(a_i \mid Q, Z_{n-1}, a_0, \cdots, a_{i-1})$：** 在已知原始问题、历史轨迹、当前上文的条件下，生成第i个token的概率
**$-\log P(\cdot)$：** 对概率取负对数，概率越低值越高，表示这个token越不确定
**$\frac{1}{|R_n|}\sum_{a_i}$：** 对序列所有token的不确定性求平均，即长度归一化
**$\mathbb{E}_{R_n}$：** 对多次采样的输出序列取期望，实践中通过多次采样估计

长度归一化的原因：长输出的熵天然比短输出高，不归一化会系统性高估长步骤的不确定性

---
## 4.3 情境权重估计
### 4.3.1 两个代理指标：

1. **Inquiry Drift** $D_a$：计算当前思维链轨迹 $Z_n$ 与原始问题 $Q$ 的语义距离，衡量**全局偏离**
2. **Inference Gap** $D_o$：计算当前步骤的 Action $A_n$ 与 Observation $O_n$ 的语义距离，衡量**局部断裂**

两者均用 RoBERTa（fine-tuned on SQuAD v2）计算
### 4.3.2 三种权重方案：

1. **SAUP-P（位置代理）**：步骤位置直接作为权重，越靠后权重越大
2. **SAUP-D（距离代理）**：$W_n = D_{a_n} + D_{o_n}$，独立处理每步
3. **SAUP-HMMD（学习代理）**：将 $(D_{a_n}, D_{o_n})$ 序列输入CHMM，用推断出的隐状态作为权重

CHMM隐状态定义为三类：正确轨迹 / 轻度偏离 / 严重偏离，用Baum-Welch算法训练。优势在于能捕捉状态跳变——第4步严重偏离后，即使第5步数值回落，CHMM仍能推断其处于偏离状态

---
## 4.4 为什么选CHMM而不是LSTM或Transformer

不确定性估计任务的标注数据天然稀少：

- HMM：参数少、数据需求低，小数据下稳定，主选
- LSTM：数据够时能追上HMM，数据少时不稳定
- Transformer：需要大量数据，完全不适合这个场景

---
# 五、实验与结论（数据说明了什么、有什么局限性）
## 5.1 实验设置

**模型**：LLaMA3-8B、LLaMA3-70B、GPT-4o

**数据集**：

- HotpotQA：多跳问答，需要跨多个文档推理，随机采样2000题
- MMLU：多选题，覆盖法律、数学等多个领域，每个子任务采样10题
- StrategyQA：隐式推理，判断题形式，229题

**外部环境**：

- HotpotQA/StrategyQA：Wikipedia API
- MMLU：SerpAPI（谷歌搜索结果）

**评估指标**：AUROC，衡量不确定性分数区分正确/错误答案的能力，越高越好，1.0满分，0.5等于随机猜

---
## 5.2 数据说明了什么

### 5.2.1 多步传播比单步有没有用？

有用。即使最简单的均匀传播（算术平均、几何平均、RMS）也全面优于只看最后一步的方法，说明中间步骤的不确定性信息本身就有价值，不是SAUP特有的
### 5.2.2 情境权重有没有用？

有用。三种加权方案（SAUP-P/D/HMMD）均优于均匀传播，HMMD效果最强，说明不是所有步骤贡献相同，情境质量确实影响权重该怎么分配
### 5.2.3 SAUP整体表现怎样？

全面优于所有baseline，最高提升20% AUROC。在StrategyQA上提升最显著，因为这个数据集的问题最复杂，推理链路最长，累积不确定性的问题最突出
### 5.2.4 HMM合不合理？

小数据下HMM最稳定。随着训练数据增加，LSTM逐渐追上，Transformer始终需要大量数据才能发挥，实际场景里不现实
### 5.2.5 题目难度影响大吗？

有影响。越复杂的任务，SAUP的优势越明显，因为复杂任务推理步骤更多，中间步骤的不确定性积累更严重，只看最后一步的问题更突出

---
### 5.3 局限性

1. **标注成本高**：学习代理版本（SAUP-HMMD）依赖人工标注的情境权重数据，费时费力，复杂场景下标注质量也难以保证

2. **单步估计的误差会传播**：SAUP假设每步的$U_i$​估计是准确的，但如果单步估计本身就有问题，误差会通过传播放大，而不是被消除

3. **未来方向**：用LLM自动生成标注数据替代人工标注，降低学习代理的使用门槛