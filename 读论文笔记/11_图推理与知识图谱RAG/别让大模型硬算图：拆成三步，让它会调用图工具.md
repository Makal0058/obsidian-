> [!info] 基本信息
> - **论文题目**：Graph-constrained Reasoning: Faithful Reasoning on Knowledge Graphs with Large Language Models
> - **中文题目**：GraphTool-Instruction：通过分解子任务指令革新大模型图推理
> - **期刊/会议**：KDD 2025 Research Track
> - **年份**：2025
> - **Tag**：  #图推理 #工具学习 #FunctionCalling #指令微调 #子任务分解 #Agent

---
```table-of-contents
```
---
# 一、研究背景（前人做到哪一步）

> [!abstract] 核心矛盾
> LLM 具备基础的图属性理解能力，能处理一些图推理任务，但**准确率严重不足**。根因在两个挑战：
> - **挑战 1 — 图理解 (Graph Understanding, GU)**：LLM 能否用自然语言真正读懂图的拓扑结构？
> - **挑战 2 — 图处理 (Graph Processing, GP)**：LLM 能否靠生成式推理真正解出图算法（如最短路）？
## 1.1 第一代：Text-Instruction（纯文本指令）

> [!note] 思路：让 LLM 一步步"想"出答案
> 以 CoT 为起点，靠 step-by-step 推理或额外提示来解图问题
> - **代表**：[[GraphWiz]]（GPT-4 生成推理路径 + 多采样）、[[GraphInstruct]]（多样图生成过程 + step mask 训练）、[[Talk like a Graph]]（研究不同图描述语言的影响）
> - **进展**：在简单任务（环检测、最短路）上 CoT 有效
> - **痛点**：
>   - 复杂任务（最大流、拓扑排序）上提升**不稳定**
>   - 主要改善 GP，**对 GU 帮助有限**
>   - **OOD（域外）任务上表现差**，泛化弱

---
## 1.2 第二代：Tool-Instruction（工具指令）

把"算"交给外部工具，LLM 只负责生成调用。源自 [[Toolformer]] 的工具学习思想

> [!note] 2a. Graph-ToolFormer —— 第一个吃螃蟹的
> [[Graph-ToolFormer]] 首次让 LLM 生成工具调用来解图推理（知识图谱、社交网络、推荐）
> - **痛点**：
>   - 工具在**外部图**上执行 → 难泛化到自然语言组织的图任务
>   - 受工具名多样性、调用格式约束 → GP 表现低
>   - 在"图信息+任务描述"混合查询上 GU 不行

> [!note] 2b. Function Calling —— 闭源大模型的新范式
> GPT-4o、Claude-3-opus、GLM4-0520 引入 FC：LLM 当控制器，把工具描述塞进 prompt，无需训练即可灵活调用
> - **进展**：免训练、灵活、调用能力强
> - **痛点**：
>   - 强依赖**工具文档质量**和 LLM 输出文本质量
>   - 输出难解析 → 调用失败 / 调错工具
>   - **图任务上 FC 的内部机制扛不住 GU 挑战**

---
## 1.3 关键缺口 (Research Gap)

> [!important] 前人留下的核心问题
> 现有 Tool-Instruction 方法**只盯着工具信息，忽略了图结构信息**
> → 后果：在 **小模型（<13B）上性能急剧下降**。即"参数填不对、图抽不准"——LLM 没法同时把抽图、选工具、填参三件事一次性做对

| 维度 | Text-Instruction | Tool-Instruction (FC) | 缺口 |
|------|------------------|------------------------|------|
| GU（读懂图） | 弱 | 弱（FC 机制扛不住） | **共同短板** |
| GP（解算法） | 不稳定 | 强（工具兜底） | Text 弱 |
| 小模型(<13B) | 差 | **急剧下降** | 本文主攻 |
| OOD 泛化 | 差 | 一般 | — |
| 图结构信息 | — | **被忽略** | 本文切入点 |

---
## 1.4 本文切入点

> [!tip] GraphTool-Instruction 的破题思路
> 不再把图推理当成一个整体任务，而是**显式拆成三个子任务**，各配专门指令：
> - **Graph-Instruction**（治 GU）→ 图抽取
> - **Task-Instruction**（治 GP）→ 工具名识别
> - **Parameter-Instruction**（治 GP）→ 工具参数抽取（专治参数遗漏/乱序/格式不合规）
>
> 即插即用 prompt，免微调；并据此做出 GTools 数据集 + 微调出 GraphForge（Llama3-8B），让小模型逼近 GPT-4o

---
# 二、拟解决的问题（为什么现有方法不行）

> [!abstract] 一句话
> 现有方法在小模型上崩，根因是把"读图"和"用工具"**揉成一个任务**，让 LLM 一口气同时完成抽图、选工具、填参——**能力不够时三件事互相拖累，错误层层传导**

## 2.1 问题总纲：GU 与 GP 没被同时解决

> [!important] 两个挑战错位
> - **Text-Instruction** 靠生成式推理硬解 GP → 复杂任务（最大流、拓扑排序）不稳定，且**完全没碰 GU**（读图本身就错）
> - **Tool-Instruction** 用工具兜住了 GP → 但**只关注工具信息，把图结构信息丢了**，GU 反而成了瓶颈
> 结论：前人要么缺 GU，要么缺 GP，没有一个方法把两者一起按住

注：
- GU （Graph Understanding）**图理解能力**，也就是：模型能不能**把题目里的图结构读对**
- GP（Graph Processing）**图处理能力 / 图算法求解能力**，也就是：模型能不能真正解决**图算法问题**

---
## 2.2 Graph-ToolFormer 不行在哪

> [!note] 痛点拆解
> - 工具在**外部图**上执行 → 对"自然语言里描述的图"泛化差
> - 受**工具名多样性 / 调用格式**约束 → GP 准确率低
> - 面对"图信息 + 任务描述"混合输入时，**抽不准图结构**（GU 崩）
> - 实测：参数遗漏、乱序、与预定义格式不一致频发（见 Figure 14 的 Syntax / Name / Para Mismatch）

---
## 2.3 Function Calling（闭源大模型）不行在哪

> [!note] 痛点拆解
> - 强依赖**工具文档质量**与 LLM 输出文本质量 → 输出难解析就调用失败
> - 图任务上 FC 的**内部参数解析机制扛不住 GU**：
>   - **GLM4-0520-FC**：处理带权图时，每次只返回单条边 → 大量 Syntax Error，甚至 `tool_calls` 直接返空（Figure 10-12）
>   - **GPT-3.5-turbo-FC**：图结构抽取存在大量信息缺失与遗漏 → Syntax Error（Figure 13）。
> - 只有 **GPT-4o-FC** 例外，靠超强指令遵循扛住了——但**高成本**

---
## 2.4 核心病灶：三件事同时做 → 不可解析

> [!important] 消融实验给出的"病因证据"（Table 7）
> 去掉 Graph-Instruction（w/o GI）后性能断崖式下跌：
> - Cycle 99.0 → 86.2，Shortest 98.2 → 40.6
> 论文诊断：**LLM 无法同时且准确地执行 graph extraction / tool name / tool parameter 三个子任务**，缺了图抽取这一环，整条链的图结构信息就变成"不可解析"，后续选工具、填参全部连带失效

| 失败模式 | 含义 | 高发方法 |
|----------|------|----------|
| Syntax Error | 参数标签返空 / 正则解析失败 | GLM4-FC、GPT-3.5-FC |
| Graph Mismatch | 抽出的图与原图不符（哪怕只差一条边） | GraphForge（长输入时） |
| Name Mismatch | 工具名识别错 | Graph-ToolFormer、GLM-FC |
| Para Mismatch | 参数乱序 / 遗漏 / 格式不合规 | Graph-ToolFormer、未上 PI 时 |

----
## 2.5 为什么小模型尤其崩

> [!warning] 规模放大了缺陷
> - <13B 小模型**指令遵循能力弱**，"同时三任务"的认知负担直接压垮它
> - 图越大（边数 >100）→ 长输入下 **Graph Mismatch** 显著上升（GraphForge 的主要残余错误）
> - 这正是论文要"拆子任务 + 各配指令"来降低单步负担的动机

---
# 三、创新点与贡献

> [!abstract] 一句话
> 核心创新是**任务分解**：第一个把图推理拆成"抽图 / 选工具 / 填参"三个子任务、各配专门指令的 GraphTool 级方法，让小模型也能在工具调用上逼近 GPT-4o；并配套交付了数据集（GTools）、模型（GraphForge）和三个新评测指标

## 3.1 方法创新：GraphTool-Instruction（核心贡献）

> [!important] 从"一个任务"到"三个子任务"
> 首次提出 **GraphTool 级**指令微调方法，把图推理任务分解为三个子任务，各自对应一套指令：
>
> | 子任务 | 对应指令 | 治哪个挑战 | 解决什么 |
> |--------|----------|-----------|----------|
> | 图抽取 (graph extraction) | **Graph-Instruction** | GU | 从自然语言/文件路径准确抽出图结构 |
> | 工具名识别 (tool name id) | **Task-Instruction** | GP | 选对工具 + 约束输出格式 |
> | 工具参数抽取 (param extraction) | **Parameter-Instruction** | GP | 治参数遗漏/乱序/格式不合规 |
>
> **关键性质**：即插即用 (plug-and-play) prompt，**免微调**即可用在不同 LLM 上

> [!note] 为什么这是真创新（对照 Table 1）
> 此前方法停留在 Text 级或 Tool 级指令：
> - Text-Instruction（GraphWiz / GraphInstruct）：只有 Text 级
> - Tool-Instruction（Graph-ToolFormer / GPT-4o-FC / GLM4-FC）：Text + Tool 级，但**不显式处理图结构**
> - **GraphTool-Instruction**：Text + Tool + **GraphTool 级**（三者全覆盖，独此一家）

## 3.2 资源创新：GTools 数据集

> [!note] 第一个 GraphTool-Instruction 数据集
> - **20 种**图推理任务（11 类经典任务 × 有向/无向/带权变体）
> - **40,000 实例**（每任务 2,000）
> - 任务分两类：
>   - **BGA-Task**（基础图分析）：只需图结构 + 工具名，如环检测
>   - **PGQ-Task**（参数化图查询）：额外需参数输入，如最短路要起止点
> - 图规模跨度大：WL-Graph（≤4096 token）到 EL-Graph（41-100 节点、最多 1000 边）
> - 生成时做了**答案均衡 / 唯一性 / 多样描述**控制，防止 LLM 靠刷分作弊

## 3.3 模型创新：GraphForge

> [!note] 开源图推理 LLM
> - 基于 **Llama3-8B**，用 GTools 经 **LoRA** 微调
> - 平均准确率 **>98%**，与高成本的 **GPT-4o-FC 相当**
> - 核心意义：**让小模型逼近闭源大模型**，绕开 GPT-4o 的高成本

## 3.4 评测创新：三个新指标

> [!important] 不只看答案对不对，而是拆开看哪一步错
> 引入 **Graph / Tool Name / Tool Parameter** 三个准确率指标，用来**深入定位**影响工具执行结果的因素
> - 发现：Name Accuracy 最高（LLM 选工具能力强）
> - Para Accuracy 略低于 Name（依赖工具名识别）
> - 对依赖完整图结构的任务（最大三角和、最短路），**Answer Accuracy 受 Graph Accuracy 制约**
> → 结论：**提升图结构抽取精度是关键瓶颈**

---
## 3.5 贡献清单（论文原文四点）

> [!tip] 速记版
> 1. 提出 GraphTool-Instruction（三子任务分解 + 三套指令）
> 2. 构建 GTools（首个 GraphTool 数据集，20 任务）+ 微调出 GraphForge
> 3. 显著提升 Tool-Instruction 精度，达 Tool 类 SOTA（仅落后 GPT-4o-FC 约 1%）
> 4. 引入 Graph/Name/Para 三指标，提升数据集可靠性并深入分析误差因素

---
# 四、方法与模型

> [!abstract] 总览
> 整条流水线：**输入图任务 → 三子任务分解（抽图/选工具/填参）→ 工具执行 → 结果比对筛选 → 构成 GTools → LoRA 微调出 GraphForge**。核心是用任务分解降低单步认知负担，再用 Tool Template Retriever 兜住参数格式
## 4.1 任务分类：BGA vs PGQ

> [!note] 两类任务决定流程分支
> - **BGA-Task**（基础图分析）：只需 图 + 工具名。如环检测、拓扑排序、节点/边计数。走"红色箭头"流程（Graph-Ins + Task-Ins）
> - **PGQ-Task**（参数化图查询）：额外需参数。如最短路（起止点）、最大流。走"蓝色箭头"流程，**多挂一道 Parameter-Instruction**

---
## 4.2 三个子任务 + 三套指令（方法核心）

> [!important] Graph-Instruction（治 GU → 抽图）
> 按图规模分两种 prompt：
> - **WL-Graph**（≤4096 token）：**Two-shot** prompt，让 LLM 按 NetworkX 列表格式抽出图（区分带权/不带权），再用**正则**解析重建图
> - **EL-Graph**（超长）：自然语言抽不全，改为 **One-shot** 让 LLM 只抽**文件路径**，由工具去路径读图

> [!important] Task-Instruction（治 GP → 选工具）
> - 手工构建工具集，每个工具定义四属性：**Tool Name / Description / Parameters / Return type**
> - 加"通用格式描述"约束 LLM 输出，强制走 `Thought → Tool_Name → Tool_Parameters` 格式
> - 实测：**工具名识别很准**，但**参数常出问题**（遗漏、乱序、不合格式）→ 引出下一步

> [!important] Parameter-Instruction（治 GP → 修参数，仅 PGQ）
> 两步走，专治参数错乱：
> 1. **Tool Template Retriever**：根据上一步识别的工具名，从工具集检索出该工具的**标准参数模板**
> 2. 把"检索到的模板 + Parameter-Instruction"拼成新输入，让 LLM **严格按模板**重新输出参数
> → 效果：上 PI 后参数乱序问题"几乎消失"（Figure 15）

---
## 4.3 形式化定义

> [!note] 子任务输出与工具执行
> 子任务集 $S = \{\mathcal{G}, \mathcal{N}, \mathcal{P}\}$（图/工具名/参数）
>
> 每个子任务输出由 LLM 按指令 $I^{(s)}$ 和任务 $x^{(s)}$ 生成：
> $$g^{(s)} = LLM(I^{(s)}, x^{(s)}), \quad s \in \{\mathcal{G},\mathcal{N},\mathcal{P}\}$$
>
> 用正则 $R^{(s)}$ 转成工具可用格式：
> $$\bar{g}^{(s)} = R^{(s)}(g^{(s)})$$
>
> 工具据三者产出结果：
> $$\hat{g} = Tool(\bar{g}^{(\mathcal{G})}, \bar{g}^{(\mathcal{N})}, \bar{g}^{(\mathcal{P})})$$

---
## 4.4 GTools 构造：严格筛选保质量

> [!note] 匹配函数把关
> 只保留**三个子任务标签全对 且 最终答案对**的样本：
> $$M(\bar{g}^{(\mathcal{G})}, \bar{g}^{(\mathcal{N})}, \bar{g}^{(\mathcal{P})}, \hat{g}) = \bigwedge_{s \in S}(\bar{g}^{(s)} = g^{*(s)}) \wedge (\hat{g} = g^{*})$$
> - 满足 $M$ → 保留 $g^{(s)}$；否则丢弃
> - 流水线：Llama3-8B 生成候选 → 工具执行 → 与标签比对 → 通过的整理成 **Alpaca 格式** → 最终 40,000 实例

> [!note] 图生成规则（防作弊）
> - **多样规模**：WL 节点 2-40 / 边≤300；EL 节点 41-100 / 边≤1000
> - **多样描述**：每任务 5 种描述，测任务识别能力
> - **答案均衡**：真/假类任务答案均匀分布，防偏置刷高分
> - **答案唯一**：拓扑排序等多解任务，生成时保证唯一解便于比对

---
## 4.5 GraphForge 训练：LoRA 微调

> [!note] 参数高效微调
> 基座 Llama3-8B，用 LoRA 做低秩更新：
> $$h_i^{(s)} = W\tilde{x}_i^{(s)} + \frac{\alpha}{r}BA\tilde{x}_i^{(s)}$$
> - 只更新低秩矩阵 $A \in \mathbb{R}^{r \times d_n}$、$B \in \mathbb{R}^{d_m \times r}$，$r \ll \min(d_m, d_n)$
> - 损失：交叉熵，比对 LLM 输出 $h_i^{(s)}$ 与期望输出 $y_i^{(s)}$
> - 超参：lr=1e-5，warmup=0.1，batch=4，cosine 调度，3 epochs，单卡 A800-80G

## 4.6 完整流程（Figure 3 六步）

> [!tip] 速记
> Step1 图生成 → Step2 选任务 → Step3 任务分解 → Step4(A 抽图 / B 选工具 / C 抽参，中间挂 Tool Template Retriever) → Step5 工具执行 → Step6 结果比对 → 沉淀为 GTools

---
# 五、实验与结论（数据说明了什么、有什么局限性）

> [!abstract] 一句话
> 三个 RQ 验证：方法有效（碾压 Text/Tool 基线、小模型逼近 GPT-4o）、两个挑战都被治住（GU 靠 Graph-Ins、GP 靠 Param-Ins）、误差可归因（图抽取是主瓶颈）。但局限也清楚：长输入下图抽取仍会崩，且工具集需手工构建。
## 5.1 三个研究问题 (RQ)

> [!note] 实验设计骨架
> - **RQ1 主结果**：GraphTool-Instruction vs Text / Tool 方法谁强？
> - **RQ2 消融**：两个挑战（GU / GP）是否真被分别解决？
> - **RQ3 误差分析**：工具执行出错的原因是什么？

---
## 5.2 RQ1 — 主结果：数据说明了什么

> [!important] 核心数字
> - **碾压 Text-Instruction**：GraphForge 平均 **98.4%**，最强 Text 基线仅 46.2%（差 40%+）
> - **碾压 Graph-ToolFormer**：WL/EL 上均领先 **40%+**（GTF 死在工具名识别 + 参数抽取）
> - **逼近 GPT-4o-FC**：与高成本 GPT-4o-FC **相当**（仅落后约 1%）；比 GLM4-0520-FC、GPT-3.5-turbo-FC 领先 **30%+**
> - **免微调也有效**：仅用 GraphTool-Instruction（不微调）的 Llama3-8B 就达 94%
> - **小模型 + 本方法 > 大模型 + FC**：GLM4-9B 用本方法超过 GLM4-0520-FC 用 Function Calling

> [!note] OOD 泛化（NLGraph 上）
> 在未训练过的 Bipartite、GNN 等 OOD 任务上，GraphForge 仍 ~99.6%，泛化强（只需把新工具描述加进工具集）

---
## 5.1 RQ2 — 消融：两个挑战是否真被解决（Table 7）

> [!important] 拆掉哪个指令掉多少
> | 任务 | 完整 GF | w/o GI | w/o PI |
> |------|---------|--------|--------|
> | Cycle | 99.0 | 86.2 | / |
> | Shortest | 98.2 | 40.6 | 92.2 |
> - **去 Graph-Instruction（GU）**：断崖下跌（Shortest 98.2→40.6）。证明图抽取是地基，缺了它图信息"不可解析"
> - **去 Parameter-Instruction（GP）**：约 **+8%** 来自 PI，证明它专治参数失配有效
> - 印证第二节诊断：**LLM 无法同时准确做三件事**，必须拆

---
## 5.3 RQ3 — 误差归因（Figure 4 & 5）

> [!note] 三指标给出的因果链
> - **Name Accuracy 最高** → LLM 选工具能力强
> - **Para 略低于 Name** → 因为填参依赖先选对工具
> - 对依赖完整图结构的任务（最大三角和、最短路）：**Answer Accuracy 受 Graph Accuracy 制约**
> - 对 Cycle 这类不依赖完整结构的任务：Answer 反而高于 Graph Accuracy
> → **结论：图抽取精度是天花板，是后续一切的瓶颈**

> [!note] 各方法错误分布（Figure 5）
> | 方法 | 正确率 | 主要病灶 |
> |------|--------|----------|
> | GraphForge | 94.29% | 残余 Graph Mismatch（长输入） |
> | GPT-3.5-turbo-FC | 52.68% | Syntax Error（信息缺失/遗漏） |
> | GLM4-0520-FC | 45.58% | Syntax Error（带权图返单边/返空） |
> | Graph-ToolFormer | 33.73% | Name/Para Mismatch + Syntax |

---
## 5.4 结论

> [!tip] 论文站住了什么
> 任务分解（三子任务 + 三指令）显著超越 Text/Tool 基线
> GTools（20 任务）+ GraphForge（Llama3-8B, >98%）证明**小模型可逼近高成本闭源大模型**
> 三个新指标提升了数据集可靠性与误差可解释性

---
## 5.5 局限性（论文明说 + 可推断）

> [!warning] 明说的局限
> - **长输入下图抽取仍崩**：边数 >100 时 GraphForge 的主要残余错误就是 Graph Mismatch → 图结构抽取能力在长上下文下不稳
> - **小模型会"自作主张"分解任务**：GLM-9B、Llama3.1-8B 在最短路上会先判断路径是否存在再算，被评测判为错（缺多步推理机制兜底）

> [!warning] 可推断的局限（我的批判）
> - **工具集靠手工构建**：每个工具的四属性人工定义，新任务需人工加工具描述 → 规模化/自动化存疑
> - **强依赖正则解析**：抽图/抽参后用正则重建，正则本身脆弱（论文自己把"正则解析失败"算进 Syntax Error）
> - **评测偏严苛**："多步合理推理"被一刀切判错，可能低估了某些模型的真实能力
> - **任务局限于经典图算法**：20 个任务都是 Cycle/Shortest/Flow 这类有标准算法的题，**对没有确定性工具的开放图推理（如知识图谱问答、推荐）未覆盖**——这恰是 future work 提到的方向
