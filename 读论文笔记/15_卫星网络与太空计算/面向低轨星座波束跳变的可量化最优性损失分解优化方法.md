## 论文主线

一句话：

> 多星低轨 beam-hopping 中，中心化 MILP 解不动，现有启发式/DRL 没有最优性界；本文提出“组内 MILP + 组间拉格朗日对偶分解”的星座级分解优化框架，并报告可量化最优性损失。

---

# 题目可以先这样定

**中文：**

> 面向低轨星座波束跳变的可量化最优性损失分解优化方法

**英文：**

> Decomposition-Based Beam-Hopping Optimization with Quantifiable Optimality Loss for LEO Satellite Constellations

---

# 章节怎么写

## 1. Introduction

讲三件事：

1. LEO 星座 beam-hopping 需要多星协同；
2. 中心化 MILP 太大，星座级不可解；
3. 现有 DRL/启发式方法可扩展，但不知道离最优差多少。

最后引出：

> 我们提出一个带 dual bound 的分解优化框架。

贡献写三条：

- 建立多星 beam-hopping 联合优化模型；
- 提出组内 MILP + 组间拉格朗日对偶分解；
- 给出 gap certificate，并通过小规模 full MILP 与大规模实验验证。

---

## 2. Related Work

分三类写：

1. **单星 beam-hopping 优化**  
    讲 MILP、time-split、illumination/power/bandwidth 联合优化。
2. **多星协同资源分配**  
    讲 TWC 2024、多星协同、DRL、MARL、启发式。
3. **分解优化与对偶方法**  
    讲 Lagrangian relaxation、dual decomposition、ADMM。

---

## 3. System Model

定义清楚：

- 卫星集合；
- 轨道面/星组；
- cell；
- beam；
- time slot；
- frequency bin；
- power；
- bandwidth；
- demand；
- inter-satellite interference。

这里不要写 LLM。

---

## 4. Centralized MILP Formulation

先写完整大问题：

目标函数可以是最小化：

UC+η1EC+η2TTS\text{UC} + \eta_1 \text{EC} + \eta_2 \text{TTS}UC+η1​EC+η2​TTS

约束包括：

- illumination 约束；
- power 约束；
- bandwidth 约束；
- frequency bin 约束；
- demand satisfaction；
- UC carry-over；
- inter-satellite interference。

然后说明：

> 这个中心化问题只能小规模求，用作 benchmark。

---

## 5. Proposed Decomposition Method

这是核心章节。

### 5.1 空间分解

按轨道面或星组分解：

S=S1,S2,…,SG\mathcal{S}=\mathcal{S}_1,\mathcal{S}_2,\dots,\mathcal{S}_GS=S1​,S2​,…,SG​

每组内部解 MILP。

### 5.2 拉格朗日松弛

把跨组干扰约束松弛：

Ig,h(xg,xh)≤Imax⁡I_{g,h}(x_g,x_h)\leq I_{\max}Ig,h​(xg​,xh​)≤Imax​

加入乘子：

λg,h(Ig,h(xg,xh)−Imax⁡)\lambda_{g,h}\bigl(I_{g,h}(x_g,x_h)-I_{\max}\bigr)λg,h​(Ig,h​(xg​,xh​)−Imax​)

直觉写成：

> λ\lambdaλ 是干扰价格，造成干扰越多，代价越高。

### 5.3 子问题

每个组独立解：

min⁡xgfg(xg)+∑hλg,hIg,h(xg,xh)\min_{x_g} f_g(x_g)+\sum_h \lambda_{g,h} I_{g,h}(x_g,x_h)xg​min​fg​(xg​)+h∑​λg,h​Ig,h​(xg​,xh​)

### 5.4 乘子更新

用次梯度：

λg,hk+1=[λg,hk+ρk(Ig,h(xgk,xhk)−Imax⁡)]+\lambda^{k+1}_{g,h} = \left[ \lambda^k_{g,h} + \rho_k \bigl(I_{g,h}(x_g^k,x_h^k)-I_{\max}\bigr) \right]^+λg,hk+1​=[λg,hk​+ρk​(Ig,h​(xgk​,xhk​)−Imax​)]+

### 5.5 可行解恢复

因为松弛解可能违反干扰约束，所以要加一个修复步骤：

- 调整边界 cell 的 frequency bin；
- 降低低优先级 beam 的功率；
- 或重新求边界局部 MILP。

### 5.6 Gap certificate

写：

Gap=Pfeasible−Ddual∣Pfeasible∣\text{Gap} = \frac{ P_{\text{feasible}}-D_{\text{dual}} }{ |P_{\text{feasible}}| }Gap=∣Pfeasible​∣Pfeasible​−Ddual​​

注意强调：

> 这是相对于对偶界的 certificate，不声称一定接近全局最优。

---

## 6. Experiments

实验分四组。

### 6.1 小规模验证

对比：

- Centralized full MILP；
- 你的 decomposition；
- Independent MILP；
- heuristic。

目的：

> 验证分解解接近中心化最优，并报告真实 gap。

### 6.2 大规模可扩展性

对比：

- 你的方法；
- MARL；
- PSO/GA；
- TWC 2024 baseline；
- independent scheduling。

看：

- UC；
- EC；
- TTS；
- runtime；
- dual gap；
- convergence。

### 6.3 消融实验

- 无拉格朗日，仅独立组内 MILP；
- 有拉格朗日，无可行解修复；
- 不同分组方式；
- 不同耦合强度。

### 6.4 参数敏感性

- 星数；
- cell 数；
- traffic load；
- interference threshold；
- time window size；
- coupling density。

---

## 7. Conclusion

收束成：

> 本文不是追求星座级精确求解，而是提供一种可扩展、可报告最优性损失的分解优化框架。

---

# 哪些不要写进第一篇

先砍掉：

- LLM；
- RAG；
- Shachi 作为核心；
- tool uncertainty；
- 语义异常；
- 期货预锁；
- 人工介入；
- 三类不确定性。

Shachi 可以最多一句：

> 可作为工程实现中的多 agent 消息组织框架。

但别让它进贡献点。

---

# 最重要的写作原则

不要写：

> near-exact constellation benchmark

要写：

> quantifiable optimality loss

不要写：

> first exact constellation optimizer

要写：

> scalable decomposition framework with dual-bound-based certificate