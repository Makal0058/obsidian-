> [!info] 基本信息
> - **论文题目**：Verifying Chain-of-Thought Reasoning via Its Computational Graph
> - **中文题目**：用模型内部计算图验证 CoT 推理是否正确
> - **期刊/会议**：ICLR 2026 Oral
> - **年份**：2026
> - **Tag**： #CoT #推理验证 #机制可解释性 #白盒方法 #归因图 #Transcoder #因果干预 #结构指纹

---
```table-of-contents
```
---
# 一、研究背景（前人做到哪一步）

> [!abstract] 一句话定位
> CoT 推理"有时是错的"已是共识，于是产生了**自动验证**这一研究方向。前人沿"黑盒 → 灰盒"两条路线推进，但都停在**相关性**层面，无法回答"为什么"这一步计算会出错

---
## 1.1 起点：CoT 强大但不可靠

- **CoT 提示** 显著提升 LLM 推理能力（Wei 2022、Kojima 2022]），并成为 DeepSeek-R1、OpenAI o1 等推理模型的核心机制
- 但推理过程本身**有时是有缺陷的**——这是贯穿各类系统的根本性脆弱点（Turpin 2023、Arcuschin 2025、Chen 2025b）
- 这一"可靠性缺口"直接催生了**自动验证（automated verification）** 研究

---
## 1.2 前人两条路线

> [!note] 两类已有方法
> 现有验证方法可归为两类，均**只观察"状态"，不触及"计算过程"**

| 路线               | 观察对象                 | 代表工作                                           | 根本局限                         |
| ---------------- | -------------------- | ---------------------------------------------- | ---------------------------- |
| **黑盒 Black-box** | 生成文本 / 最终 logit 分布   | Jacovi 2024、Baker 2025                         | 仅看输出表象                       |
| **灰盒 Gray-box**  | 模型内部状态（原始激活 / 隐状态轨迹） | CoE（Wang 2025a）、CoT-Kinetics（Bi 2025）、LR Probe | 只能发现状态与错误**相关**，无法解释**为何**出错 |

- **黑盒**：分析生成的文本或最终 logit 分布
- **灰盒**：用简单探针（probe）读取原始激活，或分析隐状态跨层的轨迹动态
- **共同短板**：二者都能 *detect*（检测）模型内部状态与错误相关，但都无法 *explain*（解释）底层计算**为什么**导向错误

---
## 1.3 留下的缺口 → 本文动机

> [!important] 关键转折
> "状态相关" ≠ "理解失败机制"。要回答 *why*，必须从**erroneous state（错误状态）**深入到 **flaw in the execution of a latent algorithm（潜在算法执行中的缺陷）**

- 机制可解释性视角：模型通过专门的**子图 / 电路（circuits）**实现潜在算法（Olah 2020、Elhage 2021）
- 因此推理失败应被视为**电路执行中的一处缺陷**，需要像查软件的 *execution trace* 那样去检视计算过程
- 本文用**归因图（attribution graph，Dunefsky 2025）**来近似这条"执行轨迹"，开辟**白盒（white-box）**路线——即下文的 CRV

---
# 二、拟解决的问题（为什么现有方法不行）

> [!abstract] 一句话
> 现有验证方法只能告诉你"这步**可能错了**"，却给不出"**为什么错**"。CRV 要解决的，正是从**错误检测**走向**错误的机制性理解**
## 2.1 核心痛点：只能 detect，不能 explain

- 黑盒（logit）和灰盒（激活/隐状态）方法本质上是**相关性探针**：它们捕捉的是"内部状态与错误相关"这一统计信号
- 但相关 ≠ 机制。它们**无法定位**导致错误的具体计算环节，因而无法回答 *why*——这是所有现有路线共同的天花板

---
## 2.2 缺一条"可检视的执行轨迹"

> [!note] 类比
> 调试软件时我们看 *execution trace*；而验证 LLM 推理时，前人手里**没有等价物**——他们只能看程序的"输出"或"某一刻的内存快照"，看不到"指令是怎么一步步执行的"

- 若把推理失败理解为**潜在算法（latent algorithm）在电路（circuit）上的执行缺陷**，就必须有一个能反映**因果信息流**的结构表示
- 现有方法都不提供这种**结构化的计算过程表示**，自然无法做机制层面的归因

---
## 2.3 数据层面的连带障碍

> [!warning] 现成数据集不可用
> 白盒方法的需求与前人根本不同，导致已有数据集**无法直接用**

- 黑盒 verifier 用的是静态的 `(text, label)` 对，如 PRM800K、REVEAL。
- 而 CRV 分析的是"产生某步推理的**因果计算图**"，必须捕获**特定模型在生成过程中的完整内部状态**
- 因此需要的是 `(text, label, computational trace)` 三元组——这要求作者**自建数据集**（合成 Boolean/Arithmetic + 标注后的 GSM8K）

---
## 2.4 可解释性的前置条件：原始激活不可读

> [!important] 为什么必须引入 Transcoder
> 直接看模型激活行不通——高维、稠密、无直接语义。要让"计算图"成为**有意义的轨迹**，其节点必须**可解释**

- 原始 MLP 激活：高维 + 缺乏直接可解释性 → 无法作为图的节点
- 解决路径：用 **transcoder**（Dunefsky 2025）作为 MLP 的**功能替代物**，把稠密计算强制改写为**稀疏、可解释特征**的组合，才能在此之上构造可读的归因图

---
## 2.5 本文要回答的三个问题（RQ）

> [!tip] 问题拆解
> 把"为什么现有方法不行"转化为三个可验证的研究问题：

1. **RQ1 — 可行吗？** 直接在计算图上验证推理，能否显著超越黑盒/灰盒基线，并对领域漂移、难度上升保持鲁棒？
2. **RQ2 — 是什么？** 图中哪些具体结构最能预测失败？能否从"检测"走向"机制理解"？
3. **RQ3 — 是因果吗？** 这些错误信号只是相关，还是因果？能否靠干预单个特征来**纠正**错误推理？

---
# 三、创新点与贡献

> [!abstract] 一句话
> 第一个把"归因图当作推理的**执行轨迹**"来做**自动验证**的工作，并把研究目标从"测得准"上升到"**理解失败机制**"——不止检测，还能解释、还能因果纠正
## 3.1 核心创新：从"看状态"到"看过程"的范式转换

> [!important] 方法论定位
> 把验证的对象从**输出/激活（状态）**换成**归因图（计算过程）**，开辟黑盒、灰盒之外的**第三条路线——白盒**

- 关键概念：把每步推理的**归因图视为潜在电路的执行轨迹（execution trace）**，并假设正确/错误步骤在图上留下**可区分的结构指纹**
- 这是据作者所知**首次**将 transcoder 归因图从"定性可视化"（Ameisen 2025）**操作化为定量的自动验证**——从"肉眼看图"变成"提取结构特征 + 训练分类器"

---
## 3.2 四点具体贡献

> [!note] 贡献清单
> 论文明确把贡献定位为"不只是性能，更是理解（understanding）"
### 3.2.1 提出 CRV（Circuit-based Reasoning Verification）

   - 一套白盒方法：MLP→transcoder 替换 → 构造归因图 → 提取三类结构特征 → 诊断分类器。
   - 证明"直接通过计算图验证推理"是**可行的**
### 3.2.2 发现错误信号高度领域特定（domain-specific）

   - 跨域迁移性能大幅下降，说明形式逻辑、算术、自然语言数学题的失败，在计算图上呈现**截然不同的结构模式**
   - 这既是限制，也反向证明信号之丰富与真实
### 3.2.3 确立错误信号的因果角色（causal role）

   - 失败预测特征可回溯到模型计算的**具体组件**
   - 通过对单个 transcoder 特征做 forward hook 干预（clamp 归零 / 放大），**成功纠正**了模型的错误推理——构成闭环（closed-loop）证据
### 3.2.4 开源数据与工具

   - 将释放带**步骤级正确性标签**的数据集（合成任务 + 真实 GSM8K），以及训练好的 transcoder，支撑后续研究

---
### 3.3 三项"他法不可得"的科学洞见

> [!tip] 白盒独有的副产品
> 这些发现是黑盒/灰盒方法**结构上拿不到**的，是 CRV 作为"科学仪器"的价值所在

- **结构签名高度可预测** → 验证"经由计算图验证推理"这一假设本身成立（RQ1）
- **失败模式因域而异** → 不同推理任务的错误是不同计算模式，而非单一通用故障（RQ2）
- **签名非纯相关** → 干预单特征即可改变结果，说明签名**因果地参与**了错误（RQ3）

---
## 3.4 一个诚实的自我定位

> [!warning] 边界声明
> 作者主动声明：CRV **计算开销巨大**，不是即插即用的实用 verifier，而是一台用于**深度机制分析的科学仪器（scientific instrument）**
> 这点反而强化了贡献的可信度——卖点不是落地性能，而是**可解释性与因果洞见**

---
# 四、方法与模型

> [!abstract] 一句话
> CRV 是一条四阶段流水线：把 **MLP 换成可解释 transcoder** → 对每步推理**构造归因图** → 提取**三类结构特征**得到固定维度指纹 → 喂给 **Gradient Boosting 分类器**判正确/错误。核心是把"内部计算"变成"可量化的图结构"
## 4.1 模型设置

> [!note] 基座
> - **目标模型**：Llama 3.1 8B Instruct（选 instruct 版，因为其 prompt-following 能力是可靠诱发 CoT 的前提）
> - **数据集**：合成 Boolean、合成 Arithmetic、标注后的 GSM8K
> - **视角差异**：不同于 PRM（只判对错），CRV 站在"对模型有完全访问权、想 debug 失败机制的开发者"视角

---
## 4.2 Step 1 — 用 Transcoder 替换 MLP（让模型可解释）

> [!important] 关键架构改造
> 把稠密、不可读的 MLP 计算，强制改写为**稀疏、可解释特征**的组合

- 为每个 MLP 模块训练一个对应的 **TopK-Transcoder（PLT，per-layer transcoder）**
- 训练目标：L2 重构损失 + TopK 激活（只保留 k 个最大激活，结构性地强制稀疏）
- 关键区别于普通 SAE：transcoder 逼近的是 MLP 的**输入-输出函数** $f(x)\approx \text{MLP}(x)$，是 MLP 的**功能替代物**，而非自重构 $f(x)\approx x$
- 替换后，前向传播被强制流经这些稀疏可解释的瓶颈；后续所有分析都在这个**改造后的替代模型**上做

> [!info] Transcoder 训练细节（Appendix B）
> - 语料：RedPajama-V2 的 10B token 子集；丢弃 BOS token（对稳定训练有害）
> - 维度：输入 4096（MLP 隐维），过完备 latent 维 **131,072**，$k=128$
> - 训练：4 epoch，AdamW，lr 7e-5，4 节点 × 8×H200，约 4000 步收敛
> - 死神经元复活机制防止特征坍塌（1000 万 token 未激活则触发辅助损失）

---
## 4.3 Step 2 — 构造步骤级归因图

> [!note] 因果信息流的结构表示
> 复用 Dunefsky 2025 的电路分析方法，得到每步 $s_i$ 的稀疏、带权、有向图 $G_i=(V,E)$

- **路径追踪**：用贪心算法，从最终 logit **反向**追踪高归因连接
- **节点 $V$**（三类的并集）：输入 token、激活的 transcoder 特征、输出 logit
- **边 $E$**：组件间的高归因因果通路（如早层特征 → 晚层特征、特征 → logit），权重量化影响强度
- **实现**：用 Hanna 2025 的 `circuit-tracer`；最多 4096 特征节点、最多 10 个 logit 节点（累计概率 0.95 选取）

> [!tip] 一个关键消融：归因位置
> 在**当前步最后一个 token（After）**计算图，显著优于在**上一步最后 token（Before）**
> 原因：失败的结构签名在该步**完整执行后**才在末 token 表示中充分固化——"执行后的轨迹"才含失败证据

---
## 4.4 Step 3 — 提取三类可解释图特征（结构指纹 $x_i$）

> [!important] 三层级特征体系
> 先把图剪枝（保留占总影响 ~80% 的节点和边），再在剪枝子图上抽取三类特征，拼成固定维度向量 $x_i=\phi(G_i)$

### 4.4.1 全局图统计（Global Stats）

   - 剪枝后活跃特征节点数、transcoder 特征节点 / 残差流节点计数
   - top token 概率、最终 logit 分布熵 → 计算的复杂度与不确定性的粗粒度度量
### 4.4.2 节点影响与激活统计（Node Stats）⭐ *消融显示最关键*

   - 节点影响分数（mean；残差流"error"节点的 total/mean influence）
   - 特征激活值的 mean / max / std → 区分"少数高激活决定性特征"vs"大量弱特征弥散驱动"
   - **逐层特征直方图** → 刻画计算深度（错误是否与特定层组件激活相关）
### 4.4.3 拓扑与路径特征（Topological Stats）

   - 边权统计（sum/mean/std）、边数、**图密度**（假设稀疏破碎图 = 信息流崩溃）
   - 中心性（degree、weighted betweenness）→ 找计算"枢纽"节点
   - 连通性 / 路径长度，尤其是**输入 token → 最终 logit 的最短路径**（衡量 prompt 信息能否高效传到决策；长或不存在 → 模型在"忽略"指令/上下文）

---
## 4.5 Step 4 — 诊断分类器

> [!note] 分类
> $f_\theta(x_i)=\hat y_i \in \{\text{correct, incorrect}\}$

- 主实验用 **Gradient Boosting Classifier (GBC)**：适配异构表格特征，且提供稳健的特征重要度（便于 RQ2 定位最具预测力的结构）
- 对比过 Logistic Regression、Random Forest、Dummy（Appendix C.3）——结论是**没有单一分类器全面占优**，GBC 胜在跨域稳健；说明强信号来自**特征本身**而非分类器调参

---
## 4.6 数据标注的方法学严谨性

> [!warning] 双重把关 + 截断策略
> 标签质量是白盒分析的前提

- **合成任务**：LLM-as-a-Judge（Llama 3.3 70B）+ 程序化状态验证（让模型输出化简后表达式，再程序求值比对），**两者一致才保留**
- **GSM8K**：自然语言不可程序验证，仅用 LLM Judge，并人工复核子集（人-人 κ=0.42 / 一致 87.3%，人-机 κ=0.26 / 一致 84.1%）
- **错误传播处理**：采用**严格截断策略**——只保留到"第一个错误步"为止，丢弃其后所有步骤，避免"基于错误前提的局部正确步"造成标签歧义

---
# 五、实验与结论（数据说明了什么、有什么局限性）

**主验证性能（incorrect 为正类）**

|范式|方法|Boolean AUROC↑|Arithmetic AUROC↑|GSM8K AUROC↑|
|---|---|---|---|---|
|黑盒|最强者（Energy/MaxProb）|58.81|76.45|62.55|
|灰盒|最强者（CoE-C）|53.62|69.39|56.54|
|白盒|**CRV**|**75.87**|**92.47**|**70.17**|

CRV 在三个数据集、所有指标上全面超越所有黑盒灰盒基线。Arithmetic 上 AUROC 92.47 vs 最强基线 76.45，FPR@95 从 63.33% 降到 37.09%——结构信号最强的地方正是结构化的合成任务

> [!abstract] 一句话
> 数据证明了三件事：**结构指纹能验证推理（RQ1 全面超越基线）**、**错误信号高度领域特定（RQ2 跨域崩溃）**、**信号是因果而非相关（RQ3 干预单特征即可纠错）**。代价是计算开销巨大，仅适合作科学工具

## 5.1 RQ1 — 可验证性：数据说明了什么

> [!important] 核心结论
> **归因图的结构签名包含可直接验证的正确性信号**这一中心假设，得到强实证支持

- **全面领先**：三数据集 × 三指标，CRV 全面超越所有黑盒/灰盒基线
- **结构化任务信号最强**：Arithmetic 上 AUROC 92.47（最强基线 76.45），FPR@95 37.09%（基线 63.33%）
  - 解释：算法推理的结构化诱导出**高度一致的有效执行轨迹**，错误因而表现为**更统一的偏离**，更易检测
- **难度鲁棒性**：随算子数 n∈{5,7,10} 上升，CRV 始终保持优势；多数基线 AUROC/FPR 平稳，而 CRV 在难度与类别失衡变化下信号依旧稳健
- **分类器不敏感**：GBC/LR/RF 互有胜负，无单一赢家 → 强信号来自**特征本身**，而非分类器调参（Appendix C.3）

---
### 5.2 RQ2 — 机制理解：错误信号长什么样

> [!warning] 最关键发现：领域特异性
> CRV 学到的错误指纹**高度领域特定**——跨域迁移性能大幅下降，常常**跌破免训练基线**

- 例：Arithmetic 上训练的 CRV 迁移到 GSM8K，AUROC 仅 57.04，低于 Energy 基线的 62.55
- 含义：形式逻辑、算术、自然语言数学题的失败，在计算图上是**不同的结构模式**，而非单一通用故障
- 双刃：限制了当前监督式验证的通用性，但反向证明信号之**丰富与真实**

> [!note] 特征重要性（Arithmetic 消融）
> - **Node Stats 最关键**：移除后性能下降最多，FPR@95 恶化 12+ 点
> - Global Stats 贡献其次；Topological Stats 对此任务最不关键（局部关键特征状态 > 整体图结构）
> - 但三类**全用**才达最优

> [!note] 结构可分性（可视化证据）
> - 五个高预测力特征（Graph Density、Total Active Features 等）在正确/错误间分布显著分离（t-test p<0.001，Cohen's d 中-大）
> - PCA：错误步是正确步分布**内部的稠密子集**（"计算上的 near miss"，解释重叠），但存在一片**只有正确推理能到达的"计算完整性区域"**
> - t-SNE 印证：CRV 成功靠的是**学到这片区域的复杂边界**，而非分两个简单簇

---
## 5.3 RQ3 — 因果性：从诊断到干预

> [!important] 闭环证据
> 错误信号不只是相关——**干预单个 transcoder 特征即可纠正错误推理**

- 案例：表达式 (7*((5+9)+7))，模型错误地先算 7\*14=98
  - CRV 标记该步错误，特征重要性指向一个异常高激活的晚层"乘法"特征（ID 91814）
  - 用 forward hook **把该特征 clamp 到零** → 模型转而正确执行 14+7=21，得到正确答案
- 反向案例（Appendix）：表达式 (-(5+(4\*9)))，模型误把一元负号当减法
  - 通过**放大**一个"乘法"特征 → 模型正确识别为乘以 -1，得正确答案
- 双向干预（抑制 + 放大）都成功 → 签名**因果地参与**错误，而非旁观相关

---
### 5.4 局限性（作者主动声明）

> [!warning] 四点局限
> 局限与"白盒机制仪器"的定位内在绑定

1. **计算开销巨大**：训练全套 transcoder + 替换模块 + 每步构图，比黑盒/灰盒方法贵几个数量级 → **不是即插即用的实时 verifier，而是深度分析的科学工具**
2. **聚合式而非语义式分析**：当前特征是图的统计/拓扑属性（节点数、影响、密度），**未利用单个特征的语义**（如"加法特征是否被数字输入恰当激活"）→ 指向未来的**神经符号 verifier**
3. **泛化性存疑**：仅在 Llama 3.1 8B 单一模型族验证；MoE、70B+ 等架构/规模是否成立未知；且如 RQ2 所示，签名本身就**高度领域特定**
4. **可解释性工具的保真度上限**：transcoder 只是一种稀疏基、归因法只是真实信息流的不完整近似；结论的可靠性受这些底层工具保真度制约

---
### 5.5 总结这篇能给我什么

> [!tip] 接入 GIF
> 三点可直接迁移到 Graph-Intervention-Faithfulness：
> 1. **指标设计逻辑**：CRV 用"相关→因果"的递进（RQ1→RQ3）支撑可信度，我的 GFI=Raw GIS−PC-GIS 同样需要一个"因果隔离"环节（四种位置版本控制）才站得住
> 2. **领域特异性是卖点不是 bug**：CRV 把"跨域崩溃"写成"信号丰富的证据"——我的位置锚定现象若在不同图结构/任务上表现不同，也可同样框定
> 3. **诚实的边界声明**：CRV 主动说"我不实用、只是仪器"反而增信。我的 Pilot（20 样本）规模小，同样应明确定位为**诊断性评测框架**而非 SOTA 方法

---
# 六、关联课题需求（该文献的方法能否解决我的实验痛点、其缺陷是否可通过我的方案弥补）

> [!abstract] 一句话判断
> CRV **不能直接搬来解决我的痛点**（层级、访问权、开销都不匹配），但它在**方法论骨架**和**因果论证范式**上能给 GIF 强力背书；反过来，GIF 恰好能补 CRV 的两个核心缺陷——**领域特异性**与**聚合式分析**。两者是互补而非竞争
### 6.1 CRV 的方法能否解决我的实验痛点

> [!warning] 结论：不能直接解决，层级与约束都不匹配
> CRV 是"打开模型看电路"，GIF 是"控制输入测行为"——下针部位不同

| 维度   | CRV                      | 我的 GIF             | 是否可借用                     |
| ---- | ------------------------ | ------------------ | ------------------------- |
| 诊断层级 | 内部归因图（transcoder 特征）     | 外部输入图序列化           | ✗ 层级不同                    |
| 模型访问 | 白盒（需替换 MLP、训 transcoder） | 黑盒（仅 prompt + 输出）  | ✗ 我用闭源 API（DeepSeek-chat） |
| 计算开销 | 极大（每步构图，作者自述"不实用"）       | 轻量（位置控制 + 答案/路径比对） | ✗ 无法承受                    |
| 攻击对象 | 计算"为何错"                  | 模型"是否真在跟图 vs 位置锚定" | △ 病灶同源                    |

- **关键不可用点**：CRV 要求对模型有完全访问权并训练全套 transcoder，而我的实验跑在闭源 API 上——**白盒路线对我物理上不可行**
- **但病灶同源**：CRV 抓"内部计算虚胖"，GIF 抓"图推理虚胖（GFI）"——都是"表面信号掩盖真实机制"，这让两者可以互为**方法论参照**而非互斥

---
## 6.2 能从 CRV 借走的三样东西（方法论层面）

> [!important] 可迁移资产
> 不借它的工具，借它的**论证结构**。
### 6.2.1 "相关 → 因果"的递进式可信度论证

   - CRV 用 RQ1（能测）→ RQ3（能干预纠错）把"相关"逼成"因果"
   - 对我的启示：GFI 若只报告"Raw GIS 高但 PC-GIS 低"，停在相关层；要更硬，需一个**因果隔离环节**——我的"四种位置版本控制（endpoint-first/middle/last/decoy-last）"正是这个角色，等价于 CRV 的"干预单特征"。decoy-last 的 EAR=100% 就是我的"clamp 实验"
### 6.2.2 把"领域特异性/失败模式"写成卖点而非 bug

   - CRV 把"跨域崩溃"重述为"信号丰富的证据"
   - 对我的启示：我的失败模式分类（Prior-locked / Trace-only / Answer-only / Endpoint Anchoring / Hop-level Drift）同样应被框定为**诊断维度的丰富性**，而非"模型表现差"
### 6.2.3 诚实的边界声明增信

   - CRV 主动说"我不实用、只是科学仪器"。
   - 对我的启示：GIF 的 Pilot（20 基础样本、单模型）规模小，应**主动定位为诊断性评测框架（diagnostic benchmark）**，而非 SOTA 方法——降低被审稿人攻击"规模不足"的风险

---
## 6.3 CRV 的缺陷是否可被我的方案弥补

> [!tip] 结论：GIF 恰好补 CRV 两个自陈缺陷
> 这正是写 related work 时"我们与 CRV 互补"的论据
### 6.3.1 缺陷①：领域特异性（错误签名不跨域）

  - CRV 的签名绑死在具体任务的计算图上，换个域就崩
  - **GIF 的弥补**：我用**符号生成的随机图 + BFS/DFS 验证 gold path 唯一性**，诊断的是**任务无关的"位置锚定"机制**——位置锚定是 transformer 序列化的通用倾向，不依赖具体领域语义。GIF 测的是**更底层、更可迁移**的失败模式
### 6.3.2 缺陷②：聚合式而非语义式分析

  - CRV 自陈：只用图的统计/拓扑属性，**不推理单个特征的语义**（"加法特征是否被数字恰当激活"）
  - **GIF 的弥补**：我的诊断直接挂钩**语义结构**——gold path 的节点身份、终点位置、诱饵节点（decoy）——EAR 直接衡量"答案=最后出现节点"，这是**语义级**而非纯统计级的探针。某种意义上 GIF 在输入侧做了 CRV 想在特征侧做、却坦言还没做到的"语义对齐检验"
### 6.3.3 缺陷③：白盒不可扩展

  - CRV 开销大、要白盒
  - **GIF 的弥补**：黑盒、轻量、可批量跑闭源模型——**可扩展性正交互补**

---
## 6.4 一句话定位（写进 related work 的草稿）

> [!note] 拟用表述
> "CRV (Zhao et al., 2025) 在**内部电路层**通过归因图的结构指纹诊断推理失败，并以特征干预确立因果性。我们的工作沿同一'表面忠实但内部虚胖'问题谱系，但下沉到**外部输入图的序列化层**：GIF 是黑盒、任务无关、语义对齐的诊断框架，恰好弥补 CRV 自陈的**领域特异性**与**聚合式（非语义）分析**两点局限。CRV 控制'看哪个特征'，我们控制'图节点出现在序列何处'——两者都用可控干预把相关性逼成因果性"

---
## 6.5 待办 / 风险

> [!warning] 需要注意
> - **不要声称 GIF"优于"CRV**——层级不同，宣称优劣会被审稿人抓。措辞用"互补 / 正交 / 不同抽象层"
> - CRV 是 arXiv 预印本（2510.09312，2025-10），引用时标注预印本状态，留意是否有后续会议版本
> - 我的"语义级探针补 CRV 聚合式缺陷"这一论点较强，需在论文里**明确限定范围**（GIF 测的是图遵循，不是 CRV 测的计算正确性），避免过度声称