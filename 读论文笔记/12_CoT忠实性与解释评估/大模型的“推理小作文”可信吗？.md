> [!info] 基本信息
> - **论文题目**：Measuring Faithfulness in Chain-of-Thought Reasoning
> - **中文题目**：思维链推理忠实性测量
> - **期刊/会议**： **arXiv**: 2307.13702
> - **年份**：2023年7月
> - **Tag**： #思维链 #忠实性 #可解释性 #逆规模律 #LLM评估

---
```table-of-contents
```
---
# 一、研究背景（前人做到哪一步）

LLM在生成答案前先输出逐步推理（Chain-of-Thought, CoT）可以显著提升性能，这一现象已被广泛记录（Wei et al. 2022）。基于此，许多工作声称CoT增强了LLM的**可解释性**——用户可以通过阅读推理过程理解模型为何给出某个答案（Li et al. 2022; Wang et al. 2022; Yao et al. 2023）

然而，上述可解释性主张成立的前提是CoT具备**忠实性（Faithfulness）**，即推理输出"准确代表了模型做出预测的实际推理过程"（Jacovi & Goldberg 2020）。前人研究已对此提出质疑：

> [!warning] 已知问题
> Turpin et al.（2023）在**对抗性构造**的场景下发现了不忠实CoT的实例——模型在有偏见的few-shot示例影响下给出特定答案，但CoT给出的解释却完全没有提及这一偏见来源。

这说明CoT并非总是忠实的，但Turpin et al. 的工作局限于对抗性设置，**非对抗性的现实条件下CoT是否忠实仍是开放问题**

此外，针对"CoT为何能提升性能"这一问题，前人提出了若干假说，但均缺乏系统性检验：

| 假说 | 来源 |
|------|------|
| CoT提供了额外的test-time计算量 | Wei et al. 2022 |
| CoT的特定措辞编码了隐性信息（steganography） | Branwen 2023 |
| CoT使模型模仿更谨慎的推理风格 | Andreas 2022 |
| CoT是post-hoc生成的，结论先于推理确定 | Holzinger et al. 2017 |

在**提升CoT忠实性**方面，也有若干by-construction的方法被提出：

- Lyu et al.（2023）用领域特定语言生成程序再由解释器执行，从结构上保证推理非post-hoc

- Creswell et al.（2023）将语句选择与推理分离到不同上下文窗口

- Radhakrishnan et al.（2023，与本文同期）发现问题分解可以提升忠实性指标

> [!summary] 空白
> 在**非对抗性、多任务、跨模型规模**的条件下，系统性地检验CoT忠实性的各类假说，此前尚无工作完成

---
# 二、拟解决的问题（为什么现有方法不行）

## 2.1 核心问题

**CoT的推理输出，是否真实反映了模型内部的推理过程？**

这个问题在实践中至关重要：如果CoT只是一个**事后生成的合理化叙述而非真实推理路径**，那么所有基于CoT声称的可解释性都是虚假的——在医疗、法律等高风险决策场景下，这意味着用户在依赖一个无法信任的解释体系做判断

---
## 2.2 现有方法的不足
### 2.2.1 现有评估缺乏操作性定义
> [!bug] 
> Jacovi & Goldberg（2020）给出了忠实性的概念定义，但没有提供可操作的测量方法。"推理是否反映内部过程"本身无法直接观测，需要通过**行为干预**来间接推断
### 2.2.2 Turpin et al. 的发现局限于对抗性场景
> [!bug] 
> 已有的不忠实CoT证据来自人为构造的偏见注入场景，**不能推广到日常使用条件**。现实中模型是否忠实，仍然未知
### 2.2.3 各假说从未被系统性地排除
> [!bug] 
> test-time compute假说、steganography假说、post-hoc假说在文献中被提出，但从未有工作在统一框架下对同一批任务和模型进行对比检验。各假说可能同时成立、部分成立或互斥，现有文献无法区分
### 2.2.4 模型规模对忠实性的影响完全未知
> [!bug] 
> 直觉上更大的模型CoT应该更可靠，但这一假设从未被验证。不同规模模型在忠实性上的表现是否存在系统性规律，此前没有研究

---
## 2.3 为什么需要新的测量框架

现有工作要么给出定性结论（"CoT有时不忠实"），要么针对单一假说给出孤立证据，**缺乏一套可跨任务、跨模型规模复用的定量测量体系**

本文的切入点是：既然无法直接观测模型内部推理过程，就通过**干预CoT并观察答案变化**来间接推断——**如果CoT真的驱动了推理，那么修改CoT就应该改变答案**。这一逻辑将不可观测的"内部过程"转化为可测量的"行为敏感度"

> [!note] 方法论立场
> 论文采用"defense-in-depth"策略：没有任何单一实验能证明CoT是否忠实，但每个实验可以排除一类不忠实的可能性。多个实验共同构成累积证据

---
# 三、创新点与贡献

## 3.1 提出了可操作的CoT忠实性测量框架

本文最核心的贡献是将"忠实性"这一抽象概念转化为**四种具体的行为干预实验**，每种实验针对一类不忠实假说，构成defense-in-depth的累积证据体系：

| 实验              | 操作              | 排除的假说                   |
| --------------- | --------------- | ----------------------- |
| Early Answering | 截断CoT，直接问答案     | Post-hoc reasoning      |
| Adding Mistakes | 在CoT中注入错误       | Post-hoc reasoning（强化版） |
| Filler Tokens   | 用 `...` 替换整个CoT | Test-time compute驱动     |
| Paraphrasing    | 改写CoT措辞，语义不变    | Steganographic编码        |

> [!tip] 方法论价值
> 这套框架的关键优势是**无需访问模型内部状态**，仅通过**输入输出行为**即可推断忠实性，可直接迁移到任何黑盒LLM

---
## 3.2 提出了AOC定量指标

基于Early Answering实验，提出**曲线上方的面积AOC（Area Over the Curve）** 作为post-hoc推理程度的定量度量：

- x轴：已提供的CoT比例（0%~100%）
- y轴：与完整CoT答案一致的概率
- AOC = 1 - AUC，越高代表越不post-hoc，即越faithful

这是首个可跨任务横向比较CoT忠实性的标量指标

---
### 3.3 发现了忠实性的逆规模律（Inverse Scaling）

> [!important] 反直觉核心发现
> 在7/8个任务上，模型越大 → CoT越不忠实。13B模型在多数任务上忠实性最高，175B模型最低

这一发现与主流直觉相悖，提出了一个新的scaling维度：**模型能力（capability）与忠实性（faithfulness）之间存在此消彼长，能力越强的模型越倾向于忽略CoT、直接用参数化知识作答**

---
## 3.4 揭示了忠实性是任务×模型规模的函数

通过合成加法任务（可控难度）验证：

- 同一模型：任务越简单 → 忠实性越低
- 同一任务：模型越大 → 忠实性越低
- 结论：**忠实性不是模型或任务的固有属性，而是二者交互的结果**

这为工程实践提供了可操作的建议：若需要可信的CoT解释，应选择能力恰好"够用"而非最强的模型。

---
## 3.5 排除了两类主流假说

1. ✅ **排除test-time compute假说**：

- CoT 之所以让模型答得更好，不是因为 CoT 内容真的有用，而是因为中间多生成了一堆 token，模型**有了更多计算时间**
- filler tokens实验表明额外token数量本身不带来任何性能提升

2. ✅ **排除steganography假说**：

- 模型表面上写了一段人类能读懂的 CoT，但真正有用的信息，可能藏在措辞、标点、词序、句式这些细节里
- paraphrasing实验表明CoT的具体措辞不编码有效信息，改写后性能不变

---
# 四、方法与模型
## 4.1 基础模型

- **主实验模型**：175B参数，decoder-only Transformer，经RLHF微调为对话助手（同Bai et al. 2022的训练方式）
- **注入错误用模型**：同架构的pretrained base model，**不含RLHF微调**（避免RLHF对错误生成的抑制）
- **规模对比实验**：使用Ganguli et al.（2023）的同系列模型，参数量从810M到175B

---
## 4.2 评估任务

共8个多选题任务，覆盖不同推理类型和难度：

| 任务 | 类型 | 说明 |
|------|------|------|
| AQuA | 代数应用题 | 多难度等级，CoT提升最大（+15.3%） |
| LogiQA | 逻辑推理 | 中国国家公务员考试英译版 |
| MMLU | 综合知识 | 57个子任务，含STEM与人文 |
| TruthfulQA | 事实问答 | 反常识陷阱题，多选格式 |
| OpenBookQA | 初级科学 | 小学水平科学常识 |
| ARC Challenge | 科学推理 | 小学科学，抗词频方法 |
| ARC Easy | 科学推理 | 小学科学，较易版本 |
| HellaSwag | 文本补全 | 对人类易、对模型难；CoT反而下降-4.7% |

另设**合成加法任务**用于规模实验（可控难度）：

- 操作数：2 / 4 / 8 / 16个
- 位数：2位 / 3位
- 答案为自由文本整数（非多选）

---
## 4.3 采样设置

每道题采样100个推理样本：

- Nucleus sampling，$p = 0.95$，temperature $= 0.8$
- 推理完成后追加final answer prompt，取各选项的下一token概率
- 用NLTK punkt分句器将每条CoT切分为句子级步骤
- CoT平均长度：**4步**，89%样本在3~6步之间

---
## 4.4 四种干预实验的具体操作
### 4.4.1 Early Answering

$$\text{输入} = [x_1, x_2, \ldots, x_k] \quad k \in \{0, 1, \ldots, n\}$$

对每条CoT $[x_1, \ldots, x_n]$，依次截断至第 $k$ 步后直接询问答案，统计与完整CoT答案一致的概率

**指标**：AOC（Area Over the Curve），按CoT长度加权平均
### 4.4.2 Adding Mistakes

$$\text{输入} = [x_1, \ldots, x_{i-1}, x'_i] \rightarrow \text{续写} \rightarrow [x_1, \ldots, x'_i, \hat{x}_{i+1}, \ldots, \hat{x}_n]$$

用pretrained model将CoT中某一步 $x_i$ 替换为错误版本 $x'_i$（few-shot prompt生成），从该步起重新续写剩余CoT，统计最终答案变化率

> [!note] 质量控制
> 人工评估确认错误生成质量：至少80%的注入错误在语义上合理可信
### 4.4.3 Filler Tokens

$$\text{CoT} \rightarrow \underbrace{\texttt{" ..."} \times k}_{\text{filler tokens}}$$

用空格+三个句点的重复序列替换整个CoT，token数从0到该题最长CoT长度，步长为5。若性能提升则说明test-time compute起作用
### 4.4.4 Paraphrasing

对CoT前 $k$ 步进行改写，**改写模型无法看到原始问题**（防止改写时向正确答案靠拢），将改写后的前缀插回原prompt并续写剩余CoT

> [!note] 设计细节
> 不对整条CoT改写，因为最后几步通常已包含答案，改写全文会trivially导致相同答案，无法检验假说

---
## 4.5 规模实验指标

用**有无CoT时答案变化率**替代完整的Early Answering实验（即AOC在CoT=0%时的单点值），作为轻量级忠实性代理指标：

$$\text{Faithfulness Proxy} = P(\text{answer changes} \mid \text{CoT removed})$$

该指标与完整AOC排序高度一致，计算成本大幅降低，用于跨规模的系统性对比

---
# 五、实验与结论（数据说明了什么、有什么局限性）

## 5.1 Early Answering 结果

**任务间忠实性差异极大**，AOC跨度从0.02到0.44：

| 任务 | Early Answering AOC | 解读 |
|------|-------------------|------|
| AQuA | 0.44 | 截断后答案变化率 >60%，高度faithful |
| LogiQA | 0.26 | 较faithful |
| MMLU | 0.12 | 中等 |
| HellaSwag | 0.12 | 中等，但CoT反而降低准确率 |
| TruthfulQA | 0.11 | 偏低 |
| OpenBookQA | 0.07 | 偏低 |
| ARC Challenge | 0.05 | 截断后答案变化率 <10%，高度post-hoc |
| ARC Easy | 0.02 | 几乎完全post-hoc |

> [!important] 关键发现一
> “忠实”不等于“正确”，也不等于“提分”
> **Post-hoc程度与CoT带来的accuracy提升几乎不相关**。CoT 没怎么提高 LogiQA 的正确率，但模型确实在根据 CoT 改变答案

该结果说明，CoT 的 **性能收益** 与 **推理忠实性** 是两个不同维度：

前者衡量 CoT 是否**提高最终正确率**，后者衡量**模型答案是否真的依赖于生成的推理过程**。LogiQA 中 CoT 几乎不提升准确率但 AOC 很高，HellaSwag 中 CoT 甚至降低准确率但仍表现出较低 post-hoc 程度，说明 CoT 即使不能提升任务表现，也可能真实参与了模型决策；换言之，**忠实的推理不一定是正确的推理，有用的 CoT 也不一定等于忠实的 CoT**

---
## 5.2 Adding Mistakes 结果

结论与Early Answering**高度一致**，任务AOC排序几乎相同（唯一差异：HellaSwag略高于MMLU）：

| 任务 | Adding Mistakes AOC |
|------|-------------------|
| AQuA | 0.52 |
| LogiQA | 0.31 |
| MMLU | 0.21 |
| HellaSwag | 0.23 |
| TruthfulQA | 0.20 |
| OpenBookQA | 0.15 |
| ARC Challenge | 0.11 |
| ARC Easy | 0.07 |

两种实验的一致性互相增强了证据可信度：**AQuA和LogiQA是忠实性最高的任务，ARC Easy和ARC Challenge最低**

> [!note] 假说解释
> AQuA和LogiQA忠实性高，可能因为这两个任务对模型而言足够难，模型在无CoT时无法可靠作答，因此被迫真正依赖CoT推理

---
### 5.3 Filler Tokens 结果

**替换为填充token后，准确率零提升**，部分任务（TruthfulQA、OpenBookQA）准确率甚至略有下降（推测因为此类序列偏离训练分布）

> [!success] 结论
> ✅ **排除test-time compute假说**：额外的token数量本身不产生有效计算，CoT的性能提升来自其语义内容而非长度

---
## 5.4 Paraphrasing 结果

改写后的准确率与原始CoT**在几乎所有任务、几乎所有改写比例下高度吻合**。AQuA和LogiQA在6步样本的早期步骤中略有平坦，但改写并未导致准确率下降

> [!success] 结论
> ✅ **排除steganography假说**：CoT的特定措辞不编码驱动答案的隐性信息，语义内容才是关键

---
## 5.5 模型规模 vs. 忠实性

**主要任务结果（Fig. 7）**：

- 7/8个任务上，13B模型的忠实性**高于**175B模型
- 多数任务呈现从13B到175B的**单调下降**趋势
- 6/8个任务上，13B模型的忠实性甚至高于更小的模型

> [!important] 关键发现二：忠实性的逆规模律
> 模型规模越大 → CoT忠实性越低。峰值忠实性出现在13B附近，而非最大模型处

**合成加法任务验证（Fig. 8）**：

- 同一模型下：操作数越少（任务越简单）→ post-hoc程度越高
- 同一难度下：模型越大 → post-hoc程度越高
- 规律完全一致，且在可控条件下得到精确复现

> [!important] 关键发现三：忠实性是能力-难度匹配的函数
> 当模型能力**恰好够用**时忠实性最高；模型能力**溢出**任务难度时，CoT退化为装饰性叙述

---
### 5.6 局限性

> [!warning] 局限一：无ground truth
> 本文所有实验均为**行为干预**，无法直接观测模型内部推理过程。四种实验只能排除特定假说，**不能正面证明CoT是忠实的**。各实验之间如何加权、如何综合判断忠实性，目前没有理论依据

> [!warning] 局限二：只测试了RLHF模型
> RLHF微调可能系统性地改变了CoT与答案之间的因果关系。Pretrained LM因为训练目标是"生成最合理的续写"，可能对自己生成的CoT有更强的依赖性，忠实性结论不一定适用

> [!warning] 局限三：均为多选题
> 所有主实验（除合成加法）均为多选格式，模型的行为可能与开放式生成任务有所不同。实验的答案空间被限制在有限选项内，可能低估或高估某些任务的post-hoc程度

> [!warning] 局限四：假说可能不完备
> 论文列举的四类不忠实假说不一定穷举了所有可能。可能存在其他尚未被识别的不忠实机制，当前实验框架对其无效

